# Requirements Archive: v1.1 Schedule Quality & Reporting

**Archived:** 2026-07-29
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: WFM Service v1.1 — Schedule Quality & Reporting

**Milestone:** v1.1
**Status:** Active
**Created:** 2026-05-07

---

## v1 Requirements

### Schedule Quality

- [x] **QUAL-01**: Solver ensures every agent receives exactly 2 contiguous days off per week (their "weekend") — *delivered as: BambooHR field 4517 sourced MANDATORY day-off blocks honoured as hard constraints; solver respects the fixed pattern rather than choosing it*
- [ ] **QUAL-02**: Solver distributes desirable weekend positions (e.g. Sat/Sun, Fri/Sat) fairly across agents — no agent consistently gets the same popular or unpopular slots — **NOT SHIPPED, deferred from Phase 6**
- [ ] **QUAL-03**: Solver applies day-to-day consistency so each agent's scheduled daily hours match their standard contracted daily pattern (no erratic variation within a week) — **NOT SHIPPED, deferred from Phase 6**
- [ ] **QUAL-04**: After each solve, operator sees preference satisfaction rate (% of agent shift preferences honoured) on the schedule results page
- [ ] **QUAL-05**: Operator can adjust solver constraint weights and time limit via the UI without code changes

### Reporting

- [ ] **RPT-01**: Operator can view a per-timeslot coverage report showing demand, assigned agent count, and gap (over/under-staffed)
- [ ] **RPT-02**: Operator can view an agent utilization report showing weekly hours per agent with overtime and underutilization flags
- [ ] **RPT-03**: Operator can export the published schedule to Excel (.xlsx) including Coverage and Utilization tabs
- [ ] **RPT-04**: Operator can export the published schedule to PDF
- [ ] **RPT-05**: Operator can view a solver score breakdown showing which constraints fired, violation counts, and affected agents
- [ ] **RPT-06**: Operator can export the solver score breakdown to Excel (.xlsx)

### Diagnostics

- [ ] **DIAG-01**: Operator can view a PTO sync status page showing which agents have BambooHR PTO imported and which failed to sync, with reason (e.g. bamboohrId mismatch)
- [ ] **DIAG-02**: System tracks and displays week-over-week hours variance per agent so operators can identify inconsistent scheduling patterns across weeks

### Data Entry & Integration

- [x] **DATA-01**: Operator can upload a spreadsheet to bulk-assign BambooHR agents to desks; existing manual per-agent UI assignment remains available
- [x] **DATA-02**: BambooHR sync pulls full-time vs part-time employment status and stores it on the Agent; operator can see and filter agents by employment type
- [x] **DATA-03**: BambooHR sync pulls job description/title and stores it on the Agent; operator can mark specific job descriptions as non-schedulable so those agents are excluded from the solver and desk allocation

---

## Future Requirements

- Week-to-week hours consistency (single-week solver cannot enforce cross-week; surface as DIAG-02 metric instead)
- Multi-week scheduling horizon
- Agent self-service preference portal
- API authentication / authorization (deferred from v1.0)

---

## Out of Scope

- Custom domain / DNS — using AWS default CloudFront URL
- Multi-environment staging — single environment only
- Monitoring dashboards / alerting — beyond basic CloudWatch logs
- AWS OIDC GitHub Actions role — blocked by `iam:CreateRole` (PowerUserAccess policy)
- Agent-facing views / self-service preferences

---

## Traceability

| Requirement | Phase | Status | Outcome |
|-------------|-------|--------|---------|
| DATA-01 | Phase 5 | Complete | Shipped — desk bulk upload (16-col enriched + 6-col legacy shapes), manual assignment retained |
| DATA-02 | Phase 5 | Complete | Shipped — `employmentHistoryStatus` pulled, `Agent.employmentType`, Emp Type column + filter |
| DATA-03 | Phase 5 | Complete | Shipped — job title pulled, `JobTitleConfig` non-schedulable marking, solver + allocation exclusion |
| QUAL-01 | Phase 6 | Complete | Shipped, **re-scoped**: BambooHR field 4517 (`customWorkingdays`) → `WorkingDaysParser` → recurring MANDATORY blocks. Solver *respects* fixed patterns rather than *choosing* 2 contiguous days off. Data-gap agents (blank/`Variable`) excluded from solving |
| QUAL-02 | — | **Not shipped** | Deferred out of Phase 6 during discussion (06-CONTEXT.md); no phase ever carried it → v1.1 backlog |
| QUAL-03 | — | **Not shipped** | Deferred out of Phase 6 during discussion (06-CONTEXT.md); no phase ever carried it → v1.1 backlog |
| RPT-01 | Phase 7 | **Not shipped** | Phase 7 never planned or executed → v1.1 backlog |
| RPT-02 | Phase 7 | **Not shipped** | Phase 7 never planned or executed → v1.1 backlog |
| QUAL-04 | Phase 7 | **Not shipped** | Phase 7 never planned or executed → v1.1 backlog |
| DIAG-01 | Phase 7 | **Not shipped** | Phase 7 never planned or executed → v1.1 backlog |
| DIAG-02 | Phase 7 | **Not shipped** | Phase 7 never planned or executed → v1.1 backlog |
| RPT-03 | Phase 8 | **Not shipped** | Phase 8 never planned or executed → v1.1 backlog |
| RPT-04 | Phase 8 | **Not shipped** | Phase 8 never planned or executed → v1.1 backlog |
| RPT-05 | Phase 8 | **Not shipped** | Phase 8 never planned or executed → v1.1 backlog |
| RPT-06 | Phase 8 | **Not shipped** | Phase 8 never planned or executed → v1.1 backlog |
| QUAL-05 | Phase 8 | **Not shipped** | Phase 8 never planned or executed → v1.1 backlog |

**Completion: 4 of 16 requirements shipped (25%).** v1.1 closed early to re-scope; the 12 unshipped requirements are preserved in `.planning/ROADMAP.md` Backlog (999.4–999.6) and are candidates for the next milestone.

## Known Gaps at Close

- **BambooHR API key rotation never performed** (`06-VERIFICATION.md` truth 7, FAILED, accepted via operator override 2026-07-29). Exposed key `ad2bb…2be` remains in tracked planning docs in a public repository; BambooHR-integration code has since deployed to the sole live environment.
- **BambooHR field 4517 (`customWorkingdays`) is incomplete at source** — company-wide only ~45% populated, ~24% parseable. Agents with blank/`Variable` values are silently excluded from solving via `Agent.workingDaysKnown`. Desk-scale exclusion proportion never measured on live desks.
- **Data-gap and outlier surfacing is `log.warn` only** — no operator-facing UI. Was explicitly deferred to Phase 7 DIAG, which never ran.
- **`BambooRefreshServiceTest` idempotency test exercises a hand-copied replica** of `persistRefreshData`'s generation loop, not the real private method — future edits could silently diverge.
