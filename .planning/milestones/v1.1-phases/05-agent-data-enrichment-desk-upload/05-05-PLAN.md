---
phase: 05-agent-data-enrichment-desk-upload
plan: 05
type: execute
wave: 3
depends_on: [02, 04]
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/pages/DeskAgents.tsx
  - frontend/src/pages/Configuration.tsx
  - frontend/src/pages/ClientManagement.tsx
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]

must_haves:
  decisions: [D-06, D-07, D-10, D-17, D-19, D-20, D-21]
  truths:
    - "After a refresh, DeskAgents table shows an Emp Type column displaying Full-time/Part-time/—"
    - "Operator can filter the DeskAgents table by employment type via a dropdown (All/Full-time/Part-time)"
    - "DeskAgents table shows a pending-PTO badge on rows with pendingPtoCount > 0; tooltip lists dates"
    - "Configuration page has a Non-Schedulable Job Titles section with a checkbox per title; toggle PATCHes immediately"
    - "Configuration page has a BambooHR Sync Status card displaying last sync, status, error, counts"
    - "ClientManagement upload result shows a modal (not a toast) with N assigned / M skipped + table of skipped rows + Download CSV + Close"
    - "Modal Close is the only way to dismiss the modal; backdrop click does not close it"
    - "Errors during upload continue to show a toast (no modal for errors)"
  artifacts:
    - path: "frontend/src/api/client.ts"
      provides: "Updated types + new endpoints for jobTitleConfig and bambooSyncStatus"
      contains: "jobTitleConfig"
    - path: "frontend/src/pages/DeskAgents.tsx"
      provides: "Emp Type column + filter + PTO badge column"
      contains: "empTypeFilter"
    - path: "frontend/src/pages/Configuration.tsx"
      provides: "Non-Schedulable Job Titles section + Sync Status card"
      contains: "Non-Schedulable Job Titles"
    - path: "frontend/src/pages/ClientManagement.tsx"
      provides: "Upload result modal replacing toast"
      contains: "uploadResult"
  key_links:
    - from: "frontend/src/pages/DeskAgents.tsx"
      to: "frontend/src/api/client.ts"
      via: "DeskAgent.employmentType + pendingPtoCount + pendingPtoDates fields"
      pattern: "employmentType"
    - from: "frontend/src/pages/Configuration.tsx"
      to: "frontend/src/api/client.ts"
      via: "jobTitleConfig.list() / setNonSchedulable() and bambooSyncStatus.get()"
      pattern: "bambooSyncStatus.get"
    - from: "frontend/src/pages/ClientManagement.tsx"
      to: "frontend/src/api/client.ts"
      via: "SkippedRow[] in DeskAssignmentUploadResult"
      pattern: "skippedDetails"
---

<objective>
Wire the new backend surfaces into the operator UI per the approved UI-SPEC:

1. **`api/client.ts`**: Update `DeskAgent` interface for the 3 new response fields; change
   `DeskAssignmentUploadResult.skippedDetails` from `string[]` to `SkippedRow[]`; add
   `jobTitleConfig` and `bambooSyncStatus` API surfaces.
2. **`DeskAgents.tsx`**: Add the Employment Type column + dropdown filter; add the Pending PTO
   badge column.
3. **`Configuration.tsx`**: Add two sibling `<section>` blocks — Non-Schedulable Job Titles
   (checkbox list, optimistic PATCH) and BambooHR Sync Status (read-only card).
4. **`ClientManagement.tsx`**: Replace the toast-only upload-result flow with the Upload Results
   modal (600px wide, scrollable skipped-rows table, CSV download via Blob, Close button only).

Purpose: Deliver the operator-visible half of Phase 5. Backend (Plans 01-04) is now consumable.
No new frontend dependencies introduced; all UI patterns extend existing inline-styled code per
UI-SPEC.

This plan has `autonomous: false` because the final task is a human-verify checkpoint covering
all 6 UI behaviours per UI-SPEC Interaction Contracts §1-6.

Output: 4 modified frontend files + 1 human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-CONTEXT.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-RESEARCH.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-PATTERNS.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-UI-SPEC.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-VALIDATION.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-02-PLAN.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-04-PLAN.md

<interfaces>
<!-- Backend contracts this plan consumes (already merged when this runs): -->

REST endpoints (live after Plan 02):
  GET    /api/v1/job-titles                     -> JobTitleConfigResponse[]
  PATCH  /api/v1/job-titles/{id}  body { nonSchedulable: boolean }  -> JobTitleConfigResponse
  GET    /api/v1/configuration/bamboohr/sync-status  -> BambooSyncEventResponse

Existing endpoints (shape extended by Plan 04):
  GET    /api/v1/desks/{deskId}/agents          -> DeskAgent[] with employmentType, pendingPtoCount, pendingPtoDates
  POST   /api/v1/client-management/upload-desk-assignments (multipart)
                                                 -> DeskAssignmentUploadResult with skippedDetails: SkippedRow[]

TypeScript types (this plan defines them in client.ts):

  interface DeskAgent {
    // existing fields...
    employmentType: 'FULL_TIME' | 'PART_TIME' | null
    pendingPtoCount: number
    pendingPtoDates: string[]   // ISO date strings
  }

  interface JobTitleConfigResponse {
    id: string
    jobTitle: string
    nonSchedulable: boolean
    createdAt: string
    updatedAt: string
  }

  interface BambooSyncEventResponse {
    startedAt: string | null
    finishedAt: string | null
    success: boolean
    errorMessage: string | null
    agentsSynced: number | null
    timeOffPulled: number | null
    retryAfterSeconds: number | null
  }

  interface SkippedRow {
    rowNumber: number
    bamboohrId: string | null
    name: string | null
    reason: string
  }

  interface DeskAssignmentUploadResult {
    assignedCount: number
    skippedCount: number
    assignedDetails: string[]
    skippedDetails: SkippedRow[]   // CHANGED from string[]
  }

  export const jobTitleConfig = {
    list: () => request<JobTitleConfigResponse[]>('/job-titles'),
    setNonSchedulable: (id: string, nonSchedulable: boolean) =>
      request<JobTitleConfigResponse>(`/job-titles/${id}`,
        { method: 'PATCH', body: JSON.stringify({ nonSchedulable }) }),
  }

  export const bambooSyncStatus = {
    get: () => request<BambooSyncEventResponse>('/configuration/bamboohr/sync-status'),
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update api/client.ts types + add jobTitleConfig and bambooSyncStatus surfaces</name>
  <files>frontend/src/api/client.ts</files>
  <read_first>
    - frontend/src/api/client.ts (full file — read all of it; pay attention to: DeskAgent interface ~line 282, DeskAssignmentUploadResult ~line 430, appConfiguration block ~lines 395-399, request() helper ~lines 50-90, ApiRequestError ~lines 32-44, the deskAgents block ~lines 125-161)
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-PATTERNS.md section "frontend/src/api/client.ts MODIFY"
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-UI-SPEC.md (review §1-6 for the consumer surface)
  </read_first>
  <action>
    Modify frontend/src/api/client.ts:

    1. Extend the existing `DeskAgent` interface (~line 282) with three new fields:
       - `employmentType: 'FULL_TIME' | 'PART_TIME' | null`
       - `pendingPtoCount: number`
       - `pendingPtoDates: string[]`

    2. Add a new exported `SkippedRow` interface near the existing upload-related types:
       - `rowNumber: number`
       - `bamboohrId: string | null`
       - `name: string | null`
       - `reason: string`

    3. Update the existing `DeskAssignmentUploadResult` interface (~line 430): change
       `skippedDetails: string[]` to `skippedDetails: SkippedRow[]`. This is a breaking change in
       lockstep with Plan 04's backend DTO. Verify no other consumer of `skippedDetails` exists
       beyond `ClientManagement.tsx` via `grep -rn "skippedDetails" frontend/src/`.

    4. Add exported `JobTitleConfigResponse` and `BambooSyncEventResponse` interfaces per
       <interfaces>.

    5. Add two new namespaced API surfaces alongside `appConfiguration` (~line 395):
       - `export const jobTitleConfig = { list, setNonSchedulable }`
       - `export const bambooSyncStatus = { get }`
       Match the `appConfiguration` style verbatim — same `request<T>(path, init)` helper, same
       export pattern. Endpoints rooted at the existing API base URL (request() adds the prefix).

    Do NOT modify the `request` helper. Do NOT add new error-handling branches. The existing
    `ApiRequestError` flow handles 503 BAMBOOHR_RATE_LIMITED correctly (errors propagate to the
    catch site in the page component).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
    <manual>Run `npm run build` and verify no type errors.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "employmentType:" frontend/src/api/client.ts` returns at least 1
    - `grep -c "pendingPtoCount" frontend/src/api/client.ts` returns at least 1
    - `grep -c "pendingPtoDates" frontend/src/api/client.ts` returns at least 1
    - `grep -c "SkippedRow" frontend/src/api/client.ts` returns at least 2 (interface + DeskAssignmentUploadResult use)
    - `grep -c "skippedDetails: SkippedRow\\[\\]" frontend/src/api/client.ts` returns 1
    - `grep -c "export const jobTitleConfig" frontend/src/api/client.ts` returns 1
    - `grep -c "export const bambooSyncStatus" frontend/src/api/client.ts` returns 1
    - `grep -c "/job-titles" frontend/src/api/client.ts` returns at least 2 (list + PATCH paths)
    - `grep -c "/configuration/bamboohr/sync-status" frontend/src/api/client.ts` returns 1
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>TypeScript compiles. Types match backend contracts. Two new API surfaces exported.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DeskAgents.tsx — add Emp Type column + filter dropdown + Pending PTO badge column</name>
  <files>frontend/src/pages/DeskAgents.tsx</files>
  <read_first>
    - frontend/src/pages/DeskAgents.tsx (full file — controls row at lines 253-269, existing showActiveOnly filter at line 227, table headers around line 292, action cell around line 359, existing inline modal at lines 382-413)
    - frontend/src/index.css (`.desk-badge` class at lines 63-71)
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-UI-SPEC.md Interaction Contracts §1, §2, §3
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-PATTERNS.md section "frontend/src/pages/DeskAgents.tsx MODIFY"
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-CONTEXT.md decisions D-06, D-07, D-21
  </read_first>
  <action>
    Modify frontend/src/pages/DeskAgents.tsx per UI-SPEC §1-3:

    1. Add `const [empTypeFilter, setEmpTypeFilter] = useState('')` state.

    2. Add a `<select>` to the controls bar (~lines 253-269), inline with the existing
       `showActiveOnly` checkbox. Options: `''` (All), `'FULL_TIME'` (Full-time), `'PART_TIME'`
       (Part-time). Use the same dropdown shape as the existing pageSize select (~lines 274-277).

    3. Compose the filter with the existing `filteredAgents = showActiveOnly ? agentList.filter(da => da.active) : agentList` (line 227):
       chain `.filter(da => empTypeFilter === '' || da.employmentType === empTypeFilter)`.

    4. Add a `<th>Emp Type</th>` after the existing Job Title header. Add a `<td>` rendering
       `da.employmentType === 'FULL_TIME' ? 'Full-time' : da.employmentType === 'PART_TIME' ? 'Part-time' : '—'`.

    5. Add a `<th>PTO</th>` between "Last Refreshed" and "Actions". Cell renders:
       - When `da.pendingPtoCount > 0`: `<span>` with inline style per UI-SPEC §3 (background
         `#fef9c3`, color `#92400e`, padding `0.25rem 0.5rem`, borderRadius `4px`, fontSize
         `0.85rem`, `title={da.pendingPtoDates.join(', ')}`). Text: `{da.pendingPtoCount} pending PTO`.
       - When `da.pendingPtoCount === 0` or `pendingPtoDates` is empty: `'—'` (em dash, plain text).

    Do NOT introduce a new CSS class. Inline styles only (matches `DeskAgents.tsx:382` pattern
    per UI-SPEC). Do NOT change column widths globally — let the table flow naturally.

    Do NOT modify the existing `loadAgents`, `loadDeskAgents`, `Assign Modal`, or `Days Off Modal`
    behaviours. The filter is a display-time concern only.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
    <manual>Captured by Task 5 checkpoint.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "empTypeFilter" frontend/src/pages/DeskAgents.tsx` returns at least 2 (state + filter use)
    - `grep -c "FULL_TIME" frontend/src/pages/DeskAgents.tsx` returns at least 2 (option + cell render)
    - `grep -c "PART_TIME" frontend/src/pages/DeskAgents.tsx` returns at least 2
    - `grep -c "pendingPtoCount" frontend/src/pages/DeskAgents.tsx` returns at least 1
    - `grep -c "pendingPtoDates" frontend/src/pages/DeskAgents.tsx` returns at least 1
    - `grep -c "#fef9c3" frontend/src/pages/DeskAgents.tsx` returns 1 (badge background hex)
    - `grep -c "#92400e" frontend/src/pages/DeskAgents.tsx` returns 1 (badge text hex)
    - `grep -c "Emp Type" frontend/src/pages/DeskAgents.tsx` returns 1
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Type-checks clean. Column, filter, and badge in place per UI-SPEC. Visual verification in Task 5.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Configuration.tsx — Non-Schedulable Job Titles section + BambooHR Sync Status card</name>
  <files>frontend/src/pages/Configuration.tsx</files>
  <read_first>
    - frontend/src/pages/Configuration.tsx (full file — note the existing single `<div style={{ maxWidth: '500px' }}>` form block at ~lines 45-87)
    - frontend/src/components/Toast.tsx (showToast usage)
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-UI-SPEC.md Interaction Contracts §5, §6 (copywriting + layout); Copywriting Contract section
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-PATTERNS.md section "frontend/src/pages/Configuration.tsx MODIFY"
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-CONTEXT.md decisions D-10, D-19, D-20
  </read_first>
  <action>
    Append two `<section>` blocks AFTER the existing BambooHR config form block (after the closing
    `</div>` at ~line 84), inside the outer `<div className="main-content">`.

    SECTION A — Non-Schedulable Job Titles (UI-SPEC §5):
      - `<h2>Non-Schedulable Job Titles</h2>` at fontSize 1.1rem, fontWeight 600.
      - Container: `<div style={{ marginTop: '2rem', maxWidth: '500px' }}>`.
      - Description text (verbatim per UI-SPEC copywriting):
        "Agents with these job titles are excluded from schedule solving and cannot be assigned to a desk."
        — fontSize 0.85rem, color #6b7280, marginBottom 0.75rem.
      - State: `const [jobTitles, setJobTitles] = useState<JobTitleConfigResponse[] | null>(null)`.
      - useEffect on mount: `jobTitleConfig.list()` → setJobTitles, with try/catch + showToast on error.
      - Loading state: `<p>Loading job titles...</p>` while null.
      - Empty state: `<p style={{ color: '#6b7280' }}>No job titles synced yet. Run a BambooHR refresh to populate this list.</p>` (verbatim).
      - For each row: `<label style={{ display:'flex', alignItems:'center', gap:'0.5rem', padding:'0.25rem 0.5rem', borderBottom:'1px solid #f3f4f6', cursor:'pointer', fontWeight: 400 }}>` containing:
        - `<input type="checkbox" checked={row.nonSchedulable} onChange={() => handleToggle(row.id, !row.nonSchedulable)}>`
        - `<span>{row.jobTitle}</span>`
        - When `row.nonSchedulable`: `<span style={{ fontSize: '0.85rem', color: '#ef4444', marginLeft: 'auto', fontWeight: 400 }}>Non-schedulable</span>`.
      - handleToggle: optimistic update; PATCH via `jobTitleConfig.setNonSchedulable(id, value)`; on error revert state + `showToast('error', 'Failed to update job title — please try again')` (verbatim).

    SECTION B — BambooHR Sync Status (UI-SPEC §6):
      - `<h2>BambooHR Sync Status</h2>` at fontSize 1.1rem, fontWeight 600.
      - Container: `<div style={{ marginTop: '2rem', padding: '1rem', background: '#f9fafb', borderRadius: '6px', border: '1px solid #e5e7eb', maxWidth: '600px' }}>`.
      - State: `const [syncStatus, setSyncStatus] = useState<BambooSyncEventResponse | null>(null)`, plus `const [loadingSyncStatus, setLoadingSyncStatus] = useState(true)`.
      - useEffect on mount: `bambooSyncStatus.get()` → setSyncStatus; finally setLoadingSyncStatus(false). On error, render the card with "Status: —" and show toast.
      - Render fields per UI-SPEC §6:
        - "Last sync": `syncStatus?.startedAt ? new Date(syncStatus.startedAt).toLocaleString() : 'Never'`
        - "Status": "Success" (green #16a34a) when success===true; "Failed" (red #dc2626) when success===false and startedAt!=null; "—" (color #6b7280) when never-synced.
        - "Error": only when success===false and errorMessage; show the verbatim backend message (truncate at 200 chars if longer).
        - "Retry in": only when retryAfterSeconds!=null; render "{retryAfterSeconds} seconds".
        - "Agents synced": only when success===true and agentsSynced!=null.
        - "PTO records pulled": only when success===true and timeOffPulled!=null.
      - Layout: `<dl style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(160px, 1fr))', gap: '1rem', margin: 0 }}>` with `<dt>` (label, fontWeight 600) + `<dd style={{ margin: 0 }}>` pairs per UI-SPEC §6.
      - No polling. Fetched once on mount per UI-SPEC §6 + CONTEXT Open Question 2.

    Do NOT introduce a new component file. All new UI is inline within Configuration.tsx per
    UI-SPEC ("Component Inventory" — no new component files). Do NOT replace the existing
    BambooHR config form — it stays as-is above the new sections.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
    <manual>Captured by Task 5 checkpoint.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "Non-Schedulable Job Titles" frontend/src/pages/Configuration.tsx` returns at least 1
    - `grep -c "BambooHR Sync Status" frontend/src/pages/Configuration.tsx` returns at least 1
    - `grep -c "jobTitleConfig" frontend/src/pages/Configuration.tsx` returns at least 1
    - `grep -c "bambooSyncStatus" frontend/src/pages/Configuration.tsx` returns at least 1
    - `grep -c "No job titles synced yet" frontend/src/pages/Configuration.tsx` returns 1
    - `grep -c "Failed to update job title" frontend/src/pages/Configuration.tsx` returns 1
    - `grep -c "Agents with these job titles are excluded" frontend/src/pages/Configuration.tsx` returns 1
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Type-checks clean. Two new sections render below the existing BambooHR config form. Visual verification in Task 5.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: ClientManagement.tsx — Upload Results modal replacing the toast-only flow</name>
  <files>frontend/src/pages/ClientManagement.tsx</files>
  <read_first>
    - frontend/src/pages/ClientManagement.tsx (full file — note handleUploadDeskAssignments at lines 156-176, handleExportEmployees at lines 178-199, section-container styles around line 294 and 428)
    - frontend/src/pages/DeskAgents.tsx (canonical inline modal pattern at lines 382-413)
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-UI-SPEC.md Interaction Contracts §4 (Upload Result Modal) — the full layout spec
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-PATTERNS.md section "frontend/src/pages/ClientManagement.tsx MODIFY"
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-CONTEXT.md decisions D-17, D-18
  </read_first>
  <action>
    Modify frontend/src/pages/ClientManagement.tsx per UI-SPEC §4:

    1. Add state: `const [uploadResult, setUploadResult] = useState<DeskAssignmentUploadResult | null>(null)`.

    2. Modify `handleUploadDeskAssignments` (lines 156-176):
       - Replace the `showToast('success', '...')` call (~line 162) with `setUploadResult(result)`.
       - Keep the `setUploading(false)` and `fileInputRef.current.value = ''` resets in `finally`.
       - Keep `showToast('error', getErrorMessage(err))` on the catch path — errors stay as toasts (no modal for errors).
       - If `viewDeskId` is set, call `loadDeskAgents(viewDeskId)` immediately after `setUploadResult(result)` (modal opens; underlying table refreshes in parallel).

    3. Add the Upload Result Modal markup at the bottom of the JSX (mirror DeskAgents.tsx:382 placement). Render conditionally when `uploadResult !== null`:
       - Outer backdrop: `<div style={{ position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.5)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 1000 }}>`.
       - Inner card: `<div style={{ background: '#fff', borderRadius: '8px', padding: '1.5rem', width: '600px', maxHeight: '80vh', display: 'flex', flexDirection: 'column' }}>`.
       - Heading: `<h3>Upload Results</h3>`.
       - Summary block:
         - `{uploadResult.assignedCount} assigned` — color #16a34a, fontWeight 600, fontSize 1.1rem.
         - When `uploadResult.skippedCount > 0`: `{uploadResult.skippedCount} skipped` — color #dc2626, fontWeight 600, fontSize 1.1rem.
       - Skipped rows table (rendered only when `skippedCount > 0`, not behind a toggle):
         - Scrollable container: `<div style={{ overflowY: 'auto', maxHeight: '300px', marginTop: '1rem', border: '1px solid #e5e7eb', borderRadius: '4px' }}>`.
         - `<table>` with thead `Row | BambooHR ID | Name | Reason` columns and one tbody row per `SkippedRow` (`uploadResult.skippedDetails`). Use `'—'` for null bamboohrId/name values.
       - Footer (right-aligned, gap 0.5rem, marginTop 0.75rem):
         - When `skippedCount > 0`: `<button onClick={handleDownloadSkippedCsv}>Download skipped as CSV</button>`.
         - Always: `<button onClick={() => setUploadResult(null)}>Close</button>`.
       - Backdrop click MUST NOT close. Implement by NOT adding an onClick to the outer backdrop (clicks bubble harmlessly).

    4. Add handler `handleDownloadSkippedCsv` (inside the component, near handleExportEmployees):
       - Build CSV string: header `Row,BambooHR ID,Name,Reason` + one data line per SkippedRow. Quote fields containing commas/quotes; escape inner quotes by doubling them. Prefix any field starting with `=`, `+`, `-`, or `@` with a single quote (CSV-injection mitigation per threat model T-05-05-02).
       - Create Blob with type `'text/csv'`, then URL.createObjectURL + anchor click + URL.revokeObjectURL (mirror handleExportEmployees pattern at lines 178-199).
       - Filename: `skipped-assignments-${new Date().toISOString().replace(/[:.]/g, '-')}.csv`.

    Do NOT change `handleExportEmployees`. Do NOT remove `showToast` import. Do NOT introduce a
    modal library or new component file.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && cd frontend && npm run build -- --mode development 2>&1 | tail -20</automated>
    <manual>Captured by Task 5 checkpoint.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "uploadResult" frontend/src/pages/ClientManagement.tsx` returns at least 4 (state, set, modal render, conditional)
    - `grep -c "setUploadResult" frontend/src/pages/ClientManagement.tsx` returns at least 2 (set to result + close to null)
    - `grep -c "Upload Results" frontend/src/pages/ClientManagement.tsx` returns 1
    - `grep -c "Download skipped as CSV" frontend/src/pages/ClientManagement.tsx` returns 1
    - `grep -c "skipped-assignments-" frontend/src/pages/ClientManagement.tsx` returns 1
    - `grep -c "rgba(0,0,0,0.5)\\|rgba(0, 0, 0, 0.5)" frontend/src/pages/ClientManagement.tsx` returns at least 1 (modal backdrop)
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm run build` succeeds (production bundle compiles).
  </acceptance_criteria>
  <done>Modal renders on successful upload. Errors still surface as toasts. CSV download works. Backdrop click does not close.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human-verify all 6 UI behaviours per UI-SPEC + VALIDATION manual checklist</name>
  <files>—</files>
  <read_first>
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-VALIDATION.md "Manual-Only Verifications" table
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-UI-SPEC.md (full document — visual reference for each behaviour)
  </read_first>
  <action>
    PAUSE for human walk-through. Tasks 1-4 have completed automated build verification; this task
    is the operator-driven UAT for the 6 manual cases listed in 05-VALIDATION.md.

    Claude's responsibilities before yielding to the human:
      1. Ensure both servers can boot:
         - Terminal 1: `./gradlew bootRun --args='--spring.profiles.active=mock'`
         - Terminal 2: `cd frontend && npm run dev`
      2. Confirm the dev URL responds with the React app (curl localhost:5173 returns 200 + HTML).
      3. Confirm the API is reachable from the SPA origin (no CORS regression).
      4. Announce the URL and the checklist below to the operator.

    Then HALT and wait for the operator's "approved" or specific failure report. Do NOT proceed
    to any further task until the resume signal is received.

    No code is written in this task. The operator drives the walk-through; Claude only resumes
    once explicitly approved.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — see acceptance_criteria for the 6-case walkthrough"</automated>
    <manual>
    Walk through each manual case from 05-VALIDATION.md "Manual-Only Verifications":

    1. **Employment type column + filter** (DATA-02 / SC-1):
       - Trigger a BambooHR refresh (DeskAgents → Refresh button).
       - Confirm an "Emp Type" column appears.
       - Confirm the filter dropdown shows All / Full-time / Part-time.
       - Select Part-time → only part-time rows visible. Select Full-time → only full-time. Select All → all rows.

    2. **Non-Schedulable Job Titles toggle** (DATA-03 / SC-2):
       - Open Configuration → see "Non-Schedulable Job Titles" section.
       - It lists every distinct job title from the refresh with an unchecked checkbox.
       - Toggle one title → confirm the row's "Non-schedulable" red label appears.
       - Reload the page → state persists.
       - Trigger a solve → confirm agents with that title are excluded from the schedule.
       - Try the manual desk-assign action on such an agent → 409 with "non-schedulable job title:" message in toast.

    3. **Upload result modal** (DATA-01 / SC-3):
       - Upload a known mixed-failure spreadsheet (use sample-data/production_agents.xlsx or a hand-crafted XLSX with intentional bad rows).
       - Confirm a modal opens (NOT a toast).
       - Confirm counts: "N assigned" (green), "M skipped" (red).
       - Confirm the skipped table lists each row with row#, BambooHR ID (or "—"), name (or "—"), and reason.
       - Click "Download skipped as CSV" → file downloads, opens in Excel/Numbers, columns match.
       - Click "Close" → modal dismisses; underlying table reflects newly assigned agents.
       - Click on the backdrop (outside the inner card) → modal does NOT close.
       - Try uploading an unrecognised-shape spreadsheet (e.g. a CSV renamed to .xlsx, or a workbook with unknown headers) → confirm a toast (not modal) appears with the backend's "Unrecognised spreadsheet shape" message.

    4. **Existing manual assignment still works** (DATA-01 / SC-3):
       - Open ClientManagement, use the existing per-agent assign button (not upload). Assign one agent to a desk. Confirm the row reflects the change.

    5. **BambooHR Sync Status card** (SC-4):
       - Open Configuration → see "BambooHR Sync Status" card with fields populated from the latest refresh.
       - To exercise the 503 path: toggle the MockBambooHRClient (or stub the integration) to return 503 on next refresh. Trigger Refresh. Confirm the card eventually shows "Status: Failed" with "Error: BambooHR is rate-limiting requests. Retry in 60 seconds." (or whatever N).

    6. **Pending PTO badge** (SC-5):
       - In MockBambooHRClient (or via direct DB insert), ensure at least one agent has an AgentDayOff row with type=PTO, status=REQUESTED, date in the future.
       - Open DeskAgents for that desk → confirm the PTO column shows the amber "N pending PTO" badge.
       - Hover the badge → confirm the tooltip lists the dates.
       - Confirm rows with no pending PTO show "—".

    Confirm CONSOLE is free of React errors/warnings.
    Confirm NETWORK tab shows the new endpoints being called (GET /api/v1/job-titles, GET /api/v1/configuration/bamboohr/sync-status).

    Resume signal: Type "approved" or describe issues (which behaviour failed; screenshot if visual).
    </manual>
  </verify>
  <acceptance_criteria>
    - Case 1 (Emp Type col + filter): PASS
    - Case 2 (Non-Schedulable toggle + solver exclusion + assign rejection): PASS
    - Case 3 (Upload modal: open, summary, table, CSV, Close, no backdrop dismiss, unknown-shape toast): PASS
    - Case 4 (Manual per-agent assign unchanged): PASS
    - Case 5 (Sync status card: success state + simulated 503 failure state): PASS
    - Case 6 (PTO badge + tooltip + em-dash empty state): PASS
    - No React console errors observed
    - Network panel shows the two new endpoints firing on Configuration mount
    - Operator types "approved" or all listed issues are addressed in a follow-up commit before resume
  </acceptance_criteria>
  <done>Operator has typed "approved" after walking through all 6 cases without unresolved defects. SUMMARY records any anomalies encountered during the walk-through with their resolution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | All requests carry X-Tenant-ID via the request() helper; existing TenantFilter resolves tenant |
| Browser → File system (CSV download) | Blob URL via URL.createObjectURL; same-origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Information Disclosure | CSV download containing PII (names, IDs) | accept | Same tenant context. Operator already had access to the data they uploaded. No new disclosure. |
| T-05-05-02 | Tampering | CSV injection via SkippedRow.reason (= or @ prefix) | mitigate | When building the CSV string, prefix any cell that starts with `=`, `+`, `-`, `@` with a single quote (`'`). This is the standard CSV-injection mitigation. Applied in handleDownloadSkippedCsv per Task 4 action. |
| T-05-05-03 | Spoofing | Tampered DeskAgent.employmentType from server | accept | Server is the source of truth; the React app trusts the API response. Tenant boundary is enforced server-side. |
| T-05-05-04 | Information Disclosure | Pending PTO dates exposed to wrong viewer | mitigate | The backend GET /desks/{id}/agents already scopes by tenant + desk; this UI just renders what the API returns. No additional client-side filtering needed. |
| T-05-05-05 | DoS | Large CSV download | accept | Skipped rows are bounded by the spreadsheet upload size (max ~10k rows). Blob construction is O(n) on the client; acceptable. |
| T-05-05-06 | Tampering | XSS via SkippedRow.reason rendered in the modal table | mitigate | React's default JSX rendering escapes all string content. Do NOT use dangerouslySetInnerHTML for any field in the modal. Confirmed in the task action — plain `{row.reason}` interpolation is safe. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0.
- `cd frontend && npm run build` succeeds.
- Task 5 human-verify checklist all green; no React console errors.
- Network: new endpoints called from the expected pages.
</verification>

<success_criteria>
- All 6 manual cases in UI-SPEC §1-6 pass human verification.
- DATA-01, DATA-02, DATA-03 user-facing surfaces complete.
- No new frontend dependencies introduced.
- Type-safe end-to-end (TypeScript matches backend DTOs).
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-data-enrichment-desk-upload/05-05-SUMMARY.md`
</output>
