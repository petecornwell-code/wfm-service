---
phase: 06-solver-quality-constraints
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/main/java/com/wfm/integration/BambooEmployee.java
  - src/main/java/com/wfm/integration/HttpBambooHRClient.java
  - src/main/java/com/wfm/integration/MockBambooHRClient.java
autonomous: true
requirements: [QUAL-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Each BambooEmployee carries the raw BambooHR field 4517 (customWorkingdays) value — sourced from field 4517, not the empty Monday..Sunday fields (D-01)"
    - "The existing bulk POST /reports/custom request fetches field 4517 — no per-employee call added (D-02)"
    - "MockBambooHRClient emits varied customWorkingdays values, including a Variable data-gap and an outlier 4-day week"
  artifacts:
    - path: "src/main/java/com/wfm/integration/BambooEmployee.java"
      provides: "customWorkingdays record component"
      contains: "customWorkingdays"
    - path: "src/main/java/com/wfm/integration/HttpBambooHRClient.java"
      provides: "field 4517 in the /reports/custom fields array; reads customWorkingdays per row"
      contains: "4517"
    - path: "src/main/java/com/wfm/integration/MockBambooHRClient.java"
      provides: "customWorkingdays values across all BambooEmployee constructions"
      contains: "customWorkingdays"
  key_links:
    - from: "HttpBambooHRClient.listEmployees"
      to: "BambooEmployee.customWorkingdays"
      via: "emp.path(\"customWorkingdays\").asText(null) -> positional constructor arg"
      pattern: "customWorkingdays"
---

<objective>
Plumb BambooHR field 4517 (`customWorkingdays`) through the integration layer: add it to the
bulk custom-report request (D-02), carry it on the `BambooEmployee` record, and make the mock
emit realistic values so tests and local runs exercise the path.

Purpose: This is the data-acquisition half of QUAL-01 — without the raw value on
`BambooEmployee`, plan 03 has nothing to parse into MANDATORY rows.

Output: modified `BambooEmployee`, `HttpBambooHRClient`, `MockBambooHRClient`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-solver-quality-constraints/06-CONTEXT.md
@.planning/phases/06-solver-quality-constraints/06-RESEARCH.md
@.planning/phases/06-solver-quality-constraints/06-PATTERNS.md

<interfaces>
<!-- Existing record being extended. New component inserted between employmentHistoryStatus and wfmTenantId. -->
Current: BambooEmployee(id, displayName, workEmail, department, jobTitle, status,
                        employmentHistoryStatus, wfmTenantId, project)
After:   BambooEmployee(id, displayName, workEmail, department, jobTitle, status,
                        employmentHistoryStatus, customWorkingdays, wfmTenantId, project)

ALL positional constructor call-sites MUST be updated in lockstep (compilation gate):
  - HttpBambooHRClient.listEmployees (~lines 172-176)
  - MockBambooHRClient.listEmployees (~lines 92-101)
  - MockBambooHRClient.buildVintedAgents (~lines 61-71)
  - MockBambooHRClient.getEmployee (~line 109)
  - src/test/java/com/wfm/integration/BambooRefreshServiceTest.java emp() helper (~lines 129-132) — handled in plan 03
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add customWorkingdays component to BambooEmployee</name>
  <files>src/main/java/com/wfm/integration/BambooEmployee.java</files>
  <read_first>
    - .planning/phases/06-solver-quality-constraints/06-PATTERNS.md (BambooEmployee section — exact insertion point and caller list)
    - src/main/java/com/wfm/integration/BambooEmployee.java (current 10-component record)
  </read_first>
  <action>
    Insert a `String customWorkingdays` record component immediately after `employmentHistoryStatus`
    and before `wfmTenantId`. This is the raw BambooHR field 4517 value; `null` means not populated.
    Order is load-bearing — every positional call-site must match (see `<interfaces>`).
  </action>
  <verify>
    <automated>grep -q "customWorkingdays" src/main/java/com/wfm/integration/BambooEmployee.java && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - BambooEmployee declares `String customWorkingdays` between `employmentHistoryStatus` and `wfmTenantId`.
    - No other component reordered.
  </acceptance_criteria>
  <done>BambooEmployee record has the new component in the correct position.</done>
</task>

<task type="auto">
  <name>Task 2: Pull field 4517 in HttpBambooHRClient.listEmployees</name>
  <files>src/main/java/com/wfm/integration/HttpBambooHRClient.java</files>
  <read_first>
    - .planning/phases/06-solver-quality-constraints/06-PATTERNS.md (HttpBambooHRClient section — fields array line ~136, read pattern, constructor call lines 172-176)
    - .planning/phases/06-solver-quality-constraints/06-RESEARCH.md (Code Examples: reading customWorkingdays; D-02; assumption A1 about the JSON key)
    - src/main/java/com/wfm/integration/HttpBambooHRClient.java:130-184 (listEmployees)
  </read_first>
  <action>
    In the `/reports/custom` request body fields array (~line 136), append `"4517"` to the existing
    list (`id, displayName, workEmail, department, jobTitle, status, employmentHistoryStatus`). In the
    per-row loop (after ~line 168) read the value with `emp.path("customWorkingdays").asText(null)` —
    `asText(null)` (not empty string) so a missing field stays `null` and the parser can treat it as a
    data gap. Pass the value as the new positional argument to the `BambooEmployee(...)` constructor
    (between `employmentHistoryStatus` and `wfmTenantId`). Do NOT add a per-employee fetch (D-02). Do
    NOT log the request body or response rows (threat T-6-ID, no PII in logs) — keep existing
    count-only logging.
  </action>
  <verify>
    <automated>grep -q '"4517"' src/main/java/com/wfm/integration/HttpBambooHRClient.java && grep -q 'customWorkingdays' src/main/java/com/wfm/integration/HttpBambooHRClient.java && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `"4517"` is present in the `/reports/custom` fields array.
    - Each row reads `emp.path("customWorkingdays").asText(null)` and passes it to the constructor in the correct position.
    - No per-employee fetch added; no request/response body logged.
  </acceptance_criteria>
  <done>listEmployees requests field 4517, reads customWorkingdays, and constructs BambooEmployee with it.</done>
</task>

<task type="auto">
  <name>Task 3: Emit customWorkingdays from MockBambooHRClient</name>
  <files>src/main/java/com/wfm/integration/MockBambooHRClient.java</files>
  <read_first>
    - .planning/phases/06-solver-quality-constraints/06-PATTERNS.md (MockBambooHRClient section — i % 5 variation, the three constructor sites, getEmployee fixed default)
    - .planning/phases/06-solver-quality-constraints/06-RESEARCH.md (Pattern 4: MockBambooHRClient Update — index-modulo value map)
    - src/main/java/com/wfm/integration/MockBambooHRClient.java (current constructor call-sites)
  </read_first>
  <action>
    Update every `new BambooEmployee(...)` construction to pass the new `customWorkingdays` argument
    in the correct position. In the indexed loops (`listEmployees` ~92-101, `buildVintedAgents`
    ~61-71) reuse the existing `i % 5` pattern to vary the value: 0 -> "Mon-Fri", 1 -> "Sun-Thu",
    2 -> "Tue-Sat", 3 -> "Variable" (data-gap, exercises D-07 exclusion downstream),
    default -> "Mon. to Thurs." (4-day-week outlier, exercises D-05 flag). For the single-employee
    `getEmployee` (~line 109) with no index, use the fixed default "Mon-Fri".
  </action>
  <verify>
    <automated>grep -q '"Variable"' src/main/java/com/wfm/integration/MockBambooHRClient.java && grep -q 'Mon-Fri' src/main/java/com/wfm/integration/MockBambooHRClient.java && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All three BambooEmployee construction sites in MockBambooHRClient pass customWorkingdays.
    - Indexed sites vary across Mon-Fri / Sun-Thu / Tue-Sat / Variable / Mon. to Thurs.; getEmployee uses "Mon-Fri".
    - At least one mock employee emits "Variable" (data gap) and one emits an outlier 4-day week.
  </acceptance_criteria>
  <done>Mock emits varied, realistic customWorkingdays values including a data gap and an outlier.</done>
</task>

<task type="auto">
  <name>Task 4: Compile-gate the record change across all main call-sites</name>
  <files>src/main/java/com/wfm/integration/BambooEmployee.java</files>
  <read_first>
    - src/main/java/com/wfm/integration/HttpBambooHRClient.java (constructor call ~172-176 — confirm arg position)
    - src/main/java/com/wfm/integration/MockBambooHRClient.java (all three construction sites)
  </read_first>
  <action>
    No new code. This task is the in-wave compilation gate: after Tasks 1-3 added the positional
    `customWorkingdays` component and updated every main-source caller, run `./gradlew compileJava`
    to prove the positional argument ordering is correct across all main call-sites BEFORE wave 3
    consumes the record. (Test sources stay broken until plan 03 updates the
    BambooRefreshServiceTest emp() helper, so use `compileJava`, NOT `compileTestJava` or `test`.)
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileJava` exits 0 — all main-source BambooEmployee call-sites match the new positional component order.
  </acceptance_criteria>
  <done>Main source compiles clean with the new customWorkingdays component; positional ordering verified before wave 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BambooHR API -> integration layer | Field 4517 free-text crosses into BambooEmployee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-ID | Information Disclosure | HttpBambooHRClient request/response logging | mitigate | Keep count-only logging; never log request body or per-row employee values (PII) |
| T-6-IV | Tampering | Missing/garbled field 4517 in response | mitigate | `asText(null)` preserves null (vs empty string) so the parser treats it as a data gap, not a crash |
| T-6-SC | Tampering | BambooHR credential used by this call | mitigate | Covered by plan 01 Task 0 BLOCKING key rotation; this plan does not deploy until that clears |
</threat_model>

<verification>
- `./gradlew test --tests "com.wfm.integration.WorkingDaysParserTest"` still green (parser untouched).
- Main source compiles in-wave: Task 4 runs `./gradlew compileJava` (exits 0) once all main call-sites are updated. Full `test` compile waits on plan 03's test-helper update (D-08 sequencing).
- `grep '"4517"'` and `grep customWorkingdays` succeed in HttpBambooHRClient.
</verification>

<success_criteria>
- BambooEmployee carries customWorkingdays; the bulk report requests field 4517; the mock emits varied values incl. a data gap and an outlier.
- No per-employee API call added; no PII logged.
</success_criteria>

<output>
Create `.planning/phases/06-solver-quality-constraints/06-02-SUMMARY.md` when done.
</output>
