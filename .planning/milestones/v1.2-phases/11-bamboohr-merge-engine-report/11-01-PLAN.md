---
phase: 11-bamboohr-merge-engine-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/wfm/integration/AgentMergeService.java
  - src/main/java/com/wfm/dto/MergeReportEntry.java
  - src/main/java/com/wfm/service/DeskAssignmentUploadService.java
  - src/main/java/com/wfm/exception/BambooHRSyncFailedException.java
  - src/main/java/com/wfm/controller/GlobalExceptionHandler.java
  - src/main/java/com/wfm/integration/HttpBambooHRClient.java
  - src/main/resources/application.yml
  - frontend/src/api/client.ts
  - frontend/src/pages/ClientManagement.tsx
  - src/test/java/com/wfm/integration/UploadFreshSyncTest.java
  - src/test/java/com/wfm/integration/MergePrecedenceTest.java
  - src/test/java/com/wfm/integration/MergeReportTest.java
  - src/test/java/com/wfm/service/UploadSyncFailureTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadAllowlistTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadDayCellTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadEnrichedShapeTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadLegacyShapeTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadMultiSheetTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadNonSchedulableRejectTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadRetiredShapeTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadSpecialtyTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadValidationTest.java
autonomous: true
requirements: [MRG-01, MRG-02, MRG-04, MRG-05, MRG-07]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward core (D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-11, D-12, D-13) ---
    - "Every upload issues exactly one bambooHRClient.listEmployees call and exactly one bambooHRClient.listTimeOff call, before any database write, regardless of whether the ClientManagementService cache is already warm (MRG-01, D-01/D-04)"
    - "Both BambooHR fetches complete before transactionTemplate.executeWithoutResult opens the write transaction; uploadDeskAssignments carries no @Transactional annotation (MRG-01, D-01/D-02)"
    - "The upload path never calls BambooRefreshService.persistRefreshData and never writes an AgentDayOff row — the merge engine is the sole writer (D-03)"
    - "For an identity field where BambooHR has a non-blank value and the sheet supplies a different non-blank value, the persisted Agent carries BambooHR's value and the Upload Results modal shows a row with outcome BambooHR override (MRG-02/MRG-05, D-06/D-07)"
    - "For an identity field where BambooHR's value is absent and the sheet supplies a non-blank value, the persisted Agent carries the sheet's value and the modal shows a row with outcome Gap-filled by spreadsheet (MRG-02/MRG-04, D-06/D-08)"
    - "Per-day contracted hours and specializations are written from the spreadsheet only and are never contested by BambooHR (D-08)"
    - "A BambooHR sync failure during upload returns HTTP 503 with a message containing the phrase no changes were made, and the desk's roster, agent_day_hours rows and specialization links are unchanged from their pre-upload state (MRG-07, D-02)"
    - "Per-row and per-sheet parse failures still skip-and-continue exactly as in Phase 10 — one invalid row never aborts the upload (D-02)"

    # --- Edge probe: MRG-01 (3 of 3) ---
    - "An upload-triggered sync does not acquire BambooRefreshService's per-desk refreshInProgress guard and never throws RefreshInProgressException; a concurrent manual desk refresh and an upload each fetch independently # edge: MRG-01/adjacency (explicit)"
    - "When the fresh listEmployees fetch returns an empty list, every data row is skipped with reason BambooHR ID not found, zero agents are written, and the upload returns a normal result rather than an error # edge: MRG-01/empty (explicit)"
    - "When listEmployees returns two records carrying the same id, the in-memory snapshot keeps the first occurrence and discards later duplicates, so repeated uploads of identical BambooHR data resolve identically # edge: MRG-01/ordering (explicit)"

    # --- Edge probe: MRG-02 (3 of 3) ---
    - "When BambooHR and the sheet supply the same non-blank value for a field, no MergeReportEntry is emitted for that field (D-11 silent agreement) # edge: MRG-02/adjacency (explicit)"
    - "When both BambooHR and the sheet are null, empty or whitespace-only for a field, the Agent's previously stored value for that field is left unchanged and no MergeReportEntry is emitted # edge: MRG-02/empty (explicit)"
    - "mergeReport entries are ordered by worksheet order, then by spreadsheet row number, then by the fixed field order First name, Last name, Email, Department, Job title, Active status # edge: MRG-02/ordering (explicit)"

    # --- Edge probe: MRG-04 (4 of 4) ---
    - "At most one MergeReportEntry is emitted per (agent, field) pair per upload # edge: MRG-04/adjacency (explicit)"
    - "When mergeReport is empty the Merge Report section is not rendered at all — there is no empty-table state # edge: MRG-04/empty (explicit)"
    - "The Merge Report table renders rows in the server-supplied mergeReport array order with no client-side sorting or grouping # edge: MRG-04/ordering (explicit)"
    - statement: "Two uploads running concurrently against workbooks that touch the same desk each return their own merge report, and neither report contains rows produced by the other upload."
      verification: backstop
      # edge: MRG-04/concurrency

    # --- Edge probe: MRG-05 (5 of 5) ---
    - "A BambooHR value consisting only of whitespace counts as absent under D-06, so the sheet's value is persisted and the entry outcome is Gap-filled by spreadsheet rather than BambooHR override # edge: MRG-05/boundary (explicit)"
    - "Values are compared after trimming, so a sheet value differing from BambooHR's only by surrounding whitespace is treated as agreement and emits no entry # edge: MRG-05/adjacency (explicit)"
    - "When the sheet cell is blank and BambooHR has data, BambooHR's value is persisted and no entry is emitted, because nothing was overridden # edge: MRG-05/empty (explicit)"
    - "Entries for one agent are emitted in the fixed field order, so re-uploading an unchanged workbook against unchanged BambooHR data produces an identical mergeReport array # edge: MRG-05/ordering (explicit)"
    - "Per-day contracted hours are spreadsheet-only under D-08 and never appear as a merge-report row, so no numeric rounding, clamping or precision contract applies to any merge-report value # edge: MRG-05/precision (explicit)"

    # --- Edge probe: MRG-07 (7 of 7) ---
    - "A BambooHR response status of exactly 429 or exactly 503 raises BambooHRRateLimitedException; a missing or non-numeric Retry-After header yields the existing 60-second default # edge: MRG-07/boundary (explicit)"
    - "If listEmployees succeeds but listTimeOff throws, transactionTemplate.executeWithoutResult is never entered and zero rows are written # edge: MRG-07/adjacency (explicit)"
    - "The 503 response message always contains the no-changes-were-made clause, including when the upstream BambooHR reason string is empty or null # edge: MRG-07/empty (explicit)"
    - statement: "A BambooHR value containing non-ASCII characters (for example a Georgian agent name from the live StubHub - GE roster) round-trips through the merge report and the sync-failure message without mojibake or truncation."
      verification: backstop
      # edge: MRG-07/encoding
    - "On sync failure no BambooSyncEvent row is recorded, no Agent is saved, no agent_day_hours row is written and clearDesk is never invoked — the failure precedes every write in the upload path # edge: MRG-07/ordering (explicit)"
    - "retryAfterSeconds is surfaced as the parsed whole-second integer with no unit conversion; a fractional or non-numeric Retry-After value falls back to 60 # edge: MRG-07/precision (explicit)"
    - statement: "If the upload HTTP request is aborted by the client after the sync succeeds but while the write transaction is open, the transaction rolls back and the desk retains its complete pre-upload roster."
      verification: backstop
      # edge: MRG-07/concurrency

    # --- UI-SPEC ## UI Considerations lift: E1 Merge Report table (7 explicit + 1 backstop) ---
    - "E1 empty: the Merge Report section is omitted via a mergeReport length guard, mirroring the existing sheetSummaries and warnings conditional-render idiom"
    - "E1 loading: the report arrives synchronously inside the same DeskAssignmentUploadResult payload as every other modal field, so no independent loading state is rendered"
    - "E1 error: per-field merge computation cannot fail independently of the whole upload; any failure surfaces as the whole-upload sync-failure toast, not a report-local error state"
    - "E1 populated: the table renders columns BambooHR ID, Agent, Field, BambooHR value, Sheet value, Outcome, one row per divergent or gap-filled field"
    - "E1 partial: every row in the table is itself a divergence signal by construction, so no separate incomplete-row state exists beyond the Outcome badge"
    - "E1 overflow: the table sits in a scroll container with overflowY auto and maxHeight 300px, matching the existing Skipped Rows table, so a large roster scrolls rather than expanding the modal"
    - "E1 zero-one-many: the section header is pluralization-aware in the form Merge Report (N fields across M agents), and N of zero means the section is absent"
    - statement: "Merge Report value cells wrap rather than overflow the 760px modal when given a genuinely long BambooHR job title or email; confirmed visually against realistic long-value fixture data."
      verification: backstop
      # ui: E1/long-text

    # --- UI-SPEC ## UI Considerations lift: E3 Upload action (4 explicit) ---
    - "E3 loading: the existing disabled Uploading button state already covers the in-flight sync call and is unchanged by this phase"
    - "E3 error: the existing showToast error catch path around clientManagement.uploadDeskAssignments handles the MRG-07 503 with no new UI code"
    - "E3 overflow: both upload-button label states are fixed string literals, so no data-driven overflow axis exists"
    - "E3 long-text: the upload control's text is a hardcoded literal with no interpolated data, so there is no long-text case to design"

    # --- UI-SPEC ## UI Considerations lift: E4 Sync-failure toast (2 backstop) ---
    - statement: "The sync-failure toast renders the lengthened MRG-07 message without overflowing the viewport, given Toast.tsx's 400px maxWidth and no explicit wrapping rule."
      verification: backstop
      # ui: E4/overflow
    - statement: "Toast.tsx wraps or clamps an unbounded backend message without truncating the no-changes-were-made clause out of view."
      verification: backstop
      # ui: E4/long-text

  prohibitions:
    - "Spreadsheet values that BambooHR overrides must never be silently discarded — every discarded value carries a merge-report row the operator can see."
    - "The fresh-sync-per-upload guarantee must never be relaxed into reuse of a cached or recent sync for latency reasons."

  artifacts:
    - path: "src/main/java/com/wfm/integration/AgentMergeService.java"
      provides: "Pre-transaction batched BambooHR snapshot fetch and per-field precedence merge"
      contains: "class AgentMergeService"
      min_lines: 120
    - path: "src/main/java/com/wfm/dto/MergeReportEntry.java"
      provides: "Ephemeral per-divergence report row returned in DeskAssignmentUploadResult"
      contains: "record MergeReportEntry"
    - path: "src/main/java/com/wfm/exception/BambooHRSyncFailedException.java"
      provides: "Non-rate-limit BambooHR sync failure carrying an operator-readable message"
      contains: "class BambooHRSyncFailedException"
    - path: "src/test/java/com/wfm/integration/UploadFreshSyncTest.java"
      provides: "End-to-end proof that the fetch precedes the transaction and the merged value reaches the report"
    - path: "src/test/java/com/wfm/integration/MergePrecedenceTest.java"
      provides: "Field-by-field precedence coverage for the D-08 contested field set"
    - path: "src/test/java/com/wfm/integration/MergeReportTest.java"
      provides: "Report shape, ordering, silent-agreement suppression and gap-fill coverage"
    - path: "src/test/java/com/wfm/service/UploadSyncFailureTest.java"
      provides: "Zero-write proof and operator-message coverage for sync failure"

  key_links:
    - from: "src/main/java/com/wfm/service/DeskAssignmentUploadService.java"
      to: "src/main/java/com/wfm/integration/AgentMergeService.java"
      via: "uploadDeskAssignments calls fetchSnapshot before opening the transaction"
      pattern: "agentMergeService\\.fetchSnapshot"
    - from: "src/main/java/com/wfm/service/DeskAssignmentUploadService.java"
      to: "org.springframework.transaction.support.TransactionTemplate"
      via: "the sheet loop and every write run inside executeWithoutResult"
      pattern: "transactionTemplate\\.executeWithoutResult"
    - from: "frontend/src/pages/ClientManagement.tsx"
      to: "frontend/src/api/client.ts"
      via: "modal renders uploadResult.mergeReport typed as MergeReportEntry[]"
      pattern: "uploadResult\\.mergeReport"
---

<objective>
Deliver the fresh-sync-before-merge upload path end to end: one batched BambooHR fetch outside the
transaction, true BambooHR-first per-field precedence for identity fields, an ephemeral merge report
carried in `DeskAssignmentUploadResult`, and a sync failure that aborts with zero writes and a clear
operator message.

Purpose: MRG-01, MRG-02, MRG-04, MRG-05 and MRG-07 all rest on one structural change — moving the
BambooHR fetch outside the transaction boundary and inverting the identity-field precedence direction
that Phase 10 established. Proving that path end to end first means every later expansion task builds
on a shape already known to work.

Output: `AgentMergeService`, `MergeReportEntry`, a restructured `uploadDeskAssignments`, a Merge
Report section in the Upload Results modal, an explicit BambooHR HTTP timeout, and four new test
classes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bamboohr-merge-engine-report/11-CONTEXT.md
@.planning/phases/11-bamboohr-merge-engine-report/11-RESEARCH.md
@.planning/phases/11-bamboohr-merge-engine-report/11-PATTERNS.md
@.planning/phases/11-bamboohr-merge-engine-report/11-UI-SPEC.md
</context>

## Artifacts this phase produces

New symbols created by Phase 11. Drift verification must treat these as newly created, not as
pre-existing symbols that changed.

**Created by this plan (11-01):**

| Symbol | Kind | File |
|---|---|---|
| `com.wfm.integration.AgentMergeService` | class | `src/main/java/com/wfm/integration/AgentMergeService.java` |
| `AgentMergeService.BambooSnapshot` | nested record (`employeesById`, `timeOffByEmployeeId`, `windowFrom`, `windowTo`) | same |
| `AgentMergeService.fetchSnapshot(long tenantId)` | method | same |
| `AgentMergeService.mergeIdentityFields(...)` | method | same |
| `AgentMergeService.hasData(String)` | static method (D-06 predicate) | same |
| `AgentMergeService.OUTCOME_BAMBOOHR_OVERRIDE` | constant | same |
| `AgentMergeService.OUTCOME_GAP_FILLED` | constant | same |
| `com.wfm.dto.MergeReportEntry` | record (`bamboohrId`, `agentName`, `field`, `bambooValue`, `sheetValue`, `outcome`) | `src/main/java/com/wfm/dto/MergeReportEntry.java` |
| `DeskAssignmentUploadResult.mergeReport` | new record component | `src/main/java/com/wfm/service/DeskAssignmentUploadService.java` |
| `com.wfm.exception.BambooHRSyncFailedException` | class | `src/main/java/com/wfm/exception/BambooHRSyncFailedException.java` |
| `GlobalExceptionHandler.handleBambooHRSyncFailed` | method | `src/main/java/com/wfm/controller/GlobalExceptionHandler.java` |
| `bamboohr.http.connect-timeout-seconds` | config key | `src/main/resources/application.yml` |
| `bamboohr.http.read-timeout-seconds` | config key | same |
| `MergeReportEntry` (TS) | interface | `frontend/src/api/client.ts` |
| `DeskAssignmentUploadResult.mergeReport` (TS) | interface field | same |
| `UploadFreshSyncTest`, `MergePrecedenceTest`, `MergeReportTest`, `UploadSyncFailureTest` | test classes | see `files_modified` |

**Created by sibling plans (listed here so drift verification sees the whole phase surface):**
`com.wfm.model.WorkingDaysSource` (enum), `Agent.workingDaysSource` (field + accessors),
`agent.working_days_source` (column), `V36__add_agent_working_days_source.sql` (migration),
`DeskAssignmentUploadResult.newlyEligibleAgents` (record component + TS field),
`AgentMergeService.mergeWorkingPattern(...)`, `SolverService.arbitratePtoAgainstBambooWindow(...)`,
`WorkingDaysKnownTest`, `PtoArbitrationTest`.

## Decisions carried into this plan

| Decision | Rating | Note |
|---|---|---|
| D-02 whole-upload atomicity for sync failure | costly | Loosening to per-sheet atomicity later means unpicking the single transaction boundary the merge engine is built inside. |
| D-03 read-only snapshot, merge engine is sole writer | costly | The precedence rules assume nothing else has already written days off for these agents. |
| Claude's discretion — `BambooSyncEvent` is NOT recorded for upload-triggered syncs | reversible | `BambooSyncEvent` is desk-scoped and drives the per-desk latest-sync display; a whole-workbook read-only snapshot that writes nothing would pollute it. Research A3 records either choice as low-risk. Undo is one added call. |
| Claude's discretion — merge engine lives in `com.wfm.integration` | reversible | Required, not preferred: `WorkingDaysParser` is package-private there (research Pitfall 3, A2). Undo is a package move. |
| Open Question 3 resolved — BambooHR HTTP timeout | reversible | Located during planning: `HttpBambooHRClient` line 48 uses `RestClient.create()` with no request factory, so **no timeout is configured today**. D-04's "longer timeout" therefore needs an explicit value, not a bump. |

**Open Question 2 (carried, not fixed):** `refreshInProgress` does not coordinate with the upload's
batched fetch. A manual desk refresh and an upload can fetch from BambooHR concurrently. This is a
pre-existing design gap that Phase 11 makes more frequent, not one it introduces. Per RESEARCH.md's
recommendation and CONTEXT.md's silence, it is **noted, not fixed** — recorded as the MRG-01/adjacency
truth above so the behaviour is documented rather than assumed away. MRG-07's 503 already covers the
rate-limit consequence.

<!-- planner-discipline-allow: @Transactional -->
<!-- planner-discipline-allow: ensureCachePopulatedForUpload -->
<!-- planner-discipline-allow: RestClient.create() -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "upload merges Email against a fresh BambooHR snapshot" — one path only</name>
  <reversibility rating="costly">Undoing the transaction-boundary restructure means re-annotating uploadDeskAssignments @Transactional and unpicking the TransactionTemplate the merge engine, its tests and every later expansion task are built inside.</reversibility>
  <files>src/main/java/com/wfm/dto/MergeReportEntry.java, src/main/java/com/wfm/integration/AgentMergeService.java, src/main/java/com/wfm/service/DeskAssignmentUploadService.java, frontend/src/api/client.ts, frontend/src/pages/ClientManagement.tsx, src/test/java/com/wfm/integration/UploadFreshSyncTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadAllowlistTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadDayCellTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadEnrichedShapeTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadLegacyShapeTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadMultiSheetTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadNonSchedulableRejectTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadRetiredShapeTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadSpecialtyTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadValidationTest.java</files>
  <read_first>
    - `src/main/java/com/wfm/service/DeskAssignmentUploadService.java` — the full file. The restructure target: `@Transactional` at line 77, `ensureCachePopulatedForUpload` at 83, try-with-resources `try (workbook)` at 132, sheet loop 199-480, ID-match gate 333-350, identity backfill 365-386, `agent_day_hours` write 456-473, return at 483, `DeskAssignmentUploadResult` record at 553.
    - `src/main/java/com/wfm/integration/BambooRefreshService.java` lines 36-145 — the exact HTTP-before-transaction template this task copies: constructor injection of `TransactionTemplate`, the `@Value` lookback/lookahead fields at 47-51, and `refreshDeskAgents`'s fetch-then-`executeWithoutResult` shape at 90-144.
    - `src/main/java/com/wfm/integration/BambooEmployee.java` — the raw record carrying `customWorkingdays` and `employmentHistoryStatus`, which the cache DTO does not.
    - `src/main/java/com/wfm/service/ClientManagementService.java` lines 140-200 — `findEmployeeStatus` and `ensureCachePopulatedForUpload`; confirm the no-op-if-warm early return before replacing them.
    - `src/main/java/com/wfm/dto/SkippedRow.java` and `src/main/java/com/wfm/dto/SkippedSheet.java` — the sibling ephemeral-DTO style `MergeReportEntry` must match.
    - `frontend/src/api/client.ts` lines 455-480 — the `DeskAssignmentUploadResult` interface to extend field-for-field.
    - `frontend/src/pages/ClientManagement.tsx` lines 486-560 — the modal container width at 490, the per-desk rollup block at 505-516, the Warnings box at 517-529, the Skipped Rows table at 530-553.
    - `src/test/java/com/wfm/service/DeskAssignmentUploadMultiSheetTest.java` lines 1-140 — the `@BeforeEach` mock-collaborator wiring and the `stubCached`-style helper every sibling upload test repeats.
  </read_first>
  <action>
Wire ONE identity field (Email) from a fresh BambooHR fetch all the way through to a rendered report
row, with no other field merged and no other call site touched.

Create `com.wfm.dto.MergeReportEntry` as a record with components `bamboohrId`, `agentName`, `field`,
`bambooValue`, `sheetValue`, `outcome`, all `String`, no behaviour — matching the `SkippedRow` style.

Create `com.wfm.integration.AgentMergeService` annotated `@Service`, in package `com.wfm.integration`
(mandatory: `WorkingDaysParser` is package-private there and later tasks need it). Constructor-inject
`BambooHRClient`. Add `@Value("${bamboohr.time-off.lookahead-weeks:8}")` and
`@Value("${bamboohr.time-off.lookback-weeks:12}")` fields reusing the same keys as
`BambooRefreshService`. Expose a nested public record `BambooSnapshot` with components
`Map<String, BambooEmployee> employeesById`, `Map<String, List<BambooTimeOff>> timeOffByEmployeeId`,
`LocalDate windowFrom`, `LocalDate windowTo`.

Add `public BambooSnapshot fetchSnapshot(long tenantId)`. It calls
`bambooHRClient.listEmployees(String.valueOf(tenantId), null)` — null project so one fetch serves all
sheets per D-01 — then computes `windowFrom` as `LocalDate.now().minusWeeks(lookbackWeeks)` and
`windowTo` as `LocalDate.now().plusWeeks(lookaheadWeeks)` and calls
`bambooHRClient.listTimeOff(String.valueOf(tenantId), windowFrom, windowTo)`. Key employees by
`BambooEmployee.id()` trimmed, keeping the FIRST occurrence on duplicate ids (use `putIfAbsent`,
mirroring the `dedupedDaysOff` idiom in `BambooRefreshService.persistRefreshData`). Retain ALL
employees regardless of `status()`, because the upload's skip-reason logic must distinguish an
unknown id from an inactive employee. Group time-off by `BambooTimeOff.employeeId()`. Perform no
database access and no writes — D-03.

Add `public static boolean hasData(String value)` returning true when the value is non-null and not
blank — the single D-06 predicate every field uses. Add public String constants
`OUTCOME_BAMBOOHR_OVERRIDE` with value `BambooHR override` and `OUTCOME_GAP_FILLED` with value
`Gap-filled by spreadsheet`, matching the UI-SPEC's fixed Outcome vocabulary.

Add `public String mergeIdentityFields(...)`-shaped support for the tracer's single field only: a
method that takes the BambooHR value, the sheet value, the field label, the BambooHR id, the agent
name and a mutable `List<MergeReportEntry>` accumulator; returns the winning value; and appends an
entry only when the two sources genuinely diverge. Winning value is the BambooHR value when
`hasData` is true, otherwise the sheet value. Append an `OUTCOME_BAMBOOHR_OVERRIDE` entry only when
BambooHR has data AND the sheet has data AND the two differ after trimming. Append an
`OUTCOME_GAP_FILLED` entry only when BambooHR lacks data AND the sheet has data. Emit nothing in
every other case, including both-blank and exact agreement.

Restructure `DeskAssignmentUploadService.uploadDeskAssignments`. Remove the `@Transactional`
annotation from the method and the now-unused `org.springframework.transaction.annotation.Transactional`
import — Spring's proxy opens the transaction before the body runs, so a fetch added at the top would
still execute inside it. Add `AgentMergeService` and
`org.springframework.transaction.support.TransactionTemplate` as constructor parameters. Delete the
`clientManagementService.ensureCachePopulatedForUpload(tenantId)` call: it returns early when the
cache key already exists, which contradicts D-04's always-fresh guarantee. Call
`agentMergeService.fetchSnapshot(tenantId)` as the first statement after reading `tenantId`, before
the workbook is opened. Keep the `try (workbook)` block, and wrap the existing sheet loop — every
statement from the `deskIdsInThisWorkbook` pre-scan through `sheetSummaries.add(...)` — in a single
`transactionTemplate.executeWithoutResult(status -> { ... })`. The accumulator lists (`assigned`,
`skipped`, `skippedSheets`, `warnings`, `sheetSummaries` and the new `mergeReport`) stay declared
outside the lambda so they remain effectively final and survive it. Per-row `continue` statements and
every skip reason stay exactly as they are — D-02 makes only sync failure abort the upload, never a
parse failure.

Replace the ID-match gate at lines 333-350. Instead of `clientManagementService.findCachedEmployee`
and `findEmployeeStatus`, look the trimmed BambooHR id up in `snapshot.employeesById()`. A miss keeps
the reason `BambooHR ID not found`. A hit whose `status()` is not `Active` keeps the reason
`Agent is not active in BambooHR (status: ` plus the status plus `)`. The reactivation line
`agent.setActive("Active".equalsIgnoreCase(cached.status()))` now reads the raw
`BambooEmployee.status()`.

Replace the identity block at lines 365-386 for the Email field ONLY, inverting Phase 10's direction:
call the merge helper with `employee.workEmail()` as the BambooHR value, the sheet's `email` cell as
the sheet value, and the field label `Email`, then `agent.setEmail(...)` with the returned winner.
Leave first name, last name, job title, department and active-status handling exactly as they are for
now — Task 2 converts them. Do not add commentary describing the old direction.

Extend the `DeskAssignmentUploadResult` record with a trailing component
`List<MergeReportEntry> mergeReport` and pass the accumulator at the return site on line 483.

Mirror the DTO in `frontend/src/api/client.ts`: export a `MergeReportEntry` interface with the six
string fields in the same order, and add `mergeReport: MergeReportEntry[]` as a trailing field on
`DeskAssignmentUploadResult`.

In `frontend/src/pages/ClientManagement.tsx`, widen the modal container's fixed `width` from `600px`
to `760px` (the six-column table crowds at 600px) and add a Merge Report section between the per-desk
rollup block and the Warnings box, guarded by a `uploadResult.mergeReport.length > 0` conditional in
the same idiom as the sibling sections. Give it a heading in the existing sibling style
(`fontWeight: 600, fontSize: '0.85rem'`) reading `Merge Report`, and a table inside a container using
`overflowY: 'auto'`, `maxHeight: '300px'`, `border: '1px solid #e5e7eb'`, `borderRadius: '4px'`,
copying the Skipped Rows table container. Columns in order: BambooHR ID, Agent, Field, BambooHR value,
Sheet value, Outcome. Render Outcome as a small pill: solid background `#92400e` with `#ffffff` text
for the override outcome, solid background `#3b82f6` with `#ffffff` text for the gap-fill outcome —
`#3b82f6` is reserved for this badge and must not appear on any other new element. Value cells set
`wordWrap: 'break-word'` and must not set `whiteSpace: 'nowrap'`. Import `MergeReportEntry` from the
API client alongside the existing `SkippedRow` and `SheetSummary` type imports. Header pluralization
and the eligibility callout are Task-2 and plan-02 work; the tracer's header may be the bare label.

Update all nine `DeskAssignmentUpload*Test` classes that construct the service. Add the two new
constructor arguments. Create the `TransactionTemplate` collaborator as a Mockito mock and stub it in
`@BeforeEach` so the lambda actually runs, using `doAnswer` to pull argument 0 as a
`java.util.function.Consumer` and call `accept(null)` on it, then `when(transactionTemplate).executeWithoutResult(any())`
— without this stub the mock silently swallows every write and all nine suites go red for the wrong
reason. Create the `AgentMergeService` collaborator as a real instance wrapping a
`mock(BambooHRClient.class)`, or as a mock returning a prepared `BambooSnapshot`; whichever is used,
replace each file's `clientManagementService.findCachedEmployee` and `findEmployeeStatus` stubbing
with BambooHR fixtures so the same agents still resolve. In `DeskAssignmentUploadAllowlistTest` the
two negative cases at lines 238-252 must keep their distinct outcomes: an id absent from the snapshot
yields the not-found reason, and an id present with status `Inactive` yields the not-active reason.

Create `src/test/java/com/wfm/integration/UploadFreshSyncTest.java` as a plain JUnit 5 unit test with
no Spring context, following `DeskAssignmentUploadMultiSheetTest`'s shape: `mock(...)` collaborators,
`MockMultipartFile` plus `XSSFWorkbook` for an in-memory workbook, `TenantContext.setTenantId(...)`
in `@BeforeEach`. Build a one-sheet workbook with one row whose Email cell holds a value that differs
from the stubbed `BambooEmployee.workEmail()`, and all seven day cells populated. Assert with a
Mockito `InOrder` across the BambooHR client and the transaction template that `listEmployees` and
`listTimeOff` each ran exactly once and both ran before `executeWithoutResult`; that the saved `Agent`
carries BambooHR's email; and that the returned result's `mergeReport()` holds exactly one entry with
field `Email` and the override outcome.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.integration.UploadFreshSyncTest" --tests "com.wfm.service.DeskAssignmentUpload*Test" && npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.wfm.integration.UploadFreshSyncTest"` exits 0
    - `./gradlew test --tests "com.wfm.service.DeskAssignmentUpload*Test"` exits 0 — all nine pre-existing suites still green
    - `npm --prefix frontend run build` exits 0
    - `grep -c '@Transactional' src/main/java/com/wfm/service/DeskAssignmentUploadService.java` returns 0
    - `grep -c 'ensureCachePopulatedForUpload' src/main/java/com/wfm/service/DeskAssignmentUploadService.java` returns 0
    - `grep -q 'transactionTemplate.executeWithoutResult' src/main/java/com/wfm/service/DeskAssignmentUploadService.java`
    - `grep -q 'agentMergeService.fetchSnapshot' src/main/java/com/wfm/service/DeskAssignmentUploadService.java`
    - `src/main/java/com/wfm/integration/AgentMergeService.java` contains `package com.wfm.integration;` and `record BambooSnapshot`
    - `grep -q 'listEmployees(String.valueOf(tenantId), null)' src/main/java/com/wfm/integration/AgentMergeService.java`
    - `src/main/java/com/wfm/dto/MergeReportEntry.java` contains `public record MergeReportEntry(`
    - `frontend/src/api/client.ts` contains `mergeReport: MergeReportEntry[]`
    - `grep -q "width: '760px'" frontend/src/pages/ClientManagement.tsx`
    - `grep -q 'uploadResult.mergeReport' frontend/src/pages/ClientManagement.tsx`
    - Behavior: uploading a row whose Email differs from BambooHR's persists BambooHR's email and returns one merge-report entry with outcome `BambooHR override`
  </acceptance_criteria>
  <done>One agent's Email merges BambooHR-first against a fetch that provably precedes the transaction, and the divergence renders in the Upload Results modal. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand precedence to the full D-08 contested field set with report coverage</name>
  <files>src/main/java/com/wfm/integration/AgentMergeService.java, src/main/java/com/wfm/service/DeskAssignmentUploadService.java, frontend/src/pages/ClientManagement.tsx, src/test/java/com/wfm/integration/MergePrecedenceTest.java, src/test/java/com/wfm/integration/MergeReportTest.java</files>
  <read_first>
    - `src/main/java/com/wfm/integration/AgentMergeService.java` — as written by Task 1; the merge helper and outcome constants are the extension point.
    - `src/main/java/com/wfm/service/DeskAssignmentUploadService.java` lines 350-400 — the identity block after Task 1, with Email already converted and five fields still on Phase 10's direction.
    - `src/main/java/com/wfm/util/AgentNameSplitter.java` — how a BambooHR `displayName` splits into first and last name, needed because BambooHR carries a combined display name where the sheet carries two columns.
    - `.planning/phases/11-bamboohr-merge-engine-report/11-UI-SPEC.md` § Copywriting Contract — the exact Field label strings and the pluralized section-header form.
    - `src/test/java/com/wfm/integration/UploadFreshSyncTest.java` — the fixture-building helpers to reuse rather than re-derive.
  </read_first>
  <behavior>
    - BambooHR populated, sheet populated and different → BambooHR value persisted, one override entry
    - BambooHR populated, sheet blank → BambooHR value persisted, zero entries
    - BambooHR blank, sheet populated → sheet value persisted, one gap-fill entry
    - BambooHR blank, sheet blank → stored value untouched, zero entries
    - BambooHR whitespace-only, sheet populated → treated as blank, gap-fill entry not override entry
    - Sheet value differing only by surrounding whitespace → agreement, zero entries
    - Two uploads of the same unchanged workbook → identical entry arrays in identical order
    - At most one entry per (agent, field)
  </behavior>
  <action>
Convert the remaining five contested identity fields to BambooHR-first precedence and give the report
its full field vocabulary.

In `AgentMergeService`, generalise the Task-1 merge helper into a reusable per-field routine and apply
it to the D-08 field set. Field labels, exactly as the UI-SPEC's Copywriting Contract fixes them:
`First name`, `Last name`, `Email`, `Department`, `Job title`, `Active status`. First and last name
take their BambooHR side by running `AgentNameSplitter.split` over `BambooEmployee.displayName()` and
using `firstName()` and `lastName()` respectively. Job title takes `BambooEmployee.jobTitle()`,
department takes `BambooEmployee.department()`, email takes `workEmail()`. Active status takes
`BambooEmployee.status()`, with the BambooHR side rendered for the report as `Active` when the status
equals `Active` case-insensitively and `Inactive` otherwise, and the sheet side rendered by running
the existing `parseActive` result through the same two labels.

Emit entries by exactly the same rules the tracer established: an override entry only when both sides
have data and differ after trimming, a gap-fill entry only when BambooHR lacks data and the sheet has
data, nothing otherwise. Compare after trimming so a whitespace-only difference is agreement. Iterate
the fields in the fixed order listed above so the emitted array is deterministic across runs.

In `DeskAssignmentUploadService`, replace the whole identity region — both the BambooHR backfill block
and the sheet-overrides block — with a single call into the merge routine per field, assigning each
returned winner via the corresponding `agent.set...` call. Keep `agent.setName(...)` derived from the
merged first and last name via the existing `joinName` helper, and keep `agent.setLastRefreshedAt(OffsetDateTime.now())`.
Preserve the surrounding skip gates unchanged: the desk-conflict check, the inactive check and the
job-title allowlist check must keep their current order and their current reasons, and must run
against the merged values.

Log merge decisions at most at INFO with the agent UUID and the field label only. Raw BambooHR or
sheet values go to DEBUG only, matching the convention stated in `WorkingDaysParser`'s class Javadoc.

In `ClientManagement.tsx`, make the section header pluralization-aware in the form
`Merge Report (N fields across M agents)`, computing N as the entry count and M as the count of
distinct `bamboohrId` values, and switching each noun's plural suffix on a not-equal-to-one test in
the same style the file already uses elsewhere.

Create `src/test/java/com/wfm/integration/MergePrecedenceTest.java` covering the persisted-value
outcome for each of the six fields across the populated/blank/whitespace combinations in the
`<behavior>` list. Create `src/test/java/com/wfm/integration/MergeReportTest.java` covering the report
side: entry presence and absence per combination, the fixed field ordering, the at-most-one-per-field
guarantee, and byte-identical arrays across two runs of the same fixture. Both follow the no-Spring,
mocked-collaborator style of the existing upload suites.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.integration.MergePrecedenceTest" --tests "com.wfm.integration.MergeReportTest" --tests "com.wfm.service.DeskAssignmentUpload*Test" && npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.wfm.integration.MergePrecedenceTest" --tests "com.wfm.integration.MergeReportTest"` exits 0
    - `./gradlew test --tests "com.wfm.service.DeskAssignmentUpload*Test"` exits 0
    - `npm --prefix frontend run build` exits 0
    - `src/main/java/com/wfm/integration/AgentMergeService.java` contains the literals `First name`, `Last name`, `Email`, `Department`, `Job title` and `Active status`
    - `grep -c 'agent.setEmail(email.trim())' src/main/java/com/wfm/service/DeskAssignmentUploadService.java` returns 0 — Phase 10's sheet-wins assignment is gone
    - `grep -c 'isBlank(agent.getEmail())' src/main/java/com/wfm/service/DeskAssignmentUploadService.java` returns 0 — the backfill-from-cache form is gone
    - `grep -q 'Merge Report (' frontend/src/pages/ClientManagement.tsx`
    - Behavior: a row where BambooHR has a job title and the sheet supplies a different one persists BambooHR's title and reports one `Job title` override row
    - Behavior: a row where BambooHR's department is whitespace-only and the sheet supplies one persists the sheet's department and reports a `Gap-filled by spreadsheet` row, not an override row
  </acceptance_criteria>
  <done>All six contested identity fields resolve BambooHR-first, every divergence and gap-fill appears exactly once in a deterministically ordered report, and the modal header counts fields and agents correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sync-failure abort — clear operator message, explicit timeout, zero writes</name>
  <reversibility rating="costly">The no-changes-were-made contract becomes operator-visible copy the UI-SPEC and success criterion 6 both cite; changing the failure message shape later means revisiting the UI contract as well as the exception surface.</reversibility>
  <files>src/main/java/com/wfm/exception/BambooHRSyncFailedException.java, src/main/java/com/wfm/controller/GlobalExceptionHandler.java, src/main/java/com/wfm/integration/AgentMergeService.java, src/main/java/com/wfm/integration/HttpBambooHRClient.java, src/main/resources/application.yml, src/test/java/com/wfm/service/UploadSyncFailureTest.java</files>
  <read_first>
    - `src/main/java/com/wfm/controller/GlobalExceptionHandler.java` — the full file. Critical: `handleUncaught` at lines 80-85 discards `ex.getMessage()` and returns the fixed string `An unexpected error occurred`, so a non-rate-limit sync failure currently reaches the operator with no diagnostic at all.
    - `src/main/java/com/wfm/exception/BambooHRRateLimitedException.java` — the two-argument constructor and `getRetryAfterSeconds`.
    - `src/main/java/com/wfm/integration/HttpBambooHRClient.java` lines 34-130 — `RestClient.create()` at line 48 with no request factory and therefore no configured timeout; `parseRetryAfterSeconds` at 102-111; `applyRateLimitHandler` at 117-127 with its 429/503 predicate and 60-second default.
    - `src/main/resources/application.yml` lines 36-43 — the existing `bamboohr` config block the new `http` sub-block joins.
    - `.planning/phases/11-bamboohr-merge-engine-report/11-UI-SPEC.md` § Copywriting Contract, Error state row — the required message shape.
    - `src/main/java/com/wfm/integration/AgentMergeService.java` — `fetchSnapshot` as written by Task 1, the single place both fetches happen.
  </read_first>
  <behavior>
    - `listEmployees` throws a rate-limit error → HTTP 503, code `BAMBOOHR_RATE_LIMITED`, message contains the no-changes clause, `retryAfterSeconds` preserved
    - `listTimeOff` throws after `listEmployees` succeeded → same 503, and the transaction was never entered
    - `listEmployees` throws a non-rate-limit runtime error → HTTP 503, code `BAMBOOHR_SYNC_FAILED`, message contains the no-changes clause and the upstream reason
    - Upstream reason is null or empty → message still contains the no-changes clause
    - Any sync failure → zero `agentRepository.save`, zero `agentDayHoursRepository.save`, zero `clearDesk` invocations
  </behavior>
  <action>
Close MRG-07 for every sync-failure mode, not just the rate-limited one.

Create `com.wfm.exception.BambooHRSyncFailedException` extending `RuntimeException` with a
message-and-cause constructor, mirroring the shape of the existing BambooHR exception in the same
package. Register a handler in `GlobalExceptionHandler` immediately after
`handleBambooHRRateLimited`, mapping it to `HttpStatus.SERVICE_UNAVAILABLE` with code
`BAMBOOHR_SYNC_FAILED` and `ex.getMessage()` as the message, using the existing `buildResponse`
helper. This is required because `handleUncaught` replaces any message with a fixed string, so
without a dedicated handler a timeout or a BambooHR 500 reaches the operator with no reason at all.

Wrap both fetch calls in `AgentMergeService.fetchSnapshot` in a single try. Catch
`BambooHRRateLimitedException` and rethrow a new `BambooHRRateLimitedException` whose message is the
upload-specific operator sentence and whose `retryAfterSeconds` is copied from the caught instance —
preserving the existing 503 and `BAMBOOHR_RATE_LIMITED` code path. Catch any other `RuntimeException`
and rethrow it as `BambooHRSyncFailedException` with the same sentence shape and the original as the
cause. Both messages take the form: `BambooHR sync failed (` then the upstream reason then
`) — no changes were made. Retry the upload once BambooHR is available.` When the upstream reason is
null or blank, substitute the literal `no detail available` inside the parentheses so the sentence
stays well-formed. Never swallow, never log-and-continue: every path out of the catch rethrows, so the
throw still precedes `transactionTemplate.executeWithoutResult` and zero writes is a structural
consequence rather than something the merge engine has to arrange.

Leave `HttpBambooHRClient.applyRateLimitHandler`'s own message untouched so the manual desk-refresh
path keeps its existing wording; the upload-specific sentence is applied only at the upload's fetch
site.

Give the BambooHR HTTP client an explicit timeout. `RestClient.create()` at line 48 configures no
request factory, so the whole-tenant `listEmployees` fetch D-04 introduces currently has no bounded
read timeout at all. Replace that call with a builder that installs a
`SimpleClientHttpRequestFactory` (or the project's equivalent JDK-backed factory) carrying a connect
timeout and a read timeout, both read from configuration via `@Value` with the keys
`bamboohr.http.connect-timeout-seconds` defaulting to 10 and `bamboohr.http.read-timeout-seconds`
defaulting to 120. The 120-second read default is D-04's "longer timeout" made concrete: it must
accommodate a whole-tenant fetch rather than a single desk's. Add both keys under an `http` block
inside the existing `bamboohr` section of `application.yml`, alongside `mock`, `api-key`, `subdomain`
and `time-off`. Leave the package-private test constructor that accepts a pre-built
`RestClient.Builder` unchanged so `HttpBambooHRClient503Test` keeps working.

Create `src/test/java/com/wfm/service/UploadSyncFailureTest.java` in the established no-Spring style.
Stub `BambooHRClient.listEmployees` to throw, then assert the thrown type and that its message
contains the no-changes clause; repeat with `listEmployees` succeeding and `listTimeOff` throwing;
repeat with a plain `RuntimeException` asserting `BambooHRSyncFailedException`; and add a case whose
upstream message is empty. In every case use `verifyNoInteractions` or `verify(..., never())` on the
agent repository, the agent-day-hours repository and the transaction template to prove no write and
no transaction were attempted.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.UploadSyncFailureTest" --tests "com.wfm.integration.HttpBambooHRClient503Test" --tests "com.wfm.integration.UploadFreshSyncTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.wfm.service.UploadSyncFailureTest"` exits 0
    - `./gradlew test --tests "com.wfm.integration.HttpBambooHRClient503Test"` exits 0 — the pre-existing 503 suite still green
    - `src/main/java/com/wfm/exception/BambooHRSyncFailedException.java` contains `class BambooHRSyncFailedException`
    - `grep -q 'BAMBOOHR_SYNC_FAILED' src/main/java/com/wfm/controller/GlobalExceptionHandler.java`
    - `grep -q 'no changes were made' src/main/java/com/wfm/integration/AgentMergeService.java`
    - `grep -q 'bamboohr.http.read-timeout-seconds' src/main/java/com/wfm/integration/HttpBambooHRClient.java`
    - `grep -q 'read-timeout-seconds' src/main/resources/application.yml`
    - `grep -c 'RestClient.create()' src/main/java/com/wfm/integration/HttpBambooHRClient.java` returns 0
    - Behavior: a rate-limited `listEmployees` yields HTTP 503 with code `BAMBOOHR_RATE_LIMITED`, a preserved `retryAfterSeconds`, and a message containing the no-changes clause
    - Behavior: a non-rate-limit `listTimeOff` failure yields HTTP 503 with code `BAMBOOHR_SYNC_FAILED` and the upstream reason inside the message
    - Behavior: after any sync failure the agent repository, the agent-day-hours repository and the transaction template record zero interactions
  </acceptance_criteria>
  <done>Every BambooHR sync-failure mode reaches the operator as a 503 stating that no changes were made, the fetch is bounded by a configured timeout, and zero-write is proven by test.</done>
</task>

</tasks>

<threat_model>
Security enforcement active. ASVS level 1. Blocking threshold: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → `POST` desk-assignment upload | Untrusted workbook bytes and untrusted cell values cross here |
| BambooHR API → `AgentMergeService.fetchSnapshot` | Untrusted third-party employee and time-off data crosses here, now on every upload rather than only on manual refresh |
| tenant → repository layer | Every read and write must be scoped by `tenantId` from `TenantContext` |
| backend → operator browser (`DeskAssignmentUploadResult`) | Agent PII (names, emails) leaves the server in the merge report |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | `AgentMergeService.fetchSnapshot` / `DeskAssignmentUploadService.uploadDeskAssignments` | high | mitigate | Both fetches complete before `transactionTemplate.executeWithoutResult` opens; every catch in `fetchSnapshot` rethrows, so a swallowed sync failure cannot write a partial merge. Proven by `UploadSyncFailureTest`'s zero-interaction assertions. |
| T-11-02 | Information Disclosure | `AgentMergeService.fetchSnapshot` snapshot map and every repository call in the merge path | high | mitigate | `listEmployees` is called with `String.valueOf(tenantId)` as the tenant argument; every repository call in the rewritten upload path keeps its existing `tenantId` parameter, matching the pattern already used throughout `DeskAssignmentUploadService`. |
| T-11-03 | Information Disclosure | merge-decision logging in `AgentMergeService` | medium | mitigate | Log agent UUID and field label at INFO; raw BambooHR and sheet values at DEBUG only, per the convention stated in `WorkingDaysParser`'s class Javadoc. |
| T-11-04 | Information Disclosure | `MergeReportEntry` payload returned to the browser | medium | mitigate | Entries are built only for rows on desks resolved from `deskRepository.findByTenantId(tenantId)`, so no other tenant's agent can appear in a report. |
| T-11-05 | Denial of Service | whole-tenant `listEmployees` on every upload | medium | accept | D-04 locks synchronous fresh sync and explicitly rejects sync reuse. The new explicit read timeout bounds the request instead; async upload is deferred by CONTEXT. Accepted risk already recorded in REQUIREMENTS.md Open Risks. |
| T-11-06 | Information Disclosure | `BambooHRSyncFailedException` message content | medium | mitigate | The message is the upstream reason wrapped in fixed operator copy. `BambooHRRateLimitedException`'s message is documented as secret-free; the wrapping adds no configuration values, no API key and no subdomain. |
| T-11-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install tasks exist in this phase. RESEARCH.md's Package Legitimacy Audit records N/A: this is a Java/Gradle project with a fixed, already-audited dependency set, and Phase 11 adds no dependency. |
</threat_model>

<verification>
- `./gradlew test` full suite green
- `npm --prefix frontend run build` exits 0
- `uploadDeskAssignments` carries no `@Transactional` and no `ensureCachePopulatedForUpload` call
- Exactly one `listEmployees` and one `listTimeOff` call per upload, both before the transaction opens
- Every merge-report row is a genuine divergence or gap-fill; agreement emits nothing
- Sync failure of any kind returns 503 with the no-changes clause and leaves zero rows written
</verification>

<success_criteria>
- MRG-01: uploading triggers a fresh sync before any merge decision, on every upload
- MRG-02: BambooHR wins on every contested identity field where it has data; the sheet fills gaps only
- MRG-04: the operator sees, per field, which value came from which source
- MRG-05: overridden spreadsheet values are visible in the report
- MRG-07: a sync failure gives a clear message and writes nothing
</success_criteria>

<output>
Create `.planning/phases/11-bamboohr-merge-engine-report/11-01-SUMMARY.md` when done
</output>
