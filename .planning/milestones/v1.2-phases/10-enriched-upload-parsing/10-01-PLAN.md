---
phase: 10-enriched-upload-parsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V30__agent_day_hours_recurring_status.sql
  - src/main/java/com/wfm/model/AgentDayHours.java
  - src/test/java/com/wfm/integration/BambooRefreshServiceTest.java
autonomous: true
requirements: [UPL-04, UPL-05]
must_haves:
  truths:
    - "A day cell of MANDATORY or PTO is stored durably and blocks the solver for that weekday indefinitely (D-05: 0/MANDATORY/PTO all = not-worked, a number >0 = hours worked)"
    - "An uploaded MANDATORY/PTO/hours value survives a BambooHR refresh unchanged (not wiped by the rolling-window delete) (D-12: recurring-PTO/label stored on agent_day_hours, which refresh never touches)"
  artifacts:
    - path: "src/main/resources/db/migration/V30__agent_day_hours_recurring_status.sql"
      provides: "nullable day_off_type column on agent_day_hours"
      contains: "ALTER TABLE agent_day_hours"
    - path: "src/main/java/com/wfm/model/AgentDayHours.java"
      provides: "dayOffType field (nullable DayOffType, @Enumerated STRING)"
      contains: "dayOffType"
  key_links:
    - from: "src/main/java/com/wfm/model/AgentDayHours.java"
      to: "src/main/java/com/wfm/model/DayOffType.java"
      via: "@Enumerated(EnumType.STRING) DayOffType"
      pattern: "DayOffType"
    - from: "src/test/java/com/wfm/integration/BambooRefreshServiceTest.java"
      to: "src/main/java/com/wfm/integration/BambooRefreshService.java"
      via: "structural: BambooRefreshService declares no AgentDayHoursRepository dependency"
      pattern: "agent_day_hours|AgentDayHours"
---

<objective>
Establish the durable, refresh-safe storage for the day cell's descriptive flavor (MANDATORY / PTO) as a nullable `day_off_type` column on the existing `agent_day_hours` table (Phase 9), per the D-12 research resolution. This is the storage foundation the parser (plan 03) writes into.

Purpose: Recurring MANDATORY/PTO must block the solver for a weekday forever and must NOT be silently wiped by `BambooRefreshService`'s rolling-window `AgentDayOff` delete-and-regenerate. Storing dated `AgentDayOff` rows (the literal reading of D-05) hits that window-wipe hazard; `agent_day_hours` is queried without a date range and a `0.00` row already durably blocks the solver via the unmodified `resolveEffectiveHours` chain — the new column is reporting/label metadata only, read by nothing solver-side.
Output: Flyway V30 migration, the `dayOffType` field on `AgentDayHours`, and a structural regression test proving `BambooRefreshService` has no `AgentDayHoursRepository` dependency (so refresh cannot touch `agent_day_hours`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-enriched-upload-parsing/10-RESEARCH.md
@.planning/phases/10-enriched-upload-parsing/10-PATTERNS.md

<interfaces>
From src/main/java/com/wfm/model/DayOffType.java (reused as-is, NO changes):
```java
public enum DayOffType { MANDATORY, PTO }
```

From src/main/java/com/wfm/model/AgentDayHours.java (current — @Table agent_day_hours, unique (agent_id, day_of_week)):
```java
@Column(nullable = false, precision = 5, scale = 2)
private BigDecimal hours;   // new dayOffType field is added directly after this
```

From src/main/java/com/wfm/model/AgentDayOff.java (the sibling-entity pattern to MIRROR, but nullable here):
```java
@Enumerated(EnumType.STRING)
@Column(nullable = false)
private DayOffType type;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V30 migration — add nullable day_off_type column</name>
  <files>src/main/resources/db/migration/V30__agent_day_hours_recurring_status.sql</files>
  <read_first>
    - src/main/resources/db/migration/V29__agent_first_last_name_and_day_hours.sql (additive-column, forward-only style to mirror; confirms V29 is the latest applied version and step 3 creates agent_day_hours)
    - .planning/phases/10-enriched-upload-parsing/10-RESEARCH.md § "Recurring PTO Storage Decision (D-12)" and § "Flyway migration (D-12)"
  </read_first>
  <action>
    Create forward-only migration `V30__agent_day_hours_recurring_status.sql` that runs `ALTER TABLE agent_day_hours ADD COLUMN day_off_type VARCHAR(9);`. No backfill (all existing rows are NULL — no spreadsheet has ever populated this before this migration; mirror V29's no-backfill-for-pre-existing style). No down migration (project is forward-only, no down scripts exist). Add SQL comments documenting the semantics: NULL = normal worked day (hours > 0) or a plain unlabelled 0; 'MANDATORY'/'PTO' = the spreadsheet keyword (D-03), reusing the existing DayOffType enum vocabulary without reusing AgentDayOff's dated materialization. Confirm no V30 file already exists before writing (V29 was latest at research time).
  </action>
  <acceptance_criteria>
    - File `src/main/resources/db/migration/V30__agent_day_hours_recurring_status.sql` exists.
    - Contains `ALTER TABLE agent_day_hours` and `day_off_type VARCHAR(9)`.
    - No `V31`+ or duplicate `V30__` file exists in the migration directory.
    - Behavior: `./gradlew flywayInfo` or an app boot against a test DB applies V30 without error (verified transitively by the build/test run in Task 3).
  </acceptance_criteria>
  <verify>
    <automated>test -f src/main/resources/db/migration/V30__agent_day_hours_recurring_status.sql && grep -q "day_off_type" src/main/resources/db/migration/V30__agent_day_hours_recurring_status.sql</automated>
  </verify>
  <done>V30 migration file exists, adds the nullable day_off_type VARCHAR(9) column, no version collision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add nullable dayOffType field to AgentDayHours</name>
  <files>src/main/java/com/wfm/model/AgentDayHours.java</files>
  <read_first>
    - src/main/java/com/wfm/model/AgentDayHours.java (current entity — add field directly after the `hours` field at the existing getter/setter block)
    - src/main/java/com/wfm/model/AgentDayOff.java (sibling `@Enumerated(EnumType.STRING) DayOffType type` pattern to mirror — but nullable here)
    - src/main/java/com/wfm/model/DayOffType.java (enum reused unchanged)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "AgentDayHours.java (modify: + dayOffType)"
  </read_first>
  <behavior>
    - A persisted AgentDayHours with dayOffType left unset stores NULL (worked day or unlabelled 0).
    - A persisted AgentDayHours with setDayOffType(DayOffType.MANDATORY) round-trips MANDATORY; PTO round-trips PTO.
    - The column maps to `day_off_type` and is nullable (unlike AgentDayOff.type which is nullable=false).
  </behavior>
  <action>
    Add a `private DayOffType dayOffType;` field annotated `@Enumerated(EnumType.STRING)` and `@Column(name = "day_off_type", length = 9)` (NOTE: nullable — do NOT add `nullable = false`; the null third-state is intentional and is the documented divergence from AgentDayOff.type). Add `getDayOffType()` / `setDayOffType(DayOffType)` accessors mirroring the existing accessor style in the file. Import `com.wfm.model.DayOffType` is same-package so no import needed. Add a short Javadoc noting null = worked/unlabelled-0, MANDATORY/PTO = spreadsheet-labelled off day, and that this column is reporting metadata only — never read by the solver.
  </action>
  <acceptance_criteria>
    - Source assertion: `AgentDayHours.java` contains `@Column(name = "day_off_type"` and `private DayOffType dayOffType` and does NOT contain `nullable = false` on the dayOffType column.
    - Behavior: getters/setters compile and `./gradlew compileJava` succeeds (verified in Task 3 run).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "day_off_type" src/main/java/com/wfm/model/AgentDayHours.java && grep -q "dayOffType" src/main/java/com/wfm/model/AgentDayHours.java</automated>
  </verify>
  <done>AgentDayHours exposes a nullable dayOffType (DayOffType) field mapped to day_off_type with getter/setter.</done>
</task>

<task type="auto">
  <name>Task 3: D-12 refresh-safety structural regression test</name>
  <files>src/test/java/com/wfm/integration/BambooRefreshServiceTest.java</files>
  <read_first>
    - src/test/java/com/wfm/integration/BambooRefreshServiceTest.java (extend — this is a PURE unit test: reflection over private methods + null-dep construction via `createServiceWithNullDeps()`; it mocks NO repositories. Mirror that idiom — do NOT introduce a Spring context, and do NOT attempt Mockito interaction-verification against a collaborator the service does not have.)
    - src/main/java/com/wfm/integration/BambooRefreshService.java lines 36-74 (the 8 constructor deps + fields: BambooHRClient, AgentRepository, DeskRepository, AgentDayOffRepository, SpecializationRepository, TransactionTemplate, JobTitleConfigService, BambooSyncEventService — CONFIRM there is NO AgentDayHoursRepository field; the rolling-window delete at ~251-333 is scoped to AgentDayOffRepository only)
    - .planning/phases/10-enriched-upload-parsing/10-RESEARCH.md § "Pitfall 2" and § "Recurring PTO Storage Decision (D-12)" (agent_day_hours is a different table refresh never queries)
    - .planning/phases/10-enriched-upload-parsing/10-VALIDATION.md (D-12 solver/refresh regression row)
  </read_first>
  <behavior>
    - BambooRefreshService declares NO field of type AgentDayHoursRepository and no field/collaborator referencing the AgentDayHours entity — therefore refreshDeskAgents structurally CANNOT delete or mutate agent_day_hours rows (the table where spreadsheet-sourced hours + MANDATORY/PTO labels live).
    - The refresh's only day-off mutation is the dated-window delete/regenerate on AgentDayOffRepository — a different table from agent_day_hours.
  </behavior>
  <action>
    Add a test to `BambooRefreshServiceTest` that codifies the D-12 durability guarantee STRUCTURALLY. Rationale: the class under test has no AgentDayHoursRepository collaborator to mock, and RESEARCH.md holds BambooRefreshService's constructor out of scope — so a Mockito interaction-verification is impossible and a structural absence assertion is the correct guard. Assert via reflection that `BambooRefreshService.class.getDeclaredFields()` contains NO field whose type simple-name is `AgentDayHoursRepository`, and NO field whose type references `AgentDayHours`. This proves at the type level that a BambooHR refresh cannot touch the `agent_day_hours` table (spreadsheet MANDATORY/PTO/hours therefore survive any refresh — the Pitfall 2 window-wipe hazard cannot reach this table). Do NOT construct the service with mocked repositories, do NOT verify Mockito interactions on a non-existent collaborator, and do NOT add a Spring context — keep the existing `createServiceWithNullDeps()` / reflection style. (Optional strengthening, if it fits the file cleanly: also read the BambooRefreshService source and assert it contains no `AgentDayHours` / `agent_day_hours` token — but the reflection field-absence check is the primary, sufficient guard.)
  </action>
  <acceptance_criteria>
    - Test asserts (via reflection over declared fields) that BambooRefreshService has no AgentDayHoursRepository dependency and no AgentDayHours-typed field.
    - No change to BambooRefreshService's constructor or fields is required or made.
    - Automated command: `./gradlew test --tests "com.wfm.integration.BambooRefreshServiceTest"` passes.
    - Build green: `./gradlew compileJava compileTestJava` succeeds (covers Tasks 1-2 wiring).
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.wfm.integration.BambooRefreshServiceTest"</automated>
  </verify>
  <done>BambooRefreshServiceTest contains a passing structural case proving BambooRefreshService has no AgentDayHoursRepository dependency, so refresh cannot delete/alter agent_day_hours rows (D-12 durability).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BambooHR sync → local DB | Refresh deletes/regenerates dated AgentDayOff rows; must not touch spreadsheet-sourced durable state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering (data loss) | BambooRefreshService rolling-window delete vs spreadsheet-sourced day-off state | mitigate | Store day-off flavor on `agent_day_hours.day_off_type` which refresh never touches; structural test (Task 3) proves BambooRefreshService has no AgentDayHoursRepository dependency, so refresh cannot touch agent_day_hours |
| T-10-02 | Tampering | Flyway V30 schema change | accept | Additive nullable column, no backfill, forward-only — no destructive migration; reversible by design |
</threat_model>

<verification>
- `./gradlew test --tests "com.wfm.integration.BambooRefreshServiceTest"` green.
- `./gradlew compileJava compileTestJava` green.
- V30 migration file present with the day_off_type column.
</verification>

<success_criteria>
- Nullable `day_off_type` column exists on `agent_day_hours` (V30) and is mapped by `AgentDayHours.dayOffType`.
- A regression test proves `refreshDeskAgents` does not delete or alter `agent_day_hours` rows (D-12 durability).
</success_criteria>

<output>
Create `.planning/phases/10-enriched-upload-parsing/10-01-SUMMARY.md` when done.
</output>
