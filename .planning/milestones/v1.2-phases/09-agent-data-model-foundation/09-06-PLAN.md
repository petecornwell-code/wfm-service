---
phase: 09-agent-data-model-foundation
plan: 06
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/main/resources/db/migration/V29__agent_first_last_name_and_day_hours.sql
autonomous: false
requirements: [MDL-03]

must_haves:
  truths:
    - "V29 adds first_name/last_name to agent and backfills them by splitting the existing name on first whitespace (D-06)"
    - "V29 creates agent_day_hours and fans each agent's non-null scalar contracted_hours_per_day out to all 7 weekday rows (D-01)"
    - "Agents whose scalar is NULL get zero agent_day_hours rows (D-02) — they keep resolving via the schedule default"
    - "Migration data-integrity is verified against a real Postgres before verify-work (row counts + name splits), since the test profile never runs Flyway SQL"
  artifacts:
    - path: "src/main/resources/db/migration/V29__agent_first_last_name_and_day_hours.sql"
      provides: "Additive DDL + name-split backfill + scalar fan-out"
      contains: "CREATE TABLE agent_day_hours"
  key_links:
    - from: "V29 fan-out INSERT"
      to: "agent.contracted_hours_per_day"
      via: "CROSS JOIN over 7 weekday literals WHERE scalar IS NOT NULL"
      pattern: "CROSS JOIN"
---

<objective>
Migrate existing data to the new model with provably no loss (MDL-03, "the highest-risk change" in v1.2): add and backfill first_name/last_name by splitting the current name, create agent_day_hours, and fan each agent's non-null scalar contracted hours out to all 7 weekday rows (D-01) while leaving NULL-scalar agents with zero rows (D-02). Because the test profile sets flyway.enabled false and there is no Testcontainers harness (RESEARCH Pitfall 4), the actual SQL is never exercised by `./gradlew test` — so migration data-integrity is gated behind a mandatory manual checkpoint against a real/seeded Postgres.

Purpose: MDL-03. This plan is autonomous:false — the manual dry-run is the phase gate before /gsd-verify-work.
Output: V29 migration SQL + a completed migration dry-run verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-agent-data-model-foundation/09-CONTEXT.md
@.planning/phases/09-agent-data-model-foundation/09-PATTERNS.md
@.planning/phases/09-agent-data-model-foundation/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write V29 migration (name columns + split backfill + agent_day_hours fan-out)</name>
  <read_first>
    - src/main/resources/db/migration/V28__add_agent_working_days_known.sql (latest migration; V29 is next; explanatory-comment style)
    - src/main/resources/db/migration/V15__merge_desk_agent_into_agent.sql (multi-step ALTER + UPDATE + CREATE TABLE + INSERT numbered-comment style)
    - .planning/phases/09-agent-data-model-foundation/09-PATTERNS.md section V29 (recommended SQL, verified) and the top-of-file note confirming gen_random_uuid() is a PG16 core builtin (RDS engine 16.6) — no CREATE EXTENSION needed
    - src/main/java/com/wfm/util/AgentNameSplitter.java (the Java rule the SQL split must agree with — Pitfall 5)
  </read_first>
  <files>src/main/resources/db/migration/V29__agent_first_last_name_and_day_hours.sql</files>
  <action>
    Write V29__agent_first_last_name_and_day_hours.sql with numbered comment steps mirroring V15/V28 style. Step 1: ALTER TABLE agent ADD COLUMN first_name VARCHAR(255), ADD COLUMN last_name VARCHAR(255). Step 2: backfill UPDATE agent SET first_name = split_part(name, single-space, 1), last_name = CASE WHEN position(single-space IN name) greater than 0 THEN trim(substring(name FROM position(single-space IN name) + 1)) ELSE empty-string END — this must reproduce the D-06 AgentNameSplitter rule (first whitespace token to first_name, trimmed remainder to last_name, single-token to empty last_name). Step 3: CREATE TABLE agent_day_hours with columns id UUID PRIMARY KEY, tenant_id BIGINT NOT NULL, agent_id UUID NOT NULL REFERENCES agent(id) ON DELETE CASCADE, day_of_week VARCHAR(9) NOT NULL, hours NUMERIC(5,2) NOT NULL, and UNIQUE (agent_id, day_of_week). Step 4: fan-out INSERT INTO agent_day_hours selecting gen_random_uuid(), a.tenant_id, a.id, dow.name, a.contracted_hours_per_day FROM agent a CROSS JOIN a VALUES list of the 7 uppercase weekday literals AS dow(name) WHERE a.contracted_hours_per_day IS NOT NULL, with a comment that NULL-scalar agents (D-02) intentionally get zero rows. Do NOT drop contracted_hours_per_day (retired only in Phase 10/11 per D-05). Use gen_random_uuid() (PG16 builtin) — do NOT add CREATE EXTENSION.
  </action>
  <verify>
    <automated>bash -c "f=src/main/resources/db/migration/V29__agent_first_last_name_and_day_hours.sql; test -f $f && grep -q 'CREATE TABLE agent_day_hours' $f && grep -q 'split_part' $f && grep -q 'contracted_hours_per_day IS NOT NULL' $f && grep -q 'gen_random_uuid' $f && echo MIGRATION_STRUCTURE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - File src/main/resources/db/migration/V29__agent_first_last_name_and_day_hours.sql exists (correct V29 sequence, latest is V28)
    - Contains the 4 steps: ADD COLUMN first_name/last_name; split_part backfill UPDATE; CREATE TABLE agent_day_hours with NUMERIC(5,2) NOT NULL hours and UNIQUE(agent_id, day_of_week); fan-out INSERT guarded by contracted_hours_per_day IS NOT NULL
    - Uses gen_random_uuid() and does NOT contain CREATE EXTENSION
    - Does NOT drop or alter contracted_hours_per_day
    - Verify command prints MIGRATION_STRUCTURE_OK
  </acceptance_criteria>
  <done>The V29 migration script exists and structurally matches D-01/D-02/D-06/D-09.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual migration dry-run against seeded Postgres (MDL-03 data-integrity gate)</name>
  <read_first>
    - .planning/phases/09-agent-data-model-foundation/09-VALIDATION.md section Manual-Only Verifications (exact assertions and rationale)
    - .planning/phases/09-agent-data-model-foundation/09-RESEARCH.md Pitfall 4 (why automated tests cannot cover this) and Pitfall 5 (SQL-vs-Java split agreement)
  </read_first>
  <what-built>
    V29 migration SQL (Task 1). The test profile has flyway.enabled:false and no Testcontainers, so `./gradlew test` never executes this SQL — data-integrity must be verified manually against a real Postgres. This is the phase gate.
  </what-built>
  <how-to-verify>
    1. Start a local Postgres 16 (e.g. docker run postgres:16) or use a disposable copy/snapshot of the dev RDS data.
    2. Seed representative agent rows BEFORE V29: at least one with a non-null contracted_hours_per_day, one with NULL scalar, one single-token name (e.g. "Alice"), and one multi-word name (e.g. "Mary Jane Watson").
    3. Run the migrations through V29 (flyway migrate, or the app pointed at that DB with flyway enabled).
    4. Assert row counts: SELECT count(*) FROM agent_day_hours equals 7 times the number of agents with a non-null contracted_hours_per_day.
    5. Assert NULL-scalar agents have zero agent_day_hours rows.
    6. Assert every fanned-out agent has exactly 7 rows (MONDAY..SUNDAY) all equal to that agent's original scalar.
    7. Spot-check name split: "Mary Jane Watson" gives first_name="Mary", last_name="Jane Watson"; "Alice" gives first_name="Alice", last_name="".
    8. Cross-check SQL vs Java (Pitfall 5): run the same representative names through AgentNameSplitter (a scratch main or the existing AgentNameSplitterTest inputs) and confirm the SQL backfill produced identical first_name/last_name for each.
    Report the count query outputs and the split spot-checks.
  </how-to-verify>
  <acceptance_criteria>
    - agent_day_hours count == 7 * (agents with non-null scalar) — exact
    - NULL-scalar agents have 0 rows; each non-null-scalar agent has exactly 7 rows equal to its original scalar
    - Multi-word and single-token names split per D-06, matching AgentNameSplitter output on the same inputs
    - No error running V29 (gen_random_uuid resolves as a core builtin; no missing-extension failure)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the count-query results, or describe the discrepancy to fix in V29.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static backfill SQL → existing data | Migration rewrites only existing columns/rows; no user-supplied input reaches the SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Tampering | SQL injection in migration | accept | V29 has no user input — it is a static backfill over existing columns; no injection surface (RESEARCH Security Domain) |
| T-09-16 | Repudiation/Data loss | incorrect fan-out or split silently corrupts data | mitigate | Manual dry-run (Task 2) asserts exact row counts, NULL-scalar zero-row rule, and SQL-vs-Java split agreement before this ships to real data |
| T-09-17 | Input Validation | hours integrity in new table | mitigate | agent_day_hours.hours is NUMERIC(5,2) NOT NULL — DB backstop consistent with agent_exception.contracted_hours_override |
| T-09-SC | Tampering | dependency installs | accept | No new packages this phase; gen_random_uuid is a Postgres 16 core builtin, no extension added |
</threat_model>

<verification>
- Task 1 verify command prints MIGRATION_STRUCTURE_OK
- Task 2 manual checkpoint approved with row-count evidence (phase gate before /gsd-verify-work)
</verification>

<success_criteria>
- V29 backfills first/last name per D-06 and fans non-null scalars to 7 weekday rows (D-01), NULL scalars to zero rows (D-02)
- Data-integrity confirmed against a real Postgres via the mandatory manual checkpoint (MDL-03)
- The scalar column is retained (dropped only in a later phase per D-05)
</success_criteria>

<output>
Create `.planning/phases/09-agent-data-model-foundation/09-06-SUMMARY.md` when done.
</output>
