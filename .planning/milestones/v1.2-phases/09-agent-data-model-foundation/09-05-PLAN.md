---
phase: 09-agent-data-model-foundation
plan: 05
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/main/java/com/wfm/dto/AgentResponse.java
  - src/main/java/com/wfm/service/AgentService.java
  - src/main/java/com/wfm/service/ClientManagementService.java
  - src/main/java/com/wfm/dto/DeskAgentResponse.java
  - src/main/java/com/wfm/service/DeskAgentService.java
  - src/main/java/com/wfm/service/DeskAgentExportService.java
  - src/test/java/com/wfm/service/DeskAgentServiceContractedHoursTest.java
autonomous: true
requirements: [MDL-01, MDL-02]

must_haves:
  truths:
    - "AgentResponse and DeskAgentResponse expose firstName and lastName as new fields while still carrying the combined name (D-08); no per-day hours are surfaced (D-12)"
    - "The Excel export includes firstName and lastName columns without shifting existing column indices"
    - "DeskAgentService.setContractedHours fans the new value out to all 7 agent_day_hours rows so an operator hours edit keeps affecting the solver post-migration (D-10 set side)"
  artifacts:
    - path: "src/main/java/com/wfm/dto/AgentResponse.java"
      provides: "firstName/lastName record fields"
      contains: "firstName"
    - path: "src/main/java/com/wfm/dto/DeskAgentResponse.java"
      provides: "firstName/lastName record fields (no per-day hours)"
      contains: "firstName"
    - path: "src/main/java/com/wfm/service/DeskAgentService.java"
      provides: "7-row per-day fan-out on setContractedHours + firstName/lastName in toResponse"
      contains: "deleteByAgent_Id"
    - path: "src/test/java/com/wfm/service/DeskAgentServiceContractedHoursTest.java"
      provides: "Proof setContractedHours writes 7 per-day rows with the normalized value"
  key_links:
    - from: "src/main/java/com/wfm/service/DeskAgentService.java"
      to: "AgentDayHoursRepository"
      via: "deleteByAgent_Id + save of 7 rows"
      pattern: "agentDayHoursRepository"
---

<objective>
Expose the new name fields to API/export consumers (D-08/D-12) and make the operator hours-edit endpoint durable against the resolution change (D-10 set side). `AgentResponse`/`DeskAgentResponse` gain `firstName`/`lastName` (keeping the combined `name`; NOT surfacing per-day hours yet), the Excel export gains the two columns, and `DeskAgentService.setContractedHours` fans the new value out to all 7 `agent_day_hours` rows so the solver — which no longer reads the scalar — still honours the edit.

Purpose: MDL-01 (consumers see first/last) and MDL-02 (operator hours edits keep affecting the solver post-migration; otherwise `setContractedHours` becomes a silent no-op per RESEARCH Pitfall 2). All DeskAgentService changes are grouped here because both the DTO update and the fan-out touch this one file.
Output: updated DTOs + construction sites, export columns, fan-out + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-agent-data-model-foundation/09-CONTEXT.md
@.planning/phases/09-agent-data-model-foundation/09-PATTERNS.md

<interfaces>
<!-- Consumed from 09-01 and 09-02 -->
Agent.getFirstName() / getLastName()
AgentDayHoursRepository.deleteByAgent_Id(UUID), save(AgentDayHours)
BigDecimals.normalize(BigDecimal) -> BigDecimal (already used in DeskAgentService)

<!-- Construction sites that MUST update together with each record -->
AgentResponse: built at AgentService.java:57 (toResponse) and ClientManagementService.java:299
DeskAgentResponse: built at DeskAgentService.java:72 (toResponse)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AgentResponse firstName/lastName + its two construction sites (D-08)</name>
  <read_first>
    - src/main/java/com/wfm/dto/AgentResponse.java (current record field list, verified)
    - src/main/java/com/wfm/service/AgentService.java lines 50-60 (toResponse builds new AgentResponse)
    - src/main/java/com/wfm/service/ClientManagementService.java lines 290-305 (new AgentResponse(...) site)
  </read_first>
  <files>src/main/java/com/wfm/dto/AgentResponse.java, src/main/java/com/wfm/service/AgentService.java, src/main/java/com/wfm/service/ClientManagementService.java</files>
  <action>
    Add `String firstName, String lastName` to the `AgentResponse` record immediately after `name` (D-08: keep the combined `name` field). Update BOTH construction sites to pass `a.getFirstName(), a.getLastName()` (AgentService.toResponse ~line 57) and `agent.getFirstName(), agent.getLastName()` (ClientManagementService ~line 299) at the matching positional args. Per D-12 do NOT add any per-day hours field.
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `AgentResponse.java` record header contains `String firstName` and `String lastName` after `name`
    - Both `new AgentResponse(...)` call sites pass firstName/lastName (compile proves arity match)
    - No per-day hours field added (D-12)
  </acceptance_criteria>
  <done>AgentResponse exposes first/last name; all its construction sites compile.</done>
</task>

<task type="auto">
  <name>Task 2: DeskAgentResponse firstName/lastName + toResponse + export columns (D-08/D-12)</name>
  <read_first>
    - src/main/java/com/wfm/dto/DeskAgentResponse.java (full record, line 11 field list)
    - src/main/java/com/wfm/service/DeskAgentService.java lines 67-90 (toResponse building new DeskAgentResponse at line 72)
    - src/main/java/com/wfm/service/DeskAgentExportService.java lines 1-70 (columns array + per-cell writes; existing name cell at index 3; last index currently 12)
  </read_first>
  <files>src/main/java/com/wfm/dto/DeskAgentResponse.java, src/main/java/com/wfm/service/DeskAgentService.java, src/main/java/com/wfm/service/DeskAgentExportService.java</files>
  <action>
    Add `String firstName, String lastName` to the `DeskAgentResponse` record after `name` (D-08; per D-12 NO per-day hours field). Update `DeskAgentService.toResponse` (~line 72) to pass `a.getFirstName(), a.getLastName()` at the matching positions. In `DeskAgentExportService`, add "First Name" and "Last Name" to the `columns[]` header array and add two matching `row.createCell(N)` writes — APPEND them as the two new trailing columns (after the current last index 12, i.e. indices 13 and 14) so existing cell indices 0-12 are not shifted; use the same null-guard style as the existing name cell (`agent.firstName() != null ? agent.firstName() : ""`). Keep the existing "Name" column (index 3) as the combined name.
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `DeskAgentResponse.java` contains `firstName` and `lastName` after `name`, and NO agent_day_hours / per-day field (D-12)
    - Source assertion: `DeskAgentExportService` columns[] length increased by 2 with "First Name"/"Last Name", and new `createCell(13)`/`createCell(14)` writes exist while cells 0-12 are unchanged
    - `DeskAgentService.toResponse` passes firstName/lastName (compile proves arity)
  </acceptance_criteria>
  <done>Desk-agent API and Excel export expose first/last name without disturbing existing fields/columns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: setContractedHours fans out to 7 agent_day_hours rows (D-10 set side)</name>
  <read_first>
    - src/main/java/com/wfm/service/DeskAgentService.java lines 1-40 (constructor/field injection) and 179-193 (setContractedHours: resolves agent via findByIdAndTenantIdAndDeskId, then agent.setContractedHoursPerDay(BigDecimals.normalize(hours)))
    - src/main/java/com/wfm/repository/AgentDayHoursRepository.java (deleteByAgent_Id + save from 09-02)
    - src/main/java/com/wfm/model/AgentDayHours.java (constructor/setters from 09-02)
    - .planning/phases/09-agent-data-model-foundation/09-CONTEXT.md §D-10
  </read_first>
  <files>src/main/java/com/wfm/service/DeskAgentService.java, src/test/java/com/wfm/service/DeskAgentServiceContractedHoursTest.java</files>
  <behavior>
    - Calling setContractedHours(deskId, agentId, 6) results in exactly 7 AgentDayHours rows for that agent (one per DayOfWeek), each with hours normalized to 6.00 and the agent's tenantId
    - Calling it again replaces (not appends) — still exactly 7 rows afterward, with the new value
  </behavior>
  <action>
    Inject `AgentDayHoursRepository agentDayHoursRepository` into `DeskAgentService` (add final field + constructor param, matching the existing repository-injection style). In `setContractedHours`, after `agent.setContractedHoursPerDay(BigDecimals.normalize(hours))` and saving the agent, fan out to per-day rows (D-10): call `agentDayHoursRepository.deleteByAgent_Id(agentId)` then create and save one `AgentDayHours` per `java.time.DayOfWeek.values()` with tenantId = the agent's tenantId, agent = the resolved agent, dayOfWeek = the value, and hours = `BigDecimals.normalize(hours)`. This mirrors D-01's migration fan-out so the solver (which no longer reads the scalar) keeps honouring operator edits. Write `DeskAgentServiceContractedHoursTest` (@DataJpaTest or a repository-backed test consistent with project convention — read an existing DeskAgentService/repository test if present, otherwise a @DataJpaTest exercising the repository + a hand-built service is acceptable) asserting the 7-row outcome and the replace-not-append behavior. Author the assertion first (RED).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.DeskAgentServiceContractedHoursTest"</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts exactly 7 AgentDayHours rows after setContractedHours, each with normalized value (scale 2) and the agent's tenantId
    - Test asserts a second call replaces rather than appends (still 7 rows, new value)
    - Source assertion: `deleteByAgent_Id` is called before the 7 saves (replace semantics, not blind insert)
    - Source assertion: hours written via `BigDecimals.normalize(hours)`
  </acceptance_criteria>
  <done>Operator hours edits fan out to all 7 per-day rows, keeping the "no solve regression" invariant durable (D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API client → DeskAgentService.setContractedHours | Operator-supplied hours cross into per-day rows for a tenant+desk-resolved agent |
| Agent fields → DTO/export output | first/last name surfaced to API and Excel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering | setContractedHours silently ignored by solver | mitigate | Fan-out writes 7 per-day rows (D-10) so the edit reaches the solver's per-day resolution; pinned by the 7-row test |
| T-09-12 | Information Disclosure | fan-out delete/write scope | mitigate | Agent resolved via `findByIdAndTenantIdAndDeskId` before any write; per-day rows carry the agent's tenantId; delete keyed on that resolved agent's id |
| T-09-13 | Input Validation | hours scale/precision | mitigate | `BigDecimals.normalize(hours)` (scale 2) on every per-day write; DB NUMERIC(5,2) NOT NULL backstop (09-06) |
| T-09-14 | Information Disclosure | DTO over-exposure of per-day hours | accept | D-12: per-day hours deliberately NOT surfaced — no premature API shape |
| T-09-SC | Tampering | dependency installs | accept | No new packages this phase |
</threat_model>

<verification>
- `./gradlew test --tests "com.wfm.service.DeskAgentServiceContractedHoursTest"` green
- `./gradlew compileJava` succeeds (all record construction sites updated)
- `grep -n "deleteByAgent_Id" src/main/java/com/wfm/service/DeskAgentService.java` shows the fan-out replace
</verification>

<success_criteria>
- AgentResponse/DeskAgentResponse and the Excel export expose firstName/lastName (combined name retained; no per-day hours per D-12)
- setContractedHours fans the value out to 7 per-day rows so operator edits keep affecting the solver (D-10)
</success_criteria>

<output>
Create `.planning/phases/09-agent-data-model-foundation/09-05-SUMMARY.md` when done.
</output>
