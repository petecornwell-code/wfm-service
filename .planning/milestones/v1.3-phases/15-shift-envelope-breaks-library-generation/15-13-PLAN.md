---
phase: 15-shift-envelope-breaks-library-generation
plan: 13
type: execute
wave: 3
depends_on: [15-09, 15-10, 15-11, 15-12]
gap_closure: true
gap_ids: [G-15-10]
files_modified:
  - src/test/java/com/wfm/solver/ShiftDeskEndToEndRegressionTest.java
  - src/test/java/com/wfm/solver/ShiftModeBreakGeometryGuardTest.java
  - .planning/debug/characterising-tests/DISPOSITION.md
  - .planning/phases/15-shift-envelope-breaks-library-generation/deferred-items.md
autonomous: true
requirements: [ENVL-02, ENVL-04, ENVL-07, XCUT-03, XCUT-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A desk carrying the live defect's SHAPE — operating window wider than some envelopes, thin and zero demand at the edges, several templates whose net hours exactly equal the roster's contracted hours — either solves to zero hard or is refused with an actionable message, and never returns a best-so-far schedule carrying residual envelope penalty."
    - "On a solve that reaches zero hard, every agent-day's held seats equal its legal slots exactly, so ENVL-04 contiguity holds observably rather than by argument."
    - "Every characterising test is either promoted to a regression test or explicitly disposed of in writing; none is left green against the defect."
    - "The two known-latent defects this round deliberately did not fix are recorded where the next planner will find them."
  artifacts:
    - src/test/java/com/wfm/solver/ShiftDeskEndToEndRegressionTest.java
    - src/test/java/com/wfm/solver/ShiftModeBreakGeometryGuardTest.java
    - .planning/debug/characterising-tests/DISPOSITION.md
    - .planning/phases/15-shift-envelope-breaks-library-generation/deferred-items.md
  key_links:
    - "The end-to-end test drives the SHIPPED solverConfig.xml with a fixed step-count limit, because the project's recorded discipline is that solver changes are judged by seeded, step-count-terminated runs and never by a single wall-clock solve."
    - "The end-to-end assertions walk the solved schedule outside the score director, reusing the ENVL-07 ground-truth walker approach rather than trusting the reported score."
---

<objective>
Prove the chain is closed end to end, and leave nothing undisposed.

The three defects behind G-15-10 were AND-gated: no single one produced the symptom. Plans 15-09,
15-10, 15-11 and 15-12 each close one link, each with its own scoped regression test. What none of
them proves is that a desk carrying the LIVE SHAPE now behaves correctly all the way through — and
shape, not scale, is what the existing suite missed. The debug lane established that a 400-agent,
60-day benchmark would have missed this identically, because `ShiftModeFixtures` excluded the shape
by construction. So the closing evidence must be a shape-complete run, not a large one.

This plan also settles the four characterising tests. Three have been promoted by earlier plans in
this round. The fourth, `ShiftModeBreakGeometryCharacterisationTest`, passes ON the defect and must
not be kept green as-is — but most of what it measures characterises a REFUTED non-defect (break
geometry is not unconstrained at feasibility; the exact-equality value range plus the two hard
contracted-hours constraints plus envelope compliance jointly force held to equal legal). That part
is worth keeping as documentation of what the mode gate does and does not do, provided it is reframed
so it no longer reads as an accepted defect. Its report-layer case is superseded by plan 15-10.

Finally, this round deliberately did not fix two real, latent, independent defects. Operator ruling
OR-2 put D5 out of scope for this round; the save-time half of the envelope-containment defect goes
with it. Both must be recorded so they are not lost.

Purpose: closing evidence, characterising-test disposition, and an honest deferral record.
Output: a shape-complete end-to-end regression test, a reframed geometry guard, a disposition note,
and deferred items filed against the next round.

**Not in this plan:** any production source change. If this plan needs one, an earlier plan is
incomplete and that is the finding to report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-shift-envelope-breaks-library-generation/15-UAT.md
@.planning/phases/15-shift-envelope-breaks-library-generation/deferred-items.md
@.planning/debug/shift-mode-break-geometry-ungoverned.md
@.planning/debug/characterising-tests/ShiftModeBreakGeometryCharacterisationTest.java
@.planning/phases/15-shift-envelope-breaks-library-generation/15-09-SUMMARY.md
@.planning/phases/15-shift-envelope-breaks-library-generation/15-11-SUMMARY.md
@src/test/java/com/wfm/solver/ShiftEnvelopeGroundTruthTest.java
@src/test/java/com/wfm/solver/ShiftModeFixtures.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shape-complete end-to-end regression on a desk built like the live one</name>
  <files>src/test/java/com/wfm/solver/ShiftDeskEndToEndRegressionTest.java</files>
  <read_first>
    - src/test/java/com/wfm/solver/ShiftEnvelopeGroundTruthTest.java — the ENVL-07 walker pattern: how
      it solves through the shipped configuration, how it asserts non-vacuous feasibility before
      asserting anything else, and how its envelope-violation walk inspects the solved schedule
      outside the score director.
    - src/test/java/com/wfm/solver/ShiftModelBenchmarkTest.java — the step-count-terminated solve
      harness and its budget, so this test's runtime stays inside the suite's existing tolerance.
    - src/test/java/com/wfm/solver/ShiftModeFixtures.java as plan 15-09 left it — the widened
      operating window and the production seat expansion are the two properties this test depends on.
    - .planning/phases/15-shift-envelope-breaks-library-generation/15-UAT.md, the `decisive_evidence`
      and `screenshot_evidence` blocks — the live desk's exact geometry, which this fixture mirrors.
  </read_first>
  <action>
    Build a desk with the live defect's shape and assert the whole chain on it.

    Shape to reproduce, taken from the live evidence rather than invented: an hourly grid; an
    operating window spanning roughly a full trading day; four templates whose envelopes START at
    staggered times and whose spans are shorter than the operating window, so early and late hours sit
    outside some envelopes; each template one hour longer than the contract with a single break band,
    so its net hours exactly equal the roster's contracted hours and the D-04 value range admits every
    template for every agent; a roster of several agents all on the same contracted hours; and a demand
    curve that is thin or zero at the window's edges and substantial in the middle. Several consecutive
    days, not one, because the live symptom accumulated across a week.

    Assert three outcomes, in this order:

    - Non-vacuity first, exactly as the ground-truth test does. A schedule with no placed shifts or no
      seated agents trivially satisfies everything, so assert placement before asserting quality.
    - Then the disjunction that is the real acceptance criterion: the solve either reaches zero hard,
      or the desk is refused by the supply gate before solving with a message naming the shortfall.
      What must NOT happen is the third outcome the live desk produced — a completed solve returning a
      best-so-far schedule that still carries envelope penalty. Assert that explicitly, so a future
      regression reintroducing the silent-residual behaviour fails here even if it happens to score
      better than it does today.
    - Then, on the zero-hard branch, walk the solved schedule outside the score director and assert
      that for every agent-day the held seats equal the legal slots exactly, in both directions — no
      seat outside the envelope or inside the assigned band, and no legal slot left unworked. This is
      ENVL-04 asserted observably. It is the assertion the previous contiguity test could not make,
      because its fixture made contiguity arithmetically unavoidable.

    Add a second case with the same shape but demand deliberately reduced below what the roster needs,
    to exercise the refusal branch on purpose and assert the message is actionable. Add a third with a
    fully covered window and ample demand as the healthy control.

    Terminate every solve by step count, not by time, and use the fixture's deterministic ids so runs
    are reproducible. Record the chosen step budget and the observed runtime in the SUMMARY. If the
    shape-complete test cannot fit the suite's runtime budget at the agent count you chose, reduce the
    agent count rather than the shape — the shape is what carries the regression value, and the debug
    lane established that scale does not.

    Do not add a production-scale quality benchmark here. The phase already has one and its finding is
    recorded; this test's job is correctness at the defect's shape.
  </action>
  <verify>
    <automated>cd /Users/pete/IdeaProjects/wfm-service &amp;&amp; ./gradlew test --tests 'com.wfm.solver.ShiftDeskEndToEndRegressionTest'</automated>
  </verify>
  <done>
    All three cases pass; the zero-hard case is proven non-vacuous and proven contiguous by an
    out-of-score-director walk in both directions; the refusal case produces an actionable message;
    the step budget and runtime are recorded in the SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reframe the geometry characterisation into a guard, and dispose of the diagnostic set</name>
  <files>src/test/java/com/wfm/solver/ShiftModeBreakGeometryGuardTest.java, .planning/debug/characterising-tests/DISPOSITION.md</files>
  <read_first>
    - .planning/debug/characterising-tests/ShiftModeBreakGeometryCharacterisationTest.java in full —
      five cases: gated break constraints silent on every geometry, slot mode ranking the three
      geometries, scattered and edge scoring identically across all reachable shift-mode constraints,
      envelope compliance pricing the seat rather than the hole, and the report-layer case.
    - .planning/debug/shift-mode-break-geometry-ungoverned.md, the Resolution block — which parts are
      real defects, which are explicitly refuted, and why restoring the gated break constraints would
      be actively wrong.
    - src/test/java/com/wfm/solver/ShiftModeBreakGatingTest.java — the existing home for gate proofs,
      so the reframed cases sit beside their siblings rather than duplicating them.
  </read_first>
  <action>
    Land the surviving cases as `src/test/java/com/wfm/solver/ShiftModeBreakGeometryGuardTest.java`.
    The rename matters: the old name says the system has this property; the new name says the system
    is held to this property.

    Keep and reframe: the case showing the mode-gated break constraints are silent in shift mode on
    every geometry, the case showing slot mode still ranks the three geometries strictly, and the case
    showing envelope compliance prices the illegal SEAT and not the compensating hole. The class
    javadoc must state plainly what these do and do not prove — the mode gate works, and shift mode has
    no per-agent geometry term — and then state the guard that now carries the property instead: at
    zero hard the exact-equality value range, the two hard contracted-hours constraints and envelope
    compliance jointly force held to equal legal, asserted directly by the end-to-end test in Task 1.
    Name that test so the two are discoverable from each other.

    Keep the case showing scattered and edge geometries score identically, but re-label it honestly: it
    demonstrates that the flatness is real and that geometry has no gradient defending it WHILE a solve
    is already infeasible. Add the conclusion the debug lane established and this round adopts — that
    this is a cosmetic property of an already-broken schedule, that the remedy is to prevent the
    infeasibility rather than to price the geometry, and that restoring the gated slot-mode break
    constraints was considered and rejected because it would fight the envelope model and could make
    under-supplied desks unsolvable. Record it as a soft tie-break candidate at most, never a hard
    constraint.

    Do NOT port the report-layer case. Plan 15-10 replaced it with an assertion of the corrected
    behaviour; carrying the old one forward would leave a live test asserting that a seat gap is a
    break. State that omission explicitly in the class javadoc and name the replacing test.

    Then write `.planning/debug/characterising-tests/DISPOSITION.md` recording, for each of the four
    files, exactly what happened to it: which regression test it became, which plan did it, and — for
    anything intentionally dropped — the reason. Note at the top that the directory is a historical
    record and that none of its files is a live guard, so nobody later mistakes a green diagnostic for
    coverage. Leave the original files in place beneath it; they are the evidence the diagnosis was
    executable.
  </action>
  <verify>
    <automated>cd /Users/pete/IdeaProjects/wfm-service &amp;&amp; ./gradlew test --tests 'com.wfm.solver.ShiftModeBreakGeometryGuardTest' --tests 'com.wfm.solver.ShiftModeBreakGatingTest' &amp;&amp; test -f .planning/debug/characterising-tests/DISPOSITION.md</automated>
  </verify>
  <done>
    The guard test exists under `src/test` with a javadoc stating what it proves, what it does not, and
    which test carries the real guarantee; the report-layer case is deliberately absent and its
    replacement is named; DISPOSITION.md accounts for all four characterising files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record what this round deliberately did not fix, and gate on the full suite</name>
  <files>.planning/phases/15-shift-envelope-breaks-library-generation/deferred-items.md</files>
  <read_first>
    - .planning/phases/15-shift-envelope-breaks-library-generation/deferred-items.md — the existing
      format for this phase's deferrals; match it rather than inventing a second shape.
    - .planning/debug/shift-mode-break-geometry-ungoverned.md, RC-2 — the D5 evidence: the blocked-break
      window constraint is gated off in shift mode and `ShiftTemplateService`'s band validation never
      checks the offset against the desk's blocked hours, so a band at offset zero or at the envelope's
      trailing edge saves cleanly and scores zero hard while producing an unbroken working day.
    - .planning/debug/shift-envelope-unsatisfiable-hard.md, evidence T3 — the containment evidence: save
      time validates only grid alignment, and nothing anywhere checks that a template's envelope fits
      inside the desk's operating window, with the bounds accessor's end time read by no caller in main
      source.
  </read_first>
  <action>
    File two deferred items against this phase, in the file's existing format, each with enough detail
    that the next planner does not have to re-diagnose.

    First: blocked-break-hours has no enforcement point in shift mode. State the mechanism, state that
    the fix belongs at SAVE time in the template service rather than as a restored solver constraint —
    that decision is already settled and should not be relitigated — state that it is invisible in the
    hard score and therefore survives this round's fix untouched, and name the live agent-day it
    explains. Mark it as put out of scope by operator ruling OR-2 for this round specifically, not
    rejected.

    Second: a template envelope is never validated against the desk's operating window. State that plan
    15-11's supply gate catches the runtime CONSEQUENCE — such a desk is now refused before solving
    rather than producing an unexplained residual — but that the operator still cannot be told at save
    time that the template they are creating cannot fit, which is where the message belongs. Note the
    dead accessor as the concrete starting point.

    While in this file, check whether anything else this round changed makes an existing deferred entry
    stale, and correct it in place rather than leaving two records that disagree.

    Finish by running the entire suite. This plan adds no production source, but it is the closing gate
    for four plans that between them touched the solver's input construction, a constraint provider
    helper, the report layer, a response DTO record shape, the export and the frontend. The project's
    recorded discipline is that no scoped run is evidence for a change of that spread.
  </action>
  <verify>
    <automated>cd /Users/pete/IdeaProjects/wfm-service &amp;&amp; ./gradlew test &amp;&amp; cd frontend &amp;&amp; npm run build</automated>
  </verify>
  <done>
    Both deferred items are filed with mechanism, evidence, chosen fix location and scope reason; no
    deferred entry contradicts what this round shipped; the whole Java suite and the frontend build are
    both green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> release confidence | the suite is the only automated instrument standing between a solver regression and an operator's schedule |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-13-01 | Repudiation | a green suite that cannot fail | high | mitigate | The end-to-end test asserts non-vacuity before quality and walks the solved schedule outside the score director, so it cannot pass on an empty or trivially-satisfied solution the way the previous contiguity test did |
| T-15-13-02 | Tampering | retained diagnostic tests mistaken for guards | medium | mitigate | DISPOSITION.md states that no file in the diagnostic directory is a live guard and maps each to the regression test that replaced it |
| T-15-13-03 | Repudiation | silently dropped latent defects | medium | mitigate | Both known-latent defects are filed with mechanism and evidence, marked out of scope for this round rather than rejected, so a later audit finds a decision instead of an omission |
| T-15-13-04 | Denial of Service | end-to-end solve runtime in CI | low | mitigate | Solves terminate by step count with a recorded budget; the plan instructs reducing agent count rather than shape if the budget is exceeded |

No package-manager installs occur in this plan, so no legitimacy gate applies.
</threat_model>

<verification>
- A shape-complete shift desk either solves to zero hard or is refused with an actionable message; a completed solve carrying residual envelope penalty fails the test.
- On the zero-hard branch, held seats equal legal slots in both directions for every agent-day, verified outside the score director.
- The geometry characterisation survives only as a reframed guard; the report-layer case is deliberately absent and its replacement is named.
- All four characterising files are accounted for in DISPOSITION.md.
- Both latent defects are filed as deferred with mechanism and chosen fix location.
- `./gradlew test` and `npm run build` are both green.
</verification>

<success_criteria>
- The suite can now fail on the shape that produced G-15-10; before this round it could not, at any scale.
- No characterising test remains green against the defect it characterised.
- The next planner can find, in one file, exactly what this round chose not to fix and why.
- Acceptance of the gap itself is UAT Test 10 re-run against the dev deployment by `/gsd-verify-work`; this plan produces the automated evidence that makes that re-run worth doing.
</success_criteria>

<output>
Create `.planning/phases/15-shift-envelope-breaks-library-generation/15-13-SUMMARY.md` when done.
</output>
