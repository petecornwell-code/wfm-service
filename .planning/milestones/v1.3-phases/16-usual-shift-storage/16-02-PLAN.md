---
phase: 16-usual-shift-storage
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/main/java/com/wfm/repository/AgentUsualShiftRepository.java
  - src/main/java/com/wfm/service/ShiftTemplateService.java
  - src/main/java/com/wfm/service/DeskAgentService.java
  - src/main/java/com/wfm/dto/DeskAgentResponse.java
  - src/test/java/com/wfm/service/ShiftTemplateServiceTest.java
  - src/test/java/com/wfm/service/UsualShiftResolutionServiceTest.java
  - src/test/java/com/wfm/service/DeskAgentServiceUsualShiftTest.java
autonomous: true
requirements: [USHF-01, USHF-04, USHF-05, USHF-06, XCUT-02]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward observable truths ---
    - "D-01's operator-language test holds: when the operator edits template Early by creating a new era rather than mutating the row, an agent whose stored usual shift points at the old era resolves to the NEW era on dates the new era covers — the stored reference follows the name, not the frozen row."
    - "D-02: when no era of the stored name is effective on the asked-for date, resolution returns nothing and the stored row survives untouched as history; the row is never deleted, rewritten, or re-pointed by the resolver."
    - "D-16's three states are all reachable and are computed server-side: NOT_SET (no row), LIVE (row + a live era + the agent works that weekday), STORED_INACTIVE with reason RETIRED or NOT_WORKED. DeskAgentServiceUsualShiftTest reaches all four combinations."
    - "D-04: a usual shift stored on a weekday the agent does not work (0 hours, MANDATORY, or PTO) is stored and inert — the row persists, resolution still finds its era, and the roster reports STORED_INACTIVE with reason NOT_WORKED. Neither setDayHours nor setUsualShift writes into the other's table."
    - "USHF-04: an agent with no stored usual shift for a weekday yields status NOT_SET and UsualShiftResolutionService.resolve returns Optional.empty() — never a desk default, never a first-alphabetical template, never any substitute."
    - "D-05: a stored usual shift whose template's net working duration matches NONE of that template's band net-hours values against the agent's effective contracted hours for that weekday carries a non-null UsualShiftEntry.hoursAdvisory message naming both numbers. D-05 is 'advisory only — store it, surface a warning', so the warning must be observable by the operator; silence would be the rejected 'store silently, no advisory' option."
    - "D-05 stays advisory, never blocking: UsualShiftService.setUsualShift performs no hours comparison and can never reject or alter a write because of an hours mismatch. The advisory is computed on the READ path only, so it also keeps reporting a mismatch that appears later when contracted hours change under an unchanged usual shift — which a write-time-only toast could not do."
    - "The hours match is exact-equality with no tolerance, via BigDecimals.normalize and compareTo, and reuses ShiftTemplate.getNetHours(int) — the same primitive ShiftLibraryValidationService's own SHLB-06 advisory uses. There is no second net-hours implementation (Phase 14 D-08 discipline)."
    - "The any-band quantifier matches ShiftLibraryValidationService.findHoursAdvisories exactly: a template is advisory-free on a weekday when AT LEAST ONE of its bands yields a net duration equal to the agent's effective hours; a template with zero bands contributes exactly one full-envelope value via getNetHours(0)."
    - "An entry whose status is NOT_SET always has a null hoursAdvisory — there is no stored template to measure, so no advisory is possible."
    - "D-12 / XCUT-02: removing an agent from a desk through the roster UI clears that agent's usual shifts, via the same UsualShiftService.clearUsualShifts helper clearDesk calls in plan 16-03 — one implementation, two callers. Because there is no atomic desk-move endpoint in this application (removeDeskAgent sets deskId to null and a later assignEmployeesToDesk sets the new desk, and that second call refuses an agent whose deskId is already non-null), removeDeskAgent IS the desk-move trigger point and clearing there is what makes 'a stored usual shift always references a template from the agent's own desk library' unconditional rather than a resolver re-check."
    - "Deleting a shift template that any agent_usual_shift row references is refused with a ConflictException naming the count, instead of silently cascade-deleting those rows. Retirement (updateShiftTemplate setting effectiveTo) stays unblocked — D-02's 'retirement is never gated by referencing rows' governs retirement, not deletion, and deleteShiftTemplate's own javadoc already scopes it to a template that has never been used."
    # --- Edge-probe lift: USHF-01 (already authored in 16-01; re-proven here by dedicated tests) ---
    - "USHF-01/adjacency is proven, not just designed: UsualShiftResolutionServiceTest constructs two eras of the name Early whose effective ranges touch on exactly one date (era A's effectiveTo equals era B's effectiveFrom, which ShiftTemplate.isEffectiveOn treats as inclusive at both ends so BOTH match) and asserts resolve returns era B — the later-starting one — on that date, deterministically across repeated calls."
    - "USHF-01/encoding is proven: two live templates named Early and 'early ' (trailing space, lowercase) form two separate era families; an agent stored against one never resolves to the other, because era resolution uses exact String equality and never EnrichedColumnLayout.normalize."
    # --- Edge-probe lift: USHF-05 (2 of 4 here; the other 2 in plan 16-04) ---
    - "USHF-05/adjacency: clearUsualShifts is idempotent across overlapping callers — when clearDesk and removeDeskAgent both run for the same agent, the second is a no-op delete, not an error."
    - "USHF-05/empty: clearUsualShifts on an agent with zero stored rows returns normally and writes nothing."
  artifacts:
    - src/test/java/com/wfm/service/UsualShiftResolutionServiceTest.java
    - src/test/java/com/wfm/service/DeskAgentServiceUsualShiftTest.java
  key_links:
    - "DeskAgentService.removeDeskAgent -> UsualShiftService.clearUsualShifts — the D-12 call site. RESEARCH.md Pitfall 1 confirms removeDeskAgent does NOT currently clear AgentDayHours, so this is a NEW call site, not an existing cleanup list this phase joins."
    - "ShiftTemplateService.deleteShiftTemplate -> AgentUsualShiftRepository.countByShiftTemplate_Id — a READ-only reference, the only non-write use of that repository in production code; plan 16-04's write-path table records it as such."
    - "DeskAgentService.toResponse -> the dayRows map it already holds -> the NOT_WORKED reason. No new repository call: the cross-model read reuses the day-hours map toResponse is already given, so D-04's orthogonality is preserved at the write layer while the READ layer is allowed to correlate them for display."
  prohibitions:
    - "removeDeskAgent must not gain an AgentDayHours deletion. RESEARCH.md Pitfall 1 records that clearDesk and removeDeskAgent already disagree about day-hours by design (day-hours follow the person, preferences and exceptions follow the desk). This phase changes that disagreement for usual shifts only; extending it to day-hours would be an unrequested behaviour change to existing hours semantics."
    - "deleteShiftTemplate's new guard must not be applied to updateShiftTemplate. Retiring a template by setting effectiveTo stays unblockable by any referencing row (D-02, Phase 14 T-14-14)."
    - "The NOT_WORKED reason must not be computed by adding an AgentDayHoursRepository call inside UsualShiftResolutionService. Resolution answers 'which era is live on this date'; whether the agent works that weekday is a separate fact the roster read already holds."
    - "No second era-resolution implementation is introduced by this plan; DeskAgentService continues to call UsualShiftResolutionService.resolve and computes only the display discrimination on top of its result."
---

<objective>
Expand the tracer's single proven path into the full D-16 state machine and close the two write
paths the tracer deliberately left open: an agent leaving a desk (D-12) and an operator deleting a
template that usual shifts reference.

Purpose: plan 16-01 proved the architecture on one happy path. This plan is where the phase's
semantic decisions — D-01's resolve-by-name era-following, D-02's retired-degrades-to-unset, D-04's
stored-and-inert, USHF-04's no-penalty absence — become tested behaviour rather than design intent,
and where the silent-data-loss consequence of 16-01's `ON DELETE CASCADE` (threat T-16-09) is
closed.

Output: the `NOT_WORKED` arm and the RETIRED-vs-NOT_WORKED precedence ruling, the `removeDeskAgent`
clear call site, a delete-refusal guard on `ShiftTemplateService`, and two new test classes covering
D-01, D-02, D-03, D-04 and USHF-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-usual-shift-storage/16-CONTEXT.md
@.planning/phases/16-usual-shift-storage/16-PATTERNS.md
@.planning/phases/16-usual-shift-storage/16-RESEARCH.md
@.planning/phases/16-usual-shift-storage/16-01-SUMMARY.md
</context>

<planner_decisions>
- **P-07 — RETIRED wins over NOT_WORKED when both hold.** An agent can have a stored usual shift
  whose era is retired AND whose weekday they do not work. D-16 merges both into one muted
  treatment, so the operator's next action is the same either way, but the `reason` field is a
  single value and needs a deterministic rule. Ruling: check RETIRED first. The stored reference
  itself being dead is the more fundamental fact — a template that no longer exists as a live era
  cannot be honoured on any weekday, whereas "not worked" is a per-weekday condition that could flip
  back with one hours edit. Document the precedence in `toResponse`'s code comment, not only in this
  plan.

- **P-08 — "Does not work this weekday" means: the agent has an `agent_day_hours` row for that
  weekday whose `dayOffType` is MANDATORY or PTO, OR whose `hours` has `signum() == 0`.** Use
  `signum()`, never `equals(BigDecimal.ZERO)` — the stored value is `NUMERIC(5,2)` and reads back
  scaled, so equality against an unscaled zero is false for a real stored `0.00`. A weekday with NO
  stored row is NOT "not worked": it resolves to the schedule default (a positive number), so the
  agent works it and the state stays LIVE. This mirrors `DeskAgentResponse.DayHoursEntry.hasRow`'s
  own existing distinction between an operator-set weekday and a resolved default.

- **P-09 — `deleteShiftTemplate` gains a refusal, not a cascade acceptance.** RESEARCH.md Open
  Question 2 framed this as a legibility choice (a raw `DataIntegrityViolationException` versus the
  existing `ConflictException`). Plan 16-01's P-02 `ON DELETE CASCADE` changes the stakes: without a
  guard there is no exception at all — the delete succeeds and silently destroys every referencing
  agent's stored usual shift. The guard is therefore data-loss prevention, not message polish. It
  mirrors the existing `agentShiftAssignmentRepository.countByTenantIdAndSourceTemplateId` guard
  line-for-line, including directing the caller to retire instead.

- **P-10 — `AgentUsualShiftRepository.countByShiftTemplate_Id(UUID)` is not tenant-scoped, and that
  is deliberate.** It mirrors `findByAgent_IdAndDayOfWeek` and `deleteByAgent_Id`, both of which
  take raw ids because the caller has already resolved tenant scope. `deleteShiftTemplate` resolves
  the template through `findByIdAndTenantIdAndDeskId` before calling it, so the id is already proven
  in-scope. Carry that reasoning as a comment on the method, as the sibling methods do.
</planner_decisions>

<planner_assumptions>
**FLAGGED — needs a human ruling. Edge-probe item 15 (USHF-04, category `unclassified`) stays
`unresolved`.**

The deterministic edge probe could not classify USHF-04 ("An agent with no stored usual shift is
scheduled without penalty rather than being forced toward an arbitrary default") into any of its
eight categories, and the spec-less fallback rules forbid auto-resolving an `unclassified` row with
a backstop marker or dismissing it. It is therefore surfaced here rather than silently absorbed.

**Why it resists classification, and the planner's reading:** USHF-04's operative verb is
*"scheduled"*, and nothing in Phase 16 schedules anything. This phase makes "no stored usual shift"
a first-class, representable state — `Optional.empty()` from the resolver, `NOT_SET` on the roster,
a blank export cell, no row in the table. The *penalty* half of the requirement is Phase 17's
consistency constraint (CONS-01…CONS-04), which does not exist yet. So the requirement spans two
phases and its edge shape genuinely belongs to the later one.

**What this plan does deliver against it, and what it cannot:** every truth above that mentions
USHF-04 is about representability and the absence of substitution — `resolve` returns empty, the
roster reports `NOT_SET`, no default is manufactured. What this plan cannot prove is that a solver
which does not yet read this table applies no penalty; that assertion has no code to run against.

**The assumption the planner is making, stated so it can be rejected:** that a stored state of
"absent, and distinguishable from present-but-inactive" is the complete Phase 16 half of USHF-04,
and that the penalty-free half is correctly deferred to Phase 17 where CONS-04 already requires the
consistency constraint be soft. **If the operator disagrees** — if USHF-04 is meant to be fully
discharged inside Phase 16 — this phase is under-scoped and needs a task that reads the table from
the solve path, which `16-CONTEXT.md` § Phase Boundary explicitly excludes ("This phase stores,
writes, resolves and displays. It does **not** make the solver read the value"). Raise it at verify
time rather than closing it silently either way.
</planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Deleting a referenced template is refused, not silently cascaded</name>

  <files>
src/main/java/com/wfm/repository/AgentUsualShiftRepository.java,
src/main/java/com/wfm/service/ShiftTemplateService.java,
src/test/java/com/wfm/service/ShiftTemplateServiceTest.java
  </files>

  <read_first>
- `src/main/java/com/wfm/service/ShiftTemplateService.java` — `deleteShiftTemplate` (the existing `agentShiftAssignmentRepository.countByTenantIdAndSourceTemplateId` guard and its `ConflictException` message, which the new guard copies), the constructor's five injected collaborators, and the javadoc block above `deleteShiftTemplate` explaining it is guarded on USE
- `src/main/java/com/wfm/repository/AgentUsualShiftRepository.java` — written in plan 16-01; the sibling comments explaining why the single-row finders are not tenant-scoped
- `src/main/resources/db/migration/V47__add_agent_usual_shift.sql` — the `ON DELETE CASCADE` this guard exists to intercept
- `src/test/java/com/wfm/service/ShiftTemplateServiceTest.java` — the existing delete-refusal test case, whose shape the new case mirrors
- `.planning/phases/16-usual-shift-storage/16-CONTEXT.md` — D-02, to see exactly what "retirement is never blocked" does and does not cover
  </read_first>

  <precondition>
Plan 16-01 has landed: `AgentUsualShiftRepository`, `AgentUsualShift` and
`V47__add_agent_usual_shift.sql` exist, and `agent_usual_shift.shift_template_id` is declared
`ON DELETE CASCADE`. Verify with
`grep -c 'ON DELETE CASCADE' src/main/resources/db/migration/V47__add_agent_usual_shift.sql`
returning 2; halt if it does not.
  </precondition>

  <action>
Add `long countByShiftTemplate_Id(UUID shiftTemplateId);` to `AgentUsualShiftRepository`, with a
comment per P-10 stating it takes a raw template id because the only caller resolves the template
through `findByIdAndTenantIdAndDeskId` first, exactly as the sibling single-row finders do.

In `ShiftTemplateService`, inject `AgentUsualShiftRepository` as a sixth constructor parameter.
Inside `deleteShiftTemplate`, immediately after the existing `agentShiftAssignmentRepository`
usage guard, add a second guard: read
`agentUsualShiftRepository.countByShiftTemplate_Id(id)` and, when greater than zero, throw
`ConflictException` with a message in the same shape as the existing one — name the template, state
the count of agent-weekday usual shifts referencing it, and direct the operator to retire it by
setting its effective-to date instead.

Extend `deleteShiftTemplate`'s javadoc with a paragraph explaining the second guard, stating three
things explicitly: (1) unlike `agent_shift_assignment.source_template_id`, which carries no FK,
`agent_usual_shift.shift_template_id` is a real FK declared `ON DELETE CASCADE` so the database
would accept the delete and silently remove the referencing rows — this guard is data-loss
prevention, not a legibility improvement; (2) the cascade exists because
`DeskService.deleteDesk` relies on `shift_template.desk_id`'s own cascade and a non-cascading
reference from `agent_usual_shift` would make every such desk deletion fail; (3) this refusal
applies to DELETE only — retiring a template through `updateShiftTemplate` by setting
`effectiveTo` stays unblockable by any referencing row, which is what D-02 and Phase 14's T-14-14
actually guarantee.

In `ShiftTemplateServiceTest`, add two cases beside the existing delete-refusal case: one asserting
that deleting a template referenced by an `agent_usual_shift` row throws `ConflictException` whose
message contains the template name and the count, and one asserting that setting `effectiveTo`
through `updateShiftTemplate` on that same referenced template succeeds — pinning that the new
guard did not leak into the retirement path.
  </action>

  <verify>
    <automated>./gradlew test --tests ShiftTemplateServiceTest</automated>
  </verify>

  <acceptance_criteria>
- `src/main/java/com/wfm/repository/AgentUsualShiftRepository.java` contains
  `countByShiftTemplate_Id`.
- `src/main/java/com/wfm/service/ShiftTemplateService.java` contains
  `agentUsualShiftRepository.countByShiftTemplate_Id` and, within `deleteShiftTemplate`'s body, two
  `throw new ConflictException` statements.
- `awk '/public void deleteShiftTemplate/,/^    }/' src/main/java/com/wfm/service/ShiftTemplateService.java | grep -c 'countByShiftTemplate_Id'` returns 1.
- `awk '/public ShiftTemplateResponse updateShiftTemplate|public ShiftTemplate updateShiftTemplate/,/^    }/' src/main/java/com/wfm/service/ShiftTemplateService.java | grep -c 'countByShiftTemplate_Id'` returns 0 — the guard did not leak into the retirement path.
- `./gradlew test --tests ShiftTemplateServiceTest` passes, including the two new cases.
  </acceptance_criteria>

  <done>
An operator can no longer destroy every agent's stored usual shift for a template by deleting that
template; they are refused with a message naming the count and pointing at retirement instead. Threat
T-16-09 is closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: The roster distinguishes all three D-16 states, with both inactive reasons</name>

  <files>
src/main/java/com/wfm/service/DeskAgentService.java,
src/main/java/com/wfm/dto/DeskAgentResponse.java,
src/test/java/com/wfm/service/UsualShiftResolutionServiceTest.java,
src/test/java/com/wfm/service/DeskAgentServiceUsualShiftTest.java
  </files>

  <read_first>
- `src/main/java/com/wfm/service/DeskAgentService.java` — `toResponse` as plan 16-01 left it (the `DAY_ORDER` loop building both `dayHours` and `usualShift`), plus `loadDayHoursByAgent`/`loadUsualShiftsByAgent`
- `src/main/java/com/wfm/service/UsualShiftResolutionService.java` — the one `resolve` implementation, written in 16-01
- `src/main/java/com/wfm/dto/DeskAgentResponse.java` — `UsualShiftEntry`, `UsualShiftStatus`, `UsualShiftReason`, and `DayHoursEntry`'s `hasRow`/`dayOffType`/`hours` fields the NOT_WORKED test uses
- `src/main/java/com/wfm/model/ShiftTemplate.java` — `isEffectiveOn(LocalDate)` (inclusive at both ends) and `getEffectiveFrom`
- `src/main/java/com/wfm/model/AgentDayHours.java` and `src/main/java/com/wfm/model/DayOffType.java` — the MANDATORY/PTO labels and the `NUMERIC(5,2)` hours field P-08's `signum()` rule is about
- `src/main/java/com/wfm/service/ShiftLibraryValidationService.java` — `findHoursAdvisories`, `netHoursForBands`, `anyHoursMatch` and `advisoryMessage` (around lines 344–368 and 650–655): the SHLB-06 advisory this task's D-05 advisory deliberately mirrors, including the any-band quantifier and the exact-equality rule
- `src/main/java/com/wfm/model/ShiftTemplate.java` — `getNetHours(int breakDurationMinutes)` (line 129), the shared net-hours primitive both advisories use
- `src/main/java/com/wfm/repository/ShiftTemplateBreakBandRepository.java` — `findByTenantIdAndShiftTemplateIdInOrderByOffsetMinutesAsc`, the bulk band fetch that keeps the advisory free of an N+1
- `src/main/java/com/wfm/util/BigDecimals.java` — `normalize`, the scale-normalising helper the exact-equality comparison uses
- `src/main/java/com/wfm/service/DeskAgentService.java` — `resolveScheduleDefault` (line 58) and the `effectiveHours` resolution in the existing `DAY_ORDER` loop, which is where the agent's effective contracted hours for a weekday already comes from
- `src/test/java/com/wfm/service/DeskAgentServiceDayHoursTest.java` — the `@DataJpaTest` + `@Import` + `TenantContext` setup/teardown convention both new test classes copy
- `src/test/java/com/wfm/service/UsualShiftTracerTest.java` — the fixture-building helpers written in 16-01, to reuse rather than reinvent
- `.planning/phases/16-usual-shift-storage/16-UI-SPEC.md` § Component Specifications §1 — the exact three-state contract the DTO must satisfy
  </read_first>

  <action>
**`DeskAgentService.toResponse` — add the NOT_WORKED arm.** Inside the existing `DAY_ORDER` loop,
after `resolve(row, LocalDate.now())` returns non-empty, decide between `LIVE` and
`STORED_INACTIVE`/`NOT_WORKED` using the `dayRows` map the method already holds — no new repository
call. Per P-08, treat the weekday as not worked when a day-hours row exists for it AND either its
`dayOffType` is MANDATORY or PTO, or its `hours` has `signum() == 0`. A weekday with no day-hours
row is worked (it resolves to the schedule default), so it stays `LIVE`. Per P-07, evaluate RETIRED
first: an empty `resolve` result yields `STORED_INACTIVE`/`RETIRED` regardless of whether the
weekday is worked. Add a code comment stating the precedence and its reason, and a comment stating
that this correlation of the two models happens at the READ layer only — D-04's orthogonality is a
constraint on writes, and neither `setDayHours` nor `setUsualShift` may touch the other's table.

**DTO `DeskAgentResponse.UsualShiftEntry` — add the D-05 advisory field.** Widen the record written
in plan 16-01 to
`public record UsualShiftEntry(UsualShiftStatus status, String name, UsualShiftReason reason, String hoursAdvisory) {}`.
Document `hoursAdvisory` as: non-null only when a template is stored for this weekday AND none of
its bands' net durations equals the agent's effective contracted hours for that weekday; always null
for `NOT_SET`. Update the `NOT_SET` construction in `toResponse` to pass `null` as the fourth
component, and update plan 16-01's `UsualShiftTracerTest` assertions that construct or match on the
three-component shape.

**`DeskAgentService.toResponse` — compute the D-05 advisory.** In the same `DAY_ORDER` loop, for any
weekday whose status is `LIVE` or `STORED_INACTIVE`, compare the stored template against the agent's
effective contracted hours for that weekday:

- Take the agent's effective hours for the weekday from the value the loop already resolves for
  `dayHours` (the `agent_day_hours` row's value, or `scheduleDefault` when no row exists) — do not
  re-resolve it a second way.
- Build the template's band net-hours list exactly as `ShiftLibraryValidationService.netHoursForBands`
  does: when the template has no bands, the single value `template.getNetHours(0)`; otherwise one
  `template.getNetHours(band.getDurationMinutes())` per band.
- The template is advisory-free when AT LEAST ONE band value equals the effective hours under
  `BigDecimals.normalize(a).compareTo(BigDecimals.normalize(b)) == 0` — exact equality, no tolerance,
  matching Phase 14 D-06/D-07. Otherwise set `hoursAdvisory` to a message naming both numbers and
  stating it is non-blocking, in the register `ShiftLibraryValidationService.advisoryMessage` already
  uses, e.g. `"Early's net duration (9h) doesn't match this agent's contracted hours on Monday (8h). The usual shift is still saved."`
- Skip the comparison entirely when the weekday is not worked (the `NOT_WORKED` arm) — there are no
  contracted hours to mismatch against, so `hoursAdvisory` stays null there. A `RETIRED` entry still
  gets the comparison, because the stored template's own duration is still measurable.

Add a code comment recording that D-05 is deliberately advisory and read-side: `setUsualShift` must
never gain an hours check, because CONTEXT.md D-05 follows Phase 14's D-06 reasoning that a
contracted-hours mismatch is a moving judgement about a library the operator is still shaping —
unlike D-03's weekday-mask violation, which is a flat contradiction and IS rejected at write time.
Note in the same comment that computing this on the read path (rather than returning it once from the
write) is what makes a mismatch introduced *later* — by a contracted-hours edit under an unchanged
usual shift — visible at all.

**Bulk band fetch — no N+1.** Extend `loadUsualShiftsByAgent` (or add a sibling loader called from
the same place) to fetch every referenced template's bands in ONE query via
`ShiftTemplateBreakBandRepository.findByTenantIdAndShiftTemplateIdInOrderByOffsetMinutesAsc`, keyed
by template id, and pass that map into `toResponse` alongside the usual-shift rows. Inject
`ShiftTemplateBreakBandRepository` into `DeskAgentService` as a further constructor parameter. The
single-agent `toResponse` call sites pass the same map built for just that agent's referenced
templates. Do not call the band repository from inside the `DAY_ORDER` loop.

**`UsualShiftResolutionServiceTest` (new).** `@DataJpaTest @Import(UsualShiftResolutionService.class)
@ActiveProfiles("test")` with the `TenantContext` convention. Cases:
1. D-01 era-following ("Ana's usual shift is Early"): create era A of the name Early effective from
   two weeks ago to yesterday and era B of the same name effective from today with a different
   start time; store an `AgentUsualShift` whose FK points at era A; assert `resolve(stored,
   LocalDate.now())` returns era B — the stored reference follows the name, not the frozen row.
2. D-02 retired outright: a single era whose `effectiveTo` is yesterday, no successor; assert
   `resolve(stored, LocalDate.now())` is empty and that the `agent_usual_shift` row is still present
   and unchanged in the repository afterwards.
3. USHF-04: `resolve(null, today)` is empty.
4. USHF-01/adjacency: two eras of the name Early where era A's `effectiveTo` equals era B's
   `effectiveFrom` — since `isEffectiveOn` is inclusive at both ends, both match on that one date;
   assert `resolve` returns era B (the later `effectiveFrom`) and that ten repeated calls return the
   same era.
5. USHF-01/encoding: two live templates on the same desk named Early and a trailing-space,
   lowercased variant; an `AgentUsualShift` stored against one never resolves to the other. Also
   assert `resolve` does not call `EnrichedColumnLayout.normalize` by keeping the assertion on
   behaviour (the two names stay distinct families), not on implementation.
6. Desk scoping: an era of the same name on a DIFFERENT desk is never returned.

**`DeskAgentServiceUsualShiftTest` (new).** `@DataJpaTest` importing `DeskAgentService`,
`UsualShiftService` and `UsualShiftResolutionService`, `@ActiveProfiles("test")`. Build one desk,
one agent, and templates as needed, then assert on the `usualShift` map returned by
`listDeskAgentResponses`:
1. All four reachable states on one agent in one response — a weekday with no row (`NOT_SET`), a
   weekday with a live era and worked hours (`LIVE`), a weekday whose only era is retired
   (`STORED_INACTIVE`/`RETIRED`), and a weekday whose era is live but whose `agent_day_hours` row is
   MANDATORY (`STORED_INACTIVE`/`NOT_WORKED`). This is UI-SPEC E1's "partial" consideration proven
   at the data layer.
2. P-08's zero-hours form: an `agent_day_hours` row storing `0.00` with a null `dayOffType` also
   yields `NOT_WORKED` — assert against a value persisted through the repository so the scale is
   real, which is what makes `signum()` rather than `equals` the correct test.
3. P-08's no-row form: a weekday with a live era and NO `agent_day_hours` row stays `LIVE`.
4. P-07's precedence: a weekday that is both retired and PTO reports reason `RETIRED`.
5. D-04 storage: writing a usual shift for a MANDATORY weekday through
   `UsualShiftService.setUsualShift` succeeds (is not rejected) and persists a row — stored and
   inert, not refused.
6. D-04 orthogonality: after `setUsualShift`, the agent's seven `agent_day_hours` rows are
   byte-identical to before; after `setDayHours`, the agent's `agent_usual_shift` rows are
   byte-identical to before.
7. USHF-04: an agent with zero `agent_usual_shift` rows returns seven `NOT_SET` entries with null
   names and null reasons — and no entry carries a substituted template name.
8. `name` is the RAW stored template name in both `LIVE` and `STORED_INACTIVE` states, so the export
   and the template pre-fill can both read it without re-resolving.
9. D-05 advisory fires: an agent contracted to 8h on Monday with a stored usual shift whose only
   band yields 9h net returns a non-null `hoursAdvisory` naming both `9` and `8`, while status stays
   `LIVE` — the mismatch does not change the state and does not block anything.
10. D-05 advisory stays silent on a match: the same agent with a template whose band yields exactly
    8h returns a null `hoursAdvisory`.
11. D-05 any-band quantifier: a multi-band template where the FIRST band yields 9h and a later band
    yields 8h returns a null `hoursAdvisory` for an 8h agent — at least one band matching is enough,
    matching `ShiftLibraryValidationService`'s own rule rather than testing only band zero.
12. D-05 zero-band template: a template with no bands is measured on `getNetHours(0)` alone.
13. D-05 never blocks: `UsualShiftService.setUsualShift` with a 9h template for an 8h Monday agent
    SUCCEEDS and persists the row — assert no exception and a row present. This is the test that
    would fail if a future change turned the advisory into a rejection.
14. D-05 is null for `NOT_SET`: a weekday with no stored row has a null `hoursAdvisory`.
15. D-05 is null for `NOT_WORKED`: a MANDATORY weekday with a stored 9h template has a null
    `hoursAdvisory` — there are no contracted hours to mismatch against.
16. D-05 surfaces a mismatch created AFTER the write: store a matching 8h usual shift, then change
    the agent's Monday contracted hours to 6h through `setDayHours`, and assert the roster now
    reports a non-null `hoursAdvisory` without the usual shift having been touched. This is the
    behaviour a write-time-only advisory could not deliver, and it is why D-05 lives on the read path.
  </action>

  <verify>
    <automated>./gradlew test --tests UsualShiftResolutionServiceTest --tests DeskAgentServiceUsualShiftTest --tests UsualShiftTracerTest</automated>
  </verify>

  <acceptance_criteria>
- `./gradlew test --tests UsualShiftResolutionServiceTest` reports 6 or more tests, 0 failures.
- `./gradlew test --tests DeskAgentServiceUsualShiftTest` reports 16 or more tests, 0 failures.
- `src/main/java/com/wfm/dto/DeskAgentResponse.java` contains `String hoursAdvisory` inside the
  `UsualShiftEntry` record declaration.
- `grep -c 'getNetHours' src/main/java/com/wfm/service/DeskAgentService.java` returns 1 or more —
  the D-05 advisory reuses `ShiftTemplate.getNetHours(int)` rather than recomputing net duration
  from `startTime`/`endTime` locally.
- `grep -cE 'Duration\.between' src/main/java/com/wfm/service/DeskAgentService.java` returns 0 — no
  second net-hours implementation was introduced (Phase 14 D-08 discipline).
- `grep -c 'getNetHours\|hoursAdvisory' src/main/java/com/wfm/service/UsualShiftService.java`
  returns 0 — D-05 never reaches the write path, so no hours mismatch can ever reject a write.
- `awk '/private DeskAgentResponse toResponse/,/^    }/' src/main/java/com/wfm/service/DeskAgentService.java | grep -c 'shiftTemplateBreakBandRepository\.'` returns 0 — bands are bulk-loaded before the loop, not fetched per weekday.
- `src/main/java/com/wfm/service/DeskAgentService.java` contains `NOT_WORKED`, `RETIRED` and
  `signum()`; `grep -c 'BigDecimal.ZERO.equals\|equals(BigDecimal.ZERO)'` on it returns 0.
- `awk '/private DeskAgentResponse toResponse/,/^    }/' src/main/java/com/wfm/service/DeskAgentService.java | grep -c 'agentDayHoursRepository\.'` returns 0 — the NOT_WORKED arm uses the `dayRows` map already passed in, not a new query.
- `grep -c 'agentDayHoursRepository\|AgentDayHoursRepository' src/main/java/com/wfm/service/UsualShiftResolutionService.java` returns 0 — resolution stays a pure era question.
- `grep -c 'agentUsualShiftRepository\|AgentUsualShiftRepository' src/main/java/com/wfm/service/UsualShiftResolutionService.java` returns 0 — the resolver takes the stored row as a parameter and owns no write access.
  </acceptance_criteria>

  <done>
The roster response distinguishes never-set from set-but-not-in-effect, and names which of the two
reasons applies — the distinction v1.2 audit finding I-1 was about, and the one Phase 17's DRFT-02
will depend on.
  </done>
</task>

<task type="auto">
  <name>Task 3: Moving an agent off a desk clears their usual shifts (D-12)</name>

  <files>
src/main/java/com/wfm/service/DeskAgentService.java,
src/test/java/com/wfm/service/DeskAgentServiceUsualShiftTest.java
  </files>

  <read_first>
- `src/main/java/com/wfm/service/DeskAgentService.java` — `removeDeskAgent` in full: note that `agentPreferenceRepository` and `agentExceptionRepository` ARE cleaned up here and `agentDayHoursRepository` deliberately is NOT
- `src/main/java/com/wfm/service/UsualShiftService.java` — `clearUsualShifts(UUID)`, written in plan 16-01 as the one implementation
- `src/main/java/com/wfm/service/DeskAssignmentUploadService.java` — `clearDesk`, the other caller (plan 16-03 adds that call site); read it to confirm both callers reach the same helper
- `src/main/java/com/wfm/service/ClientManagementService.java` — `assignEmployeesToDesk`'s "already assigned to another desk" refusal, which is why `removeDeskAgent` is the only place a desk-to-desk move actually happens
- `.planning/phases/16-usual-shift-storage/16-RESEARCH.md` § Pitfall 1 and § Pitfall 2 — the two traps this task exists to avoid
  </read_first>

  <action>
Inject `UsualShiftService` into `DeskAgentService` as a further constructor parameter and call
`usualShiftService.clearUsualShifts(agentId)`. This is safe and acyclic because plan 16-01's P-06
deliberately gave `UsualShiftService` no service dependencies at all — it injects only
`AgentRepository`, `AgentUsualShiftRepository` and `ShiftTemplateRepository`, and the response
composition happens in `DeskAgentController`, not inside the service. Do NOT reintroduce a
`UsualShiftService -> DeskAgentService` edge; that is the cycle P-06 exists to prevent.

Do not inline a second `agentUsualShiftRepository.deleteByAgent_Id(agentId)` call here.
`clearUsualShifts` is the one implementation D-11 and D-12 must both reach (Phase 14's D-08
discipline); plan 16-03 adds the identical call from `DeskAssignmentUploadService.clearDesk`, and
plan 16-04's structural guard enumerates both call sites against the write-path table.

In `removeDeskAgent`, add the clear-usual-shifts call inside the existing "Clean up associated
desk-scoped data for this agent" block, beside the preference and exception deletions and before the
`agent.setDeskId(null)` unassignment. Add a comment stating three facts: (1) `AgentDayHours` is
deliberately absent from this list because day-hours follow the person, not the desk assignment,
and this task does not change that; (2) usual shifts DO belong here because a usual shift references
a template from the desk's own library and has no meaning once the agent leaves (D-12); (3) this
method is the desk-move trigger point — there is no atomic move endpoint in this application, and
`ClientManagementService.assignEmployeesToDesk` refuses an agent whose `deskId` is already non-null,
so an agent's desk can only change through this method setting it to null first.

Extend `DeskAgentServiceUsualShiftTest` with:
1. D-12 — an agent with stored usual shifts on three weekdays is removed from the desk via
   `removeDeskAgent`; assert zero `agent_usual_shift` rows remain for that agent, and assert the
   agent's `agent_day_hours` rows are untouched (pinning that this change did not silently extend
   to hours).
2. USHF-05/adjacency — calling the clear helper twice in a row for the same agent is a no-op the
   second time, not an error.
3. USHF-05/empty — calling the clear helper for an agent with zero rows returns normally and leaves
   the other agents' rows on the desk untouched.
4. The desk-move signature RESEARCH.md Pitfall 1 names as the failure to watch for: after
   `removeDeskAgent`, re-reading the agent's usual shifts finds nothing that could still resolve
   against the old desk's library.
  </action>

  <verify>
    <automated>./gradlew test --tests DeskAgentServiceUsualShiftTest</automated>
    <automated>./gradlew test</automated>
  </verify>

  <acceptance_criteria>
- `awk '/public void removeDeskAgent/,/^    }/' src/main/java/com/wfm/service/DeskAgentService.java | grep -ci 'usualshift'` returns 1 or more.
- `awk '/public void removeDeskAgent/,/^    }/' src/main/java/com/wfm/service/DeskAgentService.java | grep -c 'agentDayHoursRepository'` returns 0 — day-hours semantics unchanged.
- `./gradlew test --tests DeskAgentServiceUsualShiftTest` passes with the four new cases.
- `./gradlew test` — full suite green, at or above the 590-test baseline, 0 failures. This gate is
  mandatory for this task: `removeDeskAgent` is asserted on by pre-existing tests.
- `grep -c 'deleteByAgent_Id' src/main/java/com/wfm/service/DeskAgentService.java` returns 0 — the
  call goes through `clearUsualShifts`, not an inlined repository call.
- `grep -c 'DeskAgentService' src/main/java/com/wfm/service/UsualShiftService.java` returns 0 — the
  P-06 acyclic invariant still holds after this task.
- Spring context starts: `./gradlew test --tests UsualShiftTracerTest` still passes, which would
  fail with a `BeanCurrentlyInCreationException` if a cycle had been introduced.
  </acceptance_criteria>

  <done>
Moving an agent to a different desk clears their usual shifts, through the same implementation
`clearDesk` uses — so "a stored usual shift always references a template from the agent's own desk
library" is unconditional rather than something the resolver has to re-check.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 16-02's share)

| Path | Symbol(s) created |
|---|---|
| `src/main/java/com/wfm/repository/AgentUsualShiftRepository.java` | method `countByShiftTemplate_Id(UUID)` |
| `src/main/java/com/wfm/service/ShiftTemplateService.java` | second usage guard inside `deleteShiftTemplate`; injected `AgentUsualShiftRepository` |
| `src/main/java/com/wfm/service/DeskAgentService.java` | `STORED_INACTIVE`/`NOT_WORKED` arm and the RETIRED-first precedence in `toResponse`; clear-usual-shifts call site in `removeDeskAgent` |
| `src/test/java/com/wfm/service/UsualShiftResolutionServiceTest.java` | new test class (6+ cases) |
| `src/test/java/com/wfm/service/DeskAgentServiceUsualShiftTest.java` | new test class (12+ cases across Tasks 2 and 3) |
| `src/test/java/com/wfm/service/ShiftTemplateServiceTest.java` | two new cases: delete-refusal on a referenced template, retirement-still-allowed |

**New CLI flags / external endpoints:** none.
</artifacts_produced>

<threat_model>
ASVS enforcement level **1**; block on **high**.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → REST API (`DELETE /api/v1/desks/{deskId}/shift-templates/{id}`) | An operator-triggered destructive action whose blast radius this plan changes |
| Browser → REST API (`DELETE /api/v1/desks/{deskId}/agents/{agentId}`) | The desk-move trigger point; operator-supplied `deskId` and `agentId` |
| Tenant → tenant | `TenantContext`-derived scoping on every repository entry point |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-09 | Tampering | `ShiftTemplateService.deleteShiftTemplate` silently cascade-wiping stored usual shifts | high | mitigate | Task 1's `countByShiftTemplate_Id > 0` refusal, raised BEFORE the delete, mirroring the existing `agent_shift_assignment` guard. Without it plan 16-01's `ON DELETE CASCADE` turns a routine template delete into silent multi-agent data loss with no exception raised at any layer. Proven by two new `ShiftTemplateServiceTest` cases. |
| T-16-10 | Tampering | Stale desk-scoped reference surviving a desk move | high | mitigate | Task 3's `removeDeskAgent` clear (D-12). Without it, an agent moved from desk A to desk B keeps a usual shift whose FK points into desk A's library — the resolver would then return a template the agent's current desk does not own, and Phase 17's consistency constraint would score against it. |
| T-16-11 | Elevation of Privilege | `AgentUsualShiftRepository.countByShiftTemplate_Id` accepting a raw template id | medium | mitigate | The only caller resolves the template through `shiftTemplateRepository.findByIdAndTenantIdAndDeskId` first, so the id is proven in tenant+desk scope before the count runs (P-10). Same posture as the sibling `findByAgent_IdAndDayOfWeek` and `deleteByAgent_Id` methods, with the same explanatory comment. |
| T-16-12 | Information Disclosure | Another desk's era leaking into a resolution result | medium | mitigate | `UsualShiftResolutionService.resolve` reads `tenantId` and `deskId` off the stored template row and passes both to `findByTenantIdAndDeskIdAndName`; `UsualShiftResolutionServiceTest` case 6 pins that a same-named era on a different desk is never returned. |
| T-16-13 | Denial of Service | `removeDeskAgent` failing on an agent with no usual shifts | low | accept | `deleteByAgent_Id` on an empty set is a no-op, asserted by Task 3 case 3; no additional guard needed. |
| T-16-SC | Tampering | Dependency supply chain | low | accept | No package added by this plan. `16-RESEARCH.md` § Package Legitimacy Audit records zero packages evaluated for this phase. |
</threat_model>

<verification>
- `./gradlew test --tests ShiftTemplateServiceTest --tests UsualShiftResolutionServiceTest --tests DeskAgentServiceUsualShiftTest --tests UsualShiftTracerTest` — all green.
- `./gradlew test` — full suite, mandatory: `removeDeskAgent`, `deleteShiftTemplate` and
  `toResponse` are all asserted on by pre-existing tests.
- All four `UsualShiftStatus`/`UsualShiftReason` combinations (`NOT_SET`, `LIVE`,
  `STORED_INACTIVE`+`RETIRED`, `STORED_INACTIVE`+`NOT_WORKED`) are reachable in one
  `DeskAgentServiceUsualShiftTest` assertion over a single agent's seven weekdays.
</verification>

<success_criteria>
- The four D-16 states are proven reachable and are computed entirely server-side.
- D-01's operator-language test ("Ana's usual shift is Early") is a passing assertion, not prose.
- The desk-move path clears usual shifts through the same implementation `clearDesk` will use.
- Deleting a referenced template is refused; retiring one is not.
- Full suite green, 0 failures.
</success_criteria>

<output>
Create `.planning/phases/16-usual-shift-storage/16-02-SUMMARY.md` when done.
</output>
