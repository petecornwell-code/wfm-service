---
phase: 15-shift-envelope-breaks-library-generation
plan: 19
type: execute
wave: 2
depends_on: [15-18]
gap_closure: true
gap_ids: [G-15-31]
files_modified:
  - src/test/java/com/wfm/solver/SeatSupplyDistributionAnalysisTest.java
  - .planning/phases/15-shift-envelope-breaks-library-generation/15-SEAT-SUPPLY-GATE-ANALYSIS.md
  - .planning/phases/15-shift-envelope-breaks-library-generation/15-BENCHMARK.md
  - .planning/phases/15-shift-envelope-breaks-library-generation/15-UAT.md
autonomous: true
requirements: [ENVL-02, ENVL-06, XCUT-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The analysis nobody has done is done: candidate within-day blocking rules are EVALUATED against fixtures known to solve and fixtures known to collapse, with the false-refusal count measured per rule, not asserted."
    - "The evaluation is executable and repeatable — a JUnit class, not a prose argument — so the next person can re-run it against a new candidate rule instead of re-deriving the reasoning."
    - "No production behaviour changes in this plan. `advisoryOnThinTimeslotDoesNotBlock` still passes untouched; the gate is not made stricter here, because the gap's own `fix` forbids promoting the tightest-hour advisory to blocking without exactly this analysis first."
    - "At least one candidate rule is a PROVABLE necessary condition for a zero-hard solve — refusing on it cannot produce a false refusal by construction, the same standard of justification plan 15-11 held its day-wide check to."
    - "The recommendation states plainly what it is founded on and how small the corpus is. A recommendation resting on two fixtures says two fixtures."
  artifacts:
    - src/test/java/com/wfm/solver/SeatSupplyDistributionAnalysisTest.java
    - .planning/phases/15-shift-envelope-breaks-library-generation/15-SEAT-SUPPLY-GATE-ANALYSIS.md
    - .planning/phases/15-shift-envelope-breaks-library-generation/15-BENCHMARK.md
    - .planning/phases/15-shift-envelope-breaks-library-generation/15-UAT.md
  key_links:
    - "The analysis measures against the gate AS FIXED BY PLAN 15-18, not as it stands today. G-15-21's over-count made the supply number wrong; evaluating distribution rules against a wrong supply number would measure the wrong thing. This is why the plan depends on 15-18."
    - "Fixture reuse, not a third fixture: `LiveShapeShiftDeskFixture` (plan 15-14) is the live-shape control that solves, and every passing fixture in `ShiftEnvelopeSupplyGateTest` is a known-good desk the rules must not refuse."
    - "The live-weights rule appended to `15-BENCHMARK.md` by plan 15-18 binds this analysis: any statement about the live desk reads the desk's configured weights, never `ConstraintWeights.java` defaults, which differ on at least five constraints."
---

<objective>
Do the analysis the gap says is the actual work, and change no behaviour while doing it.

G-15-31, measured on a live run: the operator solved at `overallocationHardLimitPct` 250 — the UI's
own default, not the 500 that `HANDOFF.md` section 3 records as load-bearing. The solve was NOT
refused. It ran ~8 minutes to -120 hard, NOT FEASIBLE, violating contracted-hours-under, envelope
compliance AND contiguity: precisely the class of outcome the gate exists to pre-empt. Nothing on
screen named the limit as the cause.

The blocking check is one comparison of two DAY TOTALS. Both sides dwarf each other by the same
margin at 250% and at 500% — roughly 1.8x contracted demand at 250%, 3.6x at 500% — so the gate is
nowhere near firing in either case, yet one configuration solves to 0 hard and the other collapses.
The entire difference lives in the dimension the comparison aggregates away.

The sharpest part: the system COMPUTES the number that predicts the failure and declines to act on
it. The tightest-hour advisory sees per-hour distribution exactly — it printed "tightest at
08:00-09:00 with 3 seat(s)" on the failing run — and it is non-blocking BY EXPLICIT DESIGN, pinned
by a passing test named `advisoryOnThinTimeslotDoesNotBlock`.

The gap is explicit about what must NOT happen here: "DO NOT simply make the tightest-hour advisory
blocking. That test name is not an accident — advisory-only may well have been a deliberate call, and
promoting a thin-hour warning to a refusal risks false refusals on desks that currently solve fine.
Nobody has done that analysis; it is the actual work here."

Purpose: produce the missing analysis as measured evidence, so the decision about the gate's
comparison can be made on numbers rather than on the plausibility of an argument.

Output: an executable rule-evaluation harness, a written analysis with a measured false-refusal
table and a recommendation, and the round's gap entries closed with their real evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/15-shift-envelope-breaks-library-generation/15-UAT.md
@.planning/phases/15-shift-envelope-breaks-library-generation/15-11-PLAN.md
@.planning/phases/15-shift-envelope-breaks-library-generation/15-11-SUMMARY.md
@.planning/phases/15-shift-envelope-breaks-library-generation/15-14-PLAN.md
@.planning/phases/15-shift-envelope-breaks-library-generation/15-BENCHMARK.md
@.planning/phases/15-shift-envelope-breaks-library-generation/HANDOFF.md
@.planning/phases/15-shift-envelope-breaks-library-generation/15-18-PLAN.md
@.planning/phases/15-shift-envelope-breaks-library-generation/15-18-SUMMARY.md

@src/main/java/com/wfm/service/SolverService.java
@src/main/java/com/wfm/model/AgentShiftAssignment.java
@src/test/java/com/wfm/solver/LiveShapeShiftDeskFixture.java
@src/test/java/com/wfm/service/ShiftEnvelopeSupplyGateTest.java
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A fixture that reproduces the distribution blindness, and the seat arithmetic that explains it</name>

  <read_first>
    - `15-UAT.md` gap `G-15-31` in full — `detail` (including the per-date supply table and its
      explicit CAVEAT that the figures are derived from the staffing-requirements API rather than
      real envelope coverage and must not be quoted as exact), `fix`, and `operator_note`.
    - `15-UAT.md` Test 10 `session_2026_09_01` Run 3 (the -120 run) and `operator_context` — the desk
      is 40% over-supplied in total and simultaneously under-supplied at the peak; both facts were
      invisible to every aggregate check.
    - `SolverService.expandOverflowAssignments` (around :1579) — `maxAgents` is
      `(requiredAgents * overallocationHardLimitPct + 99) / 100`, integer ceiling. This is the exact
      arithmetic by which seats scale with the limit, and the gap states this model reproduces both
      live runs' "tightest at HH:MM with N seat(s)" advisories byte for byte.
    - `SolverService.requireShiftEnvelopeSeatSupply` as left by plan 15-18 — the date-aware covered
      timeslot helper, `librarySupplySlots`, `contractedSlots`, and the advisory loop.
    - `AgentShiftAssignment.getEligibleShiftBandPairs` and `envelopeSlackSlots` (around :93, :202) —
      an agent-day may under-occupy its covered slots by up to the slack, which is what makes
      "this agent is FORCED at this hour" a non-trivial predicate rather than "the pair covers it".
    - `Schedule.shiftEnvelopeSlackSlots` (default 1) and `SolveRequest`'s override.
    - `src/test/java/com/wfm/solver/LiveShapeShiftDeskFixture.java` — `build`, `Fixture`,
      `TEMPLATE_SPECS`, `demandByHour`, and the P-40 note that demand is derived from a
      provably-satisfiable ideal assignment. That property is what makes it a sound control.
  </read_first>

  <files>
    src/test/java/com/wfm/solver/SeatSupplyDistributionAnalysisTest.java
  </files>

  <behavior>
    - A DISTRIBUTION-BLIND fixture exists: a shift desk on which the day-wide seat supply comfortably
      exceeds day-wide contracted slots (so the current gate does not refuse), while at least one
      hour carries fewer seats than the number of agent-days structurally forced onto it. Asserted
      both ways — the gate as shipped does NOT refuse it, and the forced-versus-seats comparison at
      that hour is violated.
    - The fixture is provably infeasible at 0 hard, and that is demonstrated by solving it through
      the shipped `solverConfig.xml` and observing a non-zero hard score across every seed, not
      argued.
    - A CONTROL fixture exists and is not distribution-blind: `LiveShapeShiftDeskFixture` at its
      documented parameters passes the gate and solves within `SolverQualityGuardTest`'s established
      violation ceiling.
    - A per-hour seat model, driven by the same integer-ceiling arithmetic
      `expandOverflowAssignments` uses, reproduces the shipped gate's own tightest-hour advisory
      figures for both fixtures — so the model and the production code are pinned to each other and
      a later change to one fails the other.
  </behavior>

  <action>
Create `SeatSupplyDistributionAnalysisTest` in the solver test package. Its job in this task is to
establish the measurement ground: a fixture that exhibits the defect, a control that does not, and a
model whose numbers are tied to the shipped code rather than transcribed from a document.

Build the distribution-blind fixture as a deliberate minimal reproduction of the live shape, NOT as a
scale replica of the live desk. The live shape is: total supply generous, one boundary hour reachable
by a single template, and agent-days on that template with zero slack so every holder is forced onto
it. That is constructible at ten-agent scale and does not need 138 agent-days. Reuse
`LiveShapeShiftDeskFixture`'s construction helpers where they fit rather than writing a third desk
builder; if the fixture genuinely cannot be expressed through that class, say so in a comment and
build only what is missing.

Assert both halves of the defect on that fixture: the shipped gate passes it, and it does not solve
to 0 hard. Solve through `SolverConfig.createFromXmlResource("solverConfig.xml")` with a fixed seed
set and a step-count limit, exactly as `SolverQualityGuardTest` does — never a wall-clock limit, and
never a single seed. Report per-seed hard violation COUNTS per constraint, never raw hard scores;
this desk's own comparison rule (`15-BENCHMARK.md`, G-15-29) makes counts the only valid currency.

Then build the per-hour seat model. Derive seats per timeslot from required FTEs and the
over-allocation percentage using the same integer-ceiling expression the production code uses — do
not reimplement it as floating-point rounding, which would diverge on exactly the boundary cases that
matter. Assert the model reproduces the shipped gate's own tightest-hour advisory line for each
fixture, so the model cannot silently drift away from production.

Attempt, and report honestly on, the calibration the gap claims: the gap records that this arithmetic
reproduces the live desk's advisory figures byte for byte at both 500% and 250%. Reproduce that
calibration if the demand figures recorded in `15-UAT.md` and `HANDOFF.md` are sufficient to
reconstruct the live per-hour curve. If they are not sufficient, state exactly which figures are
missing and record the calibration as unattempted rather than approximating it. A calibration claimed
on partial data would be worse than none, and this file's own methodology treats a recorded
falsification as the useful part.
  </action>

  <verify>
    <automated>./gradlew test --tests "com.wfm.solver.SeatSupplyDistributionAnalysisTest"</automated>
  </verify>

  <done>
The class builds a distribution-blind fixture that the shipped gate passes and that does not reach 0
hard on any seed, plus a control that passes the gate and solves cleanly; a per-hour seat model
using the production integer-ceiling arithmetic reproduces the shipped advisory figures for both;
the live-desk calibration is either reproduced with its figures recorded or explicitly reported as
unattempted with the missing inputs named.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evaluate candidate blocking rules against the corpus, and measure the false refusals</name>

  <read_first>
    - `15-UAT.md` gap `G-15-31` `fix` — the prohibition on blindly promoting the advisory, and the
      instruction to read the 15-11 discussion before changing behaviour.
    - `15-11-PLAN.md` and `15-11-SUMMARY.md` — the design discussion behind the gate, and in
      particular the justification standard it held itself to: the day-wide check was defended as a
      GENUINE NECESSARY CONDITION for a zero-hard solve, argued from D-04's value range, so refusing
      on it cannot be a false refusal. Any rule recommended here must be defensible to the same
      standard or must carry a measured false-refusal rate instead.
    - `ShiftEnvelopeSupplyGateTest.advisoryOnThinTimeslotDoesNotBlock` (around :404) — the test whose
      name the gap says is not an accident. It is a control in this evaluation, not an obstacle.
    - `AgentShiftAssignment.getEligibleShiftBandPairs` and the slack arithmetic — needed to define
      "forced at this hour" correctly. An agent-day may skip a covered slot when its pair carries
      slack; it may not when net hours equal contracted hours exactly.
  </read_first>

  <files>
    src/test/java/com/wfm/solver/SeatSupplyDistributionAnalysisTest.java
  </files>

  <behavior>
    - At least four candidate rules are implemented as pure functions over (date, timeslots, seats,
      shift rows, pairs) returning refuse-or-pass, and evaluated against every fixture in the corpus:
      R0 the shipped day-wide sum (the control), R1 the tightest-hour advisory promoted to blocking
      at its current threshold, R2 a forced-occupancy necessary condition, and R3 the same necessary
      condition relaxed to a warn-only band. The exact rule set may grow; it may not shrink below
      R0, R1 and R2.
    - R2 is stated and proven as a necessary condition: an agent-day is FORCED at hour h when every
      one of its eligible pairs covers h AND that pair's covered-slot count equals the agent-day's
      expected work slots (zero slack, so it must occupy all of them). If the count of forced
      agent-days at h exceeds the seats at h, no assignment can seat them all inside their envelopes,
      so no zero-hard solve exists. A test asserts this reasoning holds on a hand-built case where
      the forced set is known by construction.
    - The corpus is every fixture that reaches the gate anywhere in the repository: the nine in
      `ShiftEnvelopeSupplyGateTest`, the gate calls in `ShiftEnvelopeSupplyInvariantTest` and
      `ShiftDeskEndToEndRegressionTest`, `LiveShapeShiftDeskFixture`, and Task 1's
      distribution-blind fixture. Each is labelled KNOWN-SOLVES or KNOWN-COLLAPSES from measured
      evidence, never from expectation.
    - The evaluation emits a rule-by-fixture table naming, per rule, the number of KNOWN-SOLVES
      fixtures it would refuse (false refusals) and the number of KNOWN-COLLAPSES fixtures it would
      catch (true refusals).
    - R2 records ZERO false refusals across the corpus. If it does not, that is the finding and it is
      reported as such — the test asserts the table is produced, not that any particular rule wins.
  </behavior>

  <action>
Implement the candidate rules as pure static functions in the analysis class, each with a javadoc
stating its claim and, where it has one, its soundness argument. Keep them in test source: this plan
changes no production behaviour, and moving a rule into `SolverService` is plan 15-20's decision to
make with this table in hand.

Build the corpus as a labelled list. Labelling matters more than the rule implementations: a fixture
is KNOWN-SOLVES only if this session observed it reach an acceptable outcome through the shipped
solver config across a seed set, and KNOWN-COLLAPSES only if it was observed not to. Do not label
from the fixture's name, its javadoc, or what it was built to demonstrate. Where a fixture is too
degenerate to solve meaningfully (a single agent-day, no specialization), label it
NOT-SOLVE-EVALUABLE and exclude it from the false-refusal denominator rather than counting it as a
pass — and report the excluded count, since a denominator quietly padded with trivial fixtures is
how a false-refusal rate flatters itself.

Emit the rule-by-fixture table through the test's own reporting output, in the style of
`SolverQualityGuardTest.buildQualityReport`, so a future run prints the comparison rather than
requiring someone to re-derive it. Assert on the structural properties the table must have — every
corpus fixture appears, every rule appears, R2 refuses the distribution-blind fixture — and let the
false-refusal counts be reported rather than asserted, except for R2's zero, which is asserted
because it follows from R2 being a necessary condition and a violation would mean the necessary-
condition argument is wrong.

Explicitly evaluate R1 rather than dismissing it. The gap's warning is against ADOPTING it unexamined,
not against measuring it. If it turns out to refuse KNOWN-SOLVES fixtures, that measurement is the
justification for `advisoryOnThinTimeslotDoesNotBlock` having been right all along, which is a real
finding worth recording; if it does not, that is equally a finding.

Read the desk's LIVE weights wherever this analysis touches the live desk, per the rule plan 15-18
appended to `15-BENCHMARK.md`. `ConstraintWeights.java`'s defaults differ from the live desk on at
least five constraints, and reasoning from the defaults is how several earlier conclusions in
`15-UAT.md` went wrong.

Change nothing in `src/main`. `advisoryOnThinTimeslotDoesNotBlock` must still pass, untouched, at the
end of this task.
  </action>

  <verify>
    <automated>./gradlew test --tests "com.wfm.solver.SeatSupplyDistributionAnalysisTest" --tests "com.wfm.service.ShiftEnvelopeSupplyGateTest"</automated>
  </verify>

  <done>
At least R0, R1, R2 and R3 are implemented and evaluated; the corpus is labelled from measured
evidence with any NOT-SOLVE-EVALUABLE exclusions counted and reported; the rule-by-fixture table is
printed with per-rule false-refusal and true-refusal counts; R2's necessary-condition argument is
proven on a hand-built case and its false-refusal count is zero or the deviation is reported as the
finding; `git diff --stat -- src/main` is empty.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the analysis, and close this round's gap entries with their real evidence</name>

  <read_first>
    - `15-16-SUMMARY.md`, `15-17-SUMMARY.md`, `15-18-SUMMARY.md` — the measured evidence this task
      transcribes. Every closure claim must come from a SUMMARY or from a command re-run in this
      session; none may be composed from the plan text, which describes intent rather than outcome.
    - `15-UAT.md` gap entries `G-15-22`, `G-15-27`, `G-15-29`, `G-15-30` — the established
      `status` / `resolved_by` / `resolved_evidence` shape this round's closures must match.
    - `15-BENCHMARK.md`'s two appended sections and the append-only discipline stated in their
      headings.
  </read_first>

  <files>
    .planning/phases/15-shift-envelope-breaks-library-generation/15-SEAT-SUPPLY-GATE-ANALYSIS.md,
    .planning/phases/15-shift-envelope-breaks-library-generation/15-BENCHMARK.md,
    .planning/phases/15-shift-envelope-breaks-library-generation/15-UAT.md
  </files>

  <action>
Write `15-SEAT-SUPPLY-GATE-ANALYSIS.md`. It is the deliverable for G-15-31 and it must be readable by
someone who has not read this plan. Cover, in this order:

- The question, stated as the gap states it: the comparison is day-total versus day-total, so
  distribution within the day is invisible, and fixing the supply NUMBER (G-15-21) or the supply
  MODEL (G-15-25) does not touch it.
- What was measured: the corpus, its labels, how each label was established, and the exclusions.
- The rule-by-fixture table, transcribed from the test's own output rather than retyped.
- R2's necessary-condition argument in full, since a rule that cannot false-refuse is the only kind
  that can be adopted without a tolerance decision.
- A recommendation, with its confidence stated in terms of the corpus size. If the corpus is small,
  say the number.
- An explicit verdict on `advisoryOnThinTimeslotDoesNotBlock`: whether it should change, and the
  measured reason. If the answer is that it should stay as it is, say so — the gap's own warning
  makes that a legitimate and useful outcome, not a failure to deliver.
- What this analysis does NOT settle, named individually rather than gestured at.

Append a short pointer section to `15-BENCHMARK.md` — appended and dated, never an edit to any
existing section — naming the analysis file, the rule-evaluation test, and the one-line
recommendation, so the benchmark file remains the index a reader hits first for anything
measurement-shaped in this phase.

Then close this round's gap entries in `15-UAT.md`, in the existing `resolved_by` /
`resolved_evidence` shape, for the gaps whose fixes have landed in waves 1 and 2:

- `G-15-32` and `G-15-26` — evidence from `15-16-SUMMARY.md`.
- `G-15-23` — evidence from `15-17-SUMMARY.md`.
- `G-15-21` and `G-15-24` — evidence from `15-18-SUMMARY.md`. For G-15-24 the entry MUST state that
  only the advice-safety half was delivered and that the destructiveness measurement was not
  re-established, with the confounding reason. Leave `retracted_claim` in place; it is the record of
  a correction and deleting it would erase the correction.
- `G-15-31` — record the analysis as its deliverable. Set its status to what the evidence supports
  and nothing more: `resolved` only if the analysis concludes no gate change is warranted and records
  that decision; otherwise leave it open with the analysis attached and the recommendation named, so
  plan 15-20 can close it against an implemented change. Do NOT mark it resolved merely because a
  document now exists.

Leave `G-15-25` untouched — plan 15-20 owns it. Leave `G-15-28` untouched: it is DATA owned by the
operator, who has stated they are correcting the weekend forecast, and no code fix belongs to it.
Leave `G-15-10` at `closed_pending_retest`: it is a retest obligation against a live deployment, not
work this round performed.

Every closure must carry a command that was actually run and its actual output. Do not transcribe a
test count from a plan or a SUMMARY without re-running it in this session — that is the exact
failure mode `15-VERIFICATION.md`'s "Note on this report's evidentiary basis" exists to prevent.
  </action>

  <verify>
    <automated>./gradlew test --tests "com.wfm.solver.SeatSupplyDistributionAnalysisTest" --tests "com.wfm.service.ShiftEnvelopeSupplyGateTest" --tests "com.wfm.service.ScheduleServiceShiftSnapshotTest" --tests "com.wfm.service.ShiftLibraryGenerationServiceTest" --tests "com.wfm.controller.GlobalExceptionHandlerTest"</automated>
  </verify>

  <done>
`15-SEAT-SUPPLY-GATE-ANALYSIS.md` exists and covers all seven sections including an explicit verdict
on the advisory test and a named list of what is not settled; `15-BENCHMARK.md` carries a dated
appended pointer and no pre-existing section is modified; `G-15-32`, `G-15-26`, `G-15-23`, `G-15-21`
and `G-15-24` are closed with re-run evidence; `G-15-31` carries the analysis with an honestly-set
status; `G-15-25`, `G-15-28` and `G-15-10` are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live desk data -> test fixture | Any figure transcribed from the live desk into a committed fixture crosses from production data into the repository |
| Analysis recommendation -> future gate behaviour | The recommendation is the input plan 15-20 acts on; an unmeasured recommendation becomes an unmeasured behaviour change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-19-01 | Information disclosure | `SeatSupplyDistributionAnalysisTest` fixture and the analysis document | medium | mitigate | Synthetic agent identifiers only (`Agent-N` / `A-N`), following `LiveShapeShiftDeskFixture`'s T-15-33 precedent and `15-BENCHMARK.md`'s T-15-32. No real agent name, email or BambooHR identifier is read or committed; live desk figures are limited to demand counts and template geometry already recorded in `15-UAT.md` and `HANDOFF.md` |
| T-15-19-02 | Repudiation | The recommendation | high | mitigate | Every rule carries a measured false-refusal count against a labelled corpus, and every label is established from an observed solve rather than from the fixture's intent. The corpus size is stated in the recommendation itself so the confidence cannot be overread |
| T-15-19-03 | Tampering | Accidental production change while "just measuring" | high | mitigate | `git diff --stat -- src/main` must be empty at the end of Task 2, and `advisoryOnThinTimeslotDoesNotBlock` must still pass untouched — both asserted in the task's done criteria and re-checked in this plan's verification section |
| T-15-19-04 | Denial of service | Repeated solves inside the analysis test | medium | mitigate | Step-count-terminated seeded solves only, never wall-clock limits, matching `SolverQualityGuardTest`'s established pattern; the class's isolated runtime is reported against that guard's recorded 52s so a runaway cost is visible rather than absorbed into the suite |

No package-manager installs (npm/pip/cargo) occur in this plan, so no supply-chain threat row and no
package-legitimacy checkpoint applies.
</threat_model>

<verification>
**This plan changes no production behaviour, and that is checked, not asserted.**

- `git diff --stat -- src/main` is EMPTY for this plan's commits. Report the command and its output.
- `ShiftEnvelopeSupplyGateTest` passes with `advisoryOnThinTimeslotDoesNotBlock` unmodified — confirm
  by `git diff` on that test file showing no change to that method.
- `./gradlew test` — full suite green. Baseline: 600 tests / 0 failures / 0 errors / 2 skipped at
  commit `660408d`, plus whatever waves 1 added. Report the new totals verbatim.
- **G-15-29's comparison rule is binding throughout.** Every solve run in this plan reports violation
  COUNTS per constraint. No raw hard score is used to compare two runs, and no single run is
  presented as evidence of anything — seeded multi-seed runs only, medians where a central value is
  wanted, in the pattern `15-BENCHMARK.md`'s Solver Quality Guard section establishes.
- **Runtime.** Report the isolated class time for `SeatSupplyDistributionAnalysisTest` against the
  52.166s recorded for `SolverQualityGuardTest`. If the analysis class materially exceeds it, say so
  and state whether the class should be gated; do not quietly add minutes to the deploy gate.
</verification>

<success_criteria>
- A distribution-blind fixture exists that the shipped gate passes and that provably does not reach 0
  hard, plus a control that passes the gate and solves.
- At least four candidate rules are evaluated against a labelled corpus with per-rule false-refusal
  and true-refusal counts printed by the test itself.
- R2 is proven to be a necessary condition for a zero-hard solve and records zero false refusals, or
  the deviation is reported as the finding.
- `15-SEAT-SUPPLY-GATE-ANALYSIS.md` carries the table, the recommendation with its corpus size, an
  explicit verdict on `advisoryOnThinTimeslotDoesNotBlock`, and a named list of what is not settled.
- No file under `src/main` changed.
- Five gap entries closed with re-run evidence; G-15-31 carries its analysis with an honest status;
  G-15-25, G-15-28 and G-15-10 untouched.
</success_criteria>

<output>
Create `.planning/phases/15-shift-envelope-breaks-library-generation/15-19-SUMMARY.md` when done.
Record: the rule-by-fixture table verbatim; the corpus labels and how each was established; any
NOT-SOLVE-EVALUABLE exclusions; the live-desk calibration outcome (reproduced with figures, or
unattempted with the missing inputs named); the recommendation in one sentence; the empty
`src/main` diff; and the isolated class runtime against the 52.166s reference.
</output>
