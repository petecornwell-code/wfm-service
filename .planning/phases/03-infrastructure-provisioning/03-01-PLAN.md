---
phase: 03-infrastructure-provisioning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [INFRA-01, INFRA-02]

must_haves:
  truths:
    - "Stale Terraform state lock is cleared and terraform can acquire new locks"
    - "terraform plan exits 0 and reports 45 resources to add with 0 errors"
    - "terraform apply completes successfully provisioning all 45 AWS resources"
  artifacts:
    - path: "Terraform remote state (S3)"
      provides: "State file tracking all 45 provisioned resources"
    - path: "AWS resources (VPC, ECS, RDS, ECR, ALB, S3, CloudFront, IAM)"
      provides: "Full cloud environment for wfm-service dev"
  key_links:
    - from: "infra/main.tf"
      to: "S3 remote state backend"
      via: "terraform backend config"
      pattern: "backend.*s3"
    - from: "infra/terraform.tfvars"
      to: "All resource configurations"
      via: "variable injection"
      pattern: "environment.*=.*dev"
---

<objective>
Force-unlock the stale Terraform state lock, verify the plan is clean, and run terraform apply to provision all 45 AWS resources (VPC, subnets, security groups, ECR, RDS PostgreSQL, ECS cluster + service, ALB, S3 resources, CloudFront, IAM roles + OIDC provider).

Purpose: Create the full AWS environment so the application can be deployed in Phase 4.
Output: All 45 AWS resources provisioned and tracked in Terraform state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-infrastructure-provisioning/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force-unlock stale state lock and verify clean plan</name>
  <files></files>
  <read_first>
    - infra/main.tf (backend config, provider versions)
    - infra/variables.tf (all variable definitions)
    - .planning/phases/03-infrastructure-provisioning/03-RESEARCH.md (lock ID, known issues)
  </read_first>
  <action>
Step 1 — Force-unlock the stale DynamoDB state lock:

Run from `/Users/pete/IdeaProjects/wfm-service/infra`:
```
terraform force-unlock -force c082e36d-65b7-8b40-8437-ea958ca6893b
```
The `-force` flag skips the interactive yes/no prompt. This removes the orphaned lock entry from DynamoDB table `wfm-terraform-locks`. The lock was created by a previous `terraform plan` on 2026-04-03T02:08:41Z from `pete@Petes-MacBook-Air-2.local` and the process is no longer running.

Step 2 — Verify lock is cleared by running terraform plan:

Run from `/Users/pete/IdeaProjects/wfm-service/infra`:
```
terraform plan -out=tfplan
```
Save the plan to a file (`tfplan`) so it can be applied in Task 2 without re-planning and without interactive approval.

Expected output: the final summary line reads "Plan: 45 to add, 0 to change, 0 to destroy." and exit code is 0. There must be NO "Error acquiring the state lock" message.

If `terraform plan` fails with a DIFFERENT lock error (new stale lock), extract the lock ID from the error message, force-unlock that one too, and re-run plan.

If `terraform plan` fails with a permissions error, note the exact error for the SUMMARY but do not abort — PowerUserAccess should cover all needed permissions.
  </action>
  <verify>
    <automated>cd /Users/pete/IdeaProjects/wfm-service/infra && terraform plan -lock=false 2>&1 | tail -5 | grep -E "to add.*to change.*to destroy"</automated>
  </verify>
  <acceptance_criteria>
    - `terraform force-unlock` exits 0 (lock removed from DynamoDB)
    - `terraform plan` exits 0 with no lock errors and no HCL errors
    - Plan summary line contains "to add" and "0 to change" and "0 to destroy"
    - File `infra/tfplan` exists (saved plan binary for Task 2)
  </acceptance_criteria>
  <done>Stale lock cleared. terraform plan exits 0 with ~45 resources to add, 0 errors. Saved plan file ready for apply.</done>
</task>

<task type="auto">
  <name>Task 2: Run terraform apply to provision all AWS resources</name>
  <files></files>
  <read_first>
    - infra/ecs.tf (ECS service config — confirm no timeouts block)
    - infra/rds.tf (RDS config — confirm skip_final_snapshot, secret creation)
    - infra/s3_cloudfront.tf (CloudFront distribution config)
    - infra/outputs.tf (output names to verify after apply)
  </read_first>
  <action>
Run terraform apply using the saved plan from Task 1. Run from `/Users/pete/IdeaProjects/wfm-service/infra`:
```
terraform apply tfplan
```
Using the saved plan file means no interactive approval is needed.

IMPORTANT timing expectations — DO NOT interrupt the apply:
- Total: 15-30 minutes
- RDS instance creation: 8-12 minutes (appears to hang on `aws_db_instance.main` — NORMAL)
- CloudFront distribution: 5-10 minutes (deploys to 450+ edge locations)
- NAT Gateway + EIP: 1-2 minutes
- Everything else: seconds each

If you must check progress without interrupting, open a second terminal:
- RDS: `aws rds describe-db-instances --region eu-west-2 --query "DBInstances[?DBInstanceIdentifier==\`wfm-service-dev\`].DBInstanceStatus" --output text`
- CloudFront: `aws cloudfront list-distributions --region us-east-1 --query "DistributionList.Items[?Comment==\`wfm-service dev frontend\`].Status" --output text`

If apply fails mid-way:
1. Read the error message carefully
2. Do NOT run `terraform destroy`
3. Fix the issue (most likely: IAM permission denied — note the exact action)
4. Re-run `terraform apply` (state tracks what was already created, so it picks up where it left off)

After apply completes successfully, capture outputs:
```
terraform output
```

Expected outputs (5 values):
- `alb_dns_name` — something like `*.eu-west-2.elb.amazonaws.com`
- `cloudfront_domain` — something like `*.cloudfront.net`
- `ecr_repository_url` — `521757869980.dkr.ecr.eu-west-2.amazonaws.com/wfm-service`
- `rds_endpoint` — something like `wfm-service-dev.*.eu-west-2.rds.amazonaws.com:5432`
- `s3_frontend_bucket` — `wfm-service-dev-frontend`

Record ALL 5 output values in the SUMMARY — Phase 4 needs them.

Expected ECS behavior: ECS service exists but tasks FAIL (no Docker image in ECR yet). This is correct and expected — Phase 4 pushes the first image.
  </action>
  <verify>
    <automated>cd /Users/pete/IdeaProjects/wfm-service/infra && terraform output -json 2>&1 | python3 -c "import sys,json; d=json.load(sys.stdin); assert all(k in d for k in ['alb_dns_name','cloudfront_domain','ecr_repository_url','rds_endpoint','s3_frontend_bucket']); print('ALL 5 OUTPUTS PRESENT')"</automated>
  </verify>
  <acceptance_criteria>
    - `terraform apply` exits 0 with "Apply complete! Resources: N added, 0 changed, 0 destroyed."
    - `terraform output` returns all 5 outputs: alb_dns_name, cloudfront_domain, ecr_repository_url, rds_endpoint, s3_frontend_bucket
    - `terraform state list | wc -l` returns 45 or more (all resources tracked)
  </acceptance_criteria>
  <done>All AWS resources provisioned via terraform apply. 5 output values captured. Terraform state tracks all resources.</done>
</task>

</tasks>

<verification>
- `terraform plan` shows no pending changes (all resources already applied)
- `terraform output` returns all 5 expected outputs with non-empty values
- `terraform state list` shows all provisioned resources
</verification>

<success_criteria>
- terraform apply completed with exit code 0
- All 5 terraform outputs populated (ALB DNS, CloudFront domain, ECR URL, RDS endpoint, S3 bucket)
- Terraform state tracks ~45 resources
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-provisioning/03-01-SUMMARY.md`

CRITICAL: Record all 5 terraform output values in the SUMMARY — Phase 4 depends on them.
</output>
