---
phase: 06-solver-quality-constraints
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/main/resources/db/migration/V28__add_agent_working_days_known.sql
  - src/main/java/com/wfm/model/Agent.java
  - src/main/java/com/wfm/integration/BambooRefreshService.java
  - src/main/java/com/wfm/service/SolverService.java
  - src/test/java/com/wfm/integration/BambooRefreshServiceTest.java
  - src/test/java/com/wfm/service/SolverServiceEligibilityFilterTest.java
autonomous: false
requirements: [QUAL-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Each scheduled agent with a parseable working pattern gets recurring MANDATORY AgentDayOff rows for their off-days across the schedule horizon"
    - "MANDATORY rows are idempotent across re-refreshes (delete-then-reinsert in the same window)"
    - "MANDATORY wins over PTO on the same (agent,date) via the existing dedup priority logic"
    - "Agents with Variable/blank working days (data gap) are excluded from the solver via filterEligible"
    - "Outlier patterns (!=2 contiguous off-days, or 0 off-days) are surfaced to the operator"
    - "The dead \"MANDATORY\".equalsIgnoreCase(type) match is removed"
    - "Generated MANDATORY rows fall within the schedule window and render in the ScheduleResults PTO tab"
  artifacts:
    - path: "src/main/resources/db/migration/V28__add_agent_working_days_known.sql"
      provides: "working_days_known BOOLEAN NOT NULL DEFAULT TRUE on agent (default kept permanently)"
      contains: "working_days_known"
    - path: "src/main/java/com/wfm/model/Agent.java"
      provides: "workingDaysKnown field + isWorkingDaysKnown()/setWorkingDaysKnown()"
      contains: "workingDaysKnown"
    - path: "src/main/java/com/wfm/integration/BambooRefreshService.java"
      provides: "MANDATORY row generation loop feeding the shared dedup map; data-gap flagging; dead-match removal"
      contains: "WorkingDaysParser"
    - path: "src/main/java/com/wfm/service/SolverService.java"
      provides: "fourth filterEligible criterion .filter(Agent::isWorkingDaysKnown)"
      contains: "isWorkingDaysKnown"
  key_links:
    - from: "BambooRefreshService.persistRefreshData"
      to: "WorkingDaysParser.parseWorkingDays"
      via: "per-agent parse of emp.customWorkingdays()"
      pattern: "WorkingDaysParser\\.parseWorkingDays"
    - from: "BambooRefreshService MANDATORY loop"
      to: "dedupedDaysOff map"
      via: "agentId + \"|\" + date key, MANDATORY before PTO loop"
      pattern: "DayOffType\\.MANDATORY"
    - from: "SolverService.filterEligible"
      to: "Agent.isWorkingDaysKnown"
      via: ".filter(Agent::isWorkingDaysKnown)"
      pattern: "isWorkingDaysKnown"
---

<objective>
Generate recurring `DayOffType.MANDATORY` `AgentDayOff` rows from each agent's parsed working
pattern (D-03), exclude data-gap agents from the solver (D-07), flag outliers (D-05), retire the
dead MANDATORY string match, and verify the rows render in the existing PTO tab (D-10).

Purpose: This is the payoff of QUAL-01 — correct mandatory day-off data finally flows into the
already-built solver and PTO tab. The solver engine and UI are unchanged (D-09/D-10); this plan
feeds them correct data.

Output: V28 migration, `workingDaysKnown` on Agent, MANDATORY generation in
`BambooRefreshService`, the fourth `filterEligible` filter, extended tests, and a manual UI-verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-solver-quality-constraints/06-CONTEXT.md
@.planning/phases/06-solver-quality-constraints/06-RESEARCH.md
@.planning/phases/06-solver-quality-constraints/06-PATTERNS.md
@.planning/phases/06-solver-quality-constraints/06-VALIDATION.md

<interfaces>
<!-- Consumes plan 01 parser + plan 02 BambooEmployee.customWorkingdays. -->
From plan 01 (com.wfm.integration.WorkingDaysParser):
  static Optional<Set<DayOfWeek>> parseWorkingDays(String raw)
  static Set<DayOfWeek> offDaysFrom(Set<DayOfWeek> workingDays)
  static boolean isStandardTwoContiguousDaysOff(Set<DayOfWeek> offDays)

From plan 02:
  BambooEmployee.customWorkingdays() // raw field 4517 value, may be null

Existing machinery (DO NOT reinvent):
  BambooRefreshService.persistRefreshData (lines 246-296):
    - deleteByAgent_IdAndDateBetween(agentId, from, to) + flush() at :249-252 (idempotency)
    - dedupedDaysOff LinkedHashMap, key = agent.getId() + "|" + date (:268)
    - priority logic :270-273 (MANDATORY > PTO; APPROVED > REQUESTED)
    - AgentDayOff construction :274-281 (setTenantId/setAgent/setDate/setType/setStatus)
    - dead match to REMOVE at :262-264: "MANDATORY".equalsIgnoreCase(type) || "holiday"...
    - `from`/`to` window already in scope; `employees` list in scope; `agentRepository` in scope
  SolverService.filterEligible (lines 974-981): 3 existing .filter(...) criteria, package-private static
  Agent.java: add boolean field analogous to employmentType (V25 analog)
  Migration: next version is V28 (V27 = add_bamboo_sync_event.sql). Analog: V25__add_agent_employment_type.sql

Surfacing decision (Claude's Discretion, D-05/D-07): use log.warn for outliers and a single
log.warn summary line for the data-gap count in persistRefreshData (MVP). Do NOT extend
BambooSyncEvent this phase — defer the persisted diagnostics surface to Phase 7 DIAG to avoid a
breaking schema change there. State this choice in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V28 migration + workingDaysKnown on Agent + filterEligible filter</name>
  <files>src/main/resources/db/migration/V28__add_agent_working_days_known.sql, src/main/java/com/wfm/model/Agent.java, src/main/java/com/wfm/service/SolverService.java, src/test/java/com/wfm/service/SolverServiceEligibilityFilterTest.java</files>
  <read_first>
    - .planning/phases/06-solver-quality-constraints/06-PATTERNS.md (V28 migration section — keep DEFAULT TRUE, do NOT drop default; SolverService filterEligible section)
    - src/main/resources/db/migration/V25__add_agent_employment_type.sql (migration analog)
    - src/main/java/com/wfm/model/Agent.java:38-44 (employmentType/active column pattern for the new boolean)
    - src/main/java/com/wfm/service/SolverService.java:974-981 (filterEligible)
    - src/test/java/com/wfm/service/SolverServiceEligibilityFilterTest.java (existing filter tests + agent(...) builder ~line 123)
  </read_first>
  <action>
    Create `V28__add_agent_working_days_known.sql`: `ALTER TABLE agent ADD COLUMN working_days_known
    BOOLEAN NOT NULL DEFAULT TRUE;`. UNLIKE V25, do NOT drop the default — comment that DEFAULT TRUE
    is kept permanently so agents created before their first refresh are not incorrectly excluded
    (D-07). Add a `boolean workingDaysKnown = true` field on `Agent` mapped to `working_days_known`
    (mirror the `active`/`employmentType` `@Column` style) with `isWorkingDaysKnown()` /
    `setWorkingDaysKnown(boolean)`. In `SolverService.filterEligible`, append a fourth criterion
    `.filter(Agent::isWorkingDaysKnown)` after the `primarySpecialization` filter — do NOT change the
    method signature. Extend `SolverServiceEligibilityFilterTest` with a test (reusing its `agent(...)`
    builder) asserting an active agent with a primary spec and a schedulable title but
    `workingDaysKnown=false` is excluded from `filterEligible`.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.SolverServiceEligibilityFilterTest"</automated>
  </verify>
  <acceptance_criteria>
    - V28 migration adds `working_days_known BOOLEAN NOT NULL DEFAULT TRUE` and does NOT drop the default (commented why).
    - Agent has `workingDaysKnown` (default true) with isWorkingDaysKnown()/setWorkingDaysKnown().
    - filterEligible has exactly four `.filter(...)` criteria, the fourth being `Agent::isWorkingDaysKnown`; signature unchanged.
    - New test proves a workingDaysKnown=false agent is excluded; existing eligibility tests still pass.
  </acceptance_criteria>
  <done>Migration, Agent flag, and solver exclusion are in place and the eligibility test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Generate MANDATORY rows + flag data-gap/outliers + remove dead match</name>
  <files>src/main/java/com/wfm/integration/BambooRefreshService.java, src/test/java/com/wfm/integration/BambooRefreshServiceTest.java</files>
  <read_first>
    - .planning/phases/06-solver-quality-constraints/06-RESEARCH.md (Pattern 2 MANDATORY generation; Code Examples: MANDATORY Row Generation; Pitfalls 5,6,7; A3 status=APPROVED)
    - .planning/phases/06-solver-quality-constraints/06-PATTERNS.md (BambooRefreshService section — insertion point after flush() at :252, dedup key, AgentDayOff construction, dead-code removal at :263)
    - .planning/phases/06-solver-quality-constraints/06-CONTEXT.md (D-03, D-04, D-05, D-07, D-08)
    - src/main/java/com/wfm/integration/BambooRefreshService.java:246-296 (persistRefreshData)
    - src/test/java/com/wfm/integration/BambooRefreshServiceTest.java (emp() helper ~129-132 — update for new BambooEmployee component; reflection test pattern)
  </read_first>
  <action>
    In `persistRefreshData`, immediately after `agentDayOffRepository.flush()` (~:252) and BEFORE the
    existing PTO loop, build `empByBambooId` via
    `employees.stream().collect(Collectors.toMap(BambooEmployee::id, e -> e, (a,b)->a))`. For each
    refreshed agent, look up its BambooEmployee and call
    `WorkingDaysParser.parseWorkingDays(emp.customWorkingdays())`. If empty (data gap): increment a
    `dataGapCount`, call `agent.setWorkingDaysKnown(false)` and `agentRepository.save(agent)`, then
    continue (D-07). Otherwise set `workingDaysKnown(true)`, save, compute
    `offDays = WorkingDaysParser.offDaysFrom(working)`; if
    `!WorkingDaysParser.isStandardTwoContiguousDaysOff(offDays)` emit a `log.warn` outlier flag
    naming the agent and off-day count (D-05 — no raw value, no PII). Then iterate `from..to`; for each
    date whose `getDayOfWeek()` is in `offDays`, construct an `AgentDayOff` with
    `setType(DayOffType.MANDATORY)`, `setStatus(DayOffStatus.APPROVED)` (A3), the standard
    tenant/agent/date setters, and `dedupedDaysOff.putIfAbsent(agent.getId() + "|" + date, dayOff)`.
    Generating MANDATORY first means the later PTO loop's priority check naturally keeps MANDATORY on
    conflicts (Pitfall 5). After the PTO loop, emit a single `log.warn` summary if `dataGapCount > 0`.
    REMOVE the dead `"MANDATORY".equalsIgnoreCase(type)` branch at ~:262-264 (keep `"holiday"` ->
    MANDATORY if desired, else collapse to PTO). Keep all generation inside the existing
    TransactionTemplate. Extend `BambooRefreshServiceTest`: update the `emp()` helper for the new
    `customWorkingdays` component and add a test proving an agent with `"Mon-Fri"` gets MANDATORY rows
    on Sat/Sun within the window, a `"Variable"` agent gets `workingDaysKnown=false` and no MANDATORY
    rows, and a second refresh is idempotent (row count unchanged).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.integration.BambooRefreshServiceTest" --tests "com.wfm.service.SolverServicePtoFilterTest"</automated>
  </verify>
  <acceptance_criteria>
    - MANDATORY AgentDayOff rows (type=MANDATORY, status=APPROVED) generated for each off-day in [from..to] for agents with a parseable pattern.
    - Variable/blank agents set workingDaysKnown=false, produce no MANDATORY rows.
    - MANDATORY entries feed the shared dedupedDaysOff map (key agentId|date) BEFORE the PTO loop; MANDATORY wins conflicts.
    - Outlier (!=2 contiguous / 0 off-days) emits a log.warn; data-gap count summarised via log.warn; no raw customWorkingdays value logged.
    - Dead `"MANDATORY".equalsIgnoreCase(type)` match removed.
    - Re-refresh is idempotent (delete-then-reinsert window); BambooRefreshServiceTest + SolverServicePtoFilterTest green.
  </acceptance_criteria>
  <done>MANDATORY rows generate correctly and idempotently; data-gap agents flagged; dead code gone; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify MANDATORY weekends render in PTO tab + desk-scale coverage</name>
  <read_first>
    - .planning/phases/06-solver-quality-constraints/06-CONTEXT.md (D-10 verify-not-build; specifics: desk-scale coverage risk)
    - .planning/phases/06-solver-quality-constraints/06-RESEARCH.md (Pitfall 6 window alignment; Pitfall 8 desk-scale coverage; A2)
    - .planning/phases/06-solver-quality-constraints/06-VALIDATION.md (Manual-Only Verifications)
    - frontend/src/pages/ScheduleResults.tsx (PtoTab — already renders type === 'MANDATORY' red)
  </read_first>
  <what-built>
    The full QUAL-01 pipeline now generates MANDATORY AgentDayOff rows from BambooHR field 4517 within
    the schedule window. The PTO tab already renders `type === 'MANDATORY'` cells red with a
    "MANDATORY" label/legend (D-10, verify-not-build). This checkpoint confirms the data lands in the
    window and that D-07 exclusion will not gut a live desk.
  </what-built>
  <how-to-verify>
    1. Run a BambooHR refresh for a scheduled desk, then generate a schedule.
    2. Open ScheduleResults -> PTO tab. Confirm each scheduled agent's two recurring weekend days show
       as red "MANDATORY" cells WITHIN the schedule window (Pitfall 6 — if blank, the generation
       window does not cover the schedule period).
    3. Confirm the label/legend reads clearly as mandatory; only adjust the label (e.g.
       "Mandatory (Weekend)") if it is ambiguous — otherwise leave the UI untouched (D-10).
    4. Desk-scale coverage (specifics risk / A2): for the live scheduled desks (e.g. StubHub-GE,
       Vinted-UA), confirm the data-gap count (log.warn summary) is not a large fraction of desk agents
       — a high blank/Variable rate would gut the schedule via D-07. Note the observed proportion.
  </how-to-verify>
  <acceptance_criteria>
    - Recurring MANDATORY weekend cells appear red within the schedule window in the PTO tab.
    - Label/legend reads as mandatory (UI changed only if ambiguous).
    - Data-gap proportion for the live scheduled desks is recorded and is not large enough to gut the schedule (or the risk is explicitly flagged to the operator).
  </acceptance_criteria>
  <resume-signal>Type "verified" with the observed data-gap proportion, or describe what was missing/ambiguous.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BambooHR data -> persistence/solver | Parsed working pattern becomes MANDATORY blocks that drive scheduling |
| Operator -> schedule output | Data-gap/outlier agents must be visibly handled, not silently dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-ID | Information Disclosure | Outlier/data-gap logging in persistRefreshData | mitigate | Log agent identifier + off-day count only; never log raw customWorkingdays value (PII) |
| T-6-RP | Repudiation / silent failure | D-07 silently excluding desk agents | mitigate | log.warn data-gap summary + manual desk-scale coverage check (Task 3) so operator sees exclusions |
| T-6-DT | Tampering / data integrity | Non-idempotent re-refresh duplicating MANDATORY rows | mitigate | Reuse existing deleteByAgent_IdAndDateBetween+flush window; idempotency asserted in BambooRefreshServiceTest |
| T-6-SC | Tampering | BambooHR credential feeding this pipeline | mitigate | Covered by plan 01 Task 0 BLOCKING key rotation; do not deploy until cleared |
</threat_model>

<verification>
- `./gradlew test` (full suite) green before `/gsd-verify-work`.
- `./gradlew test --tests "com.wfm.integration.BambooRefreshServiceTest"` proves generation, data-gap, and idempotency.
- `./gradlew test --tests "com.wfm.service.SolverServiceEligibilityFilterTest"` proves D-07 exclusion.
- `./gradlew test --tests "com.wfm.service.SolverServicePtoFilterTest"` proves MANDATORY blocks the solver (unchanged consumer).
- Manual PTO-tab + desk-coverage verification approved (Task 3).
</verification>

<success_criteria>
- Scheduled agents with a parseable pattern get correct, idempotent MANDATORY day-off rows honoured as hard blocks by the existing solver.
- Data-gap agents are excluded (D-07); outliers/data-gaps are surfaced (D-05); dead MANDATORY match removed.
- MANDATORY weekends render in the PTO tab within the schedule window (D-10); desk-scale coverage confirmed safe.
</success_criteria>

<output>
Create `.planning/phases/06-solver-quality-constraints/06-03-SUMMARY.md` when done.
</output>
