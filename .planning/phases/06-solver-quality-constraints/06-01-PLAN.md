---
phase: 06-solver-quality-constraints
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/test/java/com/wfm/integration/WorkingDaysParserTest.java
  - src/main/java/com/wfm/integration/WorkingDaysParser.java
autonomous: false
requirements: [QUAL-01]
nyquist_compliant: true

must_haves:
  truths:
    - "BambooHR API key exposed in chat (2026-06-02) is rotated and stored in a secret manager before any deploy"
    - "Every live BambooHR `customWorkingdays` format parses to the correct working-day set"
    - "null, blank, and 'Variable' values yield an empty Optional (data gap), never an exception"
    - "Week-wrapping ranges (Fri-Tue) expand across the week boundary correctly"
    - "Off-days = {Mon..Sun} minus working days; outlier patterns are detectable"
  artifacts:
    - path: "src/main/java/com/wfm/integration/WorkingDaysParser.java"
      provides: "Tolerant String -> Optional<Set<DayOfWeek>> working-days parser; offDaysFrom; outlier check"
      contains: "parseWorkingDays"
      min_lines: 60
    - path: "src/test/java/com/wfm/integration/WorkingDaysParserTest.java"
      provides: "Parameterized coverage of the full live BambooHR value catalog"
      contains: "workingDaysSource"
  key_links:
    - from: "WorkingDaysParserTest"
      to: "WorkingDaysParser.parseWorkingDays"
      via: "same-package static call (no reflection)"
      pattern: "WorkingDaysParser\\.parseWorkingDays"
---

<objective>
Rotate the exposed BambooHR API key (blocking security gate), then build the tolerant
`WorkingDaysParser` that converts free-text BambooHR field 4517 (`customWorkingdays`)
values into a `Set<DayOfWeek>` of working days, driven RED-first by a parameterized test
covering the full live value catalog.

Purpose: The parser is the engineering core of QUAL-01 (D-06). Every downstream plan
(field plumbing, MANDATORY row generation, data-gap exclusion) depends on this contract.
The security gate (key rotation) must clear before any of this work can ship.

Output: `WorkingDaysParser.java` (package-private static util) + `WorkingDaysParserTest.java`
(Wave 0 Nyquist stub, now green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-solver-quality-constraints/06-CONTEXT.md
@.planning/phases/06-solver-quality-constraints/06-RESEARCH.md
@.planning/phases/06-solver-quality-constraints/06-PATTERNS.md
@.planning/phases/06-solver-quality-constraints/06-VALIDATION.md
@.planning/todos/pending/2026-06-02-import-bamboohr-weekly-work-pattern-for-mandatory-day-offs.md

<interfaces>
<!-- Contract this plan CREATES for plans 02 and 03. Executor implements against this. -->
Package: com.wfm.integration

WorkingDaysParser (package-private, final, static-only, private constructor):
  static Optional<Set<DayOfWeek>> parseWorkingDays(String raw)
      // null / blank / "Variable" (case-insensitive) -> Optional.empty() (data gap)
      // otherwise -> Optional.of(working-day set); NEVER throws
  static Set<DayOfWeek> offDaysFrom(Set<DayOfWeek> workingDays)
      // EnumSet of {Mon..Sun} minus workingDays
  static boolean isStandardTwoContiguousDaysOff(Set<DayOfWeek> offDays)
      // true iff exactly 2 off-days that are adjacent in week order (with Sun-Mon wrap);
      // used by plan 03 for the D-05 outlier flag

Analog for class shell: src/main/java/com/wfm/util/BigDecimals.java (final, private ctor, static methods)
Analog for in-package static string->enum helper: BambooRefreshService.java:79-81 (mapEmploymentType)
Analog for null/blank guard: AgentEligibilityService.java:24-26
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0 [BLOCKING]: Rotate exposed BambooHR API key</name>
  <read_first>
    - .planning/phases/06-solver-quality-constraints/06-RESEARCH.md (Security Domain; Runtime State Inventory "Live service config")
    - .planning/todos/pending/2026-06-02-import-bamboohr-weekly-work-pattern-for-mandatory-day-offs.md (SECURITY line: key ad2bb…2be pasted in chat 2026-06-02)
  </read_first>
  <what-built>
    Nothing automated — this is a human-only security action. The BambooHR API key for the
    helpware tenant (prefix ad2bb…2be) was pasted into chat on 2026-06-02 and must be treated
    as compromised. Threat T-6-SC / Elevation of Privilege. This gate blocks deployment of any
    Phase 6 code that touches the BambooHR integration.
  </what-built>
  <how-to-verify>
    1. Log into the BambooHR admin console for the helpware tenant and revoke/rotate the API key.
    2. Store the NEW key in the secret manager backing the deployment (AWS Secrets Manager / env
       config), NOT in source control, NOT in chat.
    3. Confirm the old key (prefix ad2bb…2be) no longer authenticates.
    4. Confirm no key value appears in any committed file: `git grep -i "ad2bb" -- . || echo "clean"`.
  </how-to-verify>
  <acceptance_criteria>
    - Old key prefix ad2bb…2be is revoked (no longer authenticates against BambooHR).
    - New key is stored only in the secret manager / deploy env, never committed.
    - `git grep -i ad2bb` returns no matches in the repo.
  </acceptance_criteria>
  <resume-signal>Type "rotated" once the key is revoked and the new key is in the secret manager, or describe the blocker.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: RED — WorkingDaysParserTest covering the live value catalog</name>
  <files>src/test/java/com/wfm/integration/WorkingDaysParserTest.java</files>
  <read_first>
    - .planning/phases/06-solver-quality-constraints/06-VALIDATION.md (WorkingDaysParserTest Design; Wave 0 Requirements)
    - .planning/phases/06-solver-quality-constraints/06-RESEARCH.md (WorkingDaysParserTest Design code block; edge-case table)
    - .planning/phases/06-solver-quality-constraints/06-PATTERNS.md (WorkingDaysParserTest section — analog SolverServicePtoFilterTest, parameterized example, static DayOfWeek imports)
    - .planning/phases/06-solver-quality-constraints/06-CONTEXT.md (specifics: full live value list)
    - src/test/java/com/wfm/service/SolverServicePtoFilterTest.java (exact test-style analog)
  </read_first>
  <behavior>
    - Mon-Fri -> {MON,TUE,WED,THU,FRI}
    - Wed-Sun -> {WED,THU,FRI,SAT,SUN}
    - Sun-Thu -> {SUN,MON,TUE,WED,THU}
    - Tue-Sat -> {TUE,WED,THU,FRI,SAT}
    - Fri-Tue -> {FRI,SAT,SUN,MON,TUE} (week-wrap)
    - Mon - Sun -> all 7 (0 off-days outlier)
    - Mon - Sun HOOP -> all 7 (trailing annotation stripped)
    - Mon. to Thurs. -> {MON,TUE,WED,THU} ("to" separator, period tokens, Thurs. variant)
    - Mon, Tue, Wed, Thu, Sat -> {MON,TUE,WED,THU,SAT} (comma list, non-consecutive off)
    - "Variable" -> empty Optional; "" -> empty Optional; null -> empty Optional
  </behavior>
  <action>
    Create the test in package `com.wfm.integration` (same package as the parser — no reflection
    needed, the parser methods are package-private). Use JUnit 5 `@ParameterizedTest` +
    `@MethodSource("workingDaysSource")` and AssertJ. Static-import `java.time.DayOfWeek.*`.
    The `Arguments` stream must include every value from the live catalog in `<behavior>` plus the
    three data-gap cases; expected-working = null encodes the empty-Optional expectation. Assert
    `result.get()` with `containsExactlyInAnyOrderElementsOf`. Because `WorkingDaysParser` does not
    exist yet, this commit MUST NOT COMPILE/RUN green — it is the RED step. Do NOT stub the parser
    in this task.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.integration.WorkingDaysParserTest" 2>&1 | grep -Eiq "compil|cannot find symbol|error:|FAIL" && echo "RED confirmed" || (echo "Expected RED (no parser yet)" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - WorkingDaysParserTest.java exists in src/test/java/com/wfm/integration/ with a `workingDaysSource` MethodSource.
    - Every live value from the catalog (incl. Fri-Tue, Mon - Sun HOOP, Mon. to Thurs., comma list, Variable/blank/null) is a parameter row.
    - Compilation fails ONLY because `WorkingDaysParser` is undefined (RED) — no other errors.
  </acceptance_criteria>
  <done>Test file committed; test run is RED solely due to the missing WorkingDaysParser symbol.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement WorkingDaysParser (token-normalise-expand)</name>
  <files>src/main/java/com/wfm/integration/WorkingDaysParser.java</files>
  <read_first>
    - .planning/phases/06-solver-quality-constraints/06-RESEARCH.md (Pattern 1: token-normalise-expand; Pitfalls 1-4; week-wrap logic via WEEK_ORDER index)
    - .planning/phases/06-solver-quality-constraints/06-PATTERNS.md (WorkingDaysParser section — BigDecimals shell, mapEmploymentType helper, null guard, imports)
    - src/main/java/com/wfm/util/BigDecimals.java (class-shell analog: final, private ctor, static)
    - src/test/java/com/wfm/integration/WorkingDaysParserTest.java (the RED spec from Task 1 — drive implementation to green)
  </read_first>
  <action>
    Create `final class WorkingDaysParser` (package-private — no `public` on the class) in package
    `com.wfm.integration` with a private constructor and only static methods. Implement
    `parseWorkingDays(String raw)`, `offDaysFrom(Set<DayOfWeek>)`, and
    `isStandardTwoContiguousDaysOff(Set<DayOfWeek>)` per the `<interfaces>` contract. Pipeline:
    (1) guard null/blank/"Variable" (case-insensitive) -> `Optional.empty()`; (2) strip trailing
    non-day annotation tokens (e.g. `HOOP`) by popping right-side tokens that do not normalise to a
    day; (3) normalise separators — replace case-insensitive `\s+to\s+` with `-`; (4) detect
    comma-list vs range; (5) normalise each token (strip trailing `.`, map `Thur`/`Thurs`->Thu and
    all Mon..Sun three-letter prefixes) to `DayOfWeek`; (6) expand ranges over a fixed
    `WEEK_ORDER` list (Mon=0..Sun=6) and wrap when `endIdx < startIdx`. Use `EnumSet`/`Set<DayOfWeek>`.
    The parser MUST NEVER throw on any input (V5 input-validation control, threat T-6-IV): any
    unrecognised/garbage input falls through to `Optional.empty()`. Do NOT log raw employee values
    at INFO+ (no PII in logs — DEBUG only if needed). No fenced code in this file beyond the
    implementation itself. Keep it package-private so the same-package test calls it without reflection.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.integration.WorkingDaysParserTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.wfm.integration.WorkingDaysParserTest"` passes (all parameter rows green).
    - `Fri-Tue` -> {FRI,SAT,SUN,MON,TUE}; `Mon. to Thurs.` -> {MON,TUE,WED,THU}; `Mon - Sun HOOP` -> all 7; `Variable`/blank/null -> empty Optional.
    - Class is package-private `final` with a private constructor and only static methods (BigDecimals shell).
    - `offDaysFrom` returns {Mon..Sun} minus working days; `isStandardTwoContiguousDaysOff` true only for exactly-2-adjacent off-days (with Sun-Mon wrap).
    - Parser throws on no input in the test catalog (manually confirm a garbage row like "xyz" would return empty, not throw — add it to the catalog if helpful).
  </acceptance_criteria>
  <done>WorkingDaysParserTest is fully green; parser is package-private static-only and never throws.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BambooHR API -> integration layer | Untrusted free-text `customWorkingdays` strings cross into the parser |
| Operator/chat -> deploy secrets | API key handling; exposed key is a live credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-SC | Tampering / Elevation of Privilege | BambooHR API key exposed in chat (ad2bb…2be) | mitigate | Task 0 BLOCKING human checkpoint: rotate key, store in secret manager, `git grep` clean before deploy |
| T-6-IV | Tampering / DoS | `WorkingDaysParser.parseWorkingDays` on malformed BambooHR input | mitigate | Parser never throws; any unrecognised input -> `Optional.empty()` (data gap). V5 input-validation control |
| T-6-ID | Information Disclosure | Logging raw employee working-pattern values (PII) | mitigate | Do not log raw `customWorkingdays` at INFO+; DEBUG only; no request/response body logging |
</threat_model>

<verification>
- `./gradlew test --tests "com.wfm.integration.WorkingDaysParserTest"` is green.
- `git grep -i ad2bb` returns no matches (key rotated, not in VCS).
- WorkingDaysParser is package-private; WorkingDaysParserTest calls it without reflection.
</verification>

<success_criteria>
- BambooHR API key rotated and stored in secret manager (Task 0 approved).
- All live BambooHR `customWorkingdays` formats parse correctly; data-gap values return empty Optional; parser never throws.
- The contract (`parseWorkingDays`, `offDaysFrom`, `isStandardTwoContiguousDaysOff`) is available for plans 02 and 03.
</success_criteria>

<output>
Create `.planning/phases/06-solver-quality-constraints/06-01-SUMMARY.md` when done.
</output>
