---
phase: 10-enriched-upload-parsing
plan: 06
type: execute
wave: 3
depends_on: ["10-03", "10-04"]
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/pages/ClientManagement.tsx
autonomous: false
requirements: [UPL-06, UPL-09]
must_haves:
  truths:
    - "Operator sees a per-sheet rollup, per-row skip reasons, clamp warnings, and unmatched-sheet notices in the Upload Results modal"
    - "Operator can download the pre-seeded per-desk template from the Client Management page"
    - "The upload helper text describes the new per-desk shape and points at the template download (retired-shape guidance)"
  artifacts:
    - path: "frontend/src/api/client.ts"
      provides: "extended DTO types + downloadDeskAssignmentTemplate call"
      contains: "downloadDeskAssignmentTemplate"
    - path: "frontend/src/pages/ClientManagement.tsx"
      provides: "extended Upload Results modal + template download button"
      contains: "sheetSummaries"
  key_links:
    - from: "frontend/src/pages/ClientManagement.tsx"
      to: "frontend/src/api/client.ts"
      via: "clientManagement.downloadDeskAssignmentTemplate() + DeskAssignmentUploadResult type"
      pattern: "downloadDeskAssignmentTemplate|sheetSummaries"
---

<objective>
Extend the frontend to surface the enriched Upload Results (D-11): a per-sheet rollup, existing per-row skip reasons, non-blocking clamp warnings, and unmatched-sheet notices; and add a pre-seeded template download button (UPL-09) plus updated helper text describing the new per-desk shape (both retired shapes are rejected server-side).

Purpose: The parser's extended result DTO (plan 03) and template endpoint (plan 04) are only useful if the operator can see the rollup/warnings and download the template. This closes the UI surface for UPL-06 and UPL-09.
Output: Extended `client.ts` DTO types + template download call, and the extended `ClientManagement.tsx` modal + download button, verified visually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-enriched-upload-parsing/10-PATTERNS.md

<interfaces>
Backend JSON shape (plan 03): DeskAssignmentUploadResult { assignedCount, skippedCount, assignedDetails: string[], skippedDetails: SkippedRow[], sheetSummaries: SheetSummary[], warnings: string[], skippedSheets: SkippedSheet[] }; SheetSummary { deskName, importedCount, skippedCount }; SkippedSheet { sheetName, reason }; SkippedRow { rowNumber, bamboohrId, name, reason }.

Template endpoint (plan 04): GET /client-management/desk-assignments/template → attachment desk-assignment-template.xlsx.

Frontend analogs (PATTERNS.md § ClientManagement.tsx / client.ts):
- Existing Upload Results modal lines 453-501; skippedDetails table.
- handleExportEmployees blob-download pattern (lines 178-199) to mirror for handleDownloadTemplate.
- clientManagement.exportEmployees fetch-blob (client.ts lines 409-412) to mirror for downloadDeskAssignmentTemplate.
- Existing DTO interfaces (client.ts lines 430, 444) to extend.
- Helper text lines 326-328 (currently describes the retired 6-col shape) to update.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend client.ts DTO types + template download call</name>
  <files>frontend/src/api/client.ts</files>
  <read_first>
    - frontend/src/api/client.ts lines 395-444 (existing DeskAssignmentUploadResult/SkippedRow interfaces + exportEmployees/uploadDeskAssignments patterns)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "frontend/src/api/client.ts" (lines 551-581)
  </read_first>
  <action>
    Add interfaces `SkippedSheet { sheetName: string; reason: string }` and `SheetSummary { deskName: string; importedCount: number; skippedCount: number }`. Extend `DeskAssignmentUploadResult` with `sheetSummaries: SheetSummary[]`, `warnings: string[]`, `skippedSheets: SkippedSheet[]`. Add `downloadDeskAssignmentTemplate: () => fetch(\`${API_BASE}/client-management/desk-assignments/template\`, { headers: { 'X-Tenant-ID': currentTenantId } })` mirroring the existing `exportEmployees` blob-fetch pattern.
  </action>
  <acceptance_criteria>
    - Source assertions: client.ts contains `downloadDeskAssignmentTemplate`, `sheetSummaries`, `warnings`, `skippedSheets`, `SkippedSheet`, `SheetSummary`.
    - Frontend typecheck passes: `cd frontend && npm run build` (or the project's tsc/lint command) succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "downloadDeskAssignmentTemplate" frontend/src/api/client.ts && grep -q "sheetSummaries" frontend/src/api/client.ts</automated>
  </verify>
  <done>client.ts exposes the extended result DTO types and the template download call.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Upload Results modal + template download button + helper text</name>
  <files>frontend/src/pages/ClientManagement.tsx</files>
  <read_first>
    - frontend/src/pages/ClientManagement.tsx lines 159-225 (upload handler, handleExportEmployees, handleDownloadSkippedCsv sanitize), 326-328 (helper text), 453-501 (Upload Results modal)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "frontend/src/pages/ClientManagement.tsx" (lines 459-547 — modal extension, handleDownloadTemplate, helper-text update)
  </read_first>
  <action>
    In the Upload Results modal, add (a) a per-sheet rollup block rendering `uploadResult.sheetSummaries` (deskName: N imported, M skipped) using the same list/table styling as skippedDetails; (b) a warnings block rendering `uploadResult.warnings` and `uploadResult.skippedSheets` as non-blocking notices in a distinct (amber) color, only shown when non-empty. Keep the existing skippedDetails table and the CSV download button unchanged. Add a `handleDownloadTemplate` handler mirroring `handleExportEmployees` (calls `clientManagement.downloadDeskAssignmentTemplate()`, blobs it, triggers an `a.download = 'desk-assignment-template.xlsx'` click) and a "Download template" button near the upload control. Update the static helper text (lines 326-328) to describe the new per-desk-sheet shape (one worksheet per desk, sheet name = desk; BambooHR ID + Monday..Sunday day cells with number/MANDATORY/PTO; Specialty 1..N) and to instruct operators to download the template first (D-15 one-time re-download). Keep the upload trigger/result-set logic unchanged.
  </action>
  <acceptance_criteria>
    - Source assertions: ClientManagement.tsx contains `sheetSummaries`, `warnings`, `handleDownloadTemplate`, and `desk-assignment-template.xlsx`; helper text no longer describes the retired "Desk Assignment" 6-col shape.
    - Frontend build passes: `cd frontend && npm run build`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "sheetSummaries" frontend/src/pages/ClientManagement.tsx && grep -q "handleDownloadTemplate" frontend/src/pages/ClientManagement.tsx</automated>
  </verify>
  <done>The modal shows per-sheet rollup + warnings + unmatched-sheet notices; a template download button and updated helper text are present; frontend builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Upload Results modal + template download</name>
  <what-built>Extended Upload Results modal (per-sheet rollup, skip reasons, clamp + unmatched-sheet warnings) and a pre-seeded template download button on the Client Management page.</what-built>
  <how-to-verify>
    1. Run the app (backend + `cd frontend && npm run dev`) and open the Client Management page.
    2. Click "Download template" — confirm an `.xlsx` downloads with one worksheet per desk, identity columns filled for the current roster, and the 7 day cells + Specialty columns blank.
    3. Fill a template sheet with a mix: a valid row, a row with a blank day cell, a row with `32` in a day cell (clamp), a row with an unknown BambooHR ID, and (optionally) an extra sheet named for no configured desk. Upload it.
    4. Confirm the Upload Results modal shows: a per-sheet rollup (imported/skipped counts), per-row skip reasons, an amber clamp warning (`... 32 -> 24` style), and an unmatched-sheet notice.
  </how-to-verify>
  <acceptance_criteria>
    - Template downloads pre-seeded (identity filled, schedule blank), one sheet per desk.
    - Modal renders rollup counts, skip reasons, clamp warning, and unmatched-sheet notice correctly.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe rendering issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API JSON → DOM | Server-supplied skip reasons / warnings (which may echo operator BambooHR IDs/names) rendered in the modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Information Disclosure / XSS | Reflected skip-reason/warning strings rendered in the modal | mitigate | Render as React text nodes (auto-escaped), not dangerouslySetInnerHTML — matches existing skippedDetails rendering |
| T-10-13 | Tampering | Operator strings re-exported via CSV download | accept | Existing `handleDownloadSkippedCsv` sanitize() already guards formula injection for the CSV path (unchanged) |
</threat_model>

<verification>
- `cd frontend && npm run build` passes.
- Human-verify checkpoint approved (modal rollup/warnings + template download render correctly).
</verification>

<success_criteria>
- Operator can download the pre-seeded template and, after upload, sees a per-sheet rollup, per-row skip reasons, clamp warnings, and unmatched-sheet notices in the Upload Results modal (UPL-06/UPL-09).
</success_criteria>

<output>
Create `.planning/phases/10-enriched-upload-parsing/10-06-SUMMARY.md` when done.
</output>
