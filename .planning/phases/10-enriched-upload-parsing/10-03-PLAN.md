---
phase: 10-enriched-upload-parsing
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/main/java/com/wfm/service/DeskAssignmentUploadService.java
  - src/main/java/com/wfm/dto/SkippedSheet.java
  - src/main/resources/application.yml
  - src/test/java/com/wfm/service/DeskAssignmentUploadRetiredShapeTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadEnrichedShapeTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadLegacyShapeTest.java
autonomous: true
requirements: [UPL-01, UPL-03, UPL-04, UPL-05, UPL-06, UPL-07, UPL-08]
must_haves:
  truths:
    - "Operator uploads a workbook with one sheet per desk (sheet name = desk); every sheet is iterated and its agents parsed"
    - "A sheet whose name matches no configured desk is skipped whole with a non-blocking notice; other sheets still import"
    - "Each Mon-Sun cell parses as hours (>=0, incl. fractional like 7.5), MANDATORY, or PTO; blank/invalid skips the row with a specific reason"
    - "A number >24 is clamped to 24 and surfaced as a non-blocking warning (not silent)"
    - "A row whose BambooHR ID is not in the BambooHR cache is rejected with 'BambooHR ID not found' and no agent is created"
    - "6-col legacy and old flat-enriched shapes are rejected with a 'download the new template' message"
    - "Day cells write agent_day_hours rows (hours + nullable day_off_type); MANDATORY/PTO write 0.00 + label and do NOT delete field-4517 AgentDayOff rows (D-16 union preserved)"
  artifacts:
    - path: "src/main/java/com/wfm/service/DeskAssignmentUploadService.java"
      provides: "rewritten multi-sheet, EnrichedColumnLayout-driven parser"
      contains: "getNumberOfSheets"
    - path: "src/main/java/com/wfm/dto/SkippedSheet.java"
      provides: "structured unmatched-sheet notice"
      contains: "record SkippedSheet"
    - path: "src/main/resources/application.yml"
      provides: "multipart max-file-size / max-request-size for multi-desk workbooks"
      contains: "multipart"
  key_links:
    - from: "src/main/java/com/wfm/service/DeskAssignmentUploadService.java"
      to: "src/main/java/com/wfm/util/EnrichedColumnLayout.java"
      via: "header->index + dayHeader + specialtyIndex"
      pattern: "EnrichedColumnLayout"
    - from: "src/main/java/com/wfm/service/DeskAssignmentUploadService.java"
      to: "src/main/java/com/wfm/repository/AgentDayHoursRepository.java"
      via: "writes agent_day_hours rows with hours + dayOffType"
      pattern: "agentDayHoursRepository|AgentDayHours"
---

<objective>
Rewrite `DeskAssignmentUploadService` from a single-sheet, two-shape parser into a per-desk-sheet parser driven by `EnrichedColumnLayout`: iterate every sheet (sheet name = desk, D-01/D-02), match agents by BambooHR ID only (D-08, drop the ~45-line fuzzy fallback), scan unbounded `Specialty N` columns (D-06), parse each Mon-Sun cell to hours/MANDATORY/PTO (D-03/04/05/10, fixing the fractional-hours truncation bug), reject both retired shapes (D-15), and surface per-sheet rollups, skip reasons, clamp warnings, and unmatched-sheet notices (D-11).

Purpose: The core of the phase — the new polymorphic 7-column day-cell contract written into the Phase 9 `agent_day_hours` model (with the plan-01 `day_off_type` label), preserving the D-16 union with BambooHR field-4517 day-off blocks.
Output: The rewritten parser, the `SkippedSheet` DTO, the extended nested result DTO, multipart config, and retired-shape rejection tests replacing the obsolete import-success tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-enriched-upload-parsing/10-CONTEXT.md
@.planning/phases/10-enriched-upload-parsing/10-RESEARCH.md
@.planning/phases/10-enriched-upload-parsing/10-PATTERNS.md

<interfaces>
From src/main/java/com/wfm/util/EnrichedColumnLayout.java (plan 02): identityHeaders(), DAY_ORDER, dayHeader(DayOfWeek), specialtyIndex(String), normalize(String), COL_* constants.

From src/main/java/com/wfm/model/AgentDayHours.java (plan 01): setHours(BigDecimal) + setDayOffType(DayOffType) (nullable), setDayOfWeek(DayOfWeek), setAgent(Agent), setTenantId(long).

From src/main/java/com/wfm/model/DayOffType.java: enum { MANDATORY, PTO }.

Current parser constructor (UNCHANGED — 8 args): (AgentRepository, DeskRepository, ClientManagementService, AgentPreferenceRepository, AgentExceptionRepository, AgentDayHoursRepository, SpecializationRepository, AgentEligibilityService).

Current nested result record — EXTEND in place, keep it nested so ClientManagementController (returns it as JSON) needs NO change:
```java
public record DeskAssignmentUploadResult(int assignedCount, int skippedCount, List<String> assignedDetails, List<SkippedRow> skippedDetails) {}
```
Extended shape adds: List<SheetSummary> sheetSummaries, List<String> warnings, List<SkippedSheet> skippedSheets; nested `record SheetSummary(String deskName, int importedCount, int skippedCount)`.

parseDayCell reference implementation: RESEARCH.md § "Code Examples" / PATTERNS.md lines 163-186 (verbatim — DayCellResult(hours, DayOffType type, clampWarning); numeric via cell.getNumericCellValue() NOT (long); >24 clamp; negative/unknown -> Optional.empty).

Multi-sheet loop precedent: FteUploadService.java lines 82-90, 142-147.
Shape rejection precedent: current DeskAssignmentUploadService lines 120-137.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SkippedSheet DTO + workbook shape rejection + multi-sheet iteration + ID-only matching</name>
  <files>src/main/java/com/wfm/dto/SkippedSheet.java, src/main/java/com/wfm/service/DeskAssignmentUploadService.java</files>
  <read_first>
    - src/main/java/com/wfm/service/DeskAssignmentUploadService.java (full current file — keep constructor, clearDesk, cellAt, the deskByName/specsByDesk pre-load)
    - src/main/java/com/wfm/service/FteUploadService.java lines 82-90, 142-147 (multi-sheet iteration precedent)
    - src/main/java/com/wfm/dto/SkippedRow.java (sibling record style)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "DeskAssignmentUploadService.java" (multi-sheet loop, shape rejection lines 147-157) and § "SkippedSheet.java"
    - .planning/phases/10-enriched-upload-parsing/10-RESEARCH.md § "Pattern 3: Shape detection / rejection" (D-15), § "Pitfall 4" (sheet-name normalization)
  </read_first>
  <action>
    (a) Create `com.wfm.dto.SkippedSheet` as `record SkippedSheet(String sheetName, String reason)`.
    (b) Classify the whole workbook against the FIRST sheet's header row (D-15): if headers indicate the 6-col legacy shape (`desk assignment`) OR the old flat-enriched shape (`desk` + `monday` + `sunday`), throw IllegalArgumentException with the message "This spreadsheet uses a retired format. Please download the new template (one worksheet per desk) and re-enter your data." Accept only the new per-desk shape (`bamboohr id` + `monday` + `sunday` + NO `desk` column). Keep an "Unrecognised spreadsheet shape ... Got headers:" fallback for anything else.
    (c) Replace the single `getSheetAt(0)` flow with `for (int s = 0; s < workbook.getNumberOfSheets(); s++)`: read `sheet.getSheetName().trim()`, look up `deskByName.get(name.toLowerCase())`; on no match append `SkippedSheet(sheetName, "no matching desk — skipped")` to a new `List<SkippedSheet> skippedSheets` and `continue` (D-02); on match call `clearDesk(tenantId, desk.getId())` (D-17) then process rows. Two sheets resolving to the same desk process in order — second re-clears and re-populates (last-sheet-wins, RESEARCH Open Question 3).
    (d) Replace the fuzzy agent-matching block (current lines 249-293) with: confirm the BambooHR ID is present in the cache via `clientManagementService.findCachedEmployee`; on cache miss → `SkippedRow(rowNum, bamboohrId, name, "BambooHR ID not found")`, no agent created (D-08/UPL-07); otherwise resolve the DB agent by `agentRepository.findByTenantIdAndBamboohrId(tenantId, bamboohrId.trim())` (creating from cache only for a matched ID, never for an unmatched one). Delete the email/name fuzzy fallbacks.
    Keep the build green — the row body's day-cell/specialty writes are completed in Task 2; the extended result-record fields are added in Task 2 (leave the current 4-arg return until then).
  </action>
  <acceptance_criteria>
    - Source assertions: `DeskAssignmentUploadService.java` contains `getNumberOfSheets` and `SkippedSheet`; the agent-resolution path no longer calls `findByTenantIdAndEmailIgnoreCase` / `findByTenantIdAndNameIgnoreCase`.
    - `com.wfm.dto.SkippedSheet` record exists.
    - `./gradlew compileJava` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "getNumberOfSheets" src/main/java/com/wfm/service/DeskAssignmentUploadService.java && test -f src/main/java/com/wfm/dto/SkippedSheet.java</automated>
  </verify>
  <done>Multi-sheet iteration, D-15 retired-shape rejection, D-08 ID-only matching, and the SkippedSheet DTO are in place; build compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Day-cell parsing, Specialty N scan, agent_day_hours writes, extended result DTO, multipart config</name>
  <files>src/main/java/com/wfm/service/DeskAssignmentUploadService.java, src/main/resources/application.yml</files>
  <read_first>
    - src/main/java/com/wfm/service/DeskAssignmentUploadService.java (state after Task 1)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md lines 115-127 (specialty first-non-blank=primary logic to keep), 129-145 (clearDesk — must NOT delete AgentDayOff), 163-186 (verbatim parseDayCell), 356-401 (result DTO extension shape)
    - .planning/phases/10-enriched-upload-parsing/10-RESEARCH.md § "Code Examples" (parseDayCell), § "Pitfall 1" (fractional-hours getCellString bug), § "Pitfall 3" (clamp must be surfaced), § "Pitfall 5" (multipart limits), § "Recurring PTO Storage Decision" (write agent_day_hours hours + day_off_type, never dated AgentDayOff)
    - .planning/phases/10-enriched-upload-parsing/10-CONTEXT.md D-05, D-10, D-16
    - src/main/java/com/wfm/model/AgentDayHours.java (setDayOffType from plan 01)
    - src/main/resources/application.yml (add spring.servlet.multipart limits — confirm none present)
  </read_first>
  <behavior>
    - Numeric cell 7.5 → agent_day_hours row hours=7.50 (NOT 7 — fractional-hours regression), day_off_type=null.
    - Numeric cell 0 → row hours=0.00, day_off_type=null.
    - Cell "MANDATORY" (any case) → row hours=0.00, day_off_type=MANDATORY.
    - Cell "PTO" (any case) → row hours=0.00, day_off_type=PTO.
    - Cell 32 → row hours=24.00 AND a non-blocking clamp warning string added to the result warnings list.
    - Blank cell OR negative number OR unrecognized word in ANY of the 7 day cells → row skipped with a specific reason (D-04); other rows still import.
    - First non-blank Specialty N = primary, rest = secondary (D-06); any number of Specialty columns detected via EnrichedColumnLayout.specialtyIndex.
    - Writing MANDATORY/PTO creates/deletes NO AgentDayOff rows (D-16 union with field-4517 preserved).
  </behavior>
  <action>
    Add the verbatim `parseDayCell` helper returning `Optional<DayCellResult(BigDecimal hours, DayOffType type, String clampWarning)>`, reading via `cell.getNumericCellValue()` directly (NOT the truncating `getCellString`), matching MANDATORY/PTO case-insensitively, returning empty for blank/negative/unknown, clamping >24 to 24.00 with a warning string. For each data row: require BambooHR ID + all 7 day cells; if any of the 7 parseDayCell calls returns empty → skip the row with a reason naming the offending day (e.g. `Row {n} (id {id}): {Day} cell blank or invalid`). For each present day cell write one `agent_day_hours` row with `hours` and `dayOffType` set per the table in RESEARCH § "Recurring PTO Storage Decision". Accumulate clamp warnings into the result warnings list (surface, do not just log — Pitfall 3). Replace the fixed spec1/spec2 lookups with a header scan using `EnrichedColumnLayout.specialtyIndex()` collecting an ordered `List<String>` of non-blank specialty values, then reuse the existing first-non-blank=primary / rest=secondary assignment (PATTERNS 115-127). Keep the existing non-schedulable job-title skip and specialization-resolution skip. Build a per-sheet `SheetSummary(deskName, importedCount, skippedCount)` for each processed sheet. Extend the nested `DeskAssignmentUploadResult` record with `List<SheetSummary> sheetSummaries`, `List<String> warnings`, `List<SkippedSheet> skippedSheets`; add nested `record SheetSummary(...)`; update the return statement. Confirm `clearDesk` still deletes only preferences/exceptions/per-day-hours and NEVER AgentDayOff (field-4517 MANDATORY blocks must survive — D-16). Do NOT add any lookback/lookahead date-expansion (anti-pattern). In `application.yml` add `spring.servlet.multipart.max-file-size: 10MB` and `max-request-size: 10MB` (Pitfall 5) — first-line defense before POI opens the stream; leave POI ZipSecureFile defaults untouched.
  </action>
  <acceptance_criteria>
    - Source assertions: `parseDayCell` present; `getNumericCellValue()` used for day cells; result record has `sheetSummaries`, `warnings`, `skippedSheets`; `clearDesk` contains no AgentDayOff deletion; `application.yml` contains `multipart` size config.
    - Behavior (verified by plan 05 tests): 7.5 stores 7.50; MANDATORY/PTO store 0.00 + label; 32 clamps to 24 with a surfaced warning; blank/negative/unknown day cell skips the row.
    - `./gradlew test --tests "com.wfm.service.DeskAssignmentUpload*"` compiles (retired-shape tests pass after Task 3).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "parseDayCell" src/main/java/com/wfm/service/DeskAssignmentUploadService.java && grep -q "getNumericCellValue" src/main/java/com/wfm/service/DeskAssignmentUploadService.java && grep -q "sheetSummaries" src/main/java/com/wfm/service/DeskAssignmentUploadService.java && grep -q "multipart" src/main/resources/application.yml</automated>
  </verify>
  <done>Day-cell parsing (fractional + MANDATORY/PTO + clamp), unbounded Specialty N, agent_day_hours writes with day_off_type, extended result DTO, and multipart limits are all in place.</done>
</task>

<task type="auto">
  <name>Task 3: Retired-shape rejection tests; retire obsolete import-success tests</name>
  <files>src/test/java/com/wfm/service/DeskAssignmentUploadRetiredShapeTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadEnrichedShapeTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadLegacyShapeTest.java</files>
  <read_first>
    - src/test/java/com/wfm/service/DeskAssignmentUploadEnrichedShapeTest.java (test scaffold to copy; its import-SUCCESS assertions for the old flat-enriched shape are now obsolete)
    - src/test/java/com/wfm/service/DeskAssignmentUploadLegacyShapeTest.java (import-success assertions now obsolete)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "NEW test files" (scaffold lines 590-641) and § "Shape-rejection assertion style" (lines 645-652)
    - .planning/phases/10-enriched-upload-parsing/10-RESEARCH.md § "Pattern 3" (RetiredShapeTest sibling guidance) and § "State of the Art" (both shapes retired)
  </read_first>
  <action>
    Create `DeskAssignmentUploadRetiredShapeTest` mirroring the existing shape-test scaffold (mock-based, no Spring context; use the workbook-builder helper idiom). Add two cases: uploading a 6-col legacy sheet (`Desk Assignment` header) and uploading an old flat-enriched sheet (`Desk` + `Monday`..`Sunday` headers) each throw IllegalArgumentException whose message contains "template" (the D-15 "download the new template" wording). Retire the now-false assertions in `DeskAssignmentUploadEnrichedShapeTest` and `DeskAssignmentUploadLegacyShapeTest`: delete (or convert into retired-rejection assertions) any case that asserts these old shapes import successfully — they contradict D-15. Keep any still-valid "Unrecognised spreadsheet shape / Got headers:" fallback assertion by moving it to target the NEW shape's unknown-header path. Do not leave the two obsolete tests asserting old behavior.
  </action>
  <acceptance_criteria>
    - `DeskAssignmentUploadRetiredShapeTest` asserts both retired shapes throw IllegalArgumentException containing "template".
    - No test in the suite asserts a 6-col legacy or old flat-enriched sheet imports successfully.
    - Automated command: `./gradlew test --tests "com.wfm.service.DeskAssignmentUpload*"` passes green.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.DeskAssignmentUpload*"</automated>
  </verify>
  <done>Retired-shape rejection is tested; obsolete import-success tests are removed; the DeskAssignmentUpload* suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → upload API | Operator uploads an untrusted multipart `.xlsx` parsed by Apache POI |
| spreadsheet cell → DB | Cell contents (hours/keywords/identity) become persisted agent state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Denial of Service | OOXML zip-bomb / oversized multi-desk workbook via XSSFWorkbook parse | mitigate | Cap `spring.servlet.multipart.max-file-size`/`max-request-size` at 10MB before POI opens the stream (Task 2); keep POI `ZipSecureFile` defaults enabled (do not disable) |
| T-10-05 | Tampering / Elevation | Invalid/negative/unknown day-cell content persisted | mitigate | Allowlist validation in `parseDayCell` (number 0-24 or MANDATORY/PTO only); negatives/unknown/blank skip the row (D-04/D-10) — never a denylist |
| T-10-06 | Spoofing / Integrity | Creating agents that do not exist in BambooHR | mitigate | BambooHR-ID-only match against the cache; unmatched → reject, no agent created (D-08/UPL-07) |
| T-10-07 | Information Disclosure | BambooHR ID / name reflected into skip-reason strings | accept | Pre-existing accepted pattern (SkippedRow already carries id/name); no new exposure introduced |
| T-10-SC | Tampering | package installs | n/a | No new packages — POI/Flyway/JPA already present (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `./gradlew test --tests "com.wfm.service.DeskAssignmentUpload*"` green.
- `./gradlew compileJava compileTestJava` green.
- Parser iterates every sheet, rejects retired shapes with the template message, writes agent_day_hours with day_off_type, and never deletes AgentDayOff.
</verification>

<success_criteria>
- One workbook (sheet=desk) fully parses identity + unbounded specialties + 7 polymorphic day cells; unmatched sheets and invalid rows are skipped with specific reasons; clamps surface as warnings; both retired shapes are rejected; unmatched BambooHR IDs create no agent.
</success_criteria>

<output>
Create `.planning/phases/10-enriched-upload-parsing/10-03-SUMMARY.md` when done.
</output>
