---
phase: 10-enriched-upload-parsing
plan: 04
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - src/main/java/com/wfm/service/DeskAssignmentTemplateService.java
  - src/main/java/com/wfm/controller/ClientManagementController.java
  - src/main/java/com/wfm/service/DeskAgentExportService.java
  - src/test/java/com/wfm/service/DeskAssignmentTemplateServiceTest.java
autonomous: true
requirements: [UPL-09]
must_haves:
  truths:
    - "Operator can download a pre-seeded per-desk template: one worksheet per desk, current roster identity filled, day cells + specialties blank (D-14: pre-seeded template, identity filled, schedule blank)"
    - "Template, parser, and export all resolve their column shape from the one EnrichedColumnLayout (no drift)"
    - "Operator-supplied strings written into the generated .xlsx are formula-injection sanitized"
  artifacts:
    - path: "src/main/java/com/wfm/service/DeskAssignmentTemplateService.java"
      provides: "pre-seeded per-desk template generation"
      contains: "createSheet"
    - path: "src/main/java/com/wfm/controller/ClientManagementController.java"
      provides: "GET desk-assignments/template download endpoint"
      contains: "desk-assignments/template"
  key_links:
    - from: "src/main/java/com/wfm/service/DeskAssignmentTemplateService.java"
      to: "src/main/java/com/wfm/util/EnrichedColumnLayout.java"
      via: "identityHeaders() + dayHeader() for header row"
      pattern: "EnrichedColumnLayout"
    - from: "src/main/java/com/wfm/controller/ClientManagementController.java"
      to: "src/main/java/com/wfm/service/DeskAssignmentTemplateService.java"
      via: "generateTemplate() byte[] attachment response"
      pattern: "deskAssignmentTemplateService|generateTemplate"
---

<objective>
Add the pre-seeded per-desk template download (D-13/D-14/UPL-09): one worksheet per desk, each seeded with the current roster's identity columns filled and the 7 day cells + specialty columns left blank for the operator to fill (the export→fill→re-upload round-trip). Template, parser, and export share `EnrichedColumnLayout` so the shapes cannot drift, and any operator-supplied string written into the `.xlsx` is formula-injection sanitized.

Purpose: Operators must re-download the new pre-seeded template (both retired shapes are rejected in plan 03). This plan builds the template generator, its download endpoint, and aligns the export's shared identity columns to the same layout.
Output: `DeskAssignmentTemplateService`, a controller download endpoint, export alignment to `EnrichedColumnLayout`, and a template-service test. Runs in parallel with plan 03 (no shared files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-enriched-upload-parsing/10-RESEARCH.md
@.planning/phases/10-enriched-upload-parsing/10-PATTERNS.md

<interfaces>
From src/main/java/com/wfm/util/EnrichedColumnLayout.java (plan 02): identityHeaders(), DAY_ORDER, dayHeader(DayOfWeek), COL_* constants.

Roster query to reuse for pre-seeding: `DeskAgentService.listDeskAgentResponses(deskId, null, null, Integer.MAX_VALUE)` (see DeskAgentController.java lines 88-91) returns per-desk `DeskAgentResponse` with identity fields.

Export/download precedents to mirror:
- DeskAgentExportService.java (full file) — POI XSSFWorkbook writer, header style, byte[] output.
- ClientManagementController.exportEmployees() lines 78-92 — byte[] .xlsx attachment response idiom.
- frontend ClientManagement.tsx handleDownloadSkippedCsv sanitize() (lines 203-210) — the exact formula-injection guard to mirror SERVER-SIDE for any operator string written into a cell (prefix leading =,+,-,@ with a single quote).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeskAssignmentTemplateService — pre-seeded per-desk template + sanitization</name>
  <files>src/main/java/com/wfm/service/DeskAssignmentTemplateService.java</files>
  <read_first>
    - src/main/java/com/wfm/service/DeskAgentExportService.java (full file — POI writer, createHeaderStyle, byte[] output; the closest analog)
    - src/main/java/com/wfm/controller/DeskAgentController.java lines 88-97 (roster query DeskAgentService.listDeskAgentResponses)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "DeskAssignmentTemplateService.java" (lines 245-293) and § "CSV/XLSX formula-injection sanitization"
    - .planning/phases/10-enriched-upload-parsing/10-RESEARCH.md § "Security Domain" (Known Threat Patterns — Tampering / formula injection)
    - src/main/java/com/wfm/util/EnrichedColumnLayout.java (plan 02)
  </read_first>
  <action>
    Create `com.wfm.service.DeskAssignmentTemplateService` (Spring `@Service`) with `byte[] generateTemplate()`. Mirror `DeskAgentExportService`'s try-with-resources `XSSFWorkbook` + `createHeaderStyle` idiom. For each desk of the tenant, `workbook.createSheet(desk.getName())`; write the header row from `EnrichedColumnLayout.identityHeaders()` followed by a header per `DAY_ORDER` via `dayHeader(d)`, followed by two blank specialty columns "Specialty 1"/"Specialty 2" (seed two per D-14's "leave specialties blank"). For each current roster agent on that desk (via `DeskAgentService.listDeskAgentResponses(deskId, null, null, Integer.MAX_VALUE)`), write the identity columns FILLED (BambooHR ID, First Name, Last Name, Job Title, Email, Department, Active) mirroring the null-guarded `setCellValue` style, and leave the 7 day cells + specialty cells BLANK. Add a private `sanitize(String)` helper mirroring the frontend guard: if a value starts with `=`, `+`, `-`, or `@`, prefix it with a single quote before writing into a cell (formula-injection mitigation); apply it to every operator/BambooHR-supplied string written into a cell. Do NOT disable POI ZipSecureFile defaults.
  </action>
  <acceptance_criteria>
    - Source assertions: class contains `createSheet`, uses `EnrichedColumnLayout.identityHeaders`/`dayHeader`, and contains a `sanitize` helper guarding leading `=`/`+`/`-`/`@`.
    - No header string literals for identity/day columns are hardcoded in this class (all via EnrichedColumnLayout).
    - `./gradlew compileJava` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "createSheet" src/main/java/com/wfm/service/DeskAssignmentTemplateService.java && grep -q "EnrichedColumnLayout" src/main/java/com/wfm/service/DeskAssignmentTemplateService.java && grep -q "sanitize" src/main/java/com/wfm/service/DeskAssignmentTemplateService.java</automated>
  </verify>
  <done>Template service generates one pre-seeded sheet per desk (identity filled, schedule blank) using EnrichedColumnLayout, with cell sanitization.</done>
</task>

<task type="auto">
  <name>Task 2: Template download endpoint + export column alignment</name>
  <files>src/main/java/com/wfm/controller/ClientManagementController.java, src/main/java/com/wfm/service/DeskAgentExportService.java</files>
  <read_first>
    - src/main/java/com/wfm/controller/ClientManagementController.java lines 78-92 (exportEmployees byte[] attachment idiom to mirror)
    - src/main/java/com/wfm/service/DeskAgentExportService.java (columns array + per-row setCellValue block to align with EnrichedColumnLayout identity headers)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "ClientManagementController.java (add template-download endpoint)" (lines 427-455) and § "DeskAgentExportService.java (modify)"
    - src/main/java/com/wfm/util/EnrichedColumnLayout.java
  </read_first>
  <action>
    (a) In `ClientManagementController` add `@GetMapping("/desk-assignments/template") ResponseEntity<byte[]> downloadDeskAssignmentTemplate()` that calls `deskAssignmentTemplateService.generateTemplate()` and returns it as an attachment named `desk-assignment-template.xlsx` with the spreadsheetml content-type — mirror `exportEmployees` exactly. Inject `DeskAssignmentTemplateService` via the constructor following the existing dependency-injection style. No `@RequestParam` (template covers all tenant desks). Leave the existing `uploadDeskAssignments` endpoint signature unchanged.
    (b) In `DeskAgentExportService`, for the columns that overlap the enriched upload/template shape (BambooHR ID, First/Last Name, Email, Department, Job Title, Active), pull the header text from `EnrichedColumnLayout` constants/`identityHeaders()` instead of hardcoding, for round-trip symmetry (D-13). Keep export-only metadata columns (internal ID, Desk ID, Last Refreshed At, contracted hours) hardcoded — only the shared identity columns move to the shared source.
  </action>
  <acceptance_criteria>
    - Source assertions: controller contains `desk-assignments/template` and returns `desk-assignment-template.xlsx`; export service references `EnrichedColumnLayout` for the shared identity headers.
    - Behavior: `./gradlew compileJava` succeeds; existing DeskAgentExportService tests (if any) remain green under `./gradlew test`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "desk-assignments/template" src/main/java/com/wfm/controller/ClientManagementController.java && grep -q "EnrichedColumnLayout" src/main/java/com/wfm/service/DeskAgentExportService.java</automated>
  </verify>
  <done>A template-download endpoint exists and the export shares EnrichedColumnLayout identity headers with the template/parser.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DeskAssignmentTemplateServiceTest</name>
  <files>src/test/java/com/wfm/service/DeskAssignmentTemplateServiceTest.java</files>
  <read_first>
    - src/main/java/com/wfm/service/DeskAssignmentTemplateService.java (from Task 1)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "NEW test files" (mock scaffold) and § "DeskAssignmentTemplateService" analog
    - src/main/java/com/wfm/util/EnrichedColumnLayout.java (for expected header text)
  </read_first>
  <behavior>
    - generateTemplate() produces one sheet per configured desk, named after the desk.
    - Each sheet's header row equals EnrichedColumnLayout.identityHeaders() + Monday..Sunday + Specialty 1/Specialty 2.
    - A seeded roster agent's identity cells are FILLED; its 7 day cells and specialty cells are BLANK.
    - A cell value beginning with `=` is written sanitized (prefixed with a single quote).
  </behavior>
  <action>
    Create `DeskAssignmentTemplateServiceTest` (mock-based, mock desk + roster queries). Read the generated bytes back with `XSSFWorkbook(new ByteArrayInputStream(...))` and assert: sheet-per-desk with correct names; header row matches the EnrichedColumnLayout-derived headers in order; a seeded agent's identity cells are populated while day/specialty cells are empty; a formula-like value (e.g. "=SUM(A1)") is stored with a leading quote. Follow the JUnit 5 + AssertJ scaffold from the existing upload tests.
  </action>
  <acceptance_criteria>
    - Automated command: `./gradlew test --tests "com.wfm.service.DeskAssignmentTemplateServiceTest"` passes.
    - Asserts identity-filled/schedule-blank pre-seeding and header alignment with EnrichedColumnLayout (UPL-09), plus sanitization of a formula-like value.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.DeskAssignmentTemplateServiceTest"</automated>
  </verify>
  <done>Template service is tested for pre-seeded per-desk output, shared-header alignment, and sanitization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB roster string → generated .xlsx | Operator/BambooHR-sourced identity strings are written into a downloadable spreadsheet |
| browser → template download API | Tenant-scoped GET; must only expose the caller's tenant desks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | CSV/formula injection via identity strings written into the template `.xlsx` | mitigate | Server-side `sanitize()` mirroring the frontend guard — prefix leading `=`/`+`/`-`/`@` with a single quote (Task 1); test asserts it (Task 3) |
| T-10-09 | Information Disclosure | Template download leaking another tenant's roster | mitigate | Template generation uses tenant-scoped desk/roster queries (`TenantContext`); no cross-tenant read |
| T-10-10 | Denial of Service | Large workbook generation for many desks | accept | Bounded by tenant desk/roster size; export precedent already returns byte[] synchronously without issue |
</threat_model>

<verification>
- `./gradlew test --tests "com.wfm.service.DeskAssignmentTemplateServiceTest"` green.
- `./gradlew compileJava` green; template endpoint returns an attachment; export shares EnrichedColumnLayout identity headers.
</verification>

<success_criteria>
- Operator can download a pre-seeded per-desk template (identity filled, schedule blank) that round-trips through the same EnrichedColumnLayout the parser reads, with operator strings sanitized against formula injection.
</success_criteria>

<output>
Create `.planning/phases/10-enriched-upload-parsing/10-04-SUMMARY.md` when done.
</output>
