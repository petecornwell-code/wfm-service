---
phase: 10-enriched-upload-parsing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/wfm/util/EnrichedColumnLayout.java
  - src/test/java/com/wfm/util/EnrichedColumnLayoutTest.java
autonomous: true
requirements: [UPL-02, UPL-09]
must_haves:
  truths:
    - "Parser, template generator, and export all resolve header text/order from one shared definition (they cannot drift)"
    - "An unbounded number of `Specialty N` headers are detected case-insensitively and whitespace-tolerantly"
  artifacts:
    - path: "src/main/java/com/wfm/util/EnrichedColumnLayout.java"
      provides: "single source of header order/names; specialtyIndex(); dayHeader(); normalize()"
      contains: "specialtyIndex"
    - path: "src/test/java/com/wfm/util/EnrichedColumnLayoutTest.java"
      provides: "unit coverage for specialtyIndex/dayHeader/normalize/identityHeaders"
      contains: "EnrichedColumnLayout"
  key_links:
    - from: "src/main/java/com/wfm/util/EnrichedColumnLayout.java"
      to: "consumers (parser, template, export)"
      via: "static identityHeaders()/dayHeader()/specialtyIndex()/normalize()"
      pattern: "identityHeaders|dayHeader|specialtyIndex"
---

<objective>
Create `EnrichedColumnLayout` — the single source of truth for the enriched per-desk upload/template/export column shape (D-13). It is consumed by the parser (header→index), the template generator (index→header), and the export service (column order) so the three shapes can never drift.

Purpose: Resolve the design tension D-13 was written to close — three services must agree on the same spreadsheet shape. This is a Wave 1 foundation so plan 03 (parser) and plan 04 (template/export) can both consume it in parallel.
Output: The `EnrichedColumnLayout` utility class and its unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-enriched-upload-parsing/10-RESEARCH.md
@.planning/phases/10-enriched-upload-parsing/10-PATTERNS.md

<interfaces>
Concrete class to implement (verbatim proposal from RESEARCH.md § "Pattern 1" / PATTERNS.md § "EnrichedColumnLayout.java"):
- Identity header constants (header order): "BambooHR ID", "First Name", "Last Name", "Job Title", "Email", "Department", "Active".
- `DayOfWeek[] DAY_ORDER` = MONDAY..SUNDAY.
- `String dayHeader(DayOfWeek d)` → "Monday".."Sunday".
- `List<String> identityHeaders()` → the seven identity headers in order.
- `Pattern SPECIALTY_HEADER = "^specialty\\s*(\\d+)$"` CASE_INSENSITIVE; `Optional<Integer> specialtyIndex(String headerLowerTrimmed)`.
- `String normalize(String header)` → trim + lowercase (matching current parser convention, PATTERNS lines 110-118).
- private constructor (final class, no instances).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement EnrichedColumnLayout utility</name>
  <files>src/main/java/com/wfm/util/EnrichedColumnLayout.java</files>
  <read_first>
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "EnrichedColumnLayout.java (NEW)" (verbatim class proposal, lines ~198-240)
    - .planning/phases/10-enriched-upload-parsing/10-RESEARCH.md § "Architecture Patterns" Pattern 1
    - src/main/java/com/wfm/service/DeskAssignmentUploadService.java lines 110-118 (the lowercase+trim header convention `normalize()` must formalize)
    - src/main/java/com/wfm/util/AgentNameSplitter.java (sibling util package/style)
  </read_first>
  <action>
    Create `com.wfm.util.EnrichedColumnLayout` as a `final` class with a private constructor. Implement the seven identity header string constants (COL_BAMBOOHR_ID="BambooHR ID", COL_FIRST_NAME="First Name", COL_LAST_NAME="Last Name", COL_JOB_TITLE="Job Title", COL_EMAIL="Email", COL_DEPARTMENT="Department", COL_ACTIVE="Active"); the `DAY_ORDER` DayOfWeek[] (MONDAY..SUNDAY); `dayHeader(DayOfWeek)` returning title-case ("Monday"); `identityHeaders()` returning the ordered List; the compiled `SPECIALTY_HEADER` regex `^specialty\s*(\d+)$` (CASE_INSENSITIVE) with `specialtyIndex(String headerLowerTrimmed)` returning `Optional<Integer>`; and `normalize(String)` returning trim+lowercase (null → ""). No header string may be hardcoded anywhere else in the codebase after this class exists.
  </action>
  <acceptance_criteria>
    - Source assertions: class contains `specialtyIndex`, `dayHeader`, `identityHeaders`, `normalize`, and the `^specialty\s*(\d+)$` pattern; constructor is private.
    - `./gradlew compileJava` succeeds (verified in Task 2 run).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "specialtyIndex" src/main/java/com/wfm/util/EnrichedColumnLayout.java && grep -q "identityHeaders" src/main/java/com/wfm/util/EnrichedColumnLayout.java</automated>
  </verify>
  <done>EnrichedColumnLayout exists with identityHeaders/dayHeader/specialtyIndex/normalize and a private constructor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EnrichedColumnLayout unit test</name>
  <files>src/test/java/com/wfm/util/EnrichedColumnLayoutTest.java</files>
  <read_first>
    - src/main/java/com/wfm/util/EnrichedColumnLayout.java (the class under test, from Task 1)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § test scaffolding conventions (JUnit 5 + AssertJ)
  </read_first>
  <behavior>
    - specialtyIndex("specialty 1") → 1; specialtyIndex("specialty  2") (extra space) → 2; specialtyIndex("SPECIALTY 3") → 3; specialtyIndex("specialties") → empty; specialtyIndex("email") → empty.
    - dayHeader(MONDAY) → "Monday"; dayHeader(SUNDAY) → "Sunday".
    - identityHeaders() has 7 entries starting with "BambooHR ID" and containing "Active".
    - normalize("  Monday ") → "monday"; normalize(null) → "".
  </behavior>
  <action>
    Create `EnrichedColumnLayoutTest` (JUnit 5 + AssertJ) covering the behaviors above: specialty index parsing across case/whitespace and non-matches, day header title-casing for the boundary days, identity header count/order/content, and normalize's trim+lowercase + null handling. Pure unit test, no Spring context.
  </action>
  <acceptance_criteria>
    - Automated command: `./gradlew test --tests "com.wfm.util.EnrichedColumnLayoutTest"` passes.
    - Covers UPL-02's unbounded `Specialty N` detection at the layout level (specialtyIndex) and UPL-09's shared-header contract (identityHeaders/dayHeader).
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.wfm.util.EnrichedColumnLayoutTest"</automated>
  </verify>
  <done>EnrichedColumnLayoutTest passes and pins specialtyIndex, dayHeader, identityHeaders, and normalize behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure in-process utility; no untrusted input crosses a boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | Header string drift across parser/template/export | mitigate | Single-source-of-truth class; downstream plans forbidden from hardcoding header literals (enforced in plans 03/04 acceptance criteria) |
</threat_model>

<verification>
- `./gradlew test --tests "com.wfm.util.EnrichedColumnLayoutTest"` green.
- No header string literal duplicated elsewhere (enforced by consuming plans 03/04).
</verification>

<success_criteria>
- EnrichedColumnLayout is the single definition of enriched column order/names and specialty-header detection, unit-tested.
</success_criteria>

<output>
Create `.planning/phases/10-enriched-upload-parsing/10-02-SUMMARY.md` when done.
</output>
