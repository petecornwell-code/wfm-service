---
phase: 10-enriched-upload-parsing
plan: 05
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - src/test/java/com/wfm/service/DeskAssignmentUploadMultiSheetTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadSpecialtyTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadDayCellTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadValidationTest.java
  - src/test/java/com/wfm/service/DeskAssignmentUploadNonSchedulableRejectTest.java
autonomous: true
requirements: [UPL-01, UPL-02, UPL-03, UPL-04, UPL-05, UPL-06, UPL-07]
must_haves:
  truths:
    - "Multi-sheet parse resolves sheet name to desk and skips unmatched sheets with a notice; other sheets still import (UPL-01/D-02)"
    - "Unbounded Specialty N columns are detected; first non-blank is primary (UPL-02)"
    - "Day cell number/MANDATORY/PTO writes agent_day_hours with correct hours + day_off_type; 7.5 stores 7.50 not 7 (UPL-03/04/05)"
    - "Blank/invalid/negative day cell skips the row with a specific reason; >24 clamps to 24 with a surfaced warning; per-sheet rollup counts are correct (UPL-06)"
    - "Unmatched BambooHR ID is rejected with 'BambooHR ID not found', no agent created (UPL-07)"
    - "Two sheets normalizing to the same desk process last-wins; upload/clearDesk never deletes AgentDayOff field-4517 MANDATORY blocks (D-17/D-16)"
  artifacts:
    - path: "src/test/java/com/wfm/service/DeskAssignmentUploadDayCellTest.java"
      provides: "day-cell parse + fractional-hours regression + PTO/MANDATORY label coverage"
      contains: "7.5"
    - path: "src/test/java/com/wfm/service/DeskAssignmentUploadMultiSheetTest.java"
      provides: "multi-sheet + unmatched-sheet-skip coverage"
      contains: "getSheetName"
  key_links:
    - from: "test suite"
      to: "src/main/java/com/wfm/service/DeskAssignmentUploadService.java"
      via: "service.uploadDeskAssignments(MockMultipartFile)"
      pattern: "uploadDeskAssignments"
---

<objective>
Provide the behavioral test suite for the rewritten parser (plan 03), covering every parser requirement with automated unit tests: multi-sheet iteration + unmatched-sheet skip (UPL-01), unbounded Specialty N detection (UPL-02), day-cell parsing to `agent_day_hours` including the fractional-hours regression and the MANDATORY/PTO `day_off_type` label (UPL-03/04/05), validation skip reasons + clamp warning + per-sheet rollup (UPL-06), and BambooHR-ID-only rejection (UPL-07 — extend the existing NonSchedulableReject test).

Purpose: These are the Wave 0 test files named in 10-VALIDATION.md; they turn each parser requirement into a fast automated verify. Runs in parallel with the frontend plan (test files only, no shared source).
Output: Four new parser test files + an extension to the existing NonSchedulableReject test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-enriched-upload-parsing/10-VALIDATION.md
@.planning/phases/10-enriched-upload-parsing/10-PATTERNS.md

<interfaces>
Service under test (plan 03): `DeskAssignmentUploadService.uploadDeskAssignments(MultipartFile)` → nested `DeskAssignmentUploadResult(assignedCount, skippedCount, assignedDetails, skippedDetails, sheetSummaries, warnings, skippedSheets)`; nested `SheetSummary(deskName, importedCount, skippedCount)`; `com.wfm.dto.SkippedSheet(sheetName, reason)`; `com.wfm.dto.SkippedRow(rowNumber, bamboohrId, name, reason)`.

Constructor (8 mocks): (AgentRepository, DeskRepository, ClientManagementService, AgentPreferenceRepository, AgentExceptionRepository, AgentDayHoursRepository, SpecializationRepository, AgentEligibilityService).

Test scaffold + workbook-builder helper: PATTERNS.md § "NEW test files" (lines 590-660), copied verbatim from DeskAssignmentUploadEnrichedShapeTest. Add a `buildMultiSheetWorkbook(Map<sheetName,String[][]>)` variant.

New-shape header row: BambooHR ID, First Name, Last Name, Job Title, Email, Department, Active, Monday..Sunday, Specialty 1..N (NO Desk column). Sheet name = desk.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MultiSheet + NonSchedulable/ID-reject tests (UPL-01, UPL-07)</name>
  <files>src/test/java/com/wfm/service/DeskAssignmentUploadMultiSheetTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadNonSchedulableRejectTest.java</files>
  <read_first>
    - src/test/java/com/wfm/service/DeskAssignmentUploadEnrichedShapeTest.java (scaffold + buildWorkbook helper to copy)
    - src/test/java/com/wfm/service/DeskAssignmentUploadNonSchedulableRejectTest.java (existing — extend for the ID-not-found rejection)
    - src/main/java/com/wfm/service/DeskAssignmentUploadService.java (post plan-03 behavior)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "Multi-sheet" and § "Shared Patterns"
    - .planning/phases/10-enriched-upload-parsing/10-VALIDATION.md (UPL-01, UPL-07 rows)
  </read_first>
  <behavior>
    - A workbook with two desk-named sheets imports agents from both; a third sheet named for no configured desk yields a SkippedSheet("...","no matching desk...") and does not abort the other sheets (D-02).
    - Sheet-name matching is case/whitespace-insensitive ("Billing " matches desk "Billing").
    - A row whose BambooHR ID is not in the cache → SkippedRow reason "BambooHR ID not found", assignedCount unaffected, no agentRepository.save for that row.
    - Two sheets whose names normalize to the SAME desk ("Billing" and "billing ") both resolve to that desk; processed in sheet order the second re-clears and re-populates, so the final imported roster for that desk reflects the LAST sheet (D-17 last-sheet-wins; RESEARCH Open Question 3).
    - Structural D-16 guard: `DeskAssignmentUploadService` declares NO `AgentDayOffRepository` dependency, so `clearDesk` / `uploadDeskAssignments` cannot delete BambooHR field-4517 MANDATORY `AgentDayOff` blocks (the D-16 union survives upload).
  </behavior>
  <action>
    Create `DeskAssignmentUploadMultiSheetTest` with a `buildMultiSheetWorkbook` helper; assert multi-sheet import, unmatched-sheet skip notice in `skippedSheets`, and case/whitespace-tolerant sheet→desk matching. Extend `DeskAssignmentUploadNonSchedulableRejectTest` (or add a sibling case) asserting an unmatched BambooHR ID is rejected with reason "BambooHR ID not found" and no agent is created (verify `agentRepository`, never save for that row via Mockito, or assert skippedDetails). Mock `clientManagementService.findCachedEmployee` to return null for the unmatched ID and a cached employee for matched IDs. Add a colliding-sheet-name case: a workbook with two sheets ("Billing" and "billing ") that both normalize to desk "Billing" — assert last-wins (the final desk state reflects the second sheet's rows; `clearDesk` ran between the two). Add a light structural D-16 guard test asserting via reflection that `DeskAssignmentUploadService.class` declares no field of type `AgentDayOffRepository` — proving upload/`clearDesk` cannot delete field-4517 MANDATORY blocks (a Mockito `verify(...never())` is impossible because the service has no such collaborator, so mirror plan 10-01 Task 3's structural-absence approach).
  </action>
  <acceptance_criteria>
    - Automated command: `./gradlew test --tests "com.wfm.service.DeskAssignmentUploadMultiSheetTest" --tests "com.wfm.service.DeskAssignmentUploadNonSchedulableRejectTest"` passes.
    - Covers UPL-01 (multi-sheet + D-02 skip) and UPL-07 (ID-not-found rejection, no agent created).
    - Covers colliding-sheet last-wins (RESEARCH Open Question 3) and the structural D-16 guard (DeskAssignmentUploadService has no AgentDayOffRepository dependency).
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.DeskAssignmentUploadMultiSheetTest" --tests "com.wfm.service.DeskAssignmentUploadNonSchedulableRejectTest"</automated>
  </verify>
  <done>Multi-sheet iteration, unmatched-sheet skip, and ID-not-found rejection are covered by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DayCell + Specialty tests (UPL-02, UPL-03, UPL-04, UPL-05)</name>
  <files>src/test/java/com/wfm/service/DeskAssignmentUploadDayCellTest.java, src/test/java/com/wfm/service/DeskAssignmentUploadSpecialtyTest.java</files>
  <read_first>
    - src/test/java/com/wfm/service/DeskAssignmentUploadEnrichedShapeTest.java (scaffold + result-assertion idiom)
    - src/main/java/com/wfm/model/AgentDayHours.java (hours + dayOffType to assert on captured rows)
    - .planning/phases/10-enriched-upload-parsing/10-RESEARCH.md § "Pitfall 1" (fractional-hours regression) and § "Recurring PTO Storage Decision" (the cell→row table)
    - .planning/phases/10-enriched-upload-parsing/10-PATTERNS.md § "Specialty N assignment" (lines 115-127) and § "Fractional-hours regression test"
    - .planning/phases/10-enriched-upload-parsing/10-VALIDATION.md (UPL-02, UPL-03/04/05 rows)
  </read_first>
  <behavior>
    - Numeric day cell 7.5 → captured agent_day_hours row hours = 7.50 (regression: NOT 7), day_off_type = null.
    - Day cell 0 → hours 0.00, day_off_type null; "MANDATORY" → 0.00 + MANDATORY; "PTO" → 0.00 + PTO (case-insensitive).
    - A row with Specialty 1, Specialty 2, Specialty 3 populated → first non-blank set as primary, remaining as secondary (UPL-02); a sheet with only Specialty 1 still works.
  </behavior>
  <action>
    Create `DeskAssignmentUploadDayCellTest`: capture the `AgentDayHours` rows written (ArgumentCaptor on the AgentDayHoursRepository save, or assert on the agent's day-hours collection depending on plan-03's write path) and assert hours + dayOffType for numeric (incl. 7.5→7.50), 0, MANDATORY, and PTO cells. Create `DeskAssignmentUploadSpecialtyTest`: a sheet with three Specialty columns asserts primary = first non-blank and two secondaries; a sheet with one Specialty column asserts primary only. Use the copied scaffold and a new-shape workbook builder.
  </action>
  <acceptance_criteria>
    - Automated command: `./gradlew test --tests "com.wfm.service.DeskAssignmentUploadDayCellTest" --tests "com.wfm.service.DeskAssignmentUploadSpecialtyTest"` passes.
    - DayCellTest explicitly asserts 7.5 stores 7.50 (fractional-hours regression) and MANDATORY/PTO store 0.00 + label.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.DeskAssignmentUploadDayCellTest" --tests "com.wfm.service.DeskAssignmentUploadSpecialtyTest"</automated>
  </verify>
  <done>Day-cell parsing (incl. fractional + MANDATORY/PTO label) and unbounded Specialty N detection are covered by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validation test — skip reasons, clamp warning, per-sheet rollup (UPL-06)</name>
  <files>src/test/java/com/wfm/service/DeskAssignmentUploadValidationTest.java</files>
  <read_first>
    - src/test/java/com/wfm/service/DeskAssignmentUploadEnrichedShapeTest.java (scaffold + SkippedRow assertion idiom lines 654-660)
    - src/main/java/com/wfm/service/DeskAssignmentUploadService.java (skip reasons, clamp warning, SheetSummary assembly from plan 03)
    - .planning/phases/10-enriched-upload-parsing/10-RESEARCH.md § "Pitfall 3" (clamp surfaced non-silently)
    - .planning/phases/10-enriched-upload-parsing/10-CONTEXT.md D-04, D-10, D-11
    - .planning/phases/10-enriched-upload-parsing/10-VALIDATION.md (UPL-06 row)
  </read_first>
  <behavior>
    - A blank day cell → row skipped with a specific reason; a negative number → skipped; an unrecognized word → skipped; a valid row in the same sheet still imports.
    - A day cell of 32 → agent imported with that day clamped to 24.00 AND a clamp warning present in result.warnings() (non-silent).
    - result.sheetSummaries() reports correct importedCount/skippedCount per desk sheet.
  </behavior>
  <action>
    Create `DeskAssignmentUploadValidationTest`: build a sheet mixing a valid row, a blank-day-cell row, a negative-hours row, an unrecognized-word row, and a >24-hours row. Assert each invalid row appears in `skippedDetails` with a specific reason; assert the >24 row imports with a warning in `warnings()`; assert `sheetSummaries()` counts. Follow the copied scaffold.
  </action>
  <acceptance_criteria>
    - Automated command: `./gradlew test --tests "com.wfm.service.DeskAssignmentUploadValidationTest"` passes.
    - Asserts blank/negative/unknown skip reasons, a surfaced clamp warning, and per-sheet rollup counts (UPL-06).
    - Full quick suite green: `./gradlew test --tests "com.wfm.service.DeskAssignmentUpload*"`.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.DeskAssignmentUpload*"</automated>
  </verify>
  <done>Validation skip reasons, non-silent clamp warning, and per-sheet rollup are covered; the full DeskAssignmentUpload* suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Test-only plan; validates the parser's own input-validation boundary rather than introducing one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering (regression) | Parser validation allowlist / clamp / ID-reject could silently regress | mitigate | This suite is the regression guard — allowlist skip reasons, clamp surfacing, and ID-only rejection are pinned by automated tests (UPL-06/07) |
</threat_model>

<verification>
- `./gradlew test --tests "com.wfm.service.DeskAssignmentUpload*"` green (all parser requirements covered).
</verification>

<success_criteria>
- Every parser requirement (UPL-01/02/03/04/05/06/07) has a passing automated unit test, including the fractional-hours regression and the MANDATORY/PTO label storage.
</success_criteria>

<output>
Create `.planning/phases/10-enriched-upload-parsing/10-05-SUMMARY.md` when done.
</output>
