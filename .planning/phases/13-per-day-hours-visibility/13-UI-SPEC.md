---
phase: 13
slug: per-day-hours-visibility
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-21
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Primary surface:** `frontend/src/pages/DeskAgents.tsx` (roster table + its two existing modals).
**Nature of this phase:** a read-path integration fix plus one new edit affordance layered onto an
existing, dense operator table — not a new page or a new visual language.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — hand-rolled, inline `style={{...}}` objects (React 19, hooks-only) |
| Icon library | none — unicode glyphs only (matches existing `↑`/`↓` sort indicators, `«`/`‹`/`›`/`»` pagination) |
| Font | `system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif` (`frontend/src/index.css:2`) |

**No design system was initialized for this phase — this is a deliberate call, not a skipped step.**
`components.json` is absent and no CSS framework (`tailwind.config.*`, `postcss.config.*`) exists
anywhere in `frontend/`. All 12 existing pages (`DeskAgents.tsx`, `ClientManagement.tsx`,
`DeskManagement.tsx`, etc.) use 100% inline `style` objects against a small shared token set in
`frontend/src/index.css`. `13-RESEARCH.md`'s Standard Stack table verifies this directly
(`frontend/package.json:8` — React 19 with no component library) and states explicitly: *"new UI
must match this convention (inline `style={{...}}` objects, no CSS framework)."* Introducing
shadcn now would mean the phase's ~7 new elements look and behave differently from the 13-column
table they sit inside, on a phase whose own research explicitly scoped itself to avoid new tooling
(see the identical reasoning RESEARCH.md applied to the frontend-test-framework decision, Wave 0
Gaps: *"keeps this phase's blast radius to the stated scope"*). **Every rule below is therefore
written as an extension of the existing hand-rolled token set, not a new one.**

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Badge/chip internal padding (`MAND`/`PTO` labels), icon-to-text gaps |
| sm | 8px | Compact element spacing — gap between the 7 weekday mini-columns |
| — | 12px (0.75rem) | **Existing house exception** — already the dominant "compact chrome" unit in this file's modals/toolbars (`padding: '0.5rem'`/`'0.75rem'` gaps appear 10+ times in `DeskAgents.tsx`). Use for the expanded row's outer padding and the "Set all days" bulk-action row, rather than inventing a new value. |
| md | 16px | Default spacing between the expanded weekday grid and the bulk-action row below it |
| lg | 24px | Not newly used this phase |
| xl | 32px | Not newly used this phase |
| 2xl | 48px | Not newly used this phase |
| 3xl | 64px | Not newly used this phase |

Exceptions: **12px (0.75rem)** is carried forward from the existing codebase convention (see
table). No touch-target minimum applies — this is a desktop-only operator tool with no mobile/touch
requirement identified anywhere in the codebase.

---

## Typography

New text introduced by this phase only (existing page `h1`, table `th`/`td` defaults are unchanged
and out of scope):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body (day-cell values, combobox input text, bulk-action input) | 14px (0.85rem) | 400 | 1.5 |
| Label (weekday headers `Mon`/`Tue`/…, `MAND`/`PTO` badges) | 13px (0.8rem) | 600 | 1.2 |
| Heading | not introduced this phase — no new `h1`/`h2`/`h3` appears; the expanded row is a peer of the existing table body, not a titled section | — | — |
| Display | not used this phase | — | — |

Weights used: **400 (regular)** for values, **600 (semibold)** for labels/badges — matches the
existing `th { font-weight: 600 }` rule in `index.css:90` exactly, so no third weight is
introduced. (Buttons continue to inherit the global `button { font-weight: 500 }` rule
unchanged — that is pre-existing chrome, not new typography this contract governs.)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#f8f9fa` | Page background (unchanged, `index.css:6`) |
| Secondary (30%) | `#fff` / `#f3f4f6` | Table surface / header row (unchanged); expanded-row background uses `#fff` to read as part of the same table card, not a new surface |
| Accent (10%) | `#3b82f6` | Reserved for: primary buttons (`button.primary`, unchanged), the expand/collapse chevron's hover color, and the combobox `:focus` outline (inherits the existing global `input:focus { outline: 2px solid #3b82f6 }` rule, `index.css:136` — no new focus treatment introduced) |
| Destructive | `#ef4444` | Destructive actions only — **not used by any new element in this phase.** Clearing a cell to "Not set" is a neutral action (deletes a row, restores the default), not a destructive one, so it must not be styled red (this preserves D-04's point that "not set" is a normal state, not a warning state) |

**New semantic roles required by this phase** (extends the template's 4-row baseline because the
per-day model has states the existing palette has no token for yet):

| Role | Value | Usage |
|------|-------|-------|
| Warning (amber) | bg `#fffbeb`, border `#fde68a`, text `#92400e` | The "set all days" overwrite-warning notice — reuses the exact palette already used for Upload Results warnings (`ClientManagement.tsx:590-601`) and the roster's own pending-PTO chip, verbatim, per 13-RESEARCH.md Pattern 4 |
| PTO badge | bg `#fef9c3`, text `#92400e` | The `PTO` label inside an expanded weekday cell — reuses the existing pending-PTO chip palette (`DeskAgents.tsx:374-375`) exactly, since it is the same underlying concept |
| MANDATORY badge | bg `#e5e7eb`, text `#374151` | The `MAND` label inside an expanded weekday cell — a distinct neutral gray (not amber) so operators never confuse a contractual mandatory day off with PTO at a glance; both colors already exist elsewhere in the file (`#e5e7eb` border color, `#374151` label text color, `index.css:146`) |
| Muted / not-set | `#9ca3af` | The resolved-default value shown in a cell with no `AgentDayHours` row — same muted gray already used for placeholder-style text (`DeskAgents.tsx:420,430`) |
| Explicit-zero | `#6b7280` | A cell with a real `hours=0.00, dayOffType=null` row (Pitfall 1/2 in 13-RESEARCH.md) — distinguishes "explicitly not worked" from a normal worked value without implying it's an error |

Accent reserved for: primary CTA buttons, the expand/collapse chevron hover state, and focus rings
only — never for badges, warnings, or the muted/explicit-zero states above.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | **"Set all days to…"** — button inside the expanded row that reveals the bulk-edit input (the relabeled, warning-guarded successor to today's `saveHours` fan-out, per D-07) |
| Empty state heading | not applicable — no list can be literally empty (weekdays are a fixed set of 7); see body copy below for the "nothing uploaded yet" case |
| Empty state body | When an agent has **zero** `AgentDayHours` rows at all: a single-line note above the 7-day grid — **"No per-day hours uploaded — every day shows the schedule default ({X}h)."** (`{X}` = the resolved schedule default per D-06) |
| Error state | Server-rejected save (toast, reusing existing `showToast('error', getErrorMessage(err))`): **"Couldn't save {Weekday} — {reason}."** Client-side validation (before submit): **"Enter a number from 0 to 24, or choose PTO / MANDATORY / Not set (default)."** |
| Destructive confirmation | "Set all days" bulk action, only when the agent's existing rows include a `MANDATORY`/`PTO` label: native `confirm()` dialog (matches the file's one existing destructive-confirmation convention, `DeskAgents.tsx:117`) — **"This will overwrite {N} day(s) currently marked MANDATORY or PTO with a single value. Continue?"** No dialog when no existing labels would be overwritten (per Pitfall 4 in 13-RESEARCH.md — checking "rows exist" instead of "rows carry a label" was flagged as the wrong trigger). |

Supporting copy (not template rows, but load-bearing — the planner/executor need these verbatim):

| Control | Copy |
|---------|------|
| Weekday header abbreviations | `Mon` `Tue` `Wed` `Thu` `Fri` `Sat` `Sun` |
| Combobox picklist entries (per D-03's mockup, verbatim) | `(type a number)` *(placeholder, not a selectable literal)*, `PTO`, `MANDATORY`, `Not set (default)` |
| MANDATORY badge text | `MAND` (4-letter abbreviation — matches the accepted mockup's `MAND` literally, keeps the mini-column narrow) |
| PTO badge text | `PTO` |
| Expand/collapse toggle | icon-only chevron (`▸` collapsed / `▾` expanded), `aria-label="Expand hours detail for {agentName}"` / `"Collapse hours detail"` — no visible text label, matching the file's existing icon-glyph convention (`↑`/`↓` sort arrows) |

---

## Component Specifications

*(Extends the template's baseline sections with implementation-level detail the planner needs to
turn D-01–D-08 into a single, unambiguous build — every choice below traces to a CONTEXT.md
decision or a concrete gap CONTEXT.md left to "Claude's discretion.")*

### 1. Collapsed "Hours/Day" cell — summary algorithm (resolves CONTEXT.md's open discretion item)

1. Resolve each of the 7 weekdays' **effective hours** server-side (per D-06: schedule default,
   never the desk default) — `MANDATORY`/`PTO` days resolve to `0.00` per the stored model
   (13-RESEARCH.md Pitfall 2), so they naturally pull into the low end of the range.
2. If `min === max` across all 7 effective values (a uniform week, no labels) → show the single
   value, e.g. `8`.
3. If `min !== max` → show `"{min}-{max}"`, e.g. `4-8` (hyphen, matching the CONTEXT.md mockup's
   literal `6-8` — not an en dash).
4. The collapsed cell **never** shows `MAND`/`PTO` markers — those appear only after expanding,
   keeping the collapsed column scannable (matches the accepted mockup exactly, which shows only a
   number/range in the collapsed state).
5. Clicking anywhere on the collapsed cell (not just a caret) toggles expansion — reuses the file's
   existing click-affordance visual language (`cursor: 'pointer', textDecoration: 'underline
   dotted'`, already used twice in this file for `startEditSpec`/`startEditHours`).

### 2. Expanded row — layout

- Renders as a full-width sub-row directly beneath the agent's row (a second `<tr>` with a single
  `<td colSpan={13}>`, or equivalent) — **not** a new column, honoring D-01's explicit rejection of
  widening the table.
- Contents, top to bottom: (a) the empty-state note from the Copywriting Contract, shown only when
  the agent has zero `AgentDayHours` rows; (b) a 7-column mini-grid, `Mon` through `Sun`, each cell
  per the rendering rules below; (c) the "Set all days to…" bulk action (Section 4).
- Background `#fff` (same as the table surface — reads as part of the same card, not a modal or a
  new visual layer, unlike the file's two existing `position: fixed` overlays which are correctly
  *not* the pattern to copy here — 13-RESEARCH.md's explicit correction to CONTEXT.md).

**Visual focal point** (closes the checker's Dimension 2 recommendation): the collapsed `Hours/Day`
value is the primary scan target on the roster — an operator sweeping the table is checking that
number against what they uploaded, and it is the one cell this phase changes the meaning of. Once a
row is expanded, the `MAND` / `PTO` badges become the secondary visual signal; the numeric day values
sit below both in priority. Nothing in this phase should out-weigh the collapsed `Hours/Day` cell at
the table level — in particular the chevron is deliberately low-contrast chrome, not an attractor.

### 3. Per-day cell — display and edit modes

**Display mode** (not currently being edited), branching in this exact order — this order is
load-bearing per 13-RESEARCH.md Pitfalls 1 and 2:

1. `dayOffType === 'MANDATORY'` → gray badge, text `MAND` (Color: MANDATORY badge role).
2. `dayOffType === 'PTO'` → amber badge, text `PTO` (Color: PTO badge role).
3. Row exists, `dayOffType === null`, `hours === 0.00` → plain text `0`, colored with the
   Explicit-zero role (`#6b7280`) — visually distinct from a normal worked value, per Pitfall 1.
4. Row exists, `dayOffType === null`, `hours > 0` → plain text, the numeric value (e.g. `7.5`),
   default body color, weight 400.
5. No row at all → the resolved default value, styled with the Muted/not-set role (`#9ca3af`,
   italic), with a `title="Not set — using schedule default"` tooltip (matches the file's existing
   tooltip convention at `DeskAgents.tsx:371`, `title={da.pendingPtoDates.join(', ')}`).

Clicking a display-mode cell swaps it into **edit mode** for that single day (mirrors the existing
`editHoursAgentId`/`editSpecAgentId` per-row edit-state pattern — recommend a per-cell state shaped
as `{ agentId, day } | null` rather than a new per-cell state variable per cell).

**Edit mode — the D-03 "type-or-pick combo," concrete control (zero new dependencies):**

A native `<input type="text" list="day-hours-options">` bound to a `<datalist>` containing the
picklist entries `PTO`, `MANDATORY`, `Not set (default)` (`(type a number)` is the input's
`placeholder`, not a selectable option — it cannot be a literal `<option>` since typing anything
resolves it away). This is HTML5's built-in "type or pick" affordance and requires no new
component library, satisfying both D-03's literal requirement and the project's zero-dependency
convention. Resolution on blur/Enter:

| Typed/picked value | Resolves to |
|---|---|
| A number, 0–24, quarter-hour step | `hours = value`, `dayOffType = null` |
| `PTO` (case-insensitive) | `hours = 0.00`, `dayOffType = PTO` |
| `MANDATORY` (case-insensitive) | `hours = 0.00`, `dayOffType = MANDATORY` |
| `Not set (default)`, or the field cleared to empty | Deletes the row entirely (D-04: "selecting a value must be what creates a row" — clearing is the one path that does the opposite, by design) |
| Anything else | Reject client-side; revert the field to the last saved value; show the client-validation copy from the Copywriting Contract inline below the cell |

Saving calls the new per-cell endpoint (D-05) and touches exactly one row. While the request is
in flight, disable the input (no distinct spinner — matches the existing `saveHours`/`saveSpec`
synchronous await-then-rerender convention, which has no loading affordance today). On failure,
revert to the pre-edit display value and show the server error via the existing toast pattern.

### 4. "Set all days to…" bulk action (D-07) — where it lives

The **existing** top-level `Hours/Day` column click-to-edit numeric input (`editHoursAgentId` /
`editHours` / `saveHours`, `DeskAgents.tsx:148-163,352-365`) is **relocated**, not duplicated: it
no longer lives in the collapsed cell (which now only expands/collapses, per Section 1) — it
becomes the explicit, labelled "Set all days to…" control inside the expanded row, positioned
below the 7-day grid exactly as shown in the CONTEXT.md mockup. This resolves the otherwise-open
question of "does clicking the collapsed cell expand the row, or start the old single-value edit?"
in favor of D-01's explicit spec (click expands) without losing the existing fan-out capability
(D-07 requires it survive as an explicit action, not the default one).

- Button reveals: numeric `<input type="number" step="0.25" min="0" max="24">` + an **Apply**
  button + a **Cancel** link, matching the existing `input` + primary-button + secondary-button
  micro-pattern already used for the spec editor (`Save`/`Cancel`, `DeskAgents.tsx:342-343`) —
  standardize on `Apply`/`Cancel` here (not `OK`/`X`) since this is now a distinct, explicitly
  labelled action rather than an inline field toggle.
- Before submitting, check whether **any** of the agent's 7 existing rows carry a non-null
  `dayOffType`. If so, show the destructive-confirmation `confirm()` dialog from the Copywriting
  Contract before calling the endpoint. If none carry a label, submit immediately — no dialog
  (Pitfall 4: checking "rows exist" instead of "rows carry a label" is the wrong trigger and must
  not be used).
- The bulk endpoint's rewrite of all 7 rows must remain a single backend transaction, so a mid-way
  failure never leaves a partial (e.g. 3-of-7) write visible to the operator — call this out to the
  planner as a requirement on the bulk endpoint, not an assumption about existing code.

### 5. Excel export columns (D-02) — visual/format contract for the file itself

Not a browser UI surface, but part of this phase's visual contract since operators open it in
Excel: 7 new Mon–Sun columns, headers sourced from `EnrichedColumnLayout.dayHeader(day)` (already
exists — do not hardcode `"Monday"`..`"Sunday"` strings locally, this is exactly the drift class
I-4 already flagged for specialty headers). Cell values mirror the same branching as Section 3's
display-mode rules 1–5 (a `MANDATORY`/`PTO` cell should render its keyword, not `0`, so the
export/re-upload round-trip in D-02 stays lossless). Column order matches the upload template's
existing day-cell order exactly (Mon→Sun, left to right, immediately following the existing
"Effective Contracted Hours Per Day" column).

### Minimum data shape the frontend needs per weekday

The exact backend DTO field names are open (Phase 9 D-12 deliberately deferred this; 13-RESEARCH.md
Assumption A3 flags it as the planner's call) — but whatever shape is chosen, it must let the
frontend distinguish all 5 states above without a second request per cell:

```
per weekday: {
  hasRow: boolean          // false = "not set", never inferred from hours/type being null
  hours: number | null     // the raw stored value when hasRow is true; null when false
  dayOffType: 'MANDATORY' | 'PTO' | null
  effectiveHours: number   // always present — the resolved display value (D-06), used by
                            // both the collapsed-cell range algorithm and the "not set" tooltip
}
```

---

## UI Considerations

> Populated by the ui-phase UI-consideration probe (Step 9.5) and lifted by plan-phase's
> `## UI Considerations` lift rule via the identical rule as SPEC `## Edge Coverage`. Shape-rooted UI *state*
> coverage (empty / loading / error / populated / partial / overflow / zero-one-many / long-text).
> Empty-state and error-state COPY live in `## Copywriting Contract` above — this section covers
> state coverage and REFERENCES those rows rather than restating the copy (de-dup).

**Probe run:** 5 elements, 38 applicable considerations.
**Resolved: 38 / 38** — 26 `verification: explicit`, 4 `verification: backstop`, 8 dismissed with reason, 0 unresolved.

Element kinds were confirmed against the heuristic classifier rather than accepted from it: E3 and E4
were re-run with `static-content` added to their detected kinds, which raised `long-text` on E3 and
`overflow` on E4 — two axes the cue-match alone would have silently dropped. E4 legitimately carries no
`populated` / `zero-one-many` row: those belong to the list-collection axis and E4 is a single cell.

**Backstop rationale (load-bearing):** this repo has **no frontend test framework at all** (zero
vitest/jest/testing-library — verified in 13-RESEARCH.md), so the four visual claims below cannot be
proven by assertion. They are marked `verification: backstop` deliberately. At verify time each routes
to `human_needed` (reason `insufficient_spec`) unless visual evidence is wired — the intended
honest-verifier behavior, not over-flagging.

### E1 — Collapsed `Hours/Day` summary cell *(list-collection, static-content)*

| Category | Status | Verification | Resolution / Reason |
|----------|--------|--------------|---------------------|
| empty | ✅ resolved | explicit | Zero `AgentDayHours` rows → the cell shows the single resolved schedule default styled Muted/not-set `#9ca3af`; never blank |
| loading | ✅ resolved | explicit | Value arrives in the existing roster GET (13-RESEARCH.md Pattern 1); no skeleton or spinner is added |
| error | ✅ resolved | explicit | Roster GET failure is unchanged existing behavior — `loadAgents()` catch → `showToast('error')`; no per-cell error state |
| populated | ✅ resolved | explicit | Single value when all 7 effective hours are equal; `min-max` range (e.g. `12-24`) when they differ; never renders `MAND`/`PTO` (Section 1) |
| partial | ✅ resolved | explicit | Unset days resolve to the schedule default *before* min/max is computed, so the summary always spans all 7 values |
| overflow | ✅ resolved | **backstop** | Range output caps at ~7 chars (`12-24` worst case) inside the existing dense 13-column row — provable only by viewing the rendered table |
| zero-one-many | ⊘ dismissed | — | Weekdays are a fixed set of 7; the zero/one/many axis cannot vary and no copy pluralizes |
| long-text | ✅ resolved | explicit | Numeric closed vocabulary ≤5 chars; truncation impossible |

### E2 — Expand/collapse chevron toggle *(list-collection, media, interactive-control, static-content)*

| Category | Status | Verification | Resolution / Reason |
|----------|--------|--------------|---------------------|
| empty | ✅ resolved | explicit | Chevron always renders, even when the agent has zero `AgentDayHours` rows (expanding reveals the "No per-day hours uploaded…" note). Never hidden |
| loading | ✅ resolved | explicit | No secondary fetch on expand — data is already in the roster response; no spinner on the toggle |
| populated | ✅ resolved | explicit | `▸` collapsed / `▾` expanded, with `aria-label="Expand hours detail for {agentName}"` / `"Collapse hours detail"` (Copywriting Contract) |
| error | ⊘ dismissed | — | Pure client-side state toggle — it cannot fail, so there is no error affordance to specify |
| partial | ⊘ dismissed | — | Not data-bound; its appearance does not vary with data completeness |
| overflow | ⊘ dismissed | — | Fixed-width unicode glyph inside pre-existing row chrome |
| zero-one-many | ⊘ dismissed | — | One toggle per roster row; row count is governed by existing pagination, unchanged by this phase |
| long-text | ⊘ dismissed | — | Icon-only; the `aria-label` is never visually rendered |

### E3 — Expanded weekday detail row *(list-collection, static-content — `static-content` added at kind confirmation)*

| Category | Status | Verification | Resolution / Reason |
|----------|--------|--------------|---------------------|
| empty | ✅ resolved | explicit | Zero rows → single-line note "No per-day hours uploaded — every day shows the schedule default ({X}h)." above the 7-day grid; all 7 cells render the default Muted |
| loading | ✅ resolved | explicit | Region renders synchronously from already-loaded data; no spinner |
| error | ✅ resolved | explicit | No independent error state; failures surface at the cell (E4) or bulk (E5) level as toasts |
| partial | ✅ resolved | explicit | The *primary* expected case after most uploads. The 5-state priority order MANDATORY → PTO → explicit-zero → worked hours → not-set must not be collapsed into 3 states (13-RESEARCH.md Pitfalls 1 and 2) |
| long-text | ✅ resolved | explicit | Badge text is fixed (`MAND`, `PTO`); the only variable-length string is the empty-state note, which wraps at full row width rather than truncating |
| populated | ✅ resolved | **backstop** | "Matches the CONTEXT.md accepted mockup — mixed numeric/`MAND`/`PTO` in one row" is a visual claim; no source assertion can prove the render matches |
| overflow | ✅ resolved | **backstop** | Adds no new columns (D-01) and inherits the existing 13-column table's horizontal-scroll behavior unmodified — verifiable only by viewing it |
| zero-one-many | ⊘ dismissed | — | Fixed set of exactly 7 weekdays; never zero/one/variable-many |

### E4 — Per-day five-state editable cell *(form, interactive-control, static-content — `static-content` added at kind confirmation)*

| Category | Status | Verification | Resolution / Reason |
|----------|--------|--------------|---------------------|
| empty | ✅ resolved | explicit | The "empty" value is the explicit `Not set (default)` datalist entry (D-04), never a blank or unstyled input |
| loading | ✅ resolved | explicit | Input disables during the PUT and re-enables on response — matches the existing `saveHours`/`saveSpec` convention; no distinct spinner |
| error | ✅ resolved | explicit | Client-side range check first ("Enter a number from 0 to 24, or choose PTO / MANDATORY / Not set (default)."); server rejection → `showToast('error', getErrorMessage(err))` as "Couldn't save {Weekday} — {reason}." |
| partial | ✅ resolved | explicit | A row with `hours=0.00, dayOffType=null` renders as Explicit-zero `#6b7280`, visually distinct from Muted/not-set `#9ca3af` (13-RESEARCH.md Pitfalls 1 and 2) |
| long-text | ✅ resolved | explicit | Free entry is numeric 0–24 and validated before submit; no operator-authored free text can reach the cell |
| overflow | ✅ resolved | **backstop** | `Not set (default)` is wider than the weekday mini-column but renders in the browser's native `<datalist>` dropdown, which the cell width does not constrain — browser-dependent and visual |

### E5 — Bulk "Set all days to…" action *(form, list-collection, interactive-control, static-content)*

| Category | Status | Verification | Resolution / Reason |
|----------|--------|--------------|---------------------|
| empty | ✅ resolved | explicit | Opens blank with placeholder `(type a number)`; Apply is inert until a value or picklist entry is chosen |
| loading | ✅ resolved | explicit | Apply disables during the transactional PUT, mirroring the existing `assigning`/`exporting`/`uploading` boolean-flag pattern |
| error | ✅ resolved | explicit | The bulk endpoint must be transactional so a failure never leaves a partial 3-of-7 write; failure → error toast with zero rows changed (Section 4) |
| populated | ✅ resolved | explicit | The amber warning notice (bg `#fffbeb`, border `#fde68a`, text `#92400e`) is always visible above the input |
| partial | ✅ resolved | explicit | Native `confirm()` fires **only** when the agent's existing rows carry a `MANDATORY`/`PTO` label — the trigger is "rows carry a label", NOT "rows exist" (13-RESEARCH.md Pitfall 4) |
| overflow | ✅ resolved | explicit | Single-line action row at the 12px house spacing below the weekday grid; no overflow path exists |
| zero-one-many | ✅ resolved | explicit | Always applies to exactly 7 days; the confirmation copy pluralizes via `{N} day(s)` |
| long-text | ⊘ dismissed | — | Same closed vocabulary as E4 — `PTO` / `MANDATORY` / `Not set (default)` / a number; no free text is possible |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no design system tool in use (see Design System) |
| third-party | none | not applicable |

No shadcn registries were consulted or vetted for this phase — `Tool: none` means the registry
safety gate does not apply.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS *(FLAG at review — no focal point declared; closed by the "Visual focal point" note in Component Specifications §2)*
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-08-21 (gsd-ui-checker — APPROVED, 5 PASS + 1 non-blocking FLAG, since closed)
