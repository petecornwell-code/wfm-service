---
context: phase
phase: 13-per-day-hours-visibility
task: null
total_tasks: 9
status: gaps_found
last_updated: 2026-08-22T01:52:02.416Z
---

# Phase 13 — Per-Day Hours Visibility

All 4 plans executed and committed. Verification returned **`gaps_found` (50/58 must-haves)**, so the
phase is **not** marked complete. Two one-line frontend fixes stand between here and a clean pass.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Plan `<action>` contradicting its own `must_haves` | `13-04-PLAN.md`'s `<action>` prose says `seedValueForEntry` returns "the empty string when `hasRow` is false", while the same plan's `must_haves.truths` (and `13-UI-SPEC.md` E4) require the literal `Not set (default)`. The executor followed the action text and shipped the wrong behaviour — this is gap 2. | advisory | When closing gap 2, fix the PLAN text as well as the code. A replan that re-reads the unedited `<action>` reintroduces the same defect. |
| Verifying UI truths by build success | `npm run build` (`tsc -b && vite build`), the code review, and all four SUMMARYs passed while two explicit UI-SPEC colour/seed-value truths were unmet. A green build proves compilation, not visual contract conformance. | advisory | UI-SPEC `## UI Considerations` rows need either a rendered check or an explicit human-verify item — never "build passed" as their evidence. |

_No `blocking`-severity constraints. Resuming agents do not need an understanding check._

<current_state>
Branch `claude/create-system-specification-451ge`, HEAD `80ec7f1`. Working tree clean apart from
pre-existing untracked test artifacts. All 18 phase commits are in.

Backend: clean-rebuild `./gradlew test --rerun-tasks` → 309 tests, 0 failures, 0 errors, 0 skipped.
Frontend: `npm --prefix frontend run build` clean.

The phase's central claim holds under direct code inspection: roster and Excel export both resolve
effective hours from `agent_day_hours`; the retired `Agent.contractedHoursPerDay` scalar is genuinely
excluded from the read path; the read path bulk-fetches rather than N+1ing; the new PUT endpoint is
tenant+desk scoped before any per-day write.
</current_state>

<completed_work>

- 13-01 (2/2 tasks, `ad3fb5b`) — Tracer. `DeskAgentService` roster read path resolves from
  `agent_day_hours` across all 4 `toResponse` call sites; `DeskAgentResponse.dayHours` always-7-key
  map; expandable Mon–Sun detail row with 5 display states. Closes I-1/F-1.
- 13-02 (3/3 tasks, `4b75f27`) — `PUT /api/v1/desks/{deskId}/agents/{agentId}/day-hours/{day}`
  single-row upsert. Closes I-3 structurally: a single-weekday write provably leaves the other six
  rows byte-identical.
- 13-03 (2/2 tasks, `6870f44`) — Export gains 7 Mon–Sun columns; specialty headers single-sourced
  from `EnrichedColumnLayout`. Closes I-4. First/Last Name shifted 13/14 → 20/21, pinned by a full
  22-cell header assertion.
- 13-04 (2/2 tasks, `84d9591`) — Per-cell type-or-pick combo + warning-guarded "Set all days to…"
  bulk action.
- Code review (`bf58a5d`) — 0 Critical / 3 Warning / 2 Info.
- Verification (`80ec7f1`) — `gaps_found`, 50/58.
</completed_work>

<remaining_work>

**Blocking the pass (both in `frontend/src/pages/DeskAgents.tsx`):**

1. **Line 506** — the collapsed Hours/Day cell renders `formatHoursSummary(da)` with no conditional
   styling; only the chevron glyph is muted. Wrap the value in `color: '#9ca3af'` when
   `DAY_ORDER.every(d => !da.dayHours[d].hasRow)`, matching the E3 treatment already used in the
   expanded cells. (UI-SPEC E1 "empty".)
2. **Lines 25–30** — `seedValueForEntry()` returns `''` for a not-set weekday, so the editor opens
   blank. Return the literal `'Not set (default)'`. `saveDayHours` already treats that literal as
   `clearRow`, so no downstream change is needed. (UI-SPEC E4 "empty".) **Also fix the contradicting
   `<action>` prose in `13-04-PLAN.md`.**

**Advisory, from `13-REVIEW.md`, independently re-confirmed by the verifier — worth folding in:**

3. **WR-01** — the surviving bulk `setContractedHours` has only a negative check, no `≤ 24` upper
   bound. `999.99` overflows `NUMERIC(5,2)` into an opaque 500, and it also breaks the E1 "at most
   5 characters" truth via undeclared precondition. The new bulk UI makes it directly reachable
   (its `min`/`max` HTML attributes are unenforced — no wrapping `<form>`, no JS range check).
4. **WR-02** — a malformed `{day}` path segment returns 500, not the 400 `13-02-PLAN.md` claimed,
   because `GlobalExceptionHandler` has a catch-all `Exception.class` handler and no
   `MethodArgumentTypeMismatchException` handler.

**Then:**

5. `/gsd-secure-phase 13` — security capability active at `verify:post`, no `13-SECURITY.md` yet.
6. `/gsd-verify-work 13` — 5 `unrun-verify` items (WINDOWS.md 1–6) + 2 `behavior_unverified` truths.

**Carried over from the (now Complete) Phase 11 handoff, still open and not phase-11-scoped:**

7. Fix the `honourPreferredStartTime` `isBefore` asymmetry — Stage 0 of the four-stage cross-day
   consistency plan. It only penalises starting *earlier*, so it is a floor, not an anchor.
8. `breakClustering` is registered as a constraint but is still a no-op stub.
</remaining_work>

<decisions_made>

- **Sequential execution, not parallel worktrees.** `worktree.base-check` returned
  `shouldDegrade=true` (#683) — HEAD had diverged from `origin/HEAD`, so worktrees would have forked
  from a stale base and tripped the executor's exit-42 branch check.
- **Manual post-merge gate.** `post-merge-gate.md`'s sniffer has no Gradle branch and would have
  emitted "No build command detected" and skipped the gate on this project.
- **Whole-repo test run as the regression gate.** There is one test command; no prior-phase subset
  exists to run separately.
- **No AI-SPEC.** The `ai-integration` plan:pre detector matched "agent" only via the domain term
  `agent_day_hours` (call-centre agents), not an AI system.
- **Descriptor-less prohibitions.** This phase had no plain `-SPEC.md`, so both `## Edge Coverage`
  and `## Prohibitions` came from the spec-less fallback. All 5 edge-probe items returned
  `unresolved` and were authored as flagged assumptions; 5 prohibitions dispose
  `{status: unverified, flagged: true}`. That is the designed outcome, not a defect — do not
  auto-dismiss them.
</decisions_made>

<blockers>
- **Live environment**: 5 human-check walkthroughs and 2 transactional-rollback truths need a real
  desk with an enriched Mon–Sun upload. No executor session had one. → `/gsd-verify-work 13`.
- **Worktree isolation**: degraded for the whole run. `gsd-tools worktree set-baseref` restores it.
  Note the isolation sentinel is re-persisted to `harness-worktree` by *any* plain
  `dispatch-isolation` read, so `--force-isolation none` must be the last call before each dispatch.
</blockers>

## Required Reading (in order)

1. `.planning/phases/13-per-day-hours-visibility/13-VERIFICATION.md` — structured `gaps:` frontmatter
   with an exact `missing:` remediation for each of the 2 failures; also
   `behavior_unverified_items` and `coincidental_reliance_items`.
2. `.planning/phases/13-per-day-hours-visibility/13-REVIEW.md` — WR-01/WR-02 detail.
3. `.planning/phases/13-per-day-hours-visibility/13-UI-SPEC.md` — §E1 and §E4 are the two contracts
   that failed; §5 pins the export column order.
4. `.planning/phases/13-per-day-hours-visibility/13-CONTEXT.md` — locked decisions D-01…D-08.
   **D-05 in particular: `Agent.contractedHoursPerDay` stays a live field. Do not drop the column
   or add a migration removing it. Five writers still touch it — do not assume it is null.**
5. `.planning/WINDOWS.md` — 6 open items from this phase (5 unrun-verify, 1 deviation).

## Infrastructure State

- Branch `claude/create-system-specification-451ge` @ `80ec7f1`; nothing pushed this session.
- Untracked test artifacts left in place: `StubHub-EN-combined-CSR-test.xlsx`,
  `stubhub_demand_ES.xlsx`, `.gsd/`, `.planning/research/.cache/`, `.planning/milestone.lock`.
- No background processes, no async external jobs.
- Codebase drift: 14 structural elements since last mapping (advisory) —
  `/gsd-map-codebase --paths .dockerignore,.gitignore,Dockerfile,README.md,SCHEDULE_BUILDER_GUIDE.md,TASKS.md,TODO.md,deployment_plan.md,gradlew,gradlew.bat,progress.md,settings.gradle,spec.md,user_manual.md`

<context>
This phase was a *reporting* fix, not a scheduling-correctness one — the solver was already resolving
hours correctly through `SolverService.resolveEffectiveHours`; only the read paths lagged. That framing
matters when closing the gaps: nothing here should touch solver behaviour, and any new resolution logic
should reuse `resolveEffectiveHours` rather than adding a third implementation.

Both remaining gaps are cosmetic-looking but genuinely user-facing: without them an operator cannot
distinguish a resolved default from a real uploaded value, which is precisely the confusion the phase
existed to eliminate.
</context>

<next_action>
Start with: `/gsd-plan-phase 13 --gaps` — it reads `13-VERIFICATION.md`'s structured `gaps:`
frontmatter and emits `gap_closure: true` plans. Consider folding WR-01 and WR-02 into the same run.
Then `/gsd-execute-phase 13 --gaps-only`.
</next_action>
