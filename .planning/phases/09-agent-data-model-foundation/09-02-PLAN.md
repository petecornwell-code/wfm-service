---
phase: 09-agent-data-model-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/wfm/model/AgentDayHours.java
  - src/main/java/com/wfm/repository/AgentDayHoursRepository.java
  - src/test/java/com/wfm/model/AgentDayHoursPersistenceTest.java
autonomous: true
requirements: [MDL-02]

must_haves:
  truths:
    - "AgentDayHours stores (agent, dayOfWeek, hours) as a sibling child row, never navigated from Agent"
    - "day_of_week is persisted as a STRING enum (e.g. 'MONDAY'); hours is NUMERIC(5,2) NOT NULL"
    - "Every AgentDayHoursRepository read query is tenant-scoped; a (agent_id, day_of_week) pair is unique"
  artifacts:
    - path: "src/main/java/com/wfm/model/AgentDayHours.java"
      provides: "Per-weekday contracted-hours child entity mirroring AgentDayOff"
      contains: "class AgentDayHours"
    - path: "src/main/java/com/wfm/repository/AgentDayHoursRepository.java"
      provides: "Tenant-scoped bulk fetch + agent-scoped delete for fan-out/clear"
      exports: ["findByTenantIdAndAgent_Id", "findByTenantIdAndDeskId", "deleteByAgent_Id"]
    - path: "src/test/java/com/wfm/model/AgentDayHoursPersistenceTest.java"
      provides: "STRING-enum + precision/scale + tenant round-trip proof"
  key_links:
    - from: "src/main/java/com/wfm/model/AgentDayHours.java"
      to: "agent(id)"
      via: "@ManyToOne @JoinColumn(name = \"agent_id\")"
      pattern: "@JoinColumn\\(name = \"agent_id\""
---

<objective>
Establish the per-day-hours half of the foundation: the new `agent_day_hours` child entity and its tenant-scoped repository, mirroring the `AgentDayOff` / `AgentException` sibling convention exactly. This is the locked storage contract (D-09) that the solver resolution (09-03), the write-path fan-out/clear (09-04, 09-05), and the Flyway migration (09-06) all write into.

Purpose: MDL-02 requires contracted hours stored per day of week. The storage must represent "absent" (no row → schedule default) distinctly from "not worked" (a row with 0.00) per D-04/D-09.
Output: `AgentDayHours` entity, `AgentDayHoursRepository`, persistence test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-agent-data-model-foundation/09-CONTEXT.md
@.planning/phases/09-agent-data-model-foundation/09-PATTERNS.md
@.planning/phases/09-agent-data-model-foundation/09-VALIDATION.md

<interfaces>
<!-- Contract this plan produces (Plans 09-03/04/05 consume it). -->
AgentDayHours: getAgent()/setAgent(Agent), getDayOfWeek()/setDayOfWeek(DayOfWeek), getHours()/setHours(BigDecimal), getTenantId()/setTenantId(long)
AgentDayHoursRepository:
  List<AgentDayHours> findByTenantIdAndAgent_Id(long tenantId, UUID agentId)
  List<AgentDayHours> findByTenantIdAndDeskId(long tenantId, UUID deskId)   // @Query join via h.agent.deskId
  void deleteByAgent_Id(UUID agentId)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentDayHours child entity</name>
  <read_first>
    - src/main/java/com/wfm/model/AgentDayOff.java (full structural template: @Entity/@Table/@UniqueConstraint, tenant_id column, @ManyToOne LAZY agent, id UUID GeneratedValue, accessors)
    - src/main/java/com/wfm/model/AgentPreference.java lines 27-29 (@Enumerated(EnumType.STRING) DayOfWeek column convention)
    - src/main/java/com/wfm/model/AgentException.java line 31 (contractedHoursOverride nullable=false precision=5 scale=2 — mirror for hours)
    - .planning/phases/09-agent-data-model-foundation/09-PATTERNS.md §"AgentDayHours.java" (full target shape, verified)
    - src/test/java/com/wfm/model/AgentEmploymentTypePersistenceTest.java (@DataJpaTest template)
  </read_first>
  <files>src/main/java/com/wfm/model/AgentDayHours.java, src/test/java/com/wfm/model/AgentDayHoursPersistenceTest.java</files>
  <behavior>
    - An AgentDayHours(agent, MONDAY, 7.50) persists and reloads with dayOfWeek=MONDAY (as STRING in DB) and hours=7.50
    - hours preserves scale 2 (a value of 8 reloads compatible with 8.00 via compareTo)
    - tenantId round-trips; the @ManyToOne agent reference reloads to the same agent id
  </behavior>
  <action>
    Create `@Entity @Table(name = "agent_day_hours", uniqueConstraints = @UniqueConstraint(columnNames = {"agent_id","day_of_week"}))` class `AgentDayHours` in `com.wfm.model`, mirroring `AgentDayOff` field-for-field: `@Id @GeneratedValue(strategy = GenerationType.UUID) UUID id`; `@Column(name="tenant_id", nullable=false) long tenantId`; `@ManyToOne(fetch=FetchType.LAZY) @JoinColumn(name="agent_id", nullable=false) Agent agent`; `@Enumerated(EnumType.STRING) @Column(name="day_of_week", nullable=false, length=9) java.time.DayOfWeek dayOfWeek`; `@Column(nullable=false, precision=5, scale=2) BigDecimal hours`. Note hours is `nullable=false` (unlike the Agent scalar) — absence is represented by row absence per D-09, so every row carries a real value. Provide a no-arg constructor plus getters/setters mirroring `AgentDayOff` exactly. Write `AgentDayHoursPersistenceTest` as `@DataJpaTest @ActiveProfiles("test")`: `em.persist(agent)` for a minimal Agent, then `em.persistFlushFind` an AgentDayHours and assert dayOfWeek/hours/tenantId/agent-id round-trip.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.model.AgentDayHoursPersistenceTest"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `AgentDayHours.java` contains `@Column(name = "tenant_id", nullable = false)` (tenant scoping present — hard project convention)
    - Source assertion: `@Enumerated(EnumType.STRING)` on dayOfWeek and `precision = 5, scale = 2` with `nullable = false` on hours
    - Source assertion: `@UniqueConstraint(columnNames = {"agent_id", "day_of_week"})`
    - Test proves MONDAY reloads as MONDAY and 7.50 reloads at scale 2
  </acceptance_criteria>
  <done>The per-day-hours storage entity exists and round-trips via @DataJpaTest.</done>
</task>

<task type="auto">
  <name>Task 2: AgentDayHoursRepository (tenant-scoped)</name>
  <read_first>
    - src/main/java/com/wfm/repository/AgentExceptionRepository.java (full: tenant+agent+desk finders and bulk-delete naming)
    - src/main/java/com/wfm/repository/AgentDayOffRepository.java lines 44-48 (the @Query "h.agent.deskId" join-through-agent pattern; also has deleteByAgent_IdAndDateBetween precedent for agent-scoped delete)
    - .planning/phases/09-agent-data-model-foundation/09-PATTERNS.md §"AgentDayHoursRepository.java" (recommended shape)
  </read_first>
  <files>src/main/java/com/wfm/repository/AgentDayHoursRepository.java</files>
  <action>
    Create `@Repository public interface AgentDayHoursRepository extends JpaRepository<AgentDayHours, UUID>` with: `List<AgentDayHours> findByTenantIdAndAgent_Id(long tenantId, UUID agentId)`; a bulk-fetch `@Query("SELECT h FROM AgentDayHours h WHERE h.tenantId = :tenantId AND h.agent.deskId = :deskId") List<AgentDayHours> findByTenantIdAndDeskId(long tenantId, UUID deskId)` (mirrors AgentDayOffRepository's join-through-agent style; used by SolverService in 09-03); and `void deleteByAgent_Id(UUID agentId)` (used by DeskAgentService fan-out replace in 09-05 and DeskAssignmentUploadService clear in 09-04 — mirrors the existing `deleteByAgent_IdAndDateBetween` agent-scoped-delete precedent). Every read query MUST include `tenantId` (V4 tenant isolation, no exceptions).
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: both read methods (`findByTenantIdAndAgent_Id`, `findByTenantIdAndDeskId`) include `tenantId` in name/query
    - Source assertion: `findByTenantIdAndDeskId` uses `h.agent.deskId` in its JPQL (no desk_id column on AgentDayHours itself)
    - `deleteByAgent_Id(UUID)` is declared
    - `./gradlew compileJava` succeeds
  </acceptance_criteria>
  <done>Tenant-scoped repository compiles and exposes the fetch + delete contract downstream plans consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository query → tenant data | Any read must not cross tenants; per-day hours are tenant-owned data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | AgentDayHoursRepository reads | mitigate | Every finder is tenant-scoped (`findByTenantId...`) — copies the verified no-exceptions convention of AgentDayOff/AgentException/AgentPreference |
| T-09-04 | Tampering | agent_day_hours.hours value integrity | mitigate | Entity column `nullable=false, precision=5, scale=2`; DB-level NUMERIC(5,2) NOT NULL backstop added in 09-06 migration; writers normalize via BigDecimals.normalize (09-05) |
| T-09-05 | Information Disclosure | deleteByAgent_Id (agent-id only) | accept | Mirrors existing AgentDayOffRepository.deleteByAgent_IdAndDateBetween precedent; callers resolve the agent within tenant scope (findByIdAndTenantIdAndDeskId) before deleting; agent id is a non-guessable UUID |
| T-09-SC | Tampering | dependency installs | accept | No new packages this phase |
</threat_model>

<verification>
- `./gradlew test --tests "com.wfm.model.AgentDayHoursPersistenceTest"` green
- `grep -c "tenantId" src/main/java/com/wfm/repository/AgentDayHoursRepository.java` ≥ 2 (both read finders tenant-scoped)
</verification>

<success_criteria>
- `agent_day_hours` storage exists with STRING-enum weekday, NUMERIC(5,2) NOT NULL hours, and a unique (agent, weekday) constraint
- Repository exposes tenant-scoped bulk fetch and agent-scoped delete for downstream fan-out/clear
- Tenant isolation preserved on the new entity and every read query
</success_criteria>

<output>
Create `.planning/phases/09-agent-data-model-foundation/09-02-SUMMARY.md` when done.
</output>
