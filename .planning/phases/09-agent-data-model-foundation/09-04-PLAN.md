---
phase: 09-agent-data-model-foundation
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/main/java/com/wfm/integration/BambooRefreshService.java
  - src/main/java/com/wfm/service/DeskAssignmentUploadService.java
autonomous: true
requirements: [MDL-01, MDL-02]

must_haves:
  truths:
    - "BambooHR refresh populates firstName/lastName by splitting displayName via AgentNameSplitter, while still setting name from displayName unchanged (D-07)"
    - "Agents created/updated through the upload path have firstName/lastName populated at both setName sites via the same splitter (D-11)"
    - "When the upload flow clears an agent's hours (desk clear/re-import), that agent's agent_day_hours rows are deleted so re-assignment inherits no stale per-day hours (D-10 clear side)"
  artifacts:
    - path: "src/main/java/com/wfm/integration/BambooRefreshService.java"
      provides: "displayName split into first/last on refresh"
      contains: "AgentNameSplitter.split"
    - path: "src/main/java/com/wfm/service/DeskAssignmentUploadService.java"
      provides: "name split at both setName sites + per-day-hours delete on clear"
      contains: "agentDayHoursRepository.deleteByAgent_Id"
  key_links:
    - from: "src/main/java/com/wfm/integration/BambooRefreshService.java"
      to: "AgentNameSplitter"
      via: "static call splitting emp.displayName()"
      pattern: "AgentNameSplitter\\.split"
    - from: "src/main/java/com/wfm/service/DeskAssignmentUploadService.java"
      to: "AgentDayHoursRepository"
      via: "deleteByAgent_Id in clearDesk"
      pattern: "agentDayHoursRepository\\.deleteByAgent_Id"
---

<objective>
Keep the two live agent write-paths consistent with the new model so the "no solve-behaviour regression" property is durable, not just true at the migration instant. BambooHR refresh and the desk-upload flow must populate `firstName`/`lastName` via the shared `AgentNameSplitter` (D-07, D-11), and the upload flow's hours-clear must delete the agent's per-day rows so a re-assigned agent inherits no stale hours (D-10 clear side).

Purpose: MDL-01 (name fields populated by real write-paths, not just the one-time migration) and MDL-02 (per-day storage stays coherent across re-imports). Without this, upload-created agents have empty first/last name until a later refresh, and cleared agents keep orphan per-day rows.
Output: split calls in `BambooRefreshService` + `DeskAssignmentUploadService`; per-day delete on clear.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-agent-data-model-foundation/09-CONTEXT.md
@.planning/phases/09-agent-data-model-foundation/09-PATTERNS.md

<interfaces>
<!-- Consumed from 09-01 and 09-02 -->
AgentNameSplitter.split(String) -> Split(firstName, lastName)
Agent.setFirstName(String) / Agent.setLastName(String)
AgentDayHoursRepository.deleteByAgent_Id(UUID)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split displayName into first/last on BambooHR refresh (D-07)</name>
  <read_first>
    - src/main/java/com/wfm/integration/BambooRefreshService.java lines 190-230 (the existing-agent refresh loop; `agent.setName(emp.displayName())` at ~line 211) and the import block ~lines 3-22 (wildcard com.wfm.* imports)
    - src/main/java/com/wfm/integration/BambooEmployee.java (confirms displayName is the only name field)
    - .planning/phases/09-agent-data-model-foundation/09-PATTERNS.md §"BambooRefreshService.java (MODIFY)"
  </read_first>
  <files>src/main/java/com/wfm/integration/BambooRefreshService.java</files>
  <action>
    At the `agent.setName(emp.displayName())` site (~line 211), keep the existing `setName(emp.displayName())` call unchanged (Pitfall 3: `name` is a real JPQL-searched column), and additionally compute `AgentNameSplitter.Split split = AgentNameSplitter.split(emp.displayName())` then `agent.setFirstName(split.firstName())` and `agent.setLastName(split.lastName())`. Add the explicit import `com.wfm.util.AgentNameSplitter`. Apply the split at every place in this service that sets an agent's name from a display string (check whether the new-agent creation branch also sets name; if so, split there too, same three lines).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.integration.*BambooRefresh*"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `BambooRefreshService.java` contains `AgentNameSplitter.split(` and calls `setFirstName`/`setLastName`
    - Source assertion: `agent.setName(emp.displayName())` is still present (name column not derived from parts)
    - Existing BambooRefresh tests (if any) still pass; `./gradlew compileJava` succeeds
  </acceptance_criteria>
  <done>Refresh populates first/last name via the shared splitter while leaving the searchable name column intact.</done>
</task>

<task type="auto">
  <name>Task 2: Upload-path name split (D-11) + per-day-hours delete on clear (D-10 clear side)</name>
  <read_first>
    - src/main/java/com/wfm/service/DeskAssignmentUploadService.java lines 1-55 (explicit repository imports + constructor field-injection style), lines 255-370 (new-agent setName ~276, spreadsheet-override setName ~301, clearDesk method ~349-363 with setContractedHoursPerDay(null))
    - .planning/phases/09-agent-data-model-foundation/09-PATTERNS.md §"DeskAssignmentUploadService.java (MODIFY)" (all three sites, verified)
    - src/main/java/com/wfm/repository/AgentDayHoursRepository.java (deleteByAgent_Id contract from 09-02)
  </read_first>
  <files>src/main/java/com/wfm/service/DeskAssignmentUploadService.java</files>
  <action>
    Add explicit imports `com.wfm.util.AgentNameSplitter` and `com.wfm.repository.AgentDayHoursRepository`. Add an `AgentDayHoursRepository agentDayHoursRepository` final field and wire it through the constructor alongside the existing `agentExceptionRepository`/`agentPreferenceRepository` fields (match the explicit constructor-injection style at the top of the class). At BOTH setName sites (~276 new-agent-from-cache using `cached.displayName()`, and ~301 spreadsheet override using `name.trim()`): keep the existing `setName(...)` call, then split that same source string via `AgentNameSplitter.split(...)` and call `setFirstName`/`setLastName` (D-11). In `clearDesk` (~349-363), immediately after `agent.setContractedHoursPerDay(null)` inside the loop, add `agentDayHoursRepository.deleteByAgent_Id(agent.getId())` (D-10 clear side) — mirroring the existing `agentPreferenceRepository`/`agentExceptionRepository` delete pattern already used at the top of the method.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.*DeskAssignmentUpload*"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `AgentNameSplitter.split(` appears at both the cache-create and spreadsheet-override sites (grep count ≥ 2)
    - Source assertion: `agentDayHoursRepository.deleteByAgent_Id(agent.getId())` appears immediately after `setContractedHoursPerDay(null)` in clearDesk
    - Source assertion: `agentDayHoursRepository` is a constructor-injected final field
    - Both original `setName(...)` calls remain (name column still written directly)
    - `./gradlew compileJava` and any existing upload tests pass
  </acceptance_criteria>
  <done>Upload-created agents get first/last name, and cleared agents lose their per-day rows so re-assignment inherits none.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BambooHR/spreadsheet string → Agent fields | External display strings split into first/last; no query construction from them |
| Upload clear → per-day row deletion | Delete scoped to the agent already resolved within the tenant/desk being cleared |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering | stale agent_day_hours on re-assignment | mitigate | clearDesk deletes the agent's per-day rows when hours are cleared (D-10), so a re-assigned agent cannot inherit prior-desk hours the solver would silently use |
| T-09-09 | Information Disclosure | deleteByAgent_Id in clearDesk | accept | The loop iterates only `agentRepository.findByTenantIdAndDeskId(tenantId, deskId)` results — every agent is already tenant+desk scoped before delete |
| T-09-10 | Denial of Service | AgentNameSplitter on hostile display strings | mitigate | Splitter (09-01) null/blank-guards and never loops; inputs are bounded display names |
| T-09-SC | Tampering | dependency installs | accept | No new packages this phase |
</threat_model>

<verification>
- `./gradlew test` compiles and passes for touched services
- `grep -c "AgentNameSplitter.split" src/main/java/com/wfm/service/DeskAssignmentUploadService.java` ≥ 2
- `grep -n "deleteByAgent_Id" src/main/java/com/wfm/service/DeskAssignmentUploadService.java` shows the delete inside clearDesk
</verification>

<success_criteria>
- Both live write-paths populate firstName/lastName via the shared splitter (D-07/D-11) without altering the searchable name column
- Upload hours-clear removes the agent's per-day rows (D-10 clear side)
</success_criteria>

<output>
Create `.planning/phases/09-agent-data-model-foundation/09-04-SUMMARY.md` when done.
</output>
