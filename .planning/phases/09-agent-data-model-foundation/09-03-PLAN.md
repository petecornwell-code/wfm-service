---
phase: 09-agent-data-model-foundation
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - src/main/java/com/wfm/service/SolverService.java
  - src/test/java/com/wfm/service/SolverServiceEffectiveHoursResolutionTest.java
autonomous: true
requirements: [MDL-02, MDL-03]

must_haves:
  truths:
    - "Effective hours for an agent+date resolve as: AgentException override → per-day value for that weekday → schedule default (D-03)"
    - "A weekday with no per-day row falls back to the schedule default (D-04); a per-day row of 0.00 yields 0 → that weekday is not scheduled (D-04/D-05)"
    - "A uniform 7-row per-day map produces the exact same effectiveHours for every date as the pre-migration scalar-only logic (Success Criterion 4)"
    - "All THREE getEffectiveHours call sites (computeAgentDayConfigs + both runPreSolveValidation checks) use the per-day map, so validation cannot diverge from the actual solve"
  artifacts:
    - path: "src/main/java/com/wfm/service/SolverService.java"
      provides: "Package-private static resolveEffectiveHours + agentDayHoursMap threaded through 3 call sites"
      contains: "static BigDecimal resolveEffectiveHours"
    - path: "src/test/java/com/wfm/service/SolverServiceEffectiveHoursResolutionTest.java"
      provides: "Precedence, zero-day, absent-day, and behaviour-equivalence proofs"
  key_links:
    - from: "src/main/java/com/wfm/service/SolverService.java"
      to: "AgentDayHoursRepository.findByTenantIdAndDeskId"
      via: "bulk load then Map<UUID, Map<DayOfWeek, BigDecimal>> build"
      pattern: "agentDayHoursMap"
---

<objective>
Move the authority for effective contracted hours from the single `Agent.contractedHoursPerDay` scalar to the new per-day `agent_day_hours` rows, without changing solve behaviour for uniform-hours agents. Extract the resolution into a package-private static `resolveEffectiveHours` (mirroring the `buildAgentDaysOffMap` testability precedent) and thread a per-agent per-weekday map through ALL THREE `getEffectiveHours` call sites — not just `computeAgentDayConfigs`, but also the two inside `runPreSolveValidation` (RESEARCH Pitfall 1, the critical divergence risk).

Purpose: MDL-02 (resolution from per-day values) and MDL-03's behaviour-equivalence bar (Success Criterion 4). This is the highest-risk change in the milestone.
Output: refactored `SolverService` + a fast Spring-free resolver unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-agent-data-model-foundation/09-CONTEXT.md
@.planning/phases/09-agent-data-model-foundation/09-RESEARCH.md
@.planning/phases/09-agent-data-model-foundation/09-PATTERNS.md
@.planning/phases/09-agent-data-model-foundation/09-VALIDATION.md

<interfaces>
<!-- Consumed from 09-02 -->
AgentDayHoursRepository.findByTenantIdAndDeskId(long tenantId, UUID deskId) -> List<AgentDayHours>
AgentDayHours.getAgent().getId() : UUID ; getDayOfWeek() : DayOfWeek ; getHours() : BigDecimal
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract package-private static resolveEffectiveHours + resolver test</name>
  <read_first>
    - src/main/java/com/wfm/service/SolverService.java lines 801-810 (current private getEffectiveHours: exception → scalar → schedule default)
    - src/test/java/com/wfm/service/SolverServicePtoFilterTest.java lines 1-80 (the static-extraction testability precedent — buildAgentDaysOffMap called directly, no Spring)
    - .planning/phases/09-agent-data-model-foundation/09-PATTERNS.md §"SolverService.java (MODIFY)" Pattern B and the compareTo idiom note
    - .planning/phases/09-agent-data-model-foundation/09-CONTEXT.md §D-03/D-04
  </read_first>
  <files>src/main/java/com/wfm/service/SolverService.java, src/test/java/com/wfm/service/SolverServiceEffectiveHoursResolutionTest.java</files>
  <behavior>
    - exception present for the date → returns the exception override (highest precedence), ignoring the per-day map
    - no exception, per-day map has the date's weekday → returns that per-day value (including 0.00)
    - no exception, weekday absent from per-day map → returns the schedule default
    - precedence uses containsKey (never a null-check) — a present AgentException row is always a real value (nullable=false)
    - EQUIVALENCE (Success Criterion 4): a map with all 7 weekdays set to the same value V, and an empty exception map, returns exactly V for every DayOfWeek — identical to the old scalar-only result
  </behavior>
  <action>
    Add a package-private `static BigDecimal resolveEffectiveHours(Map<LocalDate,BigDecimal> exceptionMap, Map<DayOfWeek,BigDecimal> dayHoursMap, LocalDate date, BigDecimal scheduleDefaultHours)` implementing D-03/D-04 precedence: if `exceptionMap.containsKey(date)` return it; else if `dayHoursMap.containsKey(date.getDayOfWeek())` return it; else return `scheduleDefaultHours`. The agent scalar is NO LONGER consulted. Keep `containsKey`-based branches (Pitfall 6). Do NOT change the downstream "0 means not worked" gate — callers keep using `effectiveHours.compareTo(BigDecimal.ZERO) <= 0` (verified at lines 488/652/734), never `.equals()`. Write `SolverServiceEffectiveHoursResolutionTest` (same package `com.wfm.service`, no Spring context, mirroring `SolverServicePtoFilterTest`) calling `SolverService.resolveEffectiveHours(...)` directly and covering every behavior row, with the equivalence case iterating all 7 `DayOfWeek` values. Author the test first (RED) then implement.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.SolverServiceEffectiveHoursResolutionTest"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `SolverService.java` declares `static BigDecimal resolveEffectiveHours(` (package-private, testable without Spring)
    - Test proves exception > per-day > default ordering and that a 0.00 per-day value is returned (not the schedule default)
    - Equivalence test iterates all 7 DayOfWeek and asserts the uniform-map result equals V for each — pinning Success Criterion 4
    - No `.equals(` used for zero comparison anywhere in the changed code
  </acceptance_criteria>
  <done>Resolution logic is extracted, testable, and behaviour-equivalence is pinned by a passing unit test.</done>
</task>

<task type="auto">
  <name>Task 2: Thread agentDayHoursMap through all 3 getEffectiveHours call sites</name>
  <read_first>
    - src/main/java/com/wfm/service/SolverService.java lines 130-190 (where daysOff/exceptions are bulk-loaded ~line 142 and the main agentExceptionMap is built at 171-175), lines 469-490 (computeAgentDayConfigs signature + call at 487), lines 565-660 (runPreSolveValidation signature ~565-572, its own local map builds ~620-628, getEffectiveHours call at 651), lines 720-740 (second runPreSolveValidation call at 733)
    - .planning/phases/09-agent-data-model-foundation/09-PATTERNS.md §"SolverService.java (MODIFY)" Pattern A (map-build shape) and the "3 call sites, build twice" note
    - .planning/phases/09-agent-data-model-foundation/09-RESEARCH.md Pitfall 1 (why validation builds its own map)
  </read_first>
  <files>src/main/java/com/wfm/service/SolverService.java</files>
  <action>
    Inject `AgentDayHoursRepository` into `SolverService` (constructor field, matching existing repository-injection style). Where `daysOff`/`exceptions` are bulk-loaded for the desk (~line 142), also load `List<AgentDayHours> agentDayHours = agentDayHoursRepository.findByTenantIdAndDeskId(tenantId, deskId)`. Build `Map<UUID, Map<DayOfWeek, BigDecimal>> agentDayHoursMap` in the main solve flow (mirroring the exception map build at 171-175) and pass the per-agent inner map into `computeAgentDayConfigs`, replacing its `getEffectiveHours(...)` call (line 487) with `resolveEffectiveHours(exceptionMap, dayHoursMap, date, schedule.getDefaultContractedHoursPerDay())`. Thread the RAW `List<AgentDayHours>` into `runPreSolveValidation` as a new parameter alongside the existing raw `daysOff`/`exceptions`/`preferences`, build the same map inside it next to its local builds (~620-628), and update BOTH internal calls (lines 651 and 733) to use `resolveEffectiveHours` with the per-agent map. Delete or reduce the old private `getEffectiveHours` (lines 801-810) so the scalar is no longer consulted anywhere. Keep every downstream `compareTo(BigDecimal.ZERO) <= 0` skip check unchanged (D-04/D-05: a 0 per-day value means that weekday emits no AgentDayConfig, unioning with MANDATORY day-offs).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.SolverService*Test"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "resolveEffectiveHours" src/main/java/com/wfm/service/SolverService.java` ≥ 3 (all three former getEffectiveHours sites now delegate to the static resolver)
    - Source assertion: the old `agent.getContractedHoursPerDay()` is no longer referenced inside the resolution path (grep the resolution methods)
    - Source assertion: `agentDayHoursMap` is built inside `runPreSolveValidation` as well as the main flow (two build sites)
    - `AgentDayHoursRepository` is a constructor-injected field of SolverService
    - Full solver test group `com.wfm.service.SolverService*Test` passes
  </acceptance_criteria>
  <done>The solver resolves effective hours from per-day rows at all three sites; validation cannot diverge from the actual solve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB rows → solver problem facts | Per-day hours + exceptions loaded per tenant+desk, folded into maps before solve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Information Disclosure | agent_day_hours bulk load | mitigate | Load via `findByTenantIdAndDeskId` (tenant+desk scoped) — no cross-tenant rows reach the solver |
| T-09-07 | Tampering | validation/solve divergence (per-day map missing from one site) | mitigate | Map built at all 3 call sites (Pitfall 1); source assertion requires ≥3 resolveEffectiveHours usages; equivalence test pins Success Criterion 4 |
| T-09-SC | Tampering | dependency installs | accept | No new packages this phase |
</threat_model>

<verification>
- `./gradlew test --tests "com.wfm.service.SolverService*Test"` green (resolver test + existing solver tests)
- `grep -c "resolveEffectiveHours" src/main/java/com/wfm/service/SolverService.java` ≥ 3
- No `.equals(` used for BigDecimal zero comparison in the resolution path
</verification>

<success_criteria>
- Effective hours resolve from per-day rows with exception override on top and schedule-default fallback (D-03/D-04)
- Uniform-hours agents produce identical effectiveHours pre/post migration (Success Criterion 4), proven by unit test
- All three getEffectiveHours call sites use the per-day map — no validation/solve divergence
</success_criteria>

<output>
Create `.planning/phases/09-agent-data-model-foundation/09-03-SUMMARY.md` when done.
</output>
