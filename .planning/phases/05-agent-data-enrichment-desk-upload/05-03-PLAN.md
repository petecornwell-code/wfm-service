---
phase: 05-agent-data-enrichment-desk-upload
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/main/java/com/wfm/service/SolverService.java
  - src/test/java/com/wfm/service/SolverServicePtoFilterTest.java
  - src/test/java/com/wfm/service/SolverServiceEligibilityFilterTest.java
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "agentDaysOffMap excludes PTO rows with status REQUESTED — those days no longer block scheduling"
    - "agentDaysOffMap includes every MANDATORY day-off regardless of its status (mandatory holidays still hard-block)"
    - "agentDaysOffMap includes every APPROVED PTO row"
    - "Agents whose jobTitle matches a JobTitleConfig with nonSchedulable=true are filtered OUT before the solver builds AgentAssignment candidates"
    - "Existing solver filters (Agent.isActive, primarySpecialization!=null) remain in place"
  artifacts:
    - path: "src/main/java/com/wfm/service/SolverService.java"
      provides: "Corrected PTO filter + non-schedulable eligibility filter"
      contains: "agentEligibilityService.isNonSchedulable"
    - path: "src/test/java/com/wfm/service/SolverServicePtoFilterTest.java"
      provides: "Wave-0 coverage for PTO filter rules"
      contains: "REQUESTED"
    - path: "src/test/java/com/wfm/service/SolverServiceEligibilityFilterTest.java"
      provides: "Wave-0 coverage for non-schedulable agent exclusion"
      contains: "nonSchedulable"
  key_links:
    - from: "src/main/java/com/wfm/service/SolverService.java"
      to: "src/main/java/com/wfm/service/AgentEligibilityService.java"
      via: "Constructor-injected service; filter step in eligibleAgents stream"
      pattern: "isNonSchedulable"
    - from: "src/main/java/com/wfm/service/SolverService.java"
      to: "src/main/java/com/wfm/model/DayOffStatus.java"
      via: "Status comparison in agentDaysOffMap loop"
      pattern: "DayOffStatus.APPROVED"
---

<objective>
Make the solver respect PTO status and the non-schedulable flag:

1. Fix the PTO bug at SolverService.java:158-161 — only APPROVED PTO rows contribute to
   `agentDaysOffMap`; MANDATORY rows always contribute regardless of their status.
2. Add the non-schedulable filter (D-11) — between the existing `isActive` and
   `primarySpecialization != null` filters, exclude agents whose jobTitle is non-schedulable per
   `AgentEligibilityService`.

Purpose: Implement DATA-03's solver-side enforcement and Success Criterion 5's PTO fix. Both are
single-line changes in a single file, but they're load-bearing — wrong filter logic breaks every
generated schedule. Plan in isolation with focused unit tests so failures here can't be confused
with BambooHR sync issues (Plan 02) or upload-side enforcement (Plan 04).

Output: 1 modified service, 2 new Wave-0 backend tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-CONTEXT.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-RESEARCH.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-PATTERNS.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-VALIDATION.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-01-PLAN.md

<interfaces>
<!-- Types/services from Plan 01 (already merged when this runs): -->
- com.wfm.service.AgentEligibilityService.isNonSchedulable(long tenantId, String jobTitle): boolean
- com.wfm.model.DayOffStatus { APPROVED, REQUESTED }   (existing)
- com.wfm.model.DayOffType { PTO, MANDATORY, ... }     (existing)
- com.wfm.model.AgentDayOff getStatus(): DayOffStatus  (existing)
- com.wfm.model.AgentDayOff getType(): DayOffType      (existing)

SolverService.startSolve(...) — existing entry method; this plan modifies its internals:
  - eligibility filter pipeline around lines 123-127 (allAgents.stream().filter(...).toList())
  - agentDaysOffMap construction around lines 158-161
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix PTO filter (APPROVED-only, MANDATORY-always) and add non-schedulable agent filter in SolverService</name>
  <files>src/main/java/com/wfm/service/SolverService.java, src/test/java/com/wfm/service/SolverServicePtoFilterTest.java, src/test/java/com/wfm/service/SolverServiceEligibilityFilterTest.java</files>
  <read_first>
    - src/main/java/com/wfm/service/SolverService.java (lines 50-200 — constructor at 57-81, eligibility filter at 123-127, agentDaysOffMap loop at 158-161)
    - src/main/java/com/wfm/model/AgentDayOff.java (getType, getStatus, getDate)
    - src/main/java/com/wfm/model/DayOffStatus.java
    - src/main/java/com/wfm/model/DayOffType.java
    - src/main/java/com/wfm/service/AgentEligibilityService.java (from Plan 01)
    - src/test/java/com/wfm/service/ResolvePreferencesPtoFilterTest.java (test infrastructure pattern: JUnit5 + AssertJ + reflection for private methods)
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-RESEARCH.md sections "Solver eligibility filter (D-11)", "Pitfall 3: agentDaysOffMap filter doesn't apply to mandatory holidays"
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-PATTERNS.md section "SolverService.java MODIFY"
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-CONTEXT.md decisions D-11, D-22
  </read_first>
  <behavior>
    PTO filter tests (SolverServicePtoFilterTest):
    - Given an AgentDayOff with type=PTO, status=APPROVED, date=D → D appears in agentDaysOffMap[agentId].
    - Given an AgentDayOff with type=PTO, status=REQUESTED, date=D → D does NOT appear in agentDaysOffMap[agentId].
    - Given an AgentDayOff with type=MANDATORY (status unset / APPROVED / null) → date appears in agentDaysOffMap[agentId].
    - Given an AgentDayOff with type=MANDATORY, status=REQUESTED → date STILL appears (mandatory always blocks).
    - Multiple agents with mixed records — each agent's set contains only their own blocking dates.

    Eligibility filter tests (SolverServiceEligibilityFilterTest):
    - Active agent with primary spec and a regular jobTitle → included.
    - Active agent with primary spec and a non-schedulable jobTitle → excluded.
    - Inactive agent (even with regular jobTitle) → excluded (existing filter still applies).
    - Active agent with no primary spec → excluded (existing filter still applies).
    - Active agent with primary spec and null jobTitle → included (AgentEligibilityService returns false for null/blank).
    - Filter result preserves the order of input agents for the agents that survive.
  </behavior>
  <action>
    Modify SolverService.java:

    1. PTO filter fix (D-22). Locate the agentDaysOffMap construction (~lines 158-161). Replace the unconditional `for` loop with:
       - Iterate `allDaysOff`; for each AgentDayOff `d`, compute `boolean blocks = d.getType() == DayOffType.MANDATORY || d.getStatus() == DayOffStatus.APPROVED;`
       - If `blocks`, `computeIfAbsent(d.getAgent().getId(), k -> new HashSet<>()).add(d.getDate());`
       Behaviour matches RESEARCH Pitfall 3 exactly. Do NOT swap to a stream chain — keep the explicit for-loop shape so it's diff-minimal and reviewable.

    2. Non-schedulable filter (D-11). Inject AgentEligibilityService via constructor (extend the existing constructor at lines 57-81 — match the existing dependency-injection style; add the field; pass through any test seams already present).
       In the eligibility pipeline (~lines 123-127), add `.filter(a -> !agentEligibilityService.isNonSchedulable(tenantId, a.getJobTitle()))` BETWEEN the existing `Agent::isActive` filter and the `primarySpecialization != null` filter. The tenantId variable is already in scope at this call site (verify via Read — if not, resolve via TenantContext.getTenantId() at top of startSolve).

    Create SolverServicePtoFilterTest at src/test/java/com/wfm/service/. Mirror the style of ResolvePreferencesPtoFilterTest: JUnit5 + AssertJ + reflection on the private agentDaysOffMap-building method OR refactor that fragment into a package-private static helper to enable direct testing (preferred — extract a method like `static Map<UUID, Set<LocalDate>> buildAgentDaysOffMap(List<AgentDayOff> daysOff)` and unit-test it directly). The helper extraction is the same line count and clearer to test; document the choice in the SUMMARY.

    Create SolverServiceEligibilityFilterTest at src/test/java/com/wfm/service/. Construct synthetic Agent instances (active, primarySpecialization set, varied jobTitle); construct AgentEligibilityService with a stub JobTitleConfigRepository (e.g., a hand-rolled lambda-backed implementation or a `Mockito.mock` if Mockito is already in build.gradle — check before introducing). Either way, mock at the repository boundary; do NOT @SpringBootTest for this. Verify the filter expression directly by extracting the pipeline into a package-private helper `static List<Agent> filterEligible(List<Agent> agents, long tenantId, AgentEligibilityService elig)` if helpful for testability.

    Do NOT change any other solver logic. Do NOT change DayOffStatus or DayOffType enums.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.SolverServicePtoFilterTest" --tests "com.wfm.service.SolverServiceEligibilityFilterTest" -q && ./gradlew test -q</automated>
    <manual>Run a small mock solve via /api/v1/solve with an agent whose jobTitle is marked non-schedulable; confirm the agent is not in the result.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "DayOffType.MANDATORY" src/main/java/com/wfm/service/SolverService.java` returns at least 1
    - `grep -c "DayOffStatus.APPROVED" src/main/java/com/wfm/service/SolverService.java` returns at least 1
    - `grep -c "agentEligibilityService.isNonSchedulable" src/main/java/com/wfm/service/SolverService.java` returns 1
    - `grep -c "private final AgentEligibilityService" src/main/java/com/wfm/service/SolverService.java` returns 1
    - File exists: src/test/java/com/wfm/service/SolverServicePtoFilterTest.java
    - File exists: src/test/java/com/wfm/service/SolverServiceEligibilityFilterTest.java
    - `./gradlew test --tests "com.wfm.service.SolverServicePtoFilterTest"` passes (all behavior cases above).
    - `./gradlew test --tests "com.wfm.service.SolverServiceEligibilityFilterTest"` passes (all behavior cases above).
    - `./gradlew test` full suite remains green (no regression in existing solver-suite tests like FullScale150AgentTest).
  </acceptance_criteria>
  <done>Solver excludes REQUESTED PTO from agentDaysOffMap; mandatory holidays still block; non-schedulable agents never enter the solver's planning entity list. Both filters are independently tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Solver | Service constructs problem facts; solver is in-process — no external boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Agent jobTitle race vs JobTitleConfig | accept | SolverService runs inside the request transaction. JobTitleConfig and Agent.jobTitle are both written by BambooRefreshService inside a single TX; concurrent refresh + solve is prevented by RefreshInProgressException (existing). |
| T-05-03-02 | Elevation of Privilege | Non-schedulable agent appears in solve output | mitigate | AgentEligibilityService is called BEFORE the solver builds AgentAssignment planning entities (the eligibility pipeline at lines 123-127 produces eligibleAgents, which feed the planning entity construction at line 130+). Tests assert this exact ordering. |
| T-05-03-03 | Information Disclosure | tenantId leakage across solver candidates | mitigate | The existing pipeline at allAgents source already scopes by tenant (verify in SolverService startSolve preamble — if not, this plan does not introduce the bug, but the executor should flag it in the SUMMARY for a follow-up). |
| T-05-03-04 | DoS | Non-schedulable check per-agent O(n*m) | accept | n=agents per desk (typically <100), m=JobTitleConfig rows per tenant (one per distinct title — small). At this scale, no cache needed. RESEARCH explicitly recommends "fewer than 50 rows per tenant" — plain repository call is fine. |
</threat_model>

<verification>
- `./gradlew test` full suite green.
- Manual smoke: trigger a solve via the existing endpoint; observe results exclude non-schedulable agents and respect APPROVED-only PTO blocking.
</verification>

<success_criteria>
- All PTO-filter behavior cases pass.
- All non-schedulable-filter behavior cases pass.
- No existing solver test regresses.
- The two changes are isolated to SolverService and visible in a single diff.
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-data-enrichment-desk-upload/05-03-SUMMARY.md`
</output>
