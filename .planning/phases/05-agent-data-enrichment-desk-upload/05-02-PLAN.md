---
phase: 05-agent-data-enrichment-desk-upload
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/main/java/com/wfm/integration/BambooEmployee.java
  - src/main/java/com/wfm/integration/HttpBambooHRClient.java
  - src/main/java/com/wfm/integration/MockBambooHRClient.java
  - src/main/java/com/wfm/integration/BambooRefreshService.java
  - src/main/java/com/wfm/service/JobTitleConfigService.java
  - src/main/java/com/wfm/service/BambooSyncEventService.java
  - src/main/java/com/wfm/controller/JobTitleConfigController.java
  - src/main/java/com/wfm/controller/AppConfigurationController.java
  - src/main/java/com/wfm/dto/JobTitleConfigResponse.java
  - src/main/java/com/wfm/dto/BambooSyncEventResponse.java
autonomous: true
requirements: [DATA-02, DATA-03]

must_haves:
  decisions: [D-01, D-03, D-04, D-05, D-09, D-19, D-20]
  truths:
    - "BambooHR sync pulls employmentHistoryStatus into Agent.employmentType using the Part-Time mapping rule"
    - "Each distinct synced job title becomes a JobTitleConfig row with nonSchedulable=false by default; existing rows are not deleted"
    - "Operators can list job titles and toggle nonSchedulable via REST API"
    - "BambooHR 503/429 responses translate to BambooHRRateLimitedException carrying the Retry-After value"
    - "Every refresh attempt persists a BambooSyncEvent row with started_at, finished_at, success, error_message"
    - "GET /api/v1/configuration/bamboohr/sync-status returns the latest BambooSyncEvent for the tenant"
  artifacts:
    - path: "src/main/java/com/wfm/integration/BambooEmployee.java"
      provides: "Extended record with employmentHistoryStatus"
      contains: "employmentHistoryStatus"
    - path: "src/main/java/com/wfm/integration/HttpBambooHRClient.java"
      provides: "503/429 translation + employmentHistoryStatus field pull"
      contains: "BambooHRRateLimitedException"
    - path: "src/main/java/com/wfm/integration/BambooRefreshService.java"
      provides: "Mapping rule + JobTitleConfig auto-populate + sync event write"
      contains: "mapEmploymentType"
    - path: "src/main/java/com/wfm/service/JobTitleConfigService.java"
      provides: "List/toggle/ensureExists for JobTitleConfig"
      contains: "ensureExists"
    - path: "src/main/java/com/wfm/service/BambooSyncEventService.java"
      provides: "REQUIRES_NEW write surface for sync outcomes"
      contains: "Propagation.REQUIRES_NEW"
    - path: "src/main/java/com/wfm/controller/JobTitleConfigController.java"
      provides: "/api/v1/job-titles REST surface"
      contains: "/api/v1/job-titles"
    - path: "src/main/java/com/wfm/dto/JobTitleConfigResponse.java"
      provides: "API response shape for job titles"
      contains: "nonSchedulable"
    - path: "src/main/java/com/wfm/dto/BambooSyncEventResponse.java"
      provides: "API response shape for sync status"
      contains: "retryAfterSeconds"
  key_links:
    - from: "src/main/java/com/wfm/integration/HttpBambooHRClient.java"
      to: "src/main/java/com/wfm/exception/BambooHRRateLimitedException.java"
      via: "onStatus(503|429) throws"
      pattern: "throw new BambooHRRateLimitedException"
    - from: "src/main/java/com/wfm/integration/BambooRefreshService.java"
      to: "src/main/java/com/wfm/service/JobTitleConfigService.java"
      via: "ensureExists(tenantId, jobTitle) called per distinct title in synced roster"
      pattern: "jobTitleConfigService.ensureExists"
    - from: "src/main/java/com/wfm/integration/BambooRefreshService.java"
      to: "src/main/java/com/wfm/service/BambooSyncEventService.java"
      via: "REQUIRES_NEW write inside finally"
      pattern: "bambooSyncEventService.record"
    - from: "src/main/java/com/wfm/controller/AppConfigurationController.java"
      to: "src/main/java/com/wfm/service/BambooSyncEventService.java"
      via: "GET /bamboohr/sync-status"
      pattern: "/bamboohr/sync-status"
---

<objective>
Wire the BambooHR integration end-to-end:
1. Extend `BambooEmployee` with `employmentHistoryStatus` and propagate the field through all 6 call sites (5 callers + 1 record).
2. Pull `employmentHistoryStatus` from the BambooHR custom report and translate 503/429 to `BambooHRRateLimitedException` via `RestClient.onStatus()`.
3. Map BambooHR status → `EmploymentType` enum at the boundary, auto-populate `JobTitleConfig` rows from each refresh, and persist a `BambooSyncEvent` capturing success/failure.
4. Expose `/api/v1/job-titles` (list, toggle non-schedulable) and `/api/v1/configuration/bamboohr/sync-status` (latest event) REST endpoints.

Purpose: Implement DATA-02 (employment type sync) and the data-collection half of DATA-03 (job-title config rows) plus the operator-visible sync-status surface (Success Criterion 4).

Output: 2 new services, 1 new controller, 1 modified controller, 2 new DTOs, 4 modified integration files, plus 2 Wave-0 backend tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-CONTEXT.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-RESEARCH.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-PATTERNS.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-VALIDATION.md
@.planning/phases/05-agent-data-enrichment-desk-upload/05-01-PLAN.md

<interfaces>
<!-- Types & methods established by Plan 01 (already merged when this plan runs): -->
- com.wfm.model.EmploymentType { FULL_TIME, PART_TIME }
- com.wfm.model.JobTitleConfig (entity) + JobTitleConfigRepository (with findByTenantId, findByTenantIdAndJobTitle, findByIdAndTenantId, findByTenantIdAndNonSchedulableTrue)
- com.wfm.model.BambooSyncEvent (entity) + BambooSyncEventRepository (with findFirstByTenantIdOrderByStartedAtDesc)
- com.wfm.exception.BambooHRRateLimitedException(String message, int retryAfterSeconds)
- com.wfm.service.AgentEligibilityService.isNonSchedulable(long, String) — NOT used in this plan, but available.

BambooEmployee record (modified):
  public record BambooEmployee(String id, String displayName, String workEmail, String department,
                               String jobTitle, String status,
                               String employmentHistoryStatus,   // NEW — inserted before wfmTenantId
                               String wfmTenantId, String project) {}

JobTitleConfigService API:
  - List<JobTitleConfigResponse> list() — uses TenantContext.getTenantId() inside
  - JobTitleConfigResponse setNonSchedulable(UUID id, boolean value)
  - void ensureExists(long tenantId, String jobTitle) — upsert helper called from BambooRefreshService; takes tenantId as parameter (caller is inside an existing transactional context with its own tenantId)

BambooSyncEventService API:
  - @Transactional(propagation = Propagation.REQUIRES_NEW)
    void record(BambooSyncEvent event)   // persist regardless of caller's TX state
  - BambooSyncEventResponse getLatest()  — uses TenantContext.getTenantId()

JobTitleConfigController:
  - @RequestMapping("/api/v1/job-titles")
  - GET / → List<JobTitleConfigResponse>
  - PATCH /{id} body { "nonSchedulable": boolean } → JobTitleConfigResponse

AppConfigurationController additions:
  - GET /api/v1/configuration/bamboohr/sync-status → BambooSyncEventResponse (may include null fields when never-synced)

DTOs:
  - JobTitleConfigResponse(UUID id, String jobTitle, boolean nonSchedulable, OffsetDateTime createdAt, OffsetDateTime updatedAt)
  - BambooSyncEventResponse(OffsetDateTime startedAt, OffsetDateTime finishedAt, boolean success,
                            String errorMessage, Integer agentsSynced, Integer timeOffPulled,
                            Integer retryAfterSeconds)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend BambooEmployee, HttpBambooHRClient (employmentHistoryStatus pull + onStatus 503/429 handler), and MockBambooHRClient parity</name>
  <files>src/main/java/com/wfm/integration/BambooEmployee.java, src/main/java/com/wfm/integration/HttpBambooHRClient.java, src/main/java/com/wfm/integration/MockBambooHRClient.java, src/test/java/com/wfm/integration/HttpBambooHRClient503Test.java</files>
  <read_first>
    - src/main/java/com/wfm/integration/BambooEmployee.java (current record shape)
    - src/main/java/com/wfm/integration/HttpBambooHRClient.java (custom-report fields list ~line 76, parsing loop ~lines 103-117, all .retrieve() call sites at lines 83, 131, 178)
    - src/main/java/com/wfm/integration/MockBambooHRClient.java (3 BambooEmployee constructor sites at lines 62, 91, 107)
    - src/main/java/com/wfm/exception/BambooHRRateLimitedException.java (from Plan 01)
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-PATTERNS.md sections "BambooEmployee MODIFY", "HttpBambooHRClient MODIFY", "MockBambooHRClient MODIFY"
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-RESEARCH.md Pattern 5 (RestClient onStatus), Pitfall 2 (mock drift), Pitfall 7 (onStatus doesn't fire on connection errors)
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-CONTEXT.md decisions D-01, D-05, D-20
  </read_first>
  <behavior>
    - BambooEmployee carries the raw employmentHistoryStatus string straight from BambooHR.
    - HttpBambooHRClient.listEmployees() sends a custom report containing employmentHistoryStatus in the fields array.
    - HttpBambooHRClient parses employmentHistoryStatus into the BambooEmployee record; missing field defaults to empty string.
    - 503 response → BambooHRRateLimitedException with retryAfterSeconds parsed from Retry-After header (default 60 when missing or unparsable).
    - 429 response → same translation (Retry-After respected).
    - Other 4xx/5xx propagate as before (no behavioural change).
    - HttpBambooHRClient503Test asserts: when a stub RestClient returns 503 with header Retry-After: 42, calling listEmployees throws BambooHRRateLimitedException with retryAfterSeconds=42 and message contains "Retry in 42 seconds".
    - HttpBambooHRClient503Test asserts: 503 with no Retry-After header → retryAfterSeconds=60 (default).
    - MockBambooHRClient supplies employmentHistoryStatus in all 3 constructor sites; randomize roughly 20% as "Part-Time" (e.g. i % 5 == 0) for dev/test diversity.
  </behavior>
  <action>
    Modify BambooEmployee.java: insert `String employmentHistoryStatus` as a new field BETWEEN `String status` and `String wfmTenantId`. Per RESEARCH Pitfall 2, all 6 callers must be updated in lockstep — verify with grep before commit.

    Modify HttpBambooHRClient.java:
      1. Add `"employmentHistoryStatus"` to the custom-report fields array (~line 76).
      2. In the per-row parse loop (~lines 103-117), read `String employmentHistoryStatus = emp.path("employmentHistoryStatus").asText("")` and pass it as the new BambooEmployee argument.
      3. Add `parseRetryAfterSeconds(String header, int defaultSeconds)` helper — returns Integer.parseInt(header) if numeric, else defaultSeconds. Handle null header.
      4. Extract a shared `RestClient.ResponseSpec applyRateLimitHandler(RestClient.ResponseSpec spec)` (or inline lambda) that wraps `.onStatus(s -> s.value() == 503 || s.value() == 429, (req, resp) -> { String h = resp.getHeaders().getFirst(HttpHeaders.RETRY_AFTER); int n = parseRetryAfterSeconds(h, 60); throw new BambooHRRateLimitedException("BambooHR is rate-limiting requests. Retry in " + n + " seconds.", n); })`.
      5. Apply applyRateLimitHandler to all 3 .retrieve() sites: listEmployees (~line 83), getEmployee (~line 131), fetchTimeOffByStatus (~line 178). For getEmployee's single-row path, pass `""` for the new BambooEmployee argument (single-employee endpoint omits the field).

    Modify MockBambooHRClient.java: update all 3 `new BambooEmployee(...)` calls (lines 62, 91, 107) to include the new employmentHistoryStatus parameter. Inject diversity for tests: at the list-builder around line 62, use `i % 5 == 0 ? "Part-Time" : "Full-time"`. For the single-employee getEmployee mock (around line 107), use `"Full-time"`. Keep the existing wfmTenantId/project fields after the new field — same positional order as the modified record.

    Create HttpBambooHRClient503Test (src/test/java/com/wfm/integration/). Use Spring's `MockRestServiceServer` bound to a RestClient.Builder — wire up a RestClient via builder, return 503 with Retry-After header, exercise listEmployees, assert BambooHRRateLimitedException with retryAfterSeconds==42. Add a second test for 503 with no Retry-After → retryAfterSeconds==60. If MockRestServiceServer setup is novel for this codebase (RESEARCH "No Analog Found"), follow Spring docs — instantiate `MockRestServiceServer.bindTo(restClient)` and `.expect().andRespond(withStatus(SERVICE_UNAVAILABLE).headers(...))`.

    Do NOT add Spring Retry. Do NOT auto-retry. Surface the exception so the operator sees the message (Phase 5 requirement is *surface*, not *retry*).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.integration.HttpBambooHRClient503Test" -q && ./gradlew compileJava -q</automated>
    <manual>Run ./gradlew bootRun with the mock profile; trigger a refresh; verify the new field appears in logs / DB.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "employmentHistoryStatus" src/main/java/com/wfm/integration/BambooEmployee.java` returns at least 1
    - `grep -c "employmentHistoryStatus" src/main/java/com/wfm/integration/HttpBambooHRClient.java` returns at least 2 (fields array + parse loop)
    - `grep -c "BambooHRRateLimitedException" src/main/java/com/wfm/integration/HttpBambooHRClient.java` returns at least 1
    - `grep -c "onStatus" src/main/java/com/wfm/integration/HttpBambooHRClient.java` returns at least 1 (could be multiple via helper)
    - `grep -c "RETRY_AFTER" src/main/java/com/wfm/integration/HttpBambooHRClient.java` returns at least 1
    - `grep -c "new BambooEmployee" src/main/java/com/wfm/integration/MockBambooHRClient.java` returns 3
    - `grep -c "Part-Time" src/main/java/com/wfm/integration/MockBambooHRClient.java` returns at least 1
    - `grep -rn "new BambooEmployee" src/ | wc -l` returns 6 (all 6 sites updated; if more or fewer, drift exists)
    - File exists: src/test/java/com/wfm/integration/HttpBambooHRClient503Test.java
    - `./gradlew test --tests "com.wfm.integration.HttpBambooHRClient503Test"` passes.
    - `./gradlew compileJava` succeeds (no positional-constructor drift).
  </acceptance_criteria>
  <done>BambooHR integration pulls the new field; 503/429 → typed exception with retryAfterSeconds; mock client parity; HttpBambooHRClient503Test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add JobTitleConfigService, BambooSyncEventService, DTOs, controllers; wire mapping + auto-populate + sync event into BambooRefreshService</name>
  <files>src/main/java/com/wfm/service/JobTitleConfigService.java, src/main/java/com/wfm/service/BambooSyncEventService.java, src/main/java/com/wfm/dto/JobTitleConfigResponse.java, src/main/java/com/wfm/dto/BambooSyncEventResponse.java, src/main/java/com/wfm/controller/JobTitleConfigController.java, src/main/java/com/wfm/controller/AppConfigurationController.java, src/main/java/com/wfm/integration/BambooRefreshService.java, src/test/java/com/wfm/integration/BambooRefreshServiceTest.java, src/test/java/com/wfm/service/JobTitleConfigServiceTest.java</files>
  <read_first>
    - src/main/java/com/wfm/service/AppConfigurationService.java (tenant-scoped service pattern; list/upsert shape)
    - src/main/java/com/wfm/controller/AppConfigurationController.java (controller pattern with TenantContext via filter)
    - src/main/java/com/wfm/integration/BambooRefreshService.java (existing refresh flow: refreshDeskAgents ~lines 71-97; persistRefreshData ~lines 100+; per-agent update loop ~lines 160-184)
    - src/main/java/com/wfm/dto/DeskAgentResponse.java (record DTO pattern)
    - src/main/java/com/wfm/model/JobTitleConfig.java (from Plan 01)
    - src/main/java/com/wfm/model/BambooSyncEvent.java (from Plan 01)
    - src/main/java/com/wfm/repository/JobTitleConfigRepository.java (from Plan 01)
    - src/main/java/com/wfm/repository/BambooSyncEventRepository.java (from Plan 01)
    - src/main/java/com/wfm/config/TenantContext.java
    - src/test/java/com/wfm/service/ResolvePreferencesPtoFilterTest.java (test pattern)
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-PATTERNS.md sections "JobTitleConfigService", "BambooSyncEventService", "JobTitleConfigController", "AppConfigurationController MODIFY", "JobTitleConfigResponse", "BambooSyncEventResponse", "BambooRefreshService MODIFY"
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-RESEARCH.md Code Examples: JobTitleConfig entity, Sync event persistence
    - .planning/phases/05-agent-data-enrichment-desk-upload/05-CONTEXT.md decisions D-03, D-04, D-05, D-08, D-09, D-10, D-19
  </read_first>
  <behavior>
    - JobTitleConfigService.list() returns all rows for the current tenant, mapped to JobTitleConfigResponse, sorted by jobTitle.
    - JobTitleConfigService.setNonSchedulable(id, value): updates the row's nonSchedulable field, refreshes updatedAt, returns the updated response. Throws EntityNotFoundException (or matches existing project pattern) when no row matches (id, tenantId).
    - JobTitleConfigService.ensureExists(tenantId, jobTitle): no-op when jobTitle null/blank; otherwise findByTenantIdAndJobTitle else create new row with nonSchedulable=false, createdAt=now, updatedAt=now. Idempotent — safe to call per agent during refresh.
    - BambooSyncEventService.record(event): @Transactional(propagation=REQUIRES_NEW) save the entity so it survives a roll-back of the caller's transaction.
    - BambooSyncEventService.getLatest() returns the latest event for the current tenant (TenantContext) mapped to BambooSyncEventResponse, or a "never-synced" response (startedAt=null, success=false, all counts null) when no row exists.
    - BambooRefreshService.refreshDeskAgents wraps its existing body in try/catch/finally: build BambooSyncEvent at entry; on BambooHRRateLimitedException set retryAfterSeconds and errorMessage and rethrow; on any other Exception set errorMessage and rethrow; in finally setFinishedAt and call bambooSyncEventService.record(event).
    - BambooRefreshService applies mapEmploymentType inside the existing per-agent update loop (after setJobTitle, ~line 168).
    - BambooRefreshService calls jobTitleConfigService.ensureExists(tenantId, employee.jobTitle()) for each distinct non-blank job title in the synced roster, INSIDE persistRefreshData's transaction. Iterate the freshly-synced employees list (NOT the DB) per RESEARCH anti-pattern.
    - JobTitleConfigController exposes GET /api/v1/job-titles and PATCH /api/v1/job-titles/{id}; tenantId resolved via TenantContext inside the service.
    - AppConfigurationController gains GET /api/v1/configuration/bamboohr/sync-status delegating to BambooSyncEventService.getLatest().
    - BambooRefreshServiceTest: feed in a synthetic List<BambooEmployee> with employmentHistoryStatus values ("Part-Time" → PART_TIME, "Full-time" → FULL_TIME, "Probation Period" → FULL_TIME, "" → FULL_TIME, null → FULL_TIME). Verify mapping rule. Verify JobTitleConfig rows are auto-created exactly once per distinct title and that an existing row with nonSchedulable=true is NOT modified.
    - JobTitleConfigServiceTest: list returns sorted by jobTitle; setNonSchedulable updates updatedAt; ensureExists is idempotent; uniqueness constraint enforced via DataIntegrityViolationException when bypassed.
  </behavior>
  <action>
    Create JobTitleConfigResponse (record). Fields: UUID id, String jobTitle, boolean nonSchedulable, OffsetDateTime createdAt, OffsetDateTime updatedAt.

    Create BambooSyncEventResponse (record). Fields: OffsetDateTime startedAt, OffsetDateTime finishedAt, boolean success, String errorMessage, Integer agentsSynced, Integer timeOffPulled, Integer retryAfterSeconds.

    Create JobTitleConfigService (@Service): inject JobTitleConfigRepository. Methods:
      - list() — @Transactional(readOnly=true); reads TenantContext.getTenantId(); calls findByTenantId; maps to JobTitleConfigResponse; sorted by jobTitle ascending.
      - setNonSchedulable(UUID id, boolean value) — @Transactional; resolves tenant; findByIdAndTenantId or throw EntityNotFoundException; setNonSchedulable; setUpdatedAt(OffsetDateTime.now()); save; return mapped response.
      - ensureExists(long tenantId, String jobTitle) — @Transactional(propagation=REQUIRED) — caller provides tenantId because this is called from inside BambooRefreshService where TenantContext may not be set per-tenant in scheduled paths. Early-return if jobTitle null/blank. findByTenantIdAndJobTitle.orElseGet to create+save a new row with nonSchedulable=false, timestamps set.

    Create BambooSyncEventService (@Service): inject BambooSyncEventRepository. Methods:
      - record(BambooSyncEvent event) — @Transactional(propagation=Propagation.REQUIRES_NEW); repository.save(event). Survives caller rollback per RESEARCH A3.
      - getLatest() — @Transactional(readOnly=true); reads TenantContext.getTenantId(); findFirstByTenantIdOrderByStartedAtDesc.map(this::toResponse).orElseGet(() -> new BambooSyncEventResponse(null, null, false, null, null, null, null)).

    Create JobTitleConfigController (@RestController @RequestMapping("/api/v1/job-titles")): GET / → service.list(); PATCH /{id} with request body record `record SetNonSchedulableRequest(boolean nonSchedulable) {}` → service.setNonSchedulable(id, body.nonSchedulable()).

    Modify AppConfigurationController.java: add @GetMapping("/bamboohr/sync-status") returning BambooSyncEventResponse delegating to BambooSyncEventService.getLatest(). Inject BambooSyncEventService via constructor (extend existing constructor signature).

    Modify BambooRefreshService.java:
      - Inject JobTitleConfigService and BambooSyncEventService via constructor (extend existing constructor signature).
      - Add a private static final String BAMBOO_PART_TIME = "Part-Time" constant + private static EmploymentType mapEmploymentType(String status) helper.
      - Inside the per-agent update loop (~line 168, after setJobTitle), call agent.setEmploymentType(mapEmploymentType(emp.employmentHistoryStatus())).
      - Inside persistRefreshData (after the per-agent loop completes), iterate distinct non-blank job titles from the synced `employees` list and call jobTitleConfigService.ensureExists(tenantId, jobTitle).
      - Wrap the body of refreshDeskAgents in try/catch/finally: at entry create BambooSyncEvent with tenantId, deskId, startedAt=now, success=false. In try: run the existing logic; on success set success=true, agentsSynced=employees.size(), timeOffPulled=timeOffs.size() (or the existing counter equivalent in this file). Catch BambooHRRateLimitedException: set errorMessage and retryAfterSeconds; rethrow. Catch generic Exception: setErrorMessage; rethrow. Finally: setFinishedAt(now()); bambooSyncEventService.record(event).

    Create BambooRefreshServiceTest with the mapping + auto-populate cases from <behavior>. Use H2 + spring-boot-starter-test, instantiating real repositories; mock HttpBambooHRClient (or use MockBambooHRClient + stub the input list). Match the existing test style in ResolvePreferencesPtoFilterTest (no @SpringBootTest preferred — use direct construction with H2 EntityManager where viable; otherwise minimal @SpringBootTest with @ActiveProfiles("test")).

    Create JobTitleConfigServiceTest covering list/setNonSchedulable/ensureExists per <behavior>. Use H2 + Spring infra to exercise the repository's uniqueness constraint via flush().

    Do NOT delete JobTitleConfig rows during refresh (D-09 explicit). Do NOT store employmentHistoryStatus verbatim on Agent (D-04 explicit).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.integration.BambooRefreshServiceTest" --tests "com.wfm.service.JobTitleConfigServiceTest" -q && ./gradlew compileJava -q</automated>
    <manual>Boot the app with mock profile; call GET /api/v1/job-titles after a refresh; expect rows for each distinct job title. Call GET /api/v1/configuration/bamboohr/sync-status; expect a populated record.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "mapEmploymentType" src/main/java/com/wfm/integration/BambooRefreshService.java` returns at least 2 (helper + caller)
    - `grep -c "setEmploymentType" src/main/java/com/wfm/integration/BambooRefreshService.java` returns 1
    - `grep -c "jobTitleConfigService.ensureExists" src/main/java/com/wfm/integration/BambooRefreshService.java` returns 1
    - `grep -c "bambooSyncEventService" src/main/java/com/wfm/integration/BambooRefreshService.java` returns at least 1
    - `grep -c "Propagation.REQUIRES_NEW" src/main/java/com/wfm/service/BambooSyncEventService.java` returns 1
    - `grep -c "@RequestMapping(\"/api/v1/job-titles\")" src/main/java/com/wfm/controller/JobTitleConfigController.java` returns 1
    - `grep -c "/bamboohr/sync-status" src/main/java/com/wfm/controller/AppConfigurationController.java` returns 1
    - `grep -c "ensureExists" src/main/java/com/wfm/service/JobTitleConfigService.java` returns at least 1
    - File exists: src/main/java/com/wfm/dto/JobTitleConfigResponse.java
    - File exists: src/main/java/com/wfm/dto/BambooSyncEventResponse.java
    - File exists: src/test/java/com/wfm/integration/BambooRefreshServiceTest.java
    - File exists: src/test/java/com/wfm/service/JobTitleConfigServiceTest.java
    - `./gradlew test --tests "com.wfm.integration.BambooRefreshServiceTest"` passes.
    - `./gradlew test --tests "com.wfm.service.JobTitleConfigServiceTest"` passes.
    - `./gradlew test` full suite remains green.
  </acceptance_criteria>
  <done>Mapping rule lives in one place; JobTitleConfig auto-populates inside the existing refresh transaction; BambooSyncEvent records every refresh attempt; two new REST endpoints reachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Operator hits PATCH /api/v1/job-titles/{id}; tenant resolved via X-Tenant-ID → TenantContext |
| API → BambooHR | Outbound HTTPS with API key in Authorization header; untrusted response body parsed into BambooEmployee |
| Service → Database | All writes scoped by tenantId; new entities from Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | BambooSyncEvent.error_message persisted from caught Exception | mitigate | In BambooRefreshService catch blocks, set errorMessage = ex.getMessage() (already a constructed string). Do NOT setErrorMessage from ex.toString() (could include stack frames with API key from log context). For the rate-limit exception specifically, the message is "BambooHR is rate-limiting requests. Retry in N seconds." — no secrets. |
| T-05-02-02 | Information Disclosure | Logs around 503 onStatus handler | mitigate | The onStatus handler MUST NOT log the response body or Authorization header. Just construct + throw the typed exception. If existing logging in HttpBambooHRClient logs request headers, redact Authorization at the log statement (use existing project pattern if present; otherwise skip — controller-layer 503 surfacing is sufficient). |
| T-05-02-03 | Elevation of Privilege | PATCH /api/v1/job-titles/{id} cross-tenant | mitigate | JobTitleConfigService.setNonSchedulable uses findByIdAndTenantId — a row from a different tenant returns Optional.empty → EntityNotFoundException (404). Confirm in JobTitleConfigServiceTest with two tenant rows. |
| T-05-02-04 | Tampering | Job title containing SQL/script payloads | mitigate | jobTitle is bound via JPA parameter binding (no string concatenation). VARCHAR(255) caps length at the DB layer (V26). Service should trim() before findByTenantIdAndJobTitle to avoid trailing-whitespace dupes (handle in ensureExists). |
| T-05-02-05 | Spoofing | BambooHR returns a forged employmentHistoryStatus | accept | BambooHR is the source of truth for employment type per requirement. No additional verification possible at this layer. Mapping rule is deterministic and reversible if needed. |
| T-05-02-06 | Repudiation | No audit trail of nonSchedulable toggles | accept | Beyond Phase 5 scope. Deferred Idea in CONTEXT.md (persistent imports history) covers the broader audit-trail concern. updatedAt timestamp provides minimal traceability. |
| T-05-02-07 | DoS | Manual refresh repeatedly triggered when BambooHR is rate-limiting | mitigate | RefreshInProgressException already prevents concurrent same-desk refresh (per BambooRefreshService:35 ConcurrentHashMap). The 503 exception surfaces with the retry value to the operator — operator sees "retry in N seconds" and is informed not to hammer. No auto-retry introduced. |
</threat_model>

<verification>
- All compile + new tests pass.
- Manual smoke against MockBambooHRClient: triggers a refresh, GET /api/v1/job-titles returns rows for every distinct mock job title with nonSchedulable=false; GET /api/v1/configuration/bamboohr/sync-status returns a populated record.
- 503 surfacing: in MockBambooHRClient (or a one-off integration test) simulate 503 — observe BAMBOOHR_RATE_LIMITED returned to client.
</verification>

<success_criteria>
- BambooHR sync writes Agent.employmentType correctly for all 5 observed employmentHistoryStatus values in the live tenant.
- JobTitleConfig populates on first refresh; existing rows unchanged on subsequent refreshes.
- BambooSyncEvent persists per refresh attempt (success or failure).
- 503 → 503 via GlobalExceptionHandler (from Plan 01) with code BAMBOOHR_RATE_LIMITED and retry-in-N-seconds message.
- New REST endpoints reachable and tenant-scoped.
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-data-enrichment-desk-upload/05-02-SUMMARY.md`
</output>
