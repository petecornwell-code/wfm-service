---
context: phase
phase: 11-bamboohr-merge-engine-report
task: 5
total_tasks: 5
status: in_progress
last_updated: 2026-08-19T23:36:15.504Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure in this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **A constraint that groups by a planning entity cannot fire where no entity exists** — `minimumStaffing` shipped green with 6 passing unit tests and was completely inert in production. Structural mitigation: before writing any constraint that keys off `AgentAssignment`, confirm an entity actually exists for every key you intend to penalise, and write at least one test for the *absent-entity* state, not just the unassigned-entity state.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Testing the adjacent scenario | `MinimumStaffingConstraintTest` supplied `assignment(ts, null)` — a seat that exists but is empty — in all six cases. The real zero-demand case is *no seat at all*. The suite went green on a constraint that could never fire. | blocking | For any groupBy-based constraint, enumerate the states of the grouping key: present-and-populated, present-and-empty, **absent**. Write the absent case first. |
| Trusting an executor's self-check | Both plan executors reported `Self-Check: PASSED` without completing `./gradlew test`; the post-merge gate then caught a real break (`UploadFreshSyncTest`, fixed in 57c9cfd). | advisory | Run the full suite at every wave boundary rather than accepting the SUMMARY's claim. It takes ~8–9 min. |
| Stale baseline assertions | Adding a constraint changes scores that tests pin exactly. Hit twice: `UploadFreshSyncTest` (merge-report row count) and `IncrementalScoringDiagnosticTest` (soft baseline −12720 → −24720). | advisory | Expect these. Both were correct new behaviour, not regressions — check the constraint breakdown in the failure output before assuming a defect. |
| `BreakAwareConstructionTest` is load-flaky | Failed once during a full-suite run, passed in isolation (2m24s). It drives a raw Timefold `SolverConfig` and asserts tolerances on a 120s local search. | advisory | It does not touch `SolverService.startSolve`. If it fails alone in an otherwise-green suite, re-run it in isolation before investigating. |

<current_state>
Phase 11 is **code-complete but not verification-complete**. Both plans have SUMMARYs; three further commits of solver work landed after verification was written.

- HEAD `1aa9578`, everything pushed, working tree clean of source changes.
- Deployed to dev and verified live: task def `wfm-service-dev:39`, health UP, V37 confirmed by the API serving `minStaffingWeight`.
- Full suite green locally (265 tests) and on CI.
- `phase_complete: false`, `uat_passed: false`, `verification_status: human_needed`.
</current_state>

<completed_work>

- Task 1: 11-01 fresh-sync merge engine, per-field precedence, merge report, 503 abort — done (`177342c`)
- Task 2: 11-02 `workingDaysSource` provenance + solve-time PTO arbitration — done (`1b5403d`), blocking checkpoint resolved by the operator in favour of **solve-time**
- Task 3: post-merge fix for the stale `UploadFreshSyncTest` assertion — done (`57c9cfd`)
- Task 4: code-review WR-01, dedupe day-off facts per (agent, date) — done (`22707bc`)
- Task 5: minimum-staffing constraint, weight, migration V37, config-page row, **and** the seat generation that makes it work — done (`1aa9578`)
</completed_work>

<remaining_work>

- Phase 11 UAT: 5 items, **all still `[pending]`** in `11-UAT.md`
- Re-run `gsd-verifier` — VERIFICATION.md was written at `5da1d13` and predates WR-01 and the whole minimum-staffing change
- `/gsd-secure-phase 11` — security enforcement is on and no SECURITY.md exists
- Consistency plan Stage 0 (see below) — agreed, not started
- `breakClustering` is a registered constraint that is a no-op stub (`penalizeConfigurable(a -> 0)`)
</remaining_work>

<decisions_made>

- **Solve-time PTO arbitration**, not merge-write time — operator decision at the 11-02 blocking checkpoint. Keeps D-03's one-writer invariant; nothing persisted.
- **Minimum staffing hard-vs-soft is configuration, not code** — `ConstraintWeights` is a `@ConstraintConfiguration` with persisted `HardSoftScore`s, so `min_staffing_weight` decides the level. Defaults soft: `contracted_hours_over` is hard, so an agent works exactly 8h, a shift spans 9h, and the window is 13h — a hard floor on a thin day returns no schedule at all (the failure mode V33 undid for `contracted_hours_under`).
- **Seat generation was the load-bearing half**, not the constraint. See the blocking constraint above.
- **`breakBlockedHours` 4.0 over 3.0** — the knob is symmetric, so the permitted offset is `[blocked, 8 − blocked]`. 4.0 is the only single-point value (+4h exactly). **+3h is not expressible.** 3.0 gave +3..+5h and all 87 breaks complied, but 51 of 138 agent-days collapsed to 3-hour shifts.
- **Consistency needs new constraints, not standing preferences** — `preferredBreakTime` is an absolute `LocalTime`, and `honourPreferredStartTime` only penalises `isBefore`, making it a floor rather than an anchor.
</decisions_made>

<blockers>
- **Schedule `b44ac8f8` is `COMPLETED` and blocks any new solve** with `CONFLICT`. It is the 3.0 run: soft −258005, 51 three-hour shifts. **Reject it, do not accept.**
- **Phase 11 completion is blocked on UAT**, not on code.
</blockers>

## Required Reading (in order)

1. `.planning/phases/11-bamboohr-merge-engine-report/11-UAT.md` — the 5 pending items; three are answerable with one upload
2. `.planning/phases/11-bamboohr-merge-engine-report/11-VERIFICATION.md` — 6/6 must-haves verified, but written before the last three commits
3. `.planning/phases/11-bamboohr-merge-engine-report/11-REVIEW.md` — WR-01 is fixed; WR-02 through WR-07 are open
4. `src/main/java/com/wfm/solver/ScheduleConstraintProvider.java` — `minimumStaffing` javadoc records why the seat is required
5. `src/main/java/com/wfm/service/SolverService.java` — `expandMinimumStaffingSeats` and its javadoc

## Infrastructure State

- **Deployed:** `wfm-service-dev:39`, rollout COMPLETED, health UP. App at `https://d2bbtcc80peap7.cloudfront.net`
- **AWS:** region **eu-west-2** (the CLI defaults to `us-east-2` — lookups silently return empty in the wrong region). Cluster `wfm-service-dev`, ALB `wfm-service-dev-1139960163.eu-west-2.elb.amazonaws.com`
- **API:** every request needs `X-Tenant-ID: 1`. Only one desk exists: `Stubhub (EN)`, id `6170be17-3bee-41da-9d81-62ddd50c786f`
- **RDS:** publicly accessible in reality but the security group excludes this machine — direct psql times out. Reach data through the app API instead. Note `infra/rds.tf` declares `publicly_accessible = false`, so **live drift from Terraform** on a database holding real employee records
- **Live weight changed this session:** `minStaffingWeight` lowered `0hard/1000soft` → **`0hard/1soft`**. Any solve now behaves differently from the one VERIFICATION.md describes
- **Deploy = push.** `deploy.yml` triggers on pushes to `main` *and* `claude/create-system-specification-451ge`; `.planning/**` -only pushes are skipped
- **Test artifacts** (untracked, project root): `StubHub-EN-combined-CSR-test.xlsx` (28 real CSRs, 18 from the desk + 10 from BambooHR dept `Stubhub (PT) - UA`, real PTO for agents 6798 and 7758, all worked cells 8h, MANDATORY rebalanced against demand) and `stubhub_demand_ES.xlsx` (7 tabs, hourly 08:00–21:00, 789 FTE-hours). Generators are in the session scratchpad and will not survive

<context>
The last stretch moved from Phase 11 delivery into solver tuning, driven by an observed symptom: schedules started at 10:00/11:00 on some days. That turned out to be correct behaviour — the demand file has leading zeros at weekends, and an 8h shift plus a 1h break spans exactly 9 of the 13h window, so there was one legal placement. That diagnosis produced the minimum-staffing work.

Two things are worth carrying forward. First, headline coverage is misleading on this data: the week reads ~140% while Saturday 11:00 sits at 24 agents against demand of 44 (55%). Judge per-hour, not per-day. Second, the demand spike (0 → 44 at 11:00, confirmed correct by the operator) is not coverable by 28 agents on flat 8-hour shifts; several tuning attempts are really fighting that, not a bug.

The agreed next direction is the consistency plan, whose Stage 0 is deliberately the smallest possible probe of whether consistency and this demand shape can coexist at all.
</context>

## Consistency plan (agreed, not started)

| Stage | Goal | Change | New config |
|---|---|---|---|
| 0 | Make standing preferences anchor | `honourPreferredStartTime` fires only on `isBefore` — starting later is free. Penalise absolute deviation both ways, in increments | none |
| 1 | Consistent start, solver-chosen anchor | New `consistentDailyStart`: groupBy (agent, date) → min start, then (agent) → spread | `consistentStartWeight` `0hard/2soft`, V38 |
| 2 | Consistent break | Same shape over break **offset from shift start** | `consistentBreakWeight` |
| 3 | Make it measurable | Per-agent start/break spread on `ScheduleDetailResponse` | none |

Target: ≥80% of agents identical start on every worked day, no spread over 2h, measured on 2026-01-05..11.
**Keep every stage soft** — a hard consistency constraint will escape by shortening shifts exactly as the 3.0 break window did.

<next_action>
Start with: reject schedule `b44ac8f8` (it blocks all solving), then pick one of —
(a) run the `breakBlockedHours: 4.0` solve the operator asked for, or
(b) begin consistency Stage 0: change the `isBefore` test in `honourPreferredStartTime` to a two-directional increment penalty.

If the goal is closing Phase 11 rather than tuning, go to `/gsd-verify-work 11` instead — the phase is blocked on 5 UAT items, not on code.
</next_action>
