---
phase: 17-consistency-constraint-drift-reporting
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/main/java/com/wfm/service/ScheduleOutputService.java
  - src/main/java/com/wfm/service/ScheduleService.java
  - src/main/java/com/wfm/service/ScheduleExportService.java
  - src/test/java/com/wfm/service/DriftReportTest.java
  - src/test/java/com/wfm/service/ScheduleExportServiceTest.java
  - src/test/java/com/wfm/service/SolverUsualShiftWritePathGuardTest.java
  - src/test/resources/ushf-05-write-paths.md
autonomous: true
requirements: [DRFT-01, DRFT-04, XCUT-01, XCUT-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The drift report carries a popularity ranking of shift templates by how many agents hold each as their current usual shift, sorted descending by agent count with an alphabetical template-name tie-break, read from stored usual shifts and not from this solve's results (DRFT-04)."
    - "The popularity ranking is scoped to the schedule's own tenant and desk — it is read through the tenant-and-desk-scoped finder, never an unscoped repository read."
    - "A date filter on the schedule detail narrows the drift entries to that date and recomputes the summary counts from the narrowed set, so the four summary numbers always agree with the rows on screen; the popularity ranking is unaffected by the date filter because it reads stored usual shifts, not solve results (DRFT-01)."
    - "A solve never writes to agent_usual_shift: the pre-solve resolution performs zero mutating interactions with AgentUsualShiftRepository, proven by a guard test rather than asserted in prose (XCUT-02)."
    - "The Excel schedule export carries a Drift Report sheet whose column headers are byte-identical to the Drift Report tab's table headers, so the same drift data is visible in the export surface as on screen (XCUT-01, D-14)."
    - "Zero drift entries still writes the Drift Report sheet's header row and nothing else, matching writePreferenceReport's existing guard-then-header-only pattern (UI E4 empty)."
    - "The Drift Report sheet writes whatever buildDriftReport returns unconditionally; a flat document has no validation state, so the writer has no error branch (UI E4 error)."
    - "Every cell in the Drift Report sheet is a plain-text value at its specified column position (UI E4 populated)."
    - "A desk where some agents have no stored usual shift exports the literal status label as plain text in the Status column — the per-row value function is independent of neighbouring rows (UI E4 partial)."
    - "Zero, one, or many drift rows all go through the identical per-row value function with no special-casing (UI E4 zero-one-many)."
    - "Shift-template names in the Drift Report sheet inherit the export's existing auto-sizing like every other column; no truncation is applied (UI E4 long-text)."
  prohibitions:
    - "MUST NOT present the drift report or the over-subscription ranking as an agent-performance or compliance measure — drift is a property of the schedule, not a mark against the agent. No per-agent drift score, no ranked list of agents by drift, and no highlighting of individual agents as worst offenders is introduced in any surface."
  artifacts:
    - path: "src/test/java/com/wfm/service/SolverUsualShiftWritePathGuardTest.java"
      provides: "XCUT-02 proof that the solver read path never writes agent_usual_shift"
      contains: "resolveUsualShiftTargets"
    - path: "src/test/resources/ushf-05-write-paths.md"
      provides: "Enumerated-write-path record extended with the solver read path"
      contains: "solver"
  key_links:
    - from: "src/main/java/com/wfm/service/ScheduleExportService.java"
      to: "src/main/java/com/wfm/dto/ScheduleDetailResponse.java"
      via: "writeDriftReport consumes detail.getDriftReport() from the same payload the UI renders"
      pattern: "getDriftReport"
    - from: "src/main/java/com/wfm/service/ScheduleService.java"
      to: "src/main/java/com/wfm/dto/ScheduleDetailResponse.java"
      via: "date-filter block narrows driftReport entries and rebuilds the summary"
      pattern: "setDriftReport"
    - from: "src/main/java/com/wfm/service/ScheduleOutputService.java"
      to: "src/main/java/com/wfm/repository/AgentUsualShiftRepository.java"
      via: "popularity ranking read through the tenant-and-desk-scoped finder"
      pattern: "findByTenantIdAndDeskId"
---

<objective>
Finish the drift report: add the over-subscription ranking DRFT-04 asks for, make the date filter and
summary agree with what the operator can see, put the same data in the Excel export, and prove the
solver never writes back to `agent_usual_shift`.

Purpose: XCUT-01 exists because the v1.2 audit found a model built and a view never migrated (finding
I-1), and XCUT-02 exists because a guarantee held on one write path only (finding I-2). This plan
closes both for this phase's data, and adds the one requirement the tracer deliberately left as a
later-fillable gap.

Output: the popularity ranking, `ScheduleService`'s drift date-filter block, the Excel `Drift Report`
sheet, and a write-path guard test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-consistency-constraint-drift-reporting/17-CONTEXT.md
@.planning/phases/17-consistency-constraint-drift-reporting/17-PATTERNS.md
@.planning/phases/17-consistency-constraint-drift-reporting/17-UI-SPEC.md
@.planning/phases/17-consistency-constraint-drift-reporting/17-01-SUMMARY.md
@.planning/milestones/v1.2-MILESTONE-AUDIT.md
</context>

<interfaces>
Consumed from plan 17-01 — already on disk, do not re-derive:

`ScheduleDetailResponse.DriftReport(List<DriftReportEntry> entries, DriftSummary summary, List<ShiftPopularityEntry> popularity)`
`ScheduleDetailResponse.ShiftPopularityEntry(String templateName, int agentCount)`
`ScheduleDetailResponse.DriftSummary(int workingAgentDays, int noUsualShiftCount, int honouredCount, int driftedCount)`
`ScheduleDetailResponse.DriftStatus { NO_USUAL_SHIFT, HONOURED, DRIFTED }`
`ScheduleOutputService.buildDriftReport(Schedule)` — returns `List.of()` for `popularity` until this plan
`SolverService.resolveUsualShiftTargets(...)` — package-private, callable from `com.wfm.service` tests

Exact literal strings fixed by `17-UI-SPEC.md` and reproduced here so the export and the tab cannot drift:
  - Drift sheet name: `Drift Report`
  - Main table headers: `Agent`, `Date`, `Status`, `Usual Start`, `Actual Start`, `Delta (min)`
  - Popularity table headers: `Shift Template`, `Agents (Usual Shift)`
  - Popularity section heading: `Most-Subscribed Usual Shifts`
  - Status labels written to the sheet: `No usual shift`, `Honoured`, `Drifted` (the operator-facing
    labels, not the raw enum constant names — mirroring `writePreferenceReport`'s existing
    `startTimeHonoured ? "Yes" : "No"` label-mapping precedent)
</interfaces>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification against the existing codebase):

| Kind | Symbol |
|---|---|
| Export method | `ScheduleExportService.writeDriftReport(XSSFWorkbook, CellStyle, DriftReport)` |
| Excel sheet | `Drift Report` |
| Service behaviour | popularity ranking inside `ScheduleOutputService.buildDriftReport` |
| Service behaviour | drift date-filter and summary recompute block in `ScheduleService` |
| Test class | `src/test/java/com/wfm/service/SolverUsualShiftWritePathGuardTest.java` |
| Doc row | solver read path entry appended to `src/test/resources/ushf-05-write-paths.md` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Over-subscription ranking and a date filter whose summary tells the truth</name>
  <files>src/main/java/com/wfm/service/ScheduleOutputService.java, src/main/java/com/wfm/service/ScheduleService.java, src/test/java/com/wfm/service/DriftReportTest.java</files>
  <read_first>
    - `.planning/phases/17-consistency-constraint-drift-reporting/17-CONTEXT.md` — D-11, D-12, D-13
    - `.planning/phases/17-consistency-constraint-drift-reporting/17-UI-SPEC.md` Component Specifications §1 (summary-bar scope) and §2 (popularity sort and tie-break)
    - `src/main/java/com/wfm/service/ScheduleOutputService.java` — `buildDriftReport` as left by plan 17-01, and `buildPreferenceReport`'s summary computation at lines 395-412
    - `src/main/java/com/wfm/service/ScheduleService.java` lines 200-228 — the existing date-filter block, including how `PreferenceReport` is rebuilt with its summary carried through unfiltered
    - `src/main/java/com/wfm/repository/AgentUsualShiftRepository.java` — the tenant-and-desk-scoped `findByTenantIdAndDeskId(long, UUID)` finder and the comment explaining that desk scoping goes through `u.agent.deskId`
    - `src/main/java/com/wfm/service/UsualShiftResolutionService.java` — `resolve(stored, date)`
    - `src/main/java/com/wfm/model/AgentUsualShift.java` — the `shiftTemplate` association and `dayOfWeek`
    - `src/test/java/com/wfm/service/DriftReportTest.java` — as left by plan 17-01
  </read_first>
  <behavior>
    - A desk where four agents hold `Early` and two hold `Late` as their usual shift returns popularity `[(Early, 4), (Late, 2)]`.
    - Two templates with the same agent count come back in ascending template-name order.
    - A template no agent holds as a usual shift does not appear in the list at all.
    - A desk with no stored usual shifts returns an empty popularity list, not null.
    - An agent holding `Early` on three weekdays counts once toward `Early`, not three times — the ranking counts agents, not stored rows.
    - Filtering the schedule detail to one date returns only that date's drift entries.
    - After filtering, `summary.workingAgentDays` equals the filtered entry count and the three status counts add up to it.
    - After filtering, the popularity list is unchanged from the unfiltered response.
    - A malformed date filter still raises the existing `IllegalArgumentException` from the surrounding block, unchanged.
  </behavior>
  <action>
Two connected pieces of report work.

**Popularity ranking (DRFT-04, D-13).** Fill the `popularity` list that plan 17-01 left as `List.of()`
in `buildDriftReport`. Read the desk's stored usual shifts with
`agentUsualShiftRepository.findByTenantIdAndDeskId(tenantId, deskId)` — the tenant-and-desk-scoped
finder, taking both values from the `Schedule` the method was handed, never from anything supplied by
the caller's request (threat T-17-05). Resolve each stored row's template name through
`UsualShiftResolutionService.resolve(...)` so an era-renamed template ranks under the name the
operator currently sees, consistent with Phase 16 D-01. Count **distinct agents** per template name,
not stored rows: an agent whose usual shift is `Early` on five weekdays contributes one to `Early`.
Sort descending by count, then ascending by template name. Emit an empty list, never null, when
nothing is stored.

Comment the method to record that this block answers a different question from the rest of the
report — it reads stored usual shifts, not this solve's results, so it does not change when the desk
is re-solved. That difference is why `17-UI-SPEC.md` gives the section its own heading and subtext,
and it is the reason D-13 keeps the ranking next to the drift it explains rather than on the Shift
Library page.

**Date filter and summary (DRFT-01).** Add an `if (response.getDriftReport() != null) { ... }` block
to `ScheduleService`'s existing date-filter section beside the `PreferenceReport` block at lines
220-224. Filter `entries` to the parsed `filterDate`, carry `popularity` through untouched, and —
unlike the `PreferenceReport` block directly above it — **recompute** the `DriftSummary` from the
filtered entry list rather than carrying the whole-schedule summary object through. State that
divergence from the copied pattern in a code comment: `17-UI-SPEC.md` requires the summary-bar counts
to reflect the currently date-filtered entry set so the numbers always agree with the rows the
operator can see, which the preference report's whole-report summary does not promise.

Extend `DriftReportTest` with one test per `<behavior>` row above, written before the implementation.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.DriftReportTest" --tests "com.wfm.service.ScheduleServiceShiftSnapshotTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.wfm.service.DriftReportTest" --tests "com.wfm.service.ScheduleServiceShiftSnapshotTest"` exits 0.
    - `./gradlew test` exits 0.
    - A test asserts popularity `[(Early, 4), (Late, 2)]` for a four-and-two fixture, in that order.
    - A test asserts ascending template-name order for two templates with equal counts.
    - A test asserts an agent with the same usual shift on three weekdays contributes a count of 1.
    - A test asserts an empty, non-null popularity list for a desk with no stored usual shifts.
    - A test asserts that after filtering to one date, `summary.workingAgentDays()` equals the filtered entry count and the three status counts sum to it.
    - A test asserts the popularity list is byte-equal before and after applying a date filter.
    - `grep -q 'findByTenantIdAndDeskId' src/main/java/com/wfm/service/ScheduleOutputService.java` succeeds.
    - `grep -q 'setDriftReport' src/main/java/com/wfm/service/ScheduleService.java` returns at least two occurrences (the initial wiring from plan 17-01 and the date-filter rebuild).
  </acceptance_criteria>
  <done>The drift report carries a correctly ranked, desk-scoped popularity list, and a date-filtered response's summary counts match its own rows.</done>
</task>

<task type="auto">
  <name>Task 2: Drift Report sheet in the Excel export</name>
  <files>src/main/java/com/wfm/service/ScheduleExportService.java, src/test/java/com/wfm/service/ScheduleExportServiceTest.java</files>
  <read_first>
    - `.planning/phases/17-consistency-constraint-drift-reporting/17-CONTEXT.md` — D-14 and the XCUT-01 rationale
    - `.planning/phases/17-consistency-constraint-drift-reporting/17-UI-SPEC.md` — the Copywriting Contract's exact header and status-label strings, and the E4 row set in `## UI Considerations`
    - `src/main/java/com/wfm/service/ScheduleExportService.java` — `exportToExcel` (lines 21-37) and `writePreferenceReport` (line 172) in full, including the guard-then-header-only early return and `autoSizeColumns`
    - `src/test/java/com/wfm/service/ScheduleExportServiceTest.java` — the whole file; this is the harness to extend, and it shows how a sheet's cells are read back through `XSSFWorkbook`
    - `src/main/java/com/wfm/dto/ScheduleDetailResponse.java` — the `DriftReport` family from plan 17-01
  </read_first>
  <action>
Add `private void writeDriftReport(XSSFWorkbook workbook, CellStyle headerStyle, DriftReport report)`
to `ScheduleExportService`, modelled line-for-line on `writePreferenceReport`, and call it from
`exportToExcel` immediately after the existing `writePreferenceReport(...)` call so the drift sheet
sits alongside the preference sheet.

One sheet named `Drift Report`, containing two blocks:

  1. The main table. Header row using the six literal strings from `<interfaces>`, styled with
     `headerStyle`. Then the same guard the preference writer uses — if the report or its entry list
     is null, return with the header row written and nothing else. Then one row per entry: agent
     name, the date's ISO string, the operator-facing status label, usual start (blank when absent),
     actual start, and the signed delta in minutes (blank when absent). Map the status through a
     small label helper rather than writing the enum constant name, mirroring the preference writer's
     yes/no label mapping.
  2. One blank spacer row, then the `Most-Subscribed Usual Shifts` heading in the first cell of its
     own row, then a header row of the two popularity literals, then one row per popularity entry in
     the order the report supplies. Do not re-sort in the writer — the report already sorts, and
     re-sorting here would be a second implementation of the ordering rule.

Finish with `autoSizeColumns` over the widest block so template names size naturally, as every other
sheet in this file already does. No column width is fixed and no value is truncated.

Extend `ScheduleExportServiceTest` with cases reading the produced workbook back: the sheet exists,
its six main headers match byte-for-byte, a mixed-status fixture writes the three expected labels, a
null report writes headers only, and the popularity block appears below the main table with its two
headers and its rows in the supplied order.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.ScheduleExportServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.wfm.service.ScheduleExportServiceTest"` exits 0.
    - `./gradlew test` exits 0.
    - A test reads back the six main header cells and asserts each equals the corresponding literal from `<interfaces>`, in order.
    - A test asserts the two popularity header cells equal `Shift Template` and `Agents (Usual Shift)`.
    - A test asserts the heading cell equals `Most-Subscribed Usual Shifts`.
    - A test with a null `DriftReport` asserts the sheet exists with exactly one populated row (the main header row).
    - A test with one entry per status asserts the Status cells read as the three operator-facing labels and that neither raw enum constant name appears in any cell of the sheet.
    - A test asserts a `NO_USUAL_SHIFT` row leaves the `Usual Start` and `Delta (min)` cells blank while `Actual Start` is populated.
    - `grep -c 'createSheet("Drift Report")' src/main/java/com/wfm/service/ScheduleExportService.java` returns 1.
  </acceptance_criteria>
  <done>Exporting a shift-desk schedule produces a Drift Report sheet whose headers and status labels match the on-screen tab exactly, verified by reading the generated workbook back.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the solver never writes the usual-shift target</name>
  <files>src/test/java/com/wfm/service/SolverUsualShiftWritePathGuardTest.java, src/test/resources/ushf-05-write-paths.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` § Cross-Cutting Requirements — XCUT-02 verbatim, and the v1.2 finding I-2 it cites
    - `src/test/resources/ushf-05-write-paths.md` — the whole file; this is Phase 16's enumerated-write-paths record and the discipline to extend
    - `src/main/java/com/wfm/service/SolverService.java` — `resolveUsualShiftTargets` and its call site as left by plan 17-01, plus the constructor's parameter list
    - `src/main/java/com/wfm/repository/AgentUsualShiftRepository.java` — the full method list, so the guard covers every mutating method the interface exposes, including the ones inherited from `JpaRepository`
    - `src/test/java/com/wfm/service/ConstraintWeightsServiceTest.java` if plan 17-02 has landed, otherwise `src/test/java/com/wfm/controller/GlobalExceptionHandlerTest.java` — the repo's plain-unit-test house style
  </read_first>
  <action>
Create `src/test/java/com/wfm/service/SolverUsualShiftWritePathGuardTest.java` in the
`com.wfm.service` package so it can reach `SolverService`'s package-private
`resolveUsualShiftTargets`. Two complementary assertions, because either alone leaves a hole:

**Behavioural.** Use `@ExtendWith(MockitoExtension.class)` with mocked collaborators and
`@InjectMocks SolverService`. Call `resolveUsualShiftTargets` directly with a fixture containing a
stored usual shift and a working day, then assert zero mutating interactions on the
`AgentUsualShiftRepository` mock — one explicit never-invoked assertion per mutating method the
interface exposes (both its own declared ones and the inherited persistence and deletion methods),
followed by an assertion that the mock received no further interactions at all.

**Structural.** Add a second test that reads `src/main/java/com/wfm/service/SolverService.java` from
the working tree, strips comment and javadoc lines before matching (leading `//`, leading `*`, and
`/*` opener lines), and asserts that no remaining code line invokes a mutating method on the
usual-shift repository field. Stripping comments first is load-bearing: this plan and the production
javadoc both discuss the prohibition in prose, and a scanner that matched comment text would fail on
its own documentation rather than on a real regression.

Then append a row to `src/test/resources/ushf-05-write-paths.md` recording the solver read path: what
it reads, that it writes nothing, and which test proves it. Keep the file's existing row shape and
tone — it is Phase 16's record, extended, not restructured.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.wfm.service.SolverUsualShiftWritePathGuardTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.wfm.service.SolverUsualShiftWritePathGuardTest"` exits 0.
    - `./gradlew test` exits 0.
    - The behavioural test contains one never-invoked assertion per mutating method exposed by `AgentUsualShiftRepository`, including the inherited ones, plus a no-further-interactions assertion.
    - The structural test demonstrably strips comment lines before matching — a test-local assertion proves the stripper removes a line that would otherwise match.
    - `src/test/resources/ushf-05-write-paths.md` gains exactly one new row and names `SolverUsualShiftWritePathGuardTest` as its evidence.
    - `git diff --stat src/test/resources/ushf-05-write-paths.md` shows additions only, no deletions.
  </acceptance_criteria>
  <done>XCUT-02 is proven by a test rather than asserted in prose, and Phase 16's write-path record names the solver read path as a verified non-writer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (`GET /schedules/{id}`) | The drift report and its popularity ranking leave the system here |
| solver ↔ persistence | The pre-solve resolution reads `agent_usual_shift` |
| API → file | The Excel export serialises the same data to a downloadable document |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | `SolverService.resolveUsualShiftTargets` | high | mitigate | `SolverUsualShiftWritePathGuardTest` asserts zero mutating interactions with `AgentUsualShiftRepository` on the solver path, plus a comment-stripped source scan; the write-path record in `ushf-05-write-paths.md` names the test as evidence |
| T-17-03 | Information Disclosure | `ScheduleOutputService.buildDriftReport` | high | mitigate | Entries are derived from the already-authorized `Schedule` object `ScheduleService.getScheduleDetail` holds; no second lookup keyed on request-supplied ids is introduced |
| T-17-05 | Information Disclosure | popularity ranking read | high | mitigate | The ranking uses `AgentUsualShiftRepository.findByTenantIdAndDeskId`, taking tenant and desk from the authorized `Schedule`; the unscoped finders on that repository are not used from this path |
| T-17-10 | Information Disclosure | Excel `Drift Report` sheet | low | accept | The sheet carries agent names and dates already present in the `Preference Report` sheet of the same workbook, produced from the same authorized payload — no new class of data leaves the system |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase — `17-RESEARCH.md` § Package Legitimacy Audit records "not applicable, no external packages are installed by this phase" |
</threat_model>

## Flagged assumptions (probe-surfaced and UI-SPEC dismissals — never silently dropped)

Edge probe, `unclassified` row (never auto-resolved with a backstop):

| Requirement | Probe | Planner assumption carried forward |
|---|---|---|
| DRFT-04 | unclassified — review manually | Assumed covered by the ranking truths and their tests. Two shape questions were considered and deliberately not planned because no source artifact specifies them: whether the list should be truncated to a top-N (UI-SPEC §2 shows the full list and adds no rank column), and whether a template with zero subscribers should appear as a zero row (UI-SPEC §2 says one row per template with at least one subscribing agent). Both are recorded here rather than decided silently. |

UI-SPEC `## UI Considerations` dismissals for element E4 (Excel export sheet), carried as explicit
planner assumptions:

- **E4 / loading — dismissed.** Not applicable: a document-generation code path, not an interactive
  surface, so there is no loading state to design.
- **E4 / overflow — dismissed.** Not applicable: Excel columns auto-size to content and the writer
  fixes no column width, so no overflow constraint exists.

## Prohibition breadcrumbs (canon — referred, not minted)

- Agent names and dates in the exported workbook are canon data-retention/GDPR territory — covered by
  `/gsd-secure-phase`; not minted as a prohibition here.

<verification>
- `./gradlew test` green.
- A schedule detail with a date filter returns drift entries for that date only, with summary counts
  summing to the filtered entry count.
- The exported workbook contains a `Drift Report` sheet whose headers match the tab's headers exactly.
</verification>

<success_criteria>
- The operator can see which templates are most over-subscribed as usual shifts, ranked, next to the
  drift they explain.
- Drift data is present in both surfaces that display it: the schedule detail payload and the Excel
  export.
- The summary numbers never disagree with the rows beneath them.
- The solver's read of `agent_usual_shift` is proven read-only by a test.
</success_criteria>

<output>
Create `.planning/phases/17-consistency-constraint-drift-reporting/17-03-SUMMARY.md` when done.
</output>
