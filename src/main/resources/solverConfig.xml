<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">

    <solutionClass>com.wfm.model.Schedule</solutionClass>
    <entityClass>com.wfm.model.AgentAssignment</entityClass>

    <scoreDirectorFactory>
        <constraintProviderClass>com.wfm.solver.ScheduleConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>

    <!-- Primary construction is handled by BreakAwareConstructionPhase
         in SolverService.startSolve() before the solver starts. This CH phase
         is a safety-net fallback: if any planning variables remain unassigned
         after the break-aware pre-assignment (e.g. demand > supply, days off
         reducing available agents), the CH initializes them before local search.
         When all variables are pre-assigned, this phase is a no-op. -->
    <constructionHeuristic/>

    <!-- Local search: improve the pre-assigned solution -->
    <localSearch>
        <termination>
            <!-- Stop after 5 minutes OR 30 seconds without improvement -->
            <spentLimit>PT5M</spentLimit>
            <unimprovedSpentLimit>PT30S</unimprovedSpentLimit>
        </termination>
    </localSearch>

</solver>
