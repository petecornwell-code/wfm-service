<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">

    <solutionClass>com.wfm.model.Schedule</solutionClass>
    <entityClass>com.wfm.model.AgentAssignment</entityClass>

    <scoreDirectorFactory>
        <constraintProviderClass>com.wfm.solver.ScheduleConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>

    <!-- Construction heuristic: builds the initial solution from scratch.
         All planning variables (AgentAssignment.agent) start as null.
         The CH assigns agents to seats one at a time, evaluating all 18
         constraints per move to find feasible break positions, respect
         contracted hours, and match specializations. -->
    <constructionHeuristic/>

    <!-- Local search: improve the CH-built solution.
         Termination is configured programmatically from the solver.time-limit property.

         The unionMoveSelector is explicit because Timefold only auto-builds its
         default change+swap union when <localSearch> declares no moveSelector at
         all. changeMoveSelector/swapMoveSelector are re-declared alongside the
         atomic shift move factory so today's fine-grained single-seat repair is
         composed with, not replaced by, the new move. -->
    <localSearch>
        <unionMoveSelector>
            <changeMoveSelector/>
            <swapMoveSelector/>
            <moveListFactory>
                <!-- Element order is XSD-significant: moveListFactoryConfig extends
                     moveSelectorConfig, so every inherited element (fixedProbabilityWeight
                     is the last of them) must precede moveListFactoryClass. -->
                <fixedProbabilityWeight>1.0</fixedProbabilityWeight>
                <moveListFactoryClass>com.wfm.solver.AtomicShiftMoveFactory</moveListFactoryClass>
            </moveListFactory>
        </unionMoveSelector>
        <acceptor>
            <simulatedAnnealingStartingTemperature>0hard/3000soft</simulatedAnnealingStartingTemperature>
        </acceptor>
    </localSearch>

</solver>
