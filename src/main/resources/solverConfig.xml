<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">

    <solutionClass>com.wfm.model.Schedule</solutionClass>
    <entityClass>com.wfm.model.AgentAssignment</entityClass>

    <scoreDirectorFactory>
        <constraintProviderClass>com.wfm.solver.ScheduleConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>

    <!-- Construction heuristic: builds the initial solution from scratch.
         All planning variables (AgentAssignment.deskAgent) start as null.
         The CH assigns agents to seats one at a time, evaluating all 18
         constraints per move to find feasible break positions, respect
         contracted hours, and match specializations. -->
    <constructionHeuristic/>

    <!-- Local search: improve the CH-built solution -->
    <localSearch>
        <termination>
            <!-- Stop after 10 minutes OR 3 minutes without improvement.
                 Break rearrangement requires coordinated multi-slot moves
                 that need longer plateaus to discover. -->
            <spentLimit>PT10M</spentLimit>
            <unimprovedSpentLimit>PT3M</unimprovedSpentLimit>
        </termination>
    </localSearch>

</solver>
